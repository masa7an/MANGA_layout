# MANGA_layout プロジェクト固有ルール

このファイルは Claude Code 向けの補足です。前半は**このリポジトリを触る人すべてに当てはまる話**、
後半の「作者の運用」は**開発者本人の環境に固有の話**です。

**フォークした場合、「作者の運用」以降は自分の環境に合わせて書き換えるか、削除して構いません。**
本人の2台のPCを前提にした記述で、他の環境では参照先そのものが存在しません。

## 開発環境

- Python の実行は **venv の `python.exe` を直接指定する**。素の `python` は使わない

  ```
  ./venv/Scripts/python.exe -m pytest
  ```

- venv は `_setup_env.bat` で作る（最初の1回だけ）。Python 3.12 を前提とする
- ファイルの読み書きで `encoding` を省略しない。Windows の既定は CP932 のため、
  UTF-8 のファイルで文字化けや `UnicodeDecodeError` を起こす

## 仕様を調べるとき

**[要件定義.md](要件定義.md) を先に読む。** 機能の仕様と、なぜその形にしたかの経緯がそこにある。
README は使う人向けの説明であって、仕様の正となる文書ではない。

**コードのどこに何があるかは [リポジトリの地図.md](リポジトリの地図.md)。**
層の規則と、各ファイルの役割と、新しく足すときに触る場所がある。

PySide6 で踏んだ問題は [PySide6の落とし穴.md](PySide6の落とし穴.md) にまとめてある。

## テストが失敗したときは止めて報告する

**テストが落ちたら、原因調査や修正に進まず、いったん作業を止めて報告し、指示を待つ。**

報告に含めるもの：

- 落ちたテスト名と、失敗した理由（assert の期待値と実際の値など）
- 直前に何を変更したか
- 原因の見立て（あれば。ただし見立てを述べるだけで、修正には着手しない）

---

# 作者の運用（本人の環境に固有）

**ここから下は、開発者本人の2台のPCを前提にした記述です。** 他の環境では参照先が存在しないので、
読み飛ばすか、自分の運用に置き換えてください。

## 共通ルールの所在

言語・エンコーディング・Git・venv の共通ルールは `~/project/CLAUDE.md` および
`~/project/claude_config/common.md` にある。**どちらも本人の環境にのみ存在する**ため、
他の環境では読み込まれない。

## テスト実行の許可

本人の環境では `.claude/settings.json` で許可プロンプトを省略している。省略の対象は
**venv の python で pytest を動かすコマンドだけ**。

- **`.claude/` は `.gitignore` で除外しており、公開していない。** したがって
  **フォークした環境では、テスト実行のたびに通常どおり許可を求められる**
- 許可ルールは「行頭からの一致」で判定されるため、`&&` やパイプで他のコマンドと
  つないだ形は対象外になり、確認を求められる。テスト実行は単独のコマンドとして出す
- pytest 以外の Python 実行（アプリ起動、確認用の使い捨てスクリプト等）は都度確認する

**なぜ2段構えなのか**: 許可プロンプトは実行「前」に出るものなので、「エラーが出ていない
ときだけ許可を省く」という後出しの条件は設定では表現できない。そこで「実行の許可は常に
省略する／失敗したときの進行は止める」の2段構えにしている。上の
「テストが失敗したときは止めて報告する」が、省略した分の歯止め。

## ブランチ運用

公開後は `main` を安定版として維持し、実験は別フォルダ（worktree）で行う。

| フォルダ | ブランチ | 役割 |
|---|---|---|
| `MANGA_layout/` | `main` | 安定版。ここにタグを打って Releases として配布する |
| `MANGA_layout_ai/` | `experiment/ai` | AI生成の実験。壊れていてよい |

利用者が手にするのは Releases の zip なので、**タグを打たない限り `main` の状態は配布されない。**

Claude に守ってほしいこと：

- **コミットの前に `git branch --show-current` で現在のブランチを確認し、報告に含める。**
  共通ルールの「区切りがついたら自動でコミット」を実行するときも、この確認を省かない
- **`main` へのマージは、指示があったときだけ行う。** push と同じ扱いで、自動ではやらない
- **タグ付け（`git tag`）も指示があったときだけ。** タグは「これが配布物です」の宣言であり、
  押し戻せる操作ではない（push 後は特に）
- **`main` で実験的な変更を始めない。** `main` にいるのに実験の依頼を受けたら、
  作業に入る前に止めて「実験フォルダへ移りますか」と確認する

### 実験フォルダの venv について

worktree には `venv/` が無い（git 管理外のため複製されない）。**改めて `_setup_env.bat` を
実行する必要はなく、本体側の venv をパス指定で使う。**

```
../MANGA_layout/venv/Scripts/python.exe -m pytest
```

- 実験用に新しいパッケージを入れると本体側の venv にも入る。実験をやめるときは戻すこと
- 707MB を二重に持たずに済むので、この方法を既定とする

### 実験をやめるとき

worktree のフォルダごと消す。`main` には何も起きない。

```bash
git worktree remove ../MANGA_layout_ai
```
