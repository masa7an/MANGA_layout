"""SAM 3 による切り抜き（→ 要件定義 10.3、`SAM3実装計画.md`）。

**PyTorch と公式 SAM 3 の import は、このフォルダの外へ出さない。**
モデル本体はアプリの `venv` ではなく専用環境で動かし、やり取りは
入力JSON・出力JSON・候補マスクPNG の3つだけにする（→ 計画 4章・5章）。

- `contracts.py` … 渡す入力と返る結果の形。**ここだけがアプリから見える**
- `runner.py`    … 専用環境の Python を別プロセスで起動し、結果を読む
- `cli.py`       … 専用環境の中でだけ動く。公式 SAM 3 を呼ぶのはここ

`model.py` という名前は使わない。アプリ全体のデータモデル（`manga_layout/model.py`）
と紛らわしいため。
"""
