# SAM 3 実装計画

作成日: 2026-08-27  
対象ブランチ: `feature/sam3`  
仕様の正本: [要件定義.md](要件定義.md) の「10.3 SAM 3 による画像の切り抜き」

## 1. この実験で確かめること

コマ内の画像を選び、利用者が短い言葉（例: 「人物」「髪」）を入力すると、SAM 3 が返した
切り抜き候補から1つを選べるかを確かめる。選んだ結果は、元画像を壊さない白黒マスクとして
保存し、画面・サムネイル・書き出し（PNG / JPG / PSD）に反映する。

評価対象は写真だけでなく、漫画と生成画像も含む。SAM 3 の得意な対象と漫画画像での実用性は
別問題なので、最初から安定版に入れることは決めない。

## 2. この実験で作らないもの

- 切り抜く対象を別のAIが自律的に考える機能
- 背景を生成して埋める機能
- 手描きでマスクを直す機能、ぼかし、境界の羽根処理
- PSDのレイヤーマスク出力
- モデル本体・重み・認証情報を同梱した配布物

SAM 3 は利用者の文・枠・点などを受けてマスクを返すモデルである。最初は利用者の文だけを
そのまま渡す。対象の決定まで別のAIへ任せると、SAM 3 の切り抜き品質を単独で評価できない。

## 3. 完成時の最小操作

1. コマ内の画像を選ぶ
2. 「画像 → AI切り抜き...」を開く
3. 切り抜きたい対象を入力する
4. 候補ごとのマスクを半透明で元画像へ重ね、候補・信頼度・範囲を比べる
5. 使う候補を1つ適用する。取り消し、マスクの解除、保存し直しができる

候補が0件なら元画像を一切変えず、「見つかりませんでした」と知らせる。モデルやGPUが使えない
場合も同じく、作品データを変えず、対処の分かる文で知らせる。

## 4. 境界の設計

```
選択した画像ファイル + 入力文
  ↓
AI提供者（最初は SAM 3）
  ↓
SegmentationResult
  - 候補ごとの白黒マスク
  - 元画像上の範囲
  - 信頼度
  ↓
ImageMask（MANGA_layout共通形式）
  ↓
画面 / サムネイル / 書き出し（PNG・JPG・PSD） / 保存
```

### 4.1 フォルダと責務

```text
manga_layout/
  ai/
    __init__.py
    sam3/
      __init__.py
      contracts.py       # AI提供者に渡す入力と返す結果
      runner.py          # 専用環境のCLIを起動し、結果を読む
      cli.py             # SAM専用環境でだけ公式SAM 3を呼ぶ
      experiment_log.py  # 実験結果を作品とは別のログへ記録する
  image_masks.py         # 8bitマスクの検証・適用・合成。SAM 3を知らない
tests/
  test_sam3.py           # 偽のAI提供者との境界テスト
  test_sam3_runner.py    # 偽のCLIを別プロセスで実際に起動して確かめる
  test_sam3_cli.py       # 専用環境側の書き出し（モデルは差し替える）
  test_sam3_log.py       # 実験ログの保存・更新テスト
  test_image_masks.py    # マスクの検証・合成のテスト（画面なし）
  test_ui_mask.py        # 適用・取り消し・保存・整理・点検・書き出し
```

`manga_layout.ai.sam3` の外では、PyTorchや公式SAM 3の型をimportしない。将来別の提供者へ
差し替えるときは、同じ契約の実装を追加するだけにする。`model.py` という名前はアプリ全体の
データモデルと紛らわしいため、SAM 3側には使わない。

### 4.2 マスクの共通形式

- 元画像と同じ幅・高さの**8bitグレースケールPNG**を使う。形式としては0〜255の濃淡を
  受け入れる。0は透明、255は不透明として、元画像の既存アルファと掛け合わせる
- 最初のSAM 3実装が生成する値だけを0または255に限る。髪・半透明素材・アンチエイリアス・
  羽根処理・手動修正で必要になれば、保存形式を変えずに中間値を生成できる
- `ImageObject` に任意の `mask_asset` を追加する。画像そのものの `asset` は変更しない
- マスクは通常の `assets/` に置き、画像と同じく作品フォルダ内で自己完結させる
- **掴む範囲は今までどおり `rect` のまま**（`canvas._selected_image_hit` は
  `rotated_rect_contains` で判定している）。切り抜いて透明になった所もクリックで選べる。
  透明な所を掴めなくすると、小さく切り抜いた画像を選び直せなくなる
- **画像とマスクを別々に変換しない。** 元画像のピクセル座標でマスクを合成して1枚の
  描画対象を作る。その1枚だけに既存の `ImageObject` の拡大縮小・回転・移動を1回だけ
  掛け、最後にコマでクリッピングする
- **トーンを使う場合は「マスク → トーン」の順**。トーンは透明な所には乗らない
  （`tone.apply_tone` が白地に載せてから判定する）ので**どちらが先でも同じ絵**になるが、
  先に切り抜いておけば PSD のトーン3枚（`psd_export.TonePieceImages`）まで
  同じ1枚から作れる

描画の順序は、常に次のとおりとする。

```
元画像 + ローカル座標のマスク
  → マスク適用済みの画像1枚
  → ImageObjectの拡大縮小・回転・移動（1回だけ）
  → コマのクリッピング
```

これにより、画像とマスクで補間・丸め・回転中心が分かれ、髪の輪郭などがずれる経路を作らない。

### 4.3 合成済み画像のキャッシュ

マスク適用済みの画像を、描画のたびに原寸で作り直さない。元画像とマスクをローカル座標で合成した
**変換前の1枚**をキャッシュする。

**新しい入れ物を並べて作らない。** 同じ役割の経路が既にある。**足すのは鍵にマスクを入れることだけ。**

| 経路 | 今 | マスクを足したあと |
|---|---|---|
| 画面・サムネイル | `EditorState.image_preview` → `ToneCache`（鍵は 画像＋トーン設定） | 鍵にマスクを足す |
| 書き出し（PNG・JPG・PSD） | `export.FullImages.__call__` → 原寸用の `ToneCache` | 同上 |
| 手放し | `EditorState.forget_if_unused` が `image_cache` と `tone_cache` を忘れる | マスク側も忘れる |

サムネイルの指紋（`pages._key`）は `page.to_dict()` から作るので、`mask_asset` を保存形式に
入れた時点で自動で追随する。**ここは何もしなくてよい。**

- キャッシュを作り直すのは、元画像アセット、トーン設定、または `mask_asset` が変わったときだけ
- `ImageObject` の移動・拡大縮小・回転・不透明度、コマの移動・形状・クリッピングの変更では、
  キャッシュを作り直さない。既存の描画処理がキャッシュ済みの1枚へ変換を掛ける
- キャッシュの鍵には、元画像アセット、トーン設定、マスクアセットを含める。画像の矩形・回転角・
  コマIDは含めない

画面用と書き出し用のキャッシュは混ぜない。画面用は既存どおり長辺1,600pxの縮小版を合成して
状態側に持つ。書き出し用は原寸で合成するが、既存の `FullImages` と同様に**1ページの
書き出しごとに作って捨てる**。これにより、縮小版を原寸へ引き伸ばして書き出す事故と、複数ページ分の
4K画像を持ち続ける事故の両方を避ける。

**画面用も、原寸で合成してから縮小する。** マスクは元画像の原寸座標に結び付いているので、
先に縮めた絵へ先に縮めたマスクを掛けると、丸めが2回に分かれて髪や1pxの線の縁が崩れる。
PSD のトーンを原寸から焼く理由（→ `psd_export.TonePieceImages`）と同じ。合成は1回きりで
キャッシュに載るので、払うのは初回だけ。**この順序は段階1〜3の実測で見直す**——4K以上で
初回が待たされるなら、縮めてから合成する形と縁を見比べて決める。

#### 4K での実測（2026-08-27）

3840 × 2160 の絵に同じ寸法のマスクを掛けたときの、1回ぶんの合成と、キャッシュが持つ量。

| 用途 | 1回の合成 | キャッシュが持つ量 |
|---|---:|---:|
| 画面用（合成 → 長辺1,600へ縮小） | 107〜126 ms | 5.5 MB（1600×900） |
| 書き出し用（原寸のまま） | 99〜106 ms | 31.6 MB（3840×2160） |

（根拠: `masked_preview` を3回ずつ実行して計測 / 確認日: 2026-08-27。
PNGの展開を含む。**べた塗りの絵で測っているので、実写より速い側の値**）

**画面用は 5.5 MB しか持たない。** 上限（`BAKED_CACHE_LIMIT` = 24）まで埋めても
130MB 程度で、既存のトーン焼き込み（1枚 10MB 前後）より軽い。**上限の見直しは要らない**
と判断した。重いのは合成そのもの（0.1秒）だが、払うのは組み合わせごとに1回だけで、
移動・拡大縮小・回転では払わない。

判断を変えるとしたら、**実写の4Kで初回の 0.1 秒が操作の妨げになったとき**。そのときも
移動や回転のたびに再合成する方式へは戻さない（操作のたびに高解像度の画素を作り直すことになる）。
既存の `BakedCache` と同じ**上限付きで古い順に捨てる**形のまま、上限の値だけを動かす。

### 4.4 PSD・JPG 書き出しでの扱い

**書き出しは PNG だけではない**（`export._EXTENSIONS` は PNG / JPG / PSD）。JPG は PNG と
同じ描画経路を通るので何もしなくてよいが、**PSD は絵を引く経路が別にある。**

- `psd_export.BareImages` は「絵」レイヤー用に**トーンを焼く前**の1枚を返す（→ 要件定義 6.28）
- `psd_export.TonePieceImages` は「トーン範囲・白ベタ・トーン」の3枚を原寸から焼く

どちらも `image.asset` しか見ていないので、**手を入れなければ PSD だけ切り抜き前の絵が出る。**
画面と食い違っていることに気づくのがクリスタで開いたあとになるため、次のとおり決める。

- **PSD でもマスクを焼き込む。** 「絵」レイヤーとトーンの3枚に、画面と同じマスクを掛ける
- レイヤーマスクとしては出さない（→ 2章）。焼き込みなので、クリスタ側で切り抜きを戻せない。
  戻したいときは、アプリでマスクを解除してから書き出す

**ここは要確認。** 「クリスタで切り抜きを直す」使い方が主になるなら、焼き込みではなく
レイヤーマスク出力を先に作る判断もありうる。その場合は2章の対象外リストを見直す。

### 4.5 SAM 3実験ログ

作品を表示するために不要な生成情報は `project.json` には入れない。一方、実験中に「どの文で
どの結果になったか」を失わないため、作品フォルダの `sam3_experiment_log.json` に別途記録する。
このファイルはマスクを使わない作品には作らない。

1回の推論について、少なくとも次を記録する。

- 実行ID、日時、画像ID、採用時のマスクアセット
- 入力文、モデルのリビジョン、候補ごとの信頼度・範囲、採用候補の番号
- Python起動時間、モデルロード時間、推論時間、入出力処理を含む総時間、GPUメモリの最大値
- 成功・候補なし・失敗・結果を無視した、のどれで終わったか

候補を受け取った時点で推論結果を記録し、利用者が適用したときに採用候補とマスクアセットを更新する。
認証トークン、元画像の絶対パス、画像そのものはログへ書かない。ログの書込みは作品保存とは別で、
失敗しても作品やマスクの保存を壊さない。

## 5. 実装の段階

| 段階 | 作業 | 完了条件 |
|---|---|---|
| 0 | 導入の前提を整える | Python 3.12、GPU、SAM 3のモデル取得権限を確認する |
| 1 | 共通契約と合成キャッシュを作る | SAM 3を入れずに、候補・マスクの型、キャッシュ、偽提供者のテストが通る |
| 2 | マスクと実験ログを保存する | 保存→開き直し、Undo/Redo、未使用アセット整理、実験ログ更新が正しく動く |
| 3 | 描画と書き出しへ接続する | 画面、サムネイル、PNG／JPG／PSD が同じマスク結果になる |
| 4 | SAM 3を単体で動かす | 画像・文から候補マスクPNG、範囲、信頼度を得られる |
| 5 | アプリからSAM 3を呼ぶ | UIを止めずに候補表示、適用、失敗表示ができる |
| 6 | 実用性を判定する | 漫画・生成画像・写真で結果と処理時間を記録する |

### 段階0: SAM 3専用環境

アプリの `venv` にPyTorchやSAM 3を混ぜない。`ai/sam3/.venv/` を専用環境とし、
Git管理外にする。モデルのダウンロード先もリポジトリ外の標準キャッシュを使う。

導入前に必要なことは次のとおり。

1. Hugging Faceで公式モデルの利用条件に同意し、取得権限を得る
2. このPCのGPUで公式要件のCUDA版PyTorchが動くか確認する
3. 実際に使えた公式SAM 3のリビジョンとPyTorchの版を、専用の依存関係ファイルへ固定する

#### 版の条件（2026-08-27 に確認した）

**見込みは当たっていた。** 公式 README の記述は次のとおり。

| 項目 | 公式 README |
|---|---|
| Python | **3.12 以上** |
| PyTorch | **2.7 以上** |
| CUDA | **12.6 以上** |
| チェックポイント | Hugging Face で**取得権限の申請が要る**（承認制）。承認後に認証して取得する |

（根拠: https://github.com/facebookresearch/sam3 の README / 確認日: 2026-08-27）

**ただし README と `pyproject.toml` が食い違っている。** `pyproject.toml` の
`requires-python` は `>=3.8`。**README の 3.12 以上に従う**——低い方を信じて後で
詰まるより、高い方に合わせるほうが安い。

宣言されている依存は `timm>=1.0.17` / `numpy>=1.26,<2` / `tqdm` / `ftfy==6.1.1` /
`regex` / `iopath>=0.1.10` / `typing_extensions` / `huggingface_hub`。

（根拠: https://raw.githubusercontent.com/facebookresearch/sam3/main/pyproject.toml / 確認日: 2026-08-27）

#### Windows は公式に言及が無い。躓く場所は2つ分かっている

**README は対応OSを書いていない。** そのうえで、Windows で報告されている問題が2つある。
**どちらも段階0で潰しておく。**

1. **`triton` が要るのに、依存に宣言されていない。** Linux では PyTorch に同梱される
   ため表面化しないが、Windows の PyTorch には入っていない。`No module named 'triton'`
   で止まる。Windows では `triton-windows` を別途入れる
2. **`numpy` の版が衝突する。** SAM 3 は `numpy>=1.26,<2` を要求するが、新しい
   `opencv-python` は `numpy>=2` を要求する。Windows 11 / Python 3.12 での報告がある。
   **専用環境に opencv を入れるなら版を固定する**

（根拠: facebookresearch/sam3 の issue #154・#179（triton）、issue #319（numpy 衝突） / 確認日: 2026-08-27）

**「動く」と書くのは、開発機で単体推論が通ってから。** 上の2つは「躓く場所が分かっている」
だけで、通った実績ではない。

#### 利用条件 — 出口は塞がらない。ただし条件付き

**SAM 3 は独自の「SAM License」で配布されている**（MIT でも Apache でもない）。

- 再配布は**できる**が、**ライセンスの写しを添えることが条件**。再配布物も同じ条件に従う
- 軍事・兵器・原子力・スパイ活動などの用途を禁じている
- 研究成果として公表する場合は SAM Materials の使用を明記する

**このリポジトリは MIT で公開しているが、実験の出口は塞がらない。** 2章で
**モデル本体・重み・認証情報を同梱しない**と決めてあり、同梱しない限り SAM License の
再配布条件は発生しないため。利用者が自分で取得権限を得て、自分の専用環境に入れる形になる。

**逆に言えば、将来同梱したくなった時点で MIT 単独では配れなくなる。** 2章の対象外リストは
単なる作業量の話ではなく、**配布条件の前提**でもある。外すときはライセンスの再検討とセットにする。

（根拠: https://github.com/facebookresearch/sam3/blob/main/LICENSE / 確認日: 2026-08-27）

### 段階1〜3: SAMなしで先に固める部分

最初に `ImageMask` と保存形式を実装する。ここでは固定の0/255マスクPNGを使い、AIは呼ばない。

- 画像とマスクの寸法が一致しない場合は適用を拒否する
- 元画像とマスクをローカル座標で1枚に合成してから、既存の画像変換を1回だけ掛ける
- 回転・拡大縮小後に画像とマスクの輪郭がずれないことを、非整数倍率・回転・細い輪郭で確かめる
- 元画像、トーン設定、マスクだけをキャッシュの再生成条件とし、移動・拡大縮小・回転・
  コマクリッピングでは再生成しない
- 4K以上の画像で、合成済みキャッシュのメモリ使用量と初回／再生成時間を測定する
- 画面用の縮小版と書き出し用の原寸を混ぜず、原寸の合成結果をページをまたいで持ち続けない
- サムネイルと書き出しは、画面と同じ描画経路を通す
- マスクの追加・差し替え・解除は、Undo/Redoの1手として扱う
- **参照と欠けを見ている所は3つあり、今はどれも `obj.asset` しか見ていない。**
  `Project.referenced_assets`（未使用整理の対象から外す）、`check.inspect_page`（点検の×印）、
  `ui.export.missing_assets_in`（書き出し前の警告）。`mask_asset` をどれに数えるかを揃える
- **マスクの実体が無い・寸法が合わない作品も開ける。** その画像はマスクを無視して元画像を
  描く。開けなくする・描画を止めるはしない（欠けた画像1枚で作品が開けないのは割に合わない、
  という既存の `ImageCache` の考え方と揃える）
- 古い作品では `mask_asset` が無いので、今までどおり元画像をそのまま描く
- 実験ログは推論結果と採用結果を追記・更新できるが、ログが壊れても作品を開ける

### 段階4: SAM 3単体確認

アプリを起動せず、専用環境からCLIを実行する。入力は画像ファイルと短い文、出力は候補ごとの
8bitグレースケールPNGとJSONの一覧にする。最初のSAM 3実装ではPNGの画素が0または255だけで
あることも確認する。少なくとも次を記録する。

- 入力文、画像寸法、候補数、各候補の信頼度と範囲
- Pythonプロセス起動時間、モデルロード時間、実推論時間、入出力を含む総時間
- GPUメモリ使用量（最大値）
- 4K以上の画像での、マスク合成キャッシュの生成時間とメモリ使用量
- 写真、生成画像、漫画で対象が意図どおり残ったか

この段階で「人物」のような一般名詞と、「右の人物」のような位置を含む文の両方を試す。
日本語の文が十分に働かない場合は、英語へ変換する機能を足さず、まず利用者が英語を入力した
場合の結果も併記して、どこに問題があるかを分けて記録する。

最初は推論ごとにCLIを起動する方式で計測する。Python起動やモデルロードが総時間の大半を占め、
繰り返し操作に耐えないと確認できた場合だけ、同じ入力JSON・出力JSONの契約を保ったまま
常駐SAMワーカーへ替える。アプリ側の画面・保存・マスク処理は、どちらの実行方式も知らない。

### 段階5: アプリとの接続

`runner.py` は専用Pythonを別プロセスとして起動する。画面の処理を待たせず、完了・失敗・
結果を無視したことを画面へ通知する。通信には、入力JSONと出力JSON、候補マスクPNGだけを使う。
将来の常駐SAMワーカーも、この通信形式を変えずに使う。

**SAM推論は常に最大1件だけ実行する。** 実行中は次の推論開始を受け付けず、画面にも待機中であることを
示す。結果を無視しても、走っている推論が終わるまでは次の要求を起動しない。初期版では待ち行列も
作らない。利用者の「やはり違う」という入力で複数のプロセスや要求が重なり、GPUメモリを使い切る
経路を作らないためである。

候補を適用するまでは作品の状態を変えない。適用した時点で初めて `mask_asset` を保存し、
履歴に積む。これにより、推論の失敗や候補の見比べが未保存変更やUndo履歴を汚さない。

初期版の「中止」は**プロセスを止める操作ではなく、結果を使わない操作**とする。画面の候補表示を
閉じて実行IDを無視済みにし、SAM 3は完了までバックグラウンドで走らせる。完了後の候補PNGは
読み込まずに片付け、作品は一切変えない。推論中はアプリの終了を保留して、GPUプロセスを孤立させない。
GPU推論の途中で安全に終了できることを実測するまでは、こちらから強制終了はしない。

**ただし、待つ時間には上限を置く。** 「強制終了を作らない」と「終了を保留する」をそのまま
組み合わせると、子プロセスが固まったときに**アプリを終了できなくなる**（モデルの取得が止まった、
GPUを掴んだまま応答しない、など）。数秒待って終わらなければ、「待つ」か「そのまま終了する」かを
利用者に選ばせる。黙って待ち続ける作りにはしない。

## 6. テスト計画

| 範囲 | 自動テスト | 実機確認 |
|---|---|---|
| 共通マスク | 0〜255の読込、寸法不一致の拒否、保存・読込、Undo/Redo、アセット整理 | 回転・トーン・コマ境界が重なる画像 |
| 合成キャッシュ | 入力変更時だけ再生成し、移動・拡大縮小・回転・クリッピングでは再利用。縮小版と原寸を混ぜない | 4K以上の画像でのメモリ使用量と再生成時間。原寸はページ書き出し後に解放されること |
| 描画 | 画像とマスクを一度だけ変換した画素一致、画面・サムネイル・PNG／JPG／PSD の一致 | 細い髪・線画・非整数倍率・回転後の境界 |
| SAM境界 | 偽のCLIで正常、候補ゼロ、壊れたJSON、専用環境不在、結果を無視、実行中の2件目を拒否 | 実モデルの起動・GPU利用・時間の内訳 |
| 実験ログ | 推論記録、候補の採用記録、壊れたログからの回復 | 実際の推論結果が自動で残ること |
| 実用性 | なし。正解は絵ごとに変わる | 24枚以上の評価セットでの切り抜き比較 |

標準の `pytest`（Pythonの自動テスト実行ツール）は、モデルをダウンロードせず、GPUも使わない。
実モデルの確認は別コマンドとし、モデルを持つPCでだけ実行する。

漫画の細線とアンチエイリアスは、モデルの出力品質とアプリ側の描画品質を分けて確認する。各試料で
次の3枚を並べる。

1. SAM 3が返した元画像座標のマスクPNG
2. 変換を掛けていない、アプリ内のマスク適用済み画像
3. 拡大縮小・回転・コマクリッピング後の画面／書き出し

1と2の比較でジャギー・細線欠損・境界太りが増えていれば、アプリの合成処理の不具合として直す。
1の時点で失われていればSAM 3の結果として記録する。自動テストには1pxの線、斜め線、階調のある
境界を置き、1→2でアルファと輪郭が変わらないことを確かめる。

## 7. 実用性の判定基準

単体動作の確認は9枚でもよいが、採用判断には**24枚以上**を使う。画像はリポジトリに入れず、
利用許諾を確認した実画像をリポジトリ外に置く。

条件は2つあり、**両方を満たす**。1枚が複数の条件を兼ねてよいので、**下の表の合計が
総枚数になるとは限らない**（兼ねさせると12枚で8条件を満たせてしまうため、総枚数を別に置く）。

1. 合計 **24枚以上**
2. 次の8条件を **それぞれ3枚以上**

| 必須の画像条件 | 最低枚数 |
|---|---:|
| アニメ塗りの人物（細い髪を含む） | 3 |
| 写実寄りの生成画像 | 3 |
| 白黒漫画（吹き出しまたは効果線を含む） | 3 |
| 人物と複雑な背景 | 3 |
| 複数人物から1人を選ぶ場面 | 3 |
| 手・指など細い形を含む場面 | 3 |
| 写真の人物（比較の基準） | 3 |
| 髪・半透明素材・アンチエイリアス境界が難しい場面 | 3 |

実験ログから、次の表を自動生成または転記して比較する。採用・見送りは、数値だけではなく
「候補から正しいものを選べるか」「境界を直す手間が元の作業より小さいか」で決める。

| 画像 | 入力文 | 候補を選べたか | 境界の問題 | 起動 / ロード / 推論 / 総時間 | GPUメモリ | 判断 |
|---|---|---|---|---|---:|---|
|  |  |  |  |  |  |  |

## 8. mainへ統合する条件

次のすべてを満たすまで `main` へマージしない。

1. 標準テストと静的解析が通る
2. マスクの保存・読込・Undo/Redo・アセット整理・PSD書き出しに回帰がない
3. SAM 3が無いPCでも、既存のアプリを起動・利用できる
4. 24枚以上の実画像で、少なくとも一部の用途に「手作業で切り抜くより速い」結果が確認できる
5. 実験ログから、採用結果・プロンプト・性能の内訳を追える
6. モデルの取得・利用条件と、配布時の案内が決まっている

満たさない場合は、`feature/sam3` に実験結果を残し、機能は安定版へ入れない。SAM 3の呼び出しを
外しても共通マスク機能だけを残すかどうかは、その時点で別に判断する。

## 9. 進捗（2026-08-27 時点）

| 段階 | 状況 |
|---|---|
| 0 導入の前提 | **調べ物は済み**（版・OS・ライセンス → 段階0の節）。**残りは実機**——取得権限の申請・専用環境の作成・GPUでの実動確認 |
| 1 共通契約と合成キャッシュ | **済み** |
| 2 マスクと実験ログの保存 | **済み** |
| 3 描画と書き出しへの接続 | **済み**（画面・サムネイル・PNG／JPG／PSD） |
| 4 SAM 3単体 | **入れ物は済み**（`cli.py` の書き出し・時間・GPU計測）。**モデルを呼ぶ所だけ空けてある**（`_predict`） |
| 5 アプリとの接続 | **呼び出しの境界は済み**（`runner.py`。1件だけ・失敗・片付け）。**画面はまだ無い** |
| 6 実用性の判定 | 未着手 |

段階1〜3で入れたもの（すべて SAM 3 を動かさずに動く）。

- `manga_layout/image_masks.py` … マスクの読み込み・検証・合成
- `manga_layout/ai/sam3/contracts.py` … 依頼と結果の形、`experiment_log.py` … 実験ログ
- `ImageObject.mask_asset` … 保存形式（**掛けていない作品の project.json は1文字も変わらない**）
- `images.BakedCache` … トーン専用だった入れ物を、鍵に切り抜きを足した形へ一般化
- `EditorState.apply_image_mask` / `clear_image_mask` … 適用・取り消し（Undo の1手）
- `check.KIND_MISSING_MASK` … マスクだけが欠けた画像を点検で拾う

段階4〜5で入れたもの。

- `runner.py` … 専用環境の python を別プロセスとして起動し、結果を読む。
  **同時に1件だけ**・失敗の受け方・置き場の片付けまで。偽の CLI を**実際に起動する**
  テストで確かめてある（専用環境不在・壊れたJSON・異常終了・2件目の拒否）
- `cli.py` … 専用環境側の入口。JSONの読み書きと、**標準ライブラリだけで書く
  8bitグレースケールPNG**（専用環境に画像ライブラリを増やさない）

**残っているのは2つだけ。**

1. **`cli._predict`**——公式 SAM 3 の呼び方。専用環境を作って実際に動かしてから書く。
   当てずっぽうで書くと、動かないコードが「実装済み」に見える
2. **画面**——「画像 → AI切り抜き...」のメニュー、文の入力、候補の見比べ、
   待機中の表示。ここは動線の設計が要るので、作る前に相談する

## 10. 直近の着手順

1. Hugging Faceの公式SAM 3モデルへのアクセスを用意する
2. 専用環境を作り、SAM 3の単体推論を確認する（段階4）
3. `runner.py` で別プロセスとして呼び、候補を受け取る（段階5）
4. 画面に「画像 → AI切り抜き...」と候補表示を足す（段階5）
5. 24枚以上の評価セットで実用性を判定する（段階6）

1〜3（契約・マスク・保存・描画）は済んでいる（→ 9章）。
