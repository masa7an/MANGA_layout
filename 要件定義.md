# 漫画レイアウタ（MANGA_layout）要件定義

作成日: 2026-08-02 / format_version: 1

---

## 1. これは何か

クリスタや forge で描いた画像を貼り込み、**コマ割り・フキダシ・セリフ配置** を行うためのエディタ。
用途は **ネーム／下書き**。清書・仕上げは全てクリスタ側で行う。

**発表先はウェブ漫画で、印刷しない。** そもそもネームは印刷するようなものでもない。
出力の単位で迷ったら**画素数**を採り、紙の上の寸法を保つ細工はしない（→ 6.7）。

### 目的

- コマ割りとセリフ位置を素早く試行錯誤する
- ページ単位で構成を並べ替え、全体の流れを確認する
- 決まったレイアウトを PNG で書き出し、クリスタでの作画の下敷きにする

### やらないこと（明示的に範囲外）

ブラシ / レイヤー効果 / **PSD の読み込み** / AI生成 / トーン / ベクター / フィルター / 色調補正 / 印刷入稿品質の出力（裁ち落とし・基本枠の厳密管理）

「PSD入出力」と書いていたが、**書き出しは 2026-08-06 に実装した**（→ 6.28）。範囲外のまま残るのは読み込みのほうで、こちらは「クリスタで作ったものをこの道具へ戻す」動線そのものが無いため要らない。トーンも 6.27 で入っており、ここに残っているのは**貼る**トーンのこと（→ 6.27）。

---

## 2. 技術構成

| 項目 | 決定 |
|---|---|
| 言語 | Python 3.12 |
| GUI | PySide6（Python 用の GUI 部品セット） |
| 描画基盤 | QGraphicsView / QGraphicsScene（図形をシーンとして配置・描画する部品） |
| 実行 | `./venv/Scripts/python.exe main.py` |
| 保存形式 | フォルダ形式（`project.json` + `assets/`） |

### QGraphicsView を選ぶ理由

選択・重ね順（Zオーダー）・座標変換・クリッピング（枠の形に切り抜く処理）が最初から用意されている。
そして最大の利点は、**画面表示と PNG 書き出しが同一の描画経路を通る**こと。「画面で見た通りに出力される」がタダで手に入る。

### PySide6 のつまずきは別の文書に集める → [PySide6の落とし穴.md](PySide6の落とし穴.md)

PySide6 は Python の下で C++ の部品を動かしているため、**Python 側の変数はまだ持っているのに、下の部品との結び付きだけが切れる**という壊れ方をする。コードを普通に読んでも気づけず、原因の見当が付くまで時間を取られる類なので、実機で確かめた事実と対処を別の文書にまとめている。

この要件定義は「何を作るか」を書く場所なので、こうした地雷は混ぜない。踏んだら向こうに足す。

---

## 3. 座標系と単位

**単位はピクセル（px、浮動小数）。すべてページ座標の絶対値で保存する。**

- A4 相当 = 1240 × 1754 / B5 相当 = 1075 × 1518
- 画面表示は倍率を掛けるだけ。**100% ＝ シーンの 1px が画面の 1px**
- PNG 書き出しは換算なし。100% ならページの寸法がそのまま画素数になる（→ 6.7）

### なぜ mm ではなく px か（2026-08-03 に変更）

**発表先がウェブで、印刷しないから**（1章）。mm はどこかで必ず dpi を挟んで画素数に直すことになるが、その dpi は「印刷したら何 mm か」を保つためだけの数字で、この道具では誰も使わない。挟んだぶん、次の食い違いが起きていた。

| mm だったときの問題 | px にして消えたもの |
|---|---|
| 「サイズを 50% に」と言われて dpi を下げ、紙の大きさを保ってしまった（→ 6.7 の撤回した設計） | 倍率は画素数に直接効く |
| 文字サイズが 3.5mm のような小さな値になり、Qt が整数の画素数しか受け取らないため 3 か 4 に丸められた。20 倍で作って 1/20 に縮める回避策（`TEXT_FONT_SCALE`）が要った | フォントの画素数をそのまま渡せる |
| 表示倍率 100% が「紙に刷ったときの大きさ」で、画面の物理解像度に依存していた | 100% ＝ 書き出した PNG を等倍で見たときの見え方 |

「A4 相当」という言い方が残っているのは、**紙の寸法が画面の中での大きさの目安として便利**なため。150dpi で換算した値を出発点にしてあるだけで、印刷を意図した値ではない。カスタムで好きな px を指定できる。

### 保存形式 version 1（mm）からの移行

`format_version` を 1 → 2 に上げ、**version 1 の作品は読み込み時に 150dpi 換算で px に直す**（`Project.scale_lengths`）。上書き保存すると新しい形式になるので、換算したことを利用者に知らせる。

換算は**組み立て終えた型のあるオブジェクトを走査**して行う。読み込む前の辞書を書き換える手もあるが、入れ子のどの数値が長さなのかを名前で見分けることになり、1つ取りこぼすとそこだけ 1/5.9 の大きさで残る。

**割合と角度には触らない。** `SlantPair.ratio`（0〜1）、`Tail.root_y`（-1〜1）、`angle`（度）、`opacity`、`src_px`（元画像の実寸）は単位を持たないので、掛けると意味が壊れる。

`samples/basic/project.json` は version 1 のまま置いてある。移行が効いていることの実地確認を兼ねているので、新形式に書き換えないこと。

#### 欠けた項目の既定値は、換算の前に px 基準へ揃える（2026-08-08 に追加）

枠線・しっぽ幅・文字サイズが**丸ごと欠けている** version 1 ファイルでは、`Border.from_dict` などが使う「項目が無いときの既定値」が**今の px 基準の値**（例: `Border().width == 3.5`）になっている。これをそのまま換算（×5.9）に通すと、既定値そのものが2重に大きくなる（文字サイズの既定 42px が 42 × 5.9 ≈ 248px、枠線 3.5 が 20.7px、しっぽ幅 35 が 206px になる）。

**アプリが書いた version 1 ファイルは `to_dict()` が全項目を書くため、実際に踏むのは手で編集して項目を削ったファイルだけ**（本人の指摘 2026-08-08）。それでも壊れた見え方になるのは望ましくないので、`Project.from_dict` は換算の前に、version 1 かつ丸ごと欠けている項目にだけ「今の既定値 ÷ 5.9」を先回りで埋める（`_fill_legacy_length_defaults`）。あとの換算が掛け戻すので、結果は**今の新規作成と同じ見た目**になる。値がある項目には一切触らない。

---

## 4. オブジェクト構造

```
Project
 ├ Page[]
      ├ Panel[]                コマ（クリッピングの主体）
      │    └ Image[]           コマ枠で切り抜かれる
      └ Floating[]             ページ直下に置かれ、切り抜かれない
           ├ Balloon           フキダシ
           └ Text              セリフ
```

### 重要: フキダシをコマの「子」にしない

漫画のフキダシは**コマ枠からはみ出す／複数のコマにまたがる**のが常態なので、コマの子にするとクリッピングに巻き込まれて切られてしまう。

そこで、親子関係ではなく **`attached_panel_id` による紐づけ** にする。

- **コマを移動すると**、紐づいたフキダシ・テキストも同じ量だけ追随する（中の画像としっぽの先端も一緒に動く）
- ただしクリッピングの対象外なので、はみ出しても切られない
- 紐づけを外して（`null`）ページに固定することもできる

**リサイズでは追随させない。** 当初は「移動・リサイズとも追随する」としていたが、リサイズは「絵に対する窓の大きさを変える操作」であり、中身が付いて回ると絵の位置を合わせ直すことになる。また「同じ量だけ」がリサイズでは定義できない（どの辺を引いたかで意味が変わる）。追随するのは移動だけとする。

「コマを動かせば中身が全部ついてくる」という狙った挙動だけを取り、副作用を避ける設計。

### 重要: コマは矩形ではなくポリゴン（多角形）で保存する

斜めのコマは漫画で日常的に使われる。**保存形式を最初から頂点リストにしてある。**

- 斜めコマを足す際、保存形式・描画・クリッピングを書き直さずに済む
- 今やる追加コストはゼロ、後からやると数日

この読みは当たった。2026-08-03 に斜めの縦割り（→ 6.10）を足したとき、`Panel` の保存形式・描画・切り抜きは**1行も変えずに済んだ**。書き換えが要ったのは、当たり判定が外接矩形で代用されていた箇所のほうだった。

---

## 5. データ形式（project.json）

```json
{
  "format_version": 1,
  "app": "MANGA_layout",
  "title": "作品タイトル",
  "default_page_size": { "w": 210.0, "h": 297.0 },
  "reading_direction": "rtl",
  "next_id": 42,
  "pages": [ /* Page */ ]
}
```

`reading_direction`（右綴じ/左綴じ）は MVP では表示に使わないが、フィールドだけ持っておく。

### Page

```json
{
  "id": "page_0001",
  "size": { "w": 210.0, "h": 297.0 },
  "panels": [ /* Panel */ ],
  "floating": [ /* Balloon | Text | Sticker */ ],
  "slant_pairs": [ /* SlantPair。斜めに割ったコマが無いページでは項目ごと省く */ ],
  "rough": { /* PageRough。ラフを敷いていないページでは項目ごと省く（→ 6.23） */ }
}
```

### PageRough（ラフ・下敷き → 6.23）

```json
{
  "asset": "assets/5c1e77b2....png",
  "rect": { "x": 0.0, "y": 257.0, "w": 1240.0, "h": 1240.0 },
  "src_px": [2048, 2048],
  "faded": true
}
```

`Page` 直下に**1つだけ**置く。項目が無い＝ラフなし、で矛盾なく読めるので、**敷いていないページの project.json はこの機能の追加前と同じ内容のまま**になる。

- `asset` / `rect` / `src_px` は `Image` と同じ意味。**`rotation` と `opacity` は持たない**（→ 6.23）。傾きは写真を撮り直せば済み、濃さは作品ではなく好みなので `settings.json` の `rough_opacity` から取る
- `faded` は**青く淡くしているか**。`false` なら写真のままの色で出る。2値しか無く `rough` 自体が省ける項目なので、**こちらは省かず必ず書く**（省くと、既定がどちらだったかを JSON を読む人が思い出さないと分からない）
- **書き出しには出ない。** 保存形式に載るのに絵の一部ではない、という点は付箋（`note`）と同じだが、**サムネイル一覧には出す**ので、指紋からの除外（→ 6.18）は要らない

### Panel（コマ）

```json
{
  "id": "panel_0003",
  "type": "panel",
  "shape": {
    "kind": "polygon",
    "points": [[10.0, 10.0], [200.0, 10.0], [200.0, 90.0], [10.0, 90.0]]
  },
  "border": { "width": 0.6, "color": "#000000", "visible": true },
  "z": 0,
  "children": [ /* Image */ ],
  "focus_lines": { /* FocusLines。集中線を入れていないコマでは項目ごと省く */ },
  "locked": true /* ロックしているコマだけ持つ。false 相当は項目ごと省く（→ 6.17） */
}
```

### FocusLines（集中線 → 6.16）

```json
{
  "center": [0.5, 0.42],
  "hole": 0.15,
  "count": 72,
  "width": 0.018,
  "seed": 1743205,
  "white": true /* 白い集中線のときだけ持つ。省くと黒（→ 6.19） */
}
```

`Panel` 直下に**1つだけ**置く。項目が無い＝集中線なし、で矛盾なく読めるので、**使っていないコマの project.json はこの機能の追加前と同じ内容のまま**になる。

- `center` は**コマの外接矩形に対する割合**。長さで覚えると、コマを縮めたときに中心がコマの外へ飛び出す（`SlantPair.ratio` と同じ理由 → 上）。**範囲は制限しない。** 画面の外から集中させるために中心をコマの外へ置くことがあり、その場合も線は正しく作れる
- `hole`（内側の空き）と `width`（外周での線の太さ）も割合で、**外接矩形の短辺**が基準。コマの大小によらず同じ見え方になる。どちらも 0〜1 の外は読み込みで弾く
- `count` は本数。4〜400 の外は弾く
- `seed` は形のばらつきを決める数字（0 以上の整数）。**保存しないと開くたびに形が変わり**、書き出した PNG と画面が一致しなくなる
- `white` は色。**黒地に白い線にするだけの単純な色違い**で、形には影響しない。既定は黒（false 相当）で、白にしたときだけ項目を持つ（→ 6.19）

### FlowLines（流線 → 6.26）

```json
{
  "angle": 0.0,
  "count": 48,
  "width": 0.006,
  "length": 0.60,
  "seed": 8812043,
  "white": true /* 白い流線のときだけ持つ。省くと黒（→ 6.26） */
}
```

`Panel` 直下に**1つだけ**置く。`focus_lines` とは**別の項目**で、混ぜない（→ 6.16 で先に決めてある）。集中線と同じく、項目が無い＝流線なし。

- `angle` は線の向き（度）。0 が水平・右向き。**-180〜180 に正規化して持つ**（`geometry.normalize_angle`。画像の `rotation` と同じ基準 → 6.3）。角度は周期的な量なので、範囲外を弾く（`hole` などの流儀）と -10 度のような正しい値まで弾くことになる。正規化は何度かけても結果が変わらないので、保存と読み込みで値が食い違うこともない（2026-08-08、実装と食い違っていた表記を直した。往復自体は一貫しており実害は無かった）
- `count` は本数。4〜400 の外は弾く（集中線と同じ範囲だが、**定数は別に持つ**。片方を変えたときに巻き添えにしない）
- `width` は線の太さ。**外接矩形の短辺**に対する割合（集中線と同じ基準）。0〜1 の外は弾く
- `length` は線の長さ。**外接矩形の対角線**に対する割合で、1.0 なら向きによらずコマを貫く。0〜1 の外は弾く。ここだけ基準が短辺でない理由は 6.26
- `seed` は形のばらつきを決める数字（0 以上の整数）。集中線と同じく、保存しないと開くたびに形が変わる
- `white` は色。既定は黒（false 相当）で、白にしたときだけ項目を持つ

`center` も `hole` も**持たない**。流線は向きだけで決まり、抜きを持たない（→ 6.26）。

### SlantPair（斜めに割ったコマの組 → 6.10）

```json
{
  "left": "panel_0003",
  "right": "panel_0004",
  "ratio": 0.42,
  "angle": 12.0,
  "direction": "/"
}
```

`Page` 直下に置く。コマ側には何も足していないので、**斜めを使っていない作品の project.json はこの機能の追加前と同じ内容のまま**になる。

パラメータを組の側に1箇所だけ持つのは、2枚のコマに分けて持たせると値が食い違う経路ができるため（`attached_panel_id` と同じ考え方）。外側の矩形は2枚の外接矩形を合わせれば厳密に戻るので、保存していない。

読み込み時、存在しないコマを指した組・左右が同じ組・1枚が2つの組に入っている状態は、**組だけ解いて開く**（コマの形は正しいので開けなくする理由がない）。直した内容は `load_warnings` に残る。

### Image（画像）

```json
{
  "id": "img_0007",
  "type": "image",
  "asset": "assets/8f3a1c9e....png",
  "rect": { "x": 12.0, "y": 8.0, "w": 120.0, "h": 90.0 },
  "src_px": [1200, 900],
  "rotation": 0.0,
  "opacity": 1.0,
  "tone": {   /* 黒をトーンに置き換える → 6.27。使っていなければ項目ごと省く */
    "threshold": 30, "angle": 45.0, "pitch": 0.008, "density": 0.35, "thin": 0.002,
    "kind": "stripes",   /* stripes（斜線）/ gray（灰色）/ white（白抜き） */
    "area": { "x": 0.35, "y": 0.0, "w": 0.30, "h": 0.22 }
  },
  "z": 0
}
```

- 拡大縮小は `rect` の書き換えで表現する（倍率を別に持たない＝ズレの元を作らない）
- `src_px` は元画像のピクセル寸法。等比を保つ計算に使う
- `rotation` は矩形の中心まわりの回転角（度、時計回りが正）。`-180`〜`180` に正規化して持つ（→ 6.3）
- `tone` は**暗い所をトーンに置き換える設定**（→ 6.27）。**長さを1つも絶対値で持たない**——`pitch`（斜線の間隔）と `thin`（これより細いものはトーンにしない）は**画像の短辺に対する割合**、`area`（絞る矩形）は**画像に対する割合**。px で持つと、画面用の縮小版と書き出しの原寸で見た目が変わる。`area` を省くと画像全体。**項目ごと無い作品はこの機能の追加前と同じ内容のまま**
- `tone.kind` は**置き換えた先の見た目**。**省いたら `stripes`（斜線）**として読むので、種類を足す前に保存した作品がそのまま開ける。`gray` では `angle` と `pitch` が、`white` ではさらに `density` が絵に出ないが、**値は消さずに持ち続ける**（斜線へ戻したときに調整が返る）

### Balloon（フキダシ）

```json
{
  "id": "bal_0002",
  "type": "balloon",
  "style": "ellipse",
  "rect": { "x": 30.0, "y": 20.0, "w": 45.0, "h": 28.0 },
  "fill": "#FFFFFF",
  "border": { "width": 0.4, "color": "#000000", "visible": true },
  "tail": {
    "enabled": true, "tip": [55.0, 52.0], "width": 6.0, "root_y": null,
    "shape": "bubbles" /* 丸い飛びしっぽのときだけ持つ。省くと三角（→ 6.21） */
  },
  "attached_panel_id": "panel_0003",
  "z": 10
}
```

`style` は `"ellipse"`（丸い_フキダシ）、`"jagged"`（ギザギザ_フキダシ）、`"spiky"`（トゲトゲ_フキダシ → 6.32）、`"wavy"`（ふわふわ_フキダシ）、`"rect"`（四角_フキダシ → 6.20）、`"cloud"`（雲_フキダシ → 6.22）の6種。

**保存形式に書くのは左の値で、画面に出る呼び名とは別に持つ。** 呼び名を変えても保存形式は変わらないので、既にある作品がそのまま開ける（→ 6.4 の改名）。

- `tip`（しっぽの先端）はフキダシからの相対ではなく**ページ座標**。しゃべっている人物を指すものなので、フキダシを動かしても付いて回らない
- `shape` はしっぽの形。`"bubbles"` が**丸い飛びしっぽ**（心の声・独り言 → 6.21）で、省くと三角。**フキダシの種類とは別に持つ**ので、丸い・ふわふわなどと自由に組み合わせられる。`width`（付け根の幅）は三角のときだけ効く
- `root_y` は**しっぽの付け根の縦位置**を、フキダシの高さに対する割合で持つ（`-1.0` が上端、`0.0` が中央、`+1.0` が下端）。`null` は自動＝先端の向きに合わせる。**長さではなく割合**なのは、フキダシの大きさを変えても付け根が同じ場所に残るようにするため。範囲外の値は読み込みで弾く（黙って直すと、保存のたびに形が変わる）

### Text（セリフ）

```json
{
  "id": "txt_0005",
  "type": "text",
  "content": "セリフ本文",
  "rect": { "x": 195.0, "y": 136.0, "w": 230.0, "h": 130.0 },
  "font": { "family": "源ノ角ゴシック", "size_px": 21.0, "bold": false },
  "align": "center",
  "direction": "horizontal",
  "attached_panel_id": "panel_0003",
  "attached_balloon_id": "bal_0002",
  "z": 11
}
```

**`direction` フィールドを最初から持たせる**（MVP では `"horizontal"` のみ）。
将来 `"vertical"` を足すときに保存形式を変えずに済ませるための担保。

`attached_balloon_id` は**フキダシへの紐づけ**で、コマへの紐づけとは別に持つ（理由は 6.5）。項目が無い作品は「紐づけなし」として読む。

### Sticker（マーク → 6.14）

```json
{
  "id": "stk_0004",
  "type": "sticker",
  "kind": "exclaim",
  "asset": "assets/2b7c5f10....png",
  "rect": { "x": 150.0, "y": 60.0, "w": 240.0, "h": 240.0 },
  "src_px": [384, 384],
  "rotation": 0.0,
  "opacity": 1.0,
  "attached_panel_id": "panel_0003",
  "z": 0
}
```

`asset` / `rect` / `src_px` / `rotation` / `opacity` は `Image` と同じ意味。違いは**置き場所**で、コマの子ではなく `Page` 直下（`floating`）に入る＝**コマ枠で切り抜かれない**（→ 6.14）。

- `kind` は**どの組み込み素材から作ったか**の控え。**描画には使わない**（描くのは `asset`）。画面の呼び名を引くためだけに持つ
- `kind` は選択肢を固定しない**自由な文字列**として読む。素材が増えたあとの作品を古いアプリで開いたとき、知らない値で読み込みごと断るのは重すぎる。表に無い値は「マーク」と呼ぶだけにする
- `rotation` は**常に 0**。傾きは素材の PNG 側に焼き込む（→ 6.14）
- 項目が無い＝この機能より前の作品、で矛盾なく読める。`floating` に `sticker` が1つも無いだけなので、**既にある作品の project.json は一切変わらない**

### assets/ の扱い

- ファイル名は**画像内容の SHA1 ハッシュ**（内容から作る指紋のような文字列）にする → 同じ画像を何度貼っても実体は1つ、重複保存が自動で防げる
- どこからも参照されていないアセットは `assets/_unused/` へ移動する（いきなり削除しない）
- **この整理は保存時に自動で行わない。** 利用者が「未使用ファイルを整理」を選んだときだけ実行する。保存のたびに動かすと、画像を消した直後に保存 → Undo で戻す、という手順で参照が切れる（実体が `_unused/` へ移った後なので復活できない）
- **整理を実行すると、その場で Undo の記録を空にする**（2026-08-08 に発見して追加）。保存のたびに動かさなくても、Undo の記録自体は保存をまたいで積み上がっているため、「保存 → 整理」のあとで整理より古い手まで Undo を遡ると、そちらが参照していた画像は既に実体が `_unused/` へ移った後で同じ壊れ方をする。整理の直後に記録を空にすることで、この組み合わせ自体を起こらなくした。整理した画像そのものは削除ではなく移動なので、フォルダから手で戻すことはできる

---

## 6. 機能要件

### 6.1 ページ → **実装済み（2026-08-03）**

| 機能 | 内容 |
|---|---|
| ページを追加 | `Ctrl+Shift+N`。既定サイズで**末尾**に足し、そのページへ移る |
| ページを挿入 | `Ctrl+Shift+I`。既定サイズで**表示中のページの前**に差し込み、そのページへ移る |
| ページ削除 | 「ページ > ページを削除...」。**確認ダイアログあり**。最後の1ページは消せない |
| ページ順変更 | 左のサムネイル一覧をドラッグ。`Ctrl+Shift+PgUp` / `Ctrl+Shift+PgDown` でも1枚ずつ動かせる |
| サムネイル一覧 | 左のドック。クリックでそのページへ移る。中身が変わったページだけ描き直す |
| ページ番号で移動 | 見出しの「ページ [2]/9」の番号を押すと入力欄になる。番号を打って Enter でそのページへ。Esc で取り消し |
| ページサイズ | 「ページ > ページサイズ...」。A4 相当 / B5 相当 / カスタム（px 指定）。表示中のページのみ／全ページを選べる |
| ページ送り | `PgUp` / `PgDown`、道具箱のボタン、一覧のクリック |

#### 「追加」と「挿入」を分ける

当初は「ページを追加」1つで、**表示中のページの次**に入れていた。これをやめ、行き先の決まった「追加（末尾）」と、位置を狙う「挿入（表示中のページの前）」の2つに分けた。

理由は、**一覧のカーソルがどこにあるかを常に意識しているわけではない**こと。ページを見て回ったあとにそのまま追加すると、意図しない場所に新しいページができる。「追加」がどこを見ていても末尾に入るなら、うっかりでは事故が起きない。位置を狙うときだけ「挿入」を選ぶ、という手数の差が、そのまま注意の差になる。

**挿入は「次」ではなく「前」。** 差し込んだページが表示中のページの番号を引き継ぎ、それまでのページは1つ後ろへ下がる（表計算の「行の挿入」と同じ向き）。「このページの手前に1枚足したい」が、ネームを描き足すときの実際の要求に合う。

履歴の名前も「ページの追加」「ページの挿入」で分けてある。Undo の表示（「元に戻す: ページの挿入」）で、どちらを戻すのか分かるようにするため。

#### 並び順の出所は `Project.pages` ひとつに絞る

一覧の並べ替えは**Qt に項目を動かさせない**。落ちた位置だけを受け取り、モデルを直してから一覧をモデルから作り直す。

一覧側にも並び順を持たせると、両方が同じことを覚えている状態になり、**Undo で戻したのに一覧だけ動いたまま**という食い違いが作れてしまう。追加・削除・並べ替え・Undo のどれで変わっても、追いつく経路は `sync()` の一本だけにしてある。

#### 表示中のページは番号ではなく id で追いかける

削除と並べ替えでは番号がずれる。番号のまま留めると、**動かした直後に別のページが表示される**。並べ替えでは動かす前に見ていたページの id を控え、並べ替え後にその id を探し直して表示する。

削除では、消した位置に繰り上がってきたページを表示する（末尾を消したときだけ1つ前へ戻る）。ページを消したあとに何も表示されない番号が残らない。

同じ理由で、**ページを切り替える処理は「番号が同じでも必ず知らせる」経路を別に持つ**。`set_page_index` は同じ番号なら何もしないので、これだけで済ませると削除・並べ替えのあとに前のページを描いたまま止まる。

#### 最後の1ページは消せない

ページが 0 枚になると、表示するものもコマの置き場所も無くなる。`page_count <= 1` のときは確認ダイアログも出さず、状態表示で知らせるだけにしてある。

削除は**メニューからだけ**にした。`Delete` キーには割り当てない。コマ1つの削除と違い、ページ1枚には積み上げた作業がまるごと乗る（Undo では戻せるが、気づかないまま進むほうが痛い）。

#### 大きさを変えても、置いてあるものは動かさない

小さい用紙に変えると、それまで紙の上にあったコマやフキダシが外へ出る。**自動で詰め直さない。** 位置は利用者が決めたもので、直し方（縮める／動かす／サイズを戻す）も場面ごとに違うため。代わりに、はみ出した数を数えて状態表示に出す。

大きさを選ぶと `default_page_size` も合わせる。**次に足す1枚だけ前の大きさ**、という食い違いを作らないため。

決まった用紙（A4 / B5）を選んでいる間は、幅と高さの入力欄を触れなくしてある。数字だけ書き換えられると「A4 と書いてあるのに A4 でない」状態が作れる。

#### サムネイルは本画面と同じ描画処理を通す

用紙・コマ・画像・フキダシ・セリフを描く処理を `ui/render.py`（`PageRenderer`）へ切り出し、本画面のシーンと一覧が同じ経路を通るようにした。別々に書くと、片方だけ直したときに一覧と本画面が食い違い、しかも気づきにくい。**選択枠・つまみ・下書きの矩形は入れない**（作品の一部ではないので、一覧にも書き出しにも出したくない）。Day 27 の PNG 書き出しもこの経路に乗る。

描き直しは**中身が変わったページだけ**。ページの保存形式そのものを指紋として持ち、変わっていなければ前のサムネイルを使い回す。保存に載らないものは見た目にも出ないので、この指紋で取りこぼしが出ない（Undo が同じ性質に乗っているのと同じ理由 → 6.8）。

サムネイルの枠は全ページで同じ大きさにし、ページごとの縦横比はその中に収めて描く。ページごとに枠を変えると、大きさの違うページが混ざったときに一覧の行がガタガタになる。

#### 一覧の幅と番号の位置

**一覧の幅は中身ぴったりに固定する。** 1行の幅（番号の欄＋余白＋縮小画像）に、枠・項目の間隔・スクロールバーのぶんだけを足した値で固定し、それ以上は広げない。用紙を見る中央をできるだけ広く取るため。脇に余らせた幅は、作業している間ずっとメインを狭め続ける。

縦のスクロールバーの幅は**出ていないときも確保する**。ページが増えて出てきた瞬間に一覧の幅が変わると画面全体が揺れるうえ、確保しないとスクロールバーが縮小画像の右端に重なる。

**ページ番号は縮小画像の左**に、右詰めで縦に揃える。既定の並び（アイコン → 文字）だと番号が画像の右に出るうえ、画像の幅はページの縦横比で変わるので、番号の位置が行ごとにずれる。ページを探すときは番号を縦に目で追うため、行の左端で揃っているほうが速い。そのために一覧の1行は自前で描いている（背景と選択の色は既定の描き方に任せ、配色を変えたときにここだけ取り残されないようにする）。

#### 見出しの「ページ [2]/9」から番号で飛べる

一覧の見出しに**表示中のページ／総数**を出す。何枚中の何枚目かを、画面下の状態表示まで目を往復させずに読めるようにするため。番号の部分を押すと入力欄に変わり、打ち込んで Enter でそのページへ移る（Esc、または欄から離れると取り消し）。ページが増えるほど、一覧をスクロールして探すより速い。

- **移動は Enter を押したときだけ。** 欄から離れただけでは動かさない。触っただけで飛ばされると、番号を確かめるつもりで押せなくなる
- **入力欄の受け付ける範囲をページ数で絞らない。** 絞ると Qt は範囲外の数字を「入力途中」と見て **Enter を握り潰す**。押しても何も起きない状態になり、なぜ動かないのか分からない。数字だけに限ったうえで、範囲の確認は決定したときに行い、外れていれば「1 〜 9 のページ番号を入力してください」と言葉で返す。信号を直接出す検証ではここを見逃すので、本物のクリックとキー入力で通しの検証も置いてある
- **表示と入力で幅を変えない。** 押した瞬間に見出しが動くと、狙いが外れる
- 見出しはドックの題名ではなく**自前の部品**にしてある。題名には入力欄を置けないうえ、題名の文字を書き換えると Qt は**表示メニューの項目名まで同じ文字に書き換える**ため。代わりに、既定で付いてくる「切り離す」「閉じる」のボタンは自前で並べている

### 6.2 コマ ★最重要・最も時間をかける → **実装済み（2026-08-02）**

| 機能 | 内容 |
|---|---|
| コマ追加 | `P` でクリック（既定の大きさ）またはドラッグ（好きな大きさ）。1つ作ると選択の道具に戻る（→ 6.9）／ページ全面コマを1つ作る（Ctrl+Shift+A） |
| コマ削除 | 中の画像も一緒に消える。紐づいたフキダシ・セリフは紐づけだけ外して残す |
| コマ分割 | `H`（横に分割）/ `J`（縦に分割）/ `K`（斜めに縦割り → 6.10）でコマの上をなぞると分割線が追従し、クリックで確定。隙間（ガター）を設定値で空ける |
| サイズ変更 | 8方向のつまみをドラッグ。最小の辺で止まり、反対側を追い越して裏返らない |
| 位置変更 | 本体をドラッグ。紐づいたフキダシ・セリフ、中の画像、しっぽの先端が追随する |
| 枠線 | 線幅・色・表示/非表示（コマごと）。太さは px なので拡大しても用紙に対する太さは変わらない |
| 吸着 | ページ端・基本枠・他のコマの辺、および**他のコマから隙間ぶん離れた位置**に吸い付く |
| 重なり順 | 重なっているコマを手前／奥へ入れ替える（→ 6.24） |

削除確認のダイアログは付けていない。Undo が効くので、確認を挟むより戻せるほうが速い。

#### コマ削除でフキダシを巻き添えにしない

コマを消しても、紐づいていたフキダシ・セリフは残す（紐づけだけ外す）。セリフはコマより手間がかかっているので、コマ割りをやり直すたびに書き直すことになるのを避ける。

### 6.3 画像 → **実装済み（2026-08-02、回転は 2026-08-05、差し替えは 2026-08-05）**

| 機能 | 内容 |
|---|---|
| Ctrl+V | クリップボードの画像を、選択中のコマに貼り付け |
| ドラッグ&ドロップ | コマの上に落として配置。**ファイル・絵そのもの・ブラウザからの住所**の3通りを受ける。複数まとめて落とせる（**全部そのコマに入る**） |
| ファイル画像 読み込み | メニューから選択。**既にある絵は消さず、上に重ねる**（背景＋キャラ） |
| 差し替え | 右クリックの「この画像を差し替え...」。読み込んだあと前の絵を消す。**重なり順は引き継ぐ** |
| 選択 | コマの中を**ダブルクリック**すると、そのコマに入って画像を選ぶ。Esc でコマに戻る。**もう一度押すと、同じ場所の次のものへ**（→ 6.25） |
| 移動 | ドラッグ |
| 拡大縮小 | つまみをドラッグ（**Shift で等比**）。角のつまみを掴むと、状態表示に Shift の案内が出る |
| 回転 | 選択枠の上に出る丸いつまみをドラッグ（**Shift で 15度刻み**）。右クリックの「回転をリセット」で 0 に戻す |
| コマにフィット | Ctrl+Shift+F。コマを埋めるサイズに一発調整。**傾きは保つ** |
| クリッピング | コマのポリゴンの形に自動で切り抜いて表示。斜めのコマでもそのまま効く |
| 削除 | Delete。コマは残る。実体（assets/）は消さない |

#### 画像を選ぶのに1クリックではなくダブルクリックを使う

1クリックで画像が選ばれると、**コマを動かすつもりのドラッグが中の絵だけを動かす**。コマの中はほぼ絵で埋まっているので、これは高い頻度で起きる。1段踏み込む操作を分けておく。

選択中はコマ（青）と画像（橙）で枠の色を変え、状態表示にも「コマを選択中／画像を選択中」を出す。見た目が似ているため、色だけでは足りない。

#### 画像とコマが重ならなくなる操作は弾く（2026-08-08 に追加、2026-08-09 に拡張）

画像を選べるのは**自分のコマの中でだけ**（→ クリッピングの表・6.25）。コマの外にはみ出した部分は切り抜かれて見えておらず、そこを押しても選べない。

これが移動と組み合わさると、**画像がコマと1pxも重ならなくなるまでドラッグで押し出せてしまい**、そのあとはどこを押しても選べない「見えない孤児」になっていた（本人の指摘 2026-08-08）。複製（→ 6.15）でも、コマの縁に寄せた画像から同じ状態が作れた。Undo でしか戻せず、保存形式にもモデルにも残り続けるので、利用者から見ると「画像が消えた」ことになる。

- **重なりが完全に無くなる操作だけを弾く。** コマの縁を大きく越えるはみ出し自体は今までどおり許す（斜めの境界を動かして絵が隠れる、というのは元から許容している挙動 → 6.10）。弾くのは「二度と選べなくなる」ときだけ
- **弾いたら理由を状態表示に出し、その場に留める。** トーンの範囲を絵の外へ出したときと同じ考え方（→ 6.27）。黙って引き戻すと、なぜ動かなかったのか分からない
- **判定はコマの外接矩形どうしの重なりで見る。** ふつうの矩形のコマではこれがそのままコマの形と一致するので正確。斜めに割ったコマ（→ 6.10）だけは外接矩形が実際の多角形よりわずかに広く、理論上は「重なっているのに実は選べない」余地が残るが、それでも「制約なし」だった今までより安全で、万一のときは Undo で戻せる

##### 動かす側は画像とは限らない（2026-08-09 に追加）

最初に塞いだのは移動と複製、つまり**画像の側が動く**経路だけだった。同じ状態は残る2つからも作れる。

| 経路 | どちらが動くか | 断り方 |
|---|---|---|
| 画像の移動・複製 | 画像 | 「コマの外まで出ると選べなくなるので、そこまでは動かせません」 |
| 画像の大きさ変更 | 画像 | 同上（「その大きさにはできません」） |
| **コマの大きさ変更** | コマ | 「中の画像がコマの外へ出て選べなくなるので、そこまでは変えられません」 |

- **大きさ変更でも矩形は向こう側へ回り込む。** つまみを反対の辺より先へ引くと、正規化した結果がコマの外に出る。「動かしていないから安全」ではない
- **コマの大きさ変更は中の画像を動かさない**（→ 4章 `set_panel_rect`。絵に対する窓の大きさを変える操作なので、中身が付いて回らないほうが扱いやすい）。だからコマだけが画像の外へ出られる
- **コマ側で断るときは「中の画像が」と言う。** 止まったのが押しているつまみの側ではないので、そう言わないと理由に見当が付かない
- 斜めの組（→ 6.10）は、作り直したあとの1枚ずつの形が事前に分からないので、**組の外側の矩形**で見る。外側にも掛からない画像だけを弾くぶん甘いが、斜めのコマではもともと外接矩形で近似している

#### 等比リサイズは、使う直前に案内を出す

Shift を押しながらのリサイズは、知らなければ一生使われない。そこで**画像の角のつまみを掴んだ時点**で状態表示に「Shift キーを押しながらドラッグで元の縦横比に戻す」と出す。押している最中は「元の縦横比に戻しています（Shift）」に変える。効いているか分からないと、押したつもりで歪んだまま確定してしまう。

角のつまみに限るのは、縦と横が同時に変わるのがそこだけだから。辺のつまみやコマの伸縮でも出すと、案内が常に出ていて読まれなくなる。

（2026-08-08 に文言を修正。「維持」ではなく`src_px`（元画像のピクセル寸法、→「Image（画像）」節の説明）基準で**元の比率に戻す**のが元々の仕様だったため、案内文を実際の挙動に合わせた。挙動そのものは変更していない。）

#### 置いた直後は「収める」、フィットは「埋める」

貼り付け・ドロップの直後は、絵の**全体が見える大きさ**でコマの中央に置く。いきなり埋めると、絵のどこが切れているのか分からないまま次に進んでしまう。埋めたくなったら Ctrl+Shift+F を押す、という順序にした。

#### ドロップは3通りの落とされ方を受ける（2026-08-07）

落とされ方は相手のアプリが決めるので、こちらでは選べない。**手前ほど確実で速いので、この順に見る。**

| 順 | 落とされ方 | どこから来るか |
|---|---|---|
| 1 | 手元のファイル | エクスプローラー |
| 2 | 絵そのもの | 画像を持たせて渡してくるアプリ |
| 3 | 住所（URL）だけ | ブラウザ。取りに行かないと絵が手に入らない |

ブラウザは 2 と 3 の両方を渡してくることがある。**2 を先に見れば、手元にある絵を捨ててわざわざ取りに行く、という無駄が起きない。**

##### 取りに行く間は画面を止める

上限10秒で切り、待っている間は砂時計と「取り込んでいます…」を出す。裏で取って後から差し込む形にすると、**落ちたのか落ちていないのか分からない時間**ができ、その間にもう一度落とされる。

##### 取りに行くのは落とし先が決まってから

コマの外に落としたときは通信しない。先に取ってしまうと、置き場所を外して断られるたびに通信が走り、**やり直すほど待たされる。**

##### 住所は拡張子で絞らない

配信元が住所に拡張子を含めないことは普通にある（`.../photo?id=1`）。そこで弾くと落とせる絵のほうが少なくなるので、画像かどうかは**取ってきてから**判定する。代わりに、絵ではないただのリンクを落とすと、一度取りに行ってからエラーになる。

##### 取りに行くのは http / https だけ。転送先も含めて

`file:` を通すと、**住所を落とすだけで手元のどのファイルでも読めてしまう。** 入口だけ見ても足りない。最初は https でも途中で別の種類へ連れて行かれうるため、転送先も同じ規則で見る。受け取る大きさは 64MB まで（上限が無いと、動画のような大物を際限なく溜め込む）。

#### 落とし先をアプリが決めることは範囲外（2026-08-07）

次の3つは**やらないと決めた**。

| やらないこと | 今どうなるか |
|---|---|
| 複数枚を隣のコマへ順に配る | まとめて落とした絵は**全部そのコマに重なる** |
| ページ一覧へ落として、枚数ぶんのページを作る | 一覧は並べ替え専用。落としても何も起きない |
| コマの外に落としたとき、そこに新しいコマを作る | 「コマの上に落としてください」と案内するだけ |

**どれも「落とし先をアプリが決める」話**で、コマ割りは作者が決めるものとして全体を組んである（→ 6.2）。落とした枚数でコマやページが増えると、**意図していない構成が勝手にでき、それを消す作業が要る。** まとめて落とすのは「同じコマに素材を重ねる」使い方のほうが多く、勝手に配られると毎回それを直すことになる。

配りたいときは、コマを選んでから1枚ずつ落とす。手数は増えるが、**どこに入るかが落とす前に分かる。**

#### 回転だけは「画像編集を持たない」方針の例外にする（2026-08-05）

このアプリは画像編集を持たない方針だが、**傾きだけは実際のコマに当てはめないと合わせられない**。別のソフトで数度傾けて保存し、貼り直して見比べる、という往復が要る。明るさや色調と違い、**傾きは配置の一部**でもあるので、こちら側で回せるようにする。

対象は**コマの中の画像だけ**。マーク（→ 6.14）は従来どおり傾きを素材の PNG に焼き込み、`rotation` は 0 のまま。要求が立ったのは画像の側だけで、両方に広げるとマーク固有の操作規則（常に等比リサイズ）との分岐が増える。

##### モデルは矩形のまま、境目でだけ回す

`rotation` は 3章の時点でデータ形式にあるので、**project.json の形式は変わらない**。既にある作品はそのまま開ける。

一方、回転を持つと当たり判定・つまみ・吸着が一斉に矩形の前提を失う（→ 6.14 でマークについて避けた問題そのもの）。そこで **`Rect`・`resize_rect()`・保存・履歴はいっさい触らず、3か所の境目でだけ角度を掛ける**。

| 境目 | 掛け方 |
|---|---|
| 描くとき | 画像の中心まわりに `+rotation` |
| マウスの位置を受け取るとき | 中心まわりに `-rotation` してから、今までの矩形の判定に渡す |
| 選択枠とつまみを描くとき | 位置に `+rotation`。掴んだ点は `-rotation` して今までのリサイズ計算に渡す |

`rotation` が 0 の画像は掛け算が恒等になり、**今までと完全に同じ経路を通る**。傾けていない作品に影響を出さないための線引き。

コマの形での切り抜きはコマ側に掛かっているので、回転の影響を受けない。斜めのコマでもそのまま効く。PNG 書き出しは描画経路が本画面と共通なので、そのまま追随する。

##### つまみも一緒に傾ける

見た目だけ回して選択枠を水平のまま残すほうが安い。だが**絵は傾いているのにつまみは傾いていない**状態になり、掴む場所と絵の角がズレる。傾きが大きいほど違和感が出るので、つまみ側も回す。

##### 傾いた状態のリサイズは、中心のズレを補正する

回転の中心が矩形の中心なので、**幅を変えると中心も動く**。局所座標で計算しただけだと、画面上では固定したはずの反対側の角まで動いて見える。計算のあとに平行移動で補正する。

見た目にはっきり出るのに**傾き 0 では絶対に再現しない**種類のズレなので、テストで固定する。

##### 傾いている間は吸着を切る

吸着は他のコマの縦横の線に吸い付く仕組みで、傾いた絵に効かせても意味を成さない。`rotation` が 0 でないときは働かせない。

##### 「コマにフィット」は傾きを保ったまま埋める

外接矩形（傾いた絵をちょうど囲む、傾いていない四角）がコマを覆う大きさにする。傾きを 0 に戻す案もあったが、**調整した傾きが黙って消える**のは押した結果として予想できない。「埋める」の意味は変えない（→ 上）。

##### 刻みは Shift のときだけ

主な用途が「数度の傾きを合わせる」なので、既定は自由角度。`Shift` を押している間だけ 15度刻みにする。等比リサイズの `Shift`（→ 上）とは**掴むつまみが違う**ので取り合いにならない。

ドラッグ中は現在の角度をその場に出す。コマの寸法表示と同じ描き方で、何度傾いているか分からないまま確定するのを防ぐ。

#### 差し替えは「重ねる」と別の項目にする（2026-08-05）

1コマに画像は**何枚でも入る**。背景の絵の上にキャラの絵を置く使い方があるため、読み込みが「入れ替え」を兼ねてはいけない。かといって、絵を描き直して貼り直すたびに「読み込む → 前の絵を探して消す」の2手を踏むのも多い。そこで**別の項目**として、右クリックに「この画像を差し替え...」を出す。

出る場所は2つ。**コマを選んだままカーソルの下の画像に効く形**（「この画像を削除」と同じ理由 → 6.12）と、ダブルクリックで踏み込んで画像を選んだ状態。後者に無いと、いったん Esc でコマへ戻る手数が挟まる。

##### 重なり順（z）は元の画像から引き継ぐ

「読み込んでから前の絵を消す」をそのまま書くと、新しい絵が**末尾に足される＝一番手前**になる。背景を差し替えただけで**キャラの手前へ出てしまう**ので、元の画像の `z` をそのまま渡す。

##### 大きさは置いたときと同じ「収める」から始める

元の矩形を使い回すと、縦横比の違う絵で歪む。差し替えは**絵そのものが変わる**操作なので、位置と大きさは置き直しと同じ扱いにして、埋めたければ Ctrl+Shift+F を押す（→ 上の「置いた直後は収める」）。

##### 取り込みが先、削除が後

壊れたファイルを選んだときに**元の絵が消えたまま何も入らない**状態を作らない。展開に失敗すれば入口で止まり（→ 下の「壊れた画像は入口で止める」）、コマの中身は手つかずで残る。差し替えは Undo 1手（「画像の差し替え」）で元に戻る。

#### 保存前に貼り付けられるようにする

新しい作品はまだフォルダを持たないため、画像を書く場所が無い。「先に保存してください」と断ると、この道具の主動線である「クリスタから Ctrl+V」がいきなり止まる。そこで **`PendingAssets` がメモリ上で預かり、保存時に `assets/` へ書き出す**。参照文字列（内容の SHA1）は預かり時に確定し、書き出し後も変わらないので、project.json の中身は保存の前後で同じになる。

保存では**画像の実体を先に、project.json を後に**書く。逆順だと、途中で落ちたときに実体の無い参照を持つ project.json が残る。この順なら最悪でも参照されない画像が余るだけで、そちらは「未使用ファイルを整理」で片付く。

#### 壊れた画像は入口で止める

`assets.py` はバイト列しか見ないので、署名だけ正しく中身が壊れたファイル（`tests/fixtures/broken.png`）を通してしまう。内容ハッシュがファイル名である以上、一度入れると人が見分けられなくなる。取り込みは必ず `manga_layout/images.py` を通し、**実際に展開できたものだけ** `assets/` へ渡す。

#### 表示は縮小版、原寸は書き出しのときだけ

クリスタからの貼り付けは実測 2048×2048（419万画素）。原寸のまま毎回描くと拡大縮小とスクロールがそのぶん重くなるため、画面には長辺 1600px までに縮めた版を使う（`images.PREVIEW_MAX_PX`）。縦横比の計算には**縮小前**の `src_px` を使う。ここを取り違えると絵が歪む。

縮小版はメモリ上のキャッシュに持ち、ディスクには書かない。**展開に失敗したことも覚える**。覚えずにいると、壊れた画像が1枚あるだけで描き直しのたびに展開を試して画面が固まる。

### 6.4 フキダシ → **実装済み（2026-08-02）**

| 機能 | 内容 |
|---|---|
| 種類 | 丸い_フキダシ（`B`）/ 雲_フキダシ（`W`、→ 6.22）/ トゲトゲ_フキダシ（→ 6.32）/ ふわふわ_フキダシ（→ 6.13）/ ギザギザ_フキダシ（`G`）/ 四角_フキダシ（→ 6.20）の6種。あとから相互に変更できる。**並ぶ順は使う頻度**（→ `BALLOON_STYLE_LABELS`。道具箱・道具メニュー・フキダシメニュー・種類を変えるメニューが全部この順） |
| 追加 | クリックで既定の大きさ、ドラッグで好きな大きさ。1つ置くと選択の道具に戻る（→ 6.9） |
| 移動・リサイズ | コマ・画像と共通の操作系（ドラッグ、8方向のつまみ） |
| しっぽ | 丸い印をドラッグして向きと長さを決める。出す/消すの切り替え可。付け根の幅は設定値 |
| しっぽの形 | 三角 / 丸い飛びしっぽ（心の声・独り言 → 6.21）。「しっぽを丸くする」で切り替える。**種類とは別に持つ**ので自由に組み合わせられる |
| しっぽの付け根 | ひし形の印を**上下にドラッグ**して、生え際を先端側の輪郭に沿って寄せる。大きく向きを変えるのはメニューの「しっぽを上/右/左/下へ」（先端ごと回る） |
| コマへの紐づけ | 作成時、中心が乗っているコマに自動で紐づく。手動で解除・付け直しができる |
| 削除 | Delete。コマを消してもフキダシは残る（紐づけだけ外れる） |

#### 本体としっぽを1つの輪郭に合成してから描く

別々に描くと**継ぎ目に枠線が残り**、しっぽが後から貼った三角形に見える。塗りを重ねて線を隠す手もあるが、半透明や色付きの塗りで破綻する。図形を合成（`QPainterPath.united`）してから一度だけ縁取れば、どの配色でも正しい。

**しっぽの付け根は、必ず輪郭より内側に置く。** ギザギザの谷にあたる位置に付け根を置くと、本体と三角形が離れて継ぎ目に隙間が空く。実測で、付け根を輪郭の上に置いた場合は 4 画素ぶんコマの下地が透けた。付け根の半径は楕円で 95%、ギザギザでは谷の位置（`1 - jagged_depth`）にしてある。ふわふわ・トゲトゲ・雲も同じ形で、それぞれの谷の位置に置く。

#### 付け根の幅は、左右に伸ばしたときの太さを基準にする（2026-08-06）

**楕円は向きによって「輪郭を1ラジアン進んだ長さ」が違う。** 付け根の幅（`Tail.width`）を、どの向きでも同じ半角（角度の広がり）に直していたため、縦長の吹き出しでは上下だけ幅が半分以下に痩せていた（実測 333×496 で左右 51.8px、上下 23.4px）。

半角をどの向きでも揃える（設定の px 値どおりに全方向を統一する）直し方も検討したが、それだと**左右のしっぽが今より細くなる**（51.8px → 35px）。既にある作品の見た目が変わるうえ、左右は困っていないとの指摘があり、**左右の太さは変えず、上下だけ左右に合わせて太くする**形にした（`_tail_base_half_angle`）。左右（角度0）での見える幅を基準に、他の向きは「半角 × その向きで進む速さ」が左右と同じ値になるよう半角を決め直す。

#### しっぽの付け根は上下にずらせる（`root_y`）

同じ位置・同じ向きのしっぽでも、**どの高さから生えるか**で印象が変わる。実際の漫画でもよく使い分けるため、付け根を上端〜下端の範囲で動かせるようにした。

- 掴み所は**ひし形**の印。先端（丸）や大きさのつまみ（四角）と形を変えて、掴む前にどれを動かすのか分かるようにしている。**判定は印より 5px 広い**（`TAIL_ROOT_HANDLE_PX`）。ひし形は同じ大きさの四角より面積が半分しかなく、狙って押しても外れることが多かった
- **縦だけ動く。** 付け根は輪郭の上を滑るので、横位置は高さから決まる。マウスの左右を拾うと形が飛ぶ
- 左右どちら側に付くかは**先端のある側**に従う。反対側から生えると、しっぽがフキダシを横切る
- 上端・下端（±1）では左右の解が同じ点に重なる。ここが不連続だと、操作中に形が跳ねる
- 既定は自動（`null`）＝先端の向きに合わせる。それまでの見た目と変わらないので、`root_y` を足す前に保存した作品もそのまま開ける

#### 付け根は先端の向きから 40 度までしか離せない（2026-08-05）

**しっぽは付け根から先端へ引いた三角形で、見えるのは本体からはみ出した部分だけ**（→ 上の「1つの輪郭に合成」）。どこから出て見えるかを決めているのは**先端の向き**であって、付け根ではない。付け根を先端の反対側へ回すと三角形のほとんどが本体に隠れ、外に出るところが針のように細くなる。

そのため「付け根を上端へ」を選んでも、しっぽは下から出たまま細くなるだけで、**動いていないように見えた**。実測（333×496 のフキダシ、既定の長さのしっぽ、先端は真下）では、見えているしっぽの面積が、ずれ 26 度で 85%、37 度で 69%、46 度で 52% まで落ちた。先端が真下のとき「下端へ」は自動と**同じ点**なので、絵は1画素も変わらなかった。

対処は2つに分けた。

- **微調整（ひし形のドラッグ）** は先端を動かさない。しゃべっている相手を指したまま、生え際だけを左右に寄せる。ただし先端の向きから **40 度**（`TAIL_ROOT_MAX_GAP`）で止める。下端から右へ約 100px（フキダシ幅の3割）まで動かせて、しっぽは痩せきらない
- **大きく向きを変える（メニューの4項目）** は**先端ごと回す**（`tail_tip_turned_to`）。楕円の潰れを打ち消した空間で回すので、しっぽの長さと傾きの具合はそのまま残る。回した先では付け根の高さと先端の向きが一致するので、`root_y` は自動へ戻す。値を残すと、あとで先端だけ動かしたときに古い指定が効いて、また痩せる

**上限は `tail_base_angle` の1箇所でかける。** 画面・サムネイル・PNG 書き出しの3つがここを通るうえ、`root_y` が飛んだまま保存された作品も開いた時点で正しい形になる。読み込みでは弾けない値であることに注意 —「離れすぎ」かどうかは先端との関係で決まるので、保存された時点では判定できない（→ 5章の検証は範囲 ±1 だけを見る）。

メニューの文言を「付け根を上端へ」から**「しっぽを上へ」**に変えたのは、動くのが付け根だけではなくなったため。先端も回ることはホバー中の状態表示に出す（→ 6.14）。

**向きは高さ（`root_y`）ではなく名前で受け取る**（`TAIL_DIRECTIONS`）。高さだけで指すと真横が「上端 0」の1項目になり、**左右どちら側に付くかを先端の現在位置任せにするしかない**。左右に2人が向かい合うコマでは同じフキダシから右も左も指したいので、「右へ」「左へ」を別の項目にした。上下は極なので左右の区別が無く、4項目で全周をまかなえる。

#### ギザギザは楕円の半径を交互に増減させて作る

要件定義 9章の方針どおり。**頂点数は必ず偶数にする**（山と谷を対にする）。奇数だと一周したときに山が隣り合い、そこだけ形が崩れる。山の数と谷の深さは `BalloonSettings` で調整できる。

種類を足すときも、**この経路にだけ形を足す**（→ 6.13 の波形）。`balloon_outline` が種類で分岐する1箇所を通るので、画面・サムネイル・PNG 書き出しの3つが同時に新しい形になる。

#### 呼び名は保存形式と切り離して1箇所に持つ（2026-08-04 に改名）

画面に出る呼び名を **丸い_フキダシ / ギザギザ_フキダシ / ふわふわ_フキダシ** に変えた。「楕円」「波形」だけでは、コマ・セリフと並んだときに何の種類なのか読み取れない。

**保存形式の値（`ellipse` / `jagged` / `wavy`）は変えていない。** ここを一緒に変えると、それまでに保存した作品が開けなくなる。呼び名は画面の都合で変わるが、保存形式は作品の寿命ぶん変わらないほうがよい。

呼び名は `BALLOON_STYLE_LABELS` の1箇所に置き、**道具の名前・メニュー・状態表示・操作後の案内がすべてそこを見る**。書き分けると改名のたびにどれかが古いまま残り、「ふわふわ_フキダシにした」のに「丸い_フキダシを選択中」と出る食い違いが作れる。実際この改名では、1箇所を書き換えるだけで全部が変わった。

改名にあわせて、**右クリックで楕円だけを「ここに吹き出しを追加」と別名で出していた例外をやめた**。「楕円」では何のことか分からなかったための逃げ道だったが、3種とも名前に「フキダシ」が入った以上、例外の理由が消えた。

**画面に出る「吹き出し」もすべて「フキダシ」に揃えた**（メニューバー・削除の項目・各種の案内・Undo の表示）。1箇所だけ変えると、同じ画面の中に2つの呼び方が混ざる。

#### 当たり判定は外接矩形ではなく楕円で行う

矩形で判定すると、フキダシの四隅の**何も無い場所で掴めてしまい**、下のコマが選べなくなる。フキダシはコマより手前にあるため、この取り違えは頻繁に起きる。

#### しっぽを押しても選べる。ただし判定は見た目より細い（2026-08-07 に足した）

しっぽの当たり判定は元々「**選んである**フキダシのしっぽを掴んで向きを変える」ためだけのものだった。選んでいないフキダシのしっぽを押しても何も起きず、**絵が見えているのに反応しない場所**が残っていた（上の 6.5「掴めるのは字の上だけ」と同じ形のズレ）。

`layout.balloon_pick_at` を足し、本体で外れたらしっぽも見るようにした。

**判定は見えている形より細い**（`TAIL_PICK_NARROW` ＝ 0.6）。しっぽは本体の外へ長く伸びるので、見えているとおりの太さで拾うと、**しっぽの下にあるコマや画像が選べなくなる**（6.4 の「四隅の何も無い場所で掴めてしまう」の裏返し）。細くしておけば、狙って押したときだけ拾い、脇をかすめた分は下へ抜ける。

- **細くするのは幅だけで、長さはそのまま。** 重心へ向けて一様に縮めると先端側も引っ込み、いちばん狙いやすい「しっぽの先」が押せなくなる
- 三角は付け根の2点を中央へ寄せ、飛びしっぽは円の半径を縮める。分岐は `tail_body_contains` の1箇所（形ごとに2通り書くと片方を直し忘れる）
- **本体としっぽはフキダシ1つぶんずつ見る。** 先に全部の本体を見てからしっぽを見ると、手前のフキダシのしっぽより奥のフキダシの本体が勝ち、描いてある前後と食い違う
- **既に選んであるフキダシのしっぽを掴むほうは細くしない**（`PageView._tail_body_at`）。下のものと取り合いにならないうえ、こちらは見えている形いっぱいに取ると決めた経緯がある（2026-08-05）
- **セリフの紐づけ先を決める `balloon_at` は本体のまま。** しっぽの上に置いた文字までフキダシの中身として扱われては困る

#### フキダシを動かしてもしっぽの先端は動かない

先端はしゃべっている人物を指すページ座標（→ 4章）。フキダシの置き場所を変えても、指す相手は変わらない。一方、**コマを動かしたときは紐づいたフキダシごと先端も動く**。こちらはコマの中身がまるごと移動する操作なので、位置関係を保つほうが正しい。

#### フキダシの道具はコマ追加と違い、どこでも作れる

コマ追加は「何も無いところを押したときだけ」作るが、フキダシは**コマの上に置くもの**なので同じ規則にすると置き場所がほとんど無くなる。押した場所に関係なく作る。1つ作ると選択の道具に戻るので、誤って増え続けることはない。

### 6.5 テキスト → **実装済み（2026-08-02）**

| 機能 | 内容 |
|---|---|
| 追加 | 道具 `T`。クリックで既定の大きさ、ドラッグで好きな大きさ。置いた直後そのまま入力に入る。**1文字も入れずに閉じたら作らない** |
| 入力 | 画面上でその場編集（**選んでいるセリフをダブルクリック**、または選択して `Enter` / `F2`）。**選んでいないセリフのダブルクリックは選ぶだけ** → 下記。Enter で改行、Ctrl+Enter で確定、Esc で取り消し |
| フォント | 種類（フォント選択ダイアログ）・サイズ（px、`Ctrl+.` / `Ctrl+,` で増減）・太字（`Ctrl+B`）。**指定した書式は次に作るセリフへ引き継ぐ**（向きも同じ） |
| 整列 | 左 / 中央 / 右。上下は常に中央（縦書きでは上 / 中央 / 下として効く → 6.11） |
| 向き | **既定は縦書き。** 横書きへは `F7` で切り替え。**セリフごとに持ち、指定した向きは次に作るセリフへ引き継ぐ** → 6.11 |
| 位置 | ドラッグ。**フキダシの上に置いた場合、フキダシに追随する** |
| 削除 | Delete。フキダシを消してもセリフは残る（紐づけだけ外れる） |

折り返しはしない（→ 9章、手動改行のみ）。枠に収まらない字は隠さずに出す。隠すと、はみ出していることに気づけない。縦書きも同じ扱い。

#### 掴めるのは字の上だけ。枠の空いた所は下へ譲る（2026-08-05 に直した）

セリフの枠は既定で 230 × 422px あるが、数文字なら**大半は空いている**。当たり判定を枠の矩形のままにしていたため、その空いた場所までクリックを吸い、**フキダシの中に置いたマークが見えているのに掴めなかった**。

これは 6.4 でフキダシに対して一度解いた問題と同じ形（矩形で判定すると四隅の何も無い場所で掴めてしまい、下のものが選べなくなる）。**セリフだけが矩形のまま残っていた。**

拾うのは**字の並んでいる帯**（`layout.text_ink_bands`）。

| | 帯の取り方 |
|---|---|
| 縦書き | 列ごとに1本。幅は列送り（字の 1.33 倍）、長さはその列の字数ぶん。位置は縦書きの計算（`vertical.layout`）から取る |
| 横書き | 行の高さぶんの帯を1本。**幅は枠のまま** |
| 空のセリフ | **枠全体。** 空のときは点線の枠を描いており、そこが「描いてある範囲」になる |

- **字1つずつではなく帯で拾う。** 字と字・列と列の隙間で下へ抜けると、掴み所が虫食いになる。列送りの隙間はセリフの一部と見るのが自然
- **横書きの幅を絞れないのは、字送りがフォント依存で計算できないから。** 既定の枠は縦長で、余っているのもほとんどが上下なので、これで用は足りる。テスト環境にフォントが1つも無い（→ 6.11）以上、幅を測る判定は検証もできない
- **空のセリフを字の無い扱いにしない。** そうすると作った直後のセリフを選べなくなる。「描いている範囲を拾う」という規則自体は空のときも守られている
- 判定の入口は `layout.text_at` の1つだけなので、選択・ドラッグ・ダブルクリックでの入力・右クリックが同時に新しい規則になる

**マークの四隅（透明な部分）はまだ矩形のまま**（→ 6.14）。原因はセリフ側だったため、6.14 の「掴みにくさが実際に出てから入れる」はそのまま残している。

**2026-08-05 に実機で確認済み**——フキダシとセリフの中に置いたマークを掴めること、字の上を押したときはこれまでどおりセリフが選ばれることの両方。掴み所は目に見えないので、**帯の広さが妥当かどうかは実機でしか確かめられない**（自動テストで押さえているのは「字の上で拾う／字から外れたら譲る」という規則のほうだけ）。

#### 選択枠も同じ範囲に合わせる（2026-08-07 に直した）

上の変更で**掴める範囲だけが狭くなり、選択枠は枠のまま残っていた。** 数文字のセリフでは枠が字よりずっと大きく、**押しても掴めない場所まで枠とつまみが伸びる**。「オレンジの枠が文字より大きすぎて、どこを掴んでいるのか分からない」（本人談 2026-08-07）はこの食い違いそのもので、枠の見た目が実際の操作を裏切っている状態だった。

枠・つまみ・移動と大きさ変更の起点を、すべて**字の並びの外接矩形**（`layout.text_frame`）へ揃えた。入口は `EditorState.selected_bounds` の1つだけなので、4つが同時に新しい規則になる。

- **大きさ変更は、その外接矩形をそのまま枠（`TextObject.rect`）に入れる。** 字は枠の中で寄せに従って置かれるので、外接矩形へ入れ直しても**字は 1px も動かない**——寄せが left / center / right のどれでも、縦書きでも横書きでも同じ。外接矩形の外接矩形は自分自身なので、**掴み直すたびに縮み続けることもない**
- **掴んだだけで動かさなかったときは何もしない**（`ResizeDrag.commit`）。他の型は起点が枠そのものなので `_apply_resize` の「同じ矩形なら帰る」で弾かれていたが、セリフはそこを素通りし、見た目が変わらないまま履歴が1手増えてしまう
- **横書きは高さだけが縮む。** 幅を絞れない理由は当たり判定と同じ（字送りがフォント依存）。既定の枠は縦長で余りも上下に出るので、これで用は足りる
- **空のセリフは枠のまま。** 点線の枠が唯一の掴み所なので、ここを狭めると作った直後のセリフに手が届かない

#### 入力へ入るのは、選んでいるセリフをダブルクリックしたときだけ（2026-08-07 に直した）

以前はセリフの上のダブルクリック1回目から入力に入っていた。**並んだセリフを選び分けるときに、狙いを外したセリフの入力へ飛び込んでしまう。**

質が悪いのは、その場編集の**入力欄が縦書きのセリフでも横書きで開く**こと（Qt に日本語の縦書き入力欄が無いため）。飛び込んだセリフはその場で横書きに化けて見えるので、**縦書きの設定を1度も変えていないのに、変えてしまったように見える**。実際「ダブルクリックで縦書きと横書きが切り替わる」という報告になった（本人談 2026-08-07）。**データは変わっておらず、見えているのは入力欄だけ**であることを、Qt が送る順（押下 → 離す → ダブルクリック）で叩いて確認済み。

1回目は**選ぶだけ**にした。外したときの損が「別のものが選ばれた」だけになる。入力へはもう一度ダブルクリックすれば入る。

- **既に選んでいるセリフなら、1回のダブルクリックで入力へ入る。** 「選んでから打つ」という手順自体は変わっておらず、**狙いが合っているときの手数は今までと同じ**
- **判定は「押される前の選択」から数える**（`PageView._selected_before_press`）。ダブルクリックの手前には必ず押下が挟まり、そこで既にそのセリフへ選び直されている。今の選択を見ると必ず「選んでいた」になり、判定にならない。6.25 の巡回が同じ理由で同じ控えを使っている
- **`Enter` / `F2` は今までどおり。** 手数を増やしたくない場面はこちらで足りる
- **置いたばかりのセリフはそのまま入力に入る**（→ 6.5 の「追加」）。こちらはダブルクリックを経由しない

#### フキダシへの紐づけはコマへの紐づけと別に持つ

`Text.attached_balloon_id` を `attached_panel_id` とは別に用意した。**フキダシはコマの中で単独に動かせる**ので、コマへの紐づけだけを見ていると、フキダシを動かしたときにセリフが取り残される。

移動の連鎖は「コマ → 紐づいたフキダシ → その上のセリフ」の順に伝わる。**同じものを2回動かさないよう、動かした id を控えながら進む。** コマにもその上のフキダシにも紐づいたセリフがあると、二重に動いて位置がずれる。

#### 文字の大きさは px で持つ（2026-08-03 に mm から変更）

Qt のフォントは**整数の画素数**でしか大きさを指定できない。座標系が px なので、そのまま丸めて渡せる。

mm だった頃は 3.5 のような小さな値になり、3 か 4 に丸められて狙った大きさにならなかった。20倍で作って 1/20 に縮める回避策（`TEXT_FONT_SCALE`）を置いていたが、px にしたことで丸ごと不要になった（→ 3章）。

mm 時代の実測では 4 / 8 / 12mm の指定に対し、漢字の墨の高さが指定の 0.83〜0.86 倍だった。この比は**漢字が全角の枠いっぱいには描かれない**ぶんで、単位が px になっても変わらない（21px の指定なら墨の高さは約 18px）。

##### px は画面の点ではなくページの座標（2026-08-03 に表示を補った）

ページの px は**紙の寸法を 150dpi で換算した値**なので（`MM_TO_PX`）、`20px` は画面上の 20 点ではなく、紙の上での**約 3.4mm・約 9.6pt** にあたる。数字の見た目に比べて小さい。

| px | mm | pt |
|---|---|---|
| 20 | 3.39 | 9.6 |
| 25 | 4.23 | 12.0 |
| 30 | 5.08 | 14.4 |
| 42 | 7.11 | 20.2 |

状態表示と操作の案内には**ポイントを併記する**（`MainWindow._size_label`）。px だけだと画面の点の数と取り違え、フォント設定の窓（ポイントで喋る）とも突き合わせられない。

##### フォント設定の窓に今の書式を渡していなかった（2026-08-03 に直した）

`QFontDialog` へ `QFont(family)` だけを渡していたため、窓には Qt の既定である **12pt** が出ていた。**選択中のセリフとは無関係な数字**で、実際の 20px（約 9.6pt）とも合わない。しかも窓で選んだ大きさは捨てていたので、**そこから文字の大きさを変えることができなかった**。

今は大きさと太さを持っていき、選んだ大きさを持ち帰る。窓はポイントでしか喋らないので `PT_TO_PX`（150dpi 換算）で往復する。**画面の解像度で Qt に換算させてはいけない** — 表示倍率 125% の PC では 20px が 12pt に見えるなど、紙の上での大きさと合わなくなる。

`pointSizeF()` は px 指定の `QFont` では -1 を返す。そのまま保存すると**負の大きさで文字が描かれなくなる**ので、その場合は元の値を残す。

##### 窓は広げてから開く（2026-08-07 に追加）

開いたときの大きさを **820 × 620px**（`window.FONT_DIALOG_SIZE`）にしてある。画面より大きくはしない（作業領域に収めるのは起動時の窓と同じ扱い → `_apply_initial_geometry`）。

**Qt の既定では書体の一覧が数行しか見えず**、目当ての書体まで延々と送ることになる。隅を引けば広がるが、**広げられること自体に気づけない**（本人の指摘 2026-08-07）。

そのため `QFontDialog.getFont`（1行で済む静的な呼び方）は使えない。**あちらは窓の大きさを指定できない。** `QFontDialog` を自分で組み立て、`resize` してから開く（`MainWindow._ask_font`）。窓を出す場所がこの1か所になったので、自動テストもここを差し替えて動かしている。

##### 打ち込んで書体を絞り込む（2026-08-07 に追加）

書体は 199 件並ぶ（このPCでの実測）。窓を広げても一度に見えるのは十数件で、**目当ての書体まで送る手間は残る**（本人の要望 2026-08-07）。名前が分かっているときは、打ったほうが速い。

一覧のすぐ上に絞り込みの欄を置き、打った言葉を含む書体だけを残す（`ui/font_dialog.py` の `FontChooser`）。

- **途中の文字でも当たる。** Qt の一覧を打つと先頭からの一致しか拾えず、「メイリオ」を「iryo」で手繰れない
- **大文字小文字・全角半角は区別しない。** これが効くのは「ＭＳ ゴシック」で、あれは**書体名自体が全角**で登録されている。半角で「MS」と打つほうが普通なのに、そのままでは当たらない
- **空白で区切ると、全部を含むものに絞る**（「ud 教科」）。全角の空白も区切りにする（→ 6.30 のメニュー検索と同じ扱い）
- **言い換えの表は持たない**（→ 6.30 の `SYNONYMS`）。一覧には同じ書体が英語名と日本語名の両方で並んでいる（「Yu Gothic」と「游ゴシック」）ので、どちらで打っても何かしらには当たる
- **件数は見出しに出す**（「書体（2件）」）。0件のときは「見つかりません」——一覧が白いまま残るだけだと、**窓が壊れたように見える**
- **Enter で、絞り込んだ先頭の書体を選ぶ。** Enter は OK ボタンへ渡るので、ここで選び直しておかないと**絞り込む前に選ばれていた書体のまま決まってしまう**

###### Qt が組み立てた窓に、後から割り込む

`QFontDialog` は中身が Qt 側で組まれているため、部品を差し替えて足している。置き場所は**今の書体名が出ていた欄**（読み取り専用）で、一覧のすぐ上という打ちに行く位置がそこしかない。選んでいる書体は一覧の反転と見本の文字に出るので、名前の欄が無くなっても分からなくならない。

- **元の欄は隠すだけで、片付けない。** 窓の中身はこの欄を持ったままで、書体が変わるたびに文字を書き込む。消すとそこで落ちる
- **一覧そのものは作り直さず、当たらない行を隠す。** 窓の中身は一覧の**並び**を見て書体を決めているので、入れ替えると選んだ書体が食い違う
- **書字系（Writing System）を変えると一覧が丸ごと入れ替わる。** 隠した行の番号はそのまま残るので、入れ替わったら当て直す
- **思っていた組み方でなければ、絞り込みを付けずに開く。** Qt の版が上がって中身が変わったときに、フォントを選ぶ手段ごと無くなるのがいちばん困る

自動テストは表示装置なしで動くため**書体が1つも並ばない**ので、一覧の中身を差し込んで確かめている（`tests/test_ui_font_dialog.py`）。同じ理由で、Qt が持ち帰る書体名（`selectedFont()`）は既定の `Sans Serif` に置き換わってしまうため、当てる先は一覧で選ばれている行にしてある。**実機での動作は 2026-08-07 に確認済み**（「yu goth」と打って Enter で `Yu Gothic` が決まる）。

#### セリフの既定の書式（2026-08-03 に変更）

新しく作るセリフは **`UD デジタル 教科書体 N` / 42px**（`model.DEFAULT_FONT_FAMILY` / `DEFAULT_FONT_SIZE_PX`）。読みやすさを狙って作られた書体で、Windows 10 以降に標準で入っている。

大きさは**紙の上で 20pt 相当**として決めた。px はページの座標（150dpi 換算）なので数字の見た目より小さく、当初の 20px では紙の上で約 9.6pt しかなくネームの下書きとして読みにくかった（2026-08-03、実機で確認して決め直した）。

##### 枠の既定は縦長にし、文字の大きさと連動させる

クリックだけで置いたときの寸法は**文字の大きさと連動して決まる**。

| | px | 根拠 |
|---|---|---|
| 文字 | 42 | 紙の上で 20pt |
| セリフ枠 | 230 × 422 | 縦書きで 4 列 × 10 文字 ＝ **40 文字**が収まる |
| フキダシ | 333 × 496 | セリフ枠が中に収まる（A4 の幅の 27%・高さの 28%） |

**どちらも縦長にする。** 日本語のマンガは縦書きなので、セリフは少ない列数に長く連なり、フキダシも縦長になる。横長にすると列が余って下だけが詰まり、置くたびに縦へ伸ばし直すことになる。

**文字だけを大きくしない。** 20px → 42px にしたとき枠は 201 × 106px のままで、**6 文字**しか入らなくなった。はみ出した字は消さずに描く仕様なので壊れはしないが、既定のまま使えない。**「クリックで置いてそのまま打てる」が成立する寸法**を既定とする。

フキダシもセリフ枠に合わせて広げる。セリフ枠より小さいままだと、置いた瞬間にセリフがはみ出す。

**書体名のスペースは省けない。** Qt に渡すのは `UD デジタル 教科書体 N`（3か所にスペース）で、`UDデジタル教科書体N` と詰めると一致せず**黙って Tahoma で描かれる**（2026-08-03 実測）。エラーにならないので名前が崩れても気づけない。末尾の `N` は等幅の一族（`NK` は仮名が詰まり、`NP` は全体が詰まる）。太さの `-R` / `-B` は付けない——Qt では同じ一族の style 扱いで、太字は `Font.bold` から切り替わる。

**「作るときの既定」と「読み込むときの既定」は分けてある。** 項目の欠けた `project.json` は、書かれていた頃の既定（`Yu Gothic UI` / 21px、`model.LEGACY_FONT_*`）で読む。同じにすると**アプリを更新しただけで既にある作品の見た目が変わる**。向きの既定（`DEFAULT_TEXT_DIRECTION` → 6.11）と同じ考え方。

#### 指定した書式は次に作るセリフへ引き継ぐ（2026-08-07 に追加）

書体・大きさ・太字を指定すると、**選んでいるセリフだけでなく「次に作るセリフの書式」も同じ値になる**（`EditorState.next_text_font`）。以後の追加はそれを使う。

**以前は選択中の1つにしか効かず、次に置いたセリフは毎回既定へ戻っていた。** 作品の中で書体と大きさが揃っているのが普通なので、置くたびに選び直すことになる。利用者からは「フォントを選んでも反映されない」と見えていた（本人の指摘 2026-08-07）。

- **写す場所は `EditorState.set_text_font` の1か所。** 書式を指定する操作（種類を選ぶ・`Ctrl+.` / `Ctrl+,`・`Ctrl+B`）は全部ここを通る。呼び出し側で写すと、3か所のうち1つを直し忘れたときに「この操作だけ引き継がれない」という説明の付かない差ができる
- **引き継ぐのは「これから作るもの」だけ。** 既に置いてあるセリフは動かさない
- **作品ではなく好みなので `project.json` には載せない。** 別の作品を開いても持ち越し、アプリを閉じると既定へ戻る（`rough_opacity` と同じ扱い）
- **向き（縦書き / 横書き）も引き継ぐ**（`EditorState.next_text_direction`、2026-08-07 に追加）。写す場所は `set_text_direction` の1か所。既定は `DEFAULT_TEXT_DIRECTION`（縦書き）のままで、変えるのは「1つ指定したあと」の話だけ

###### 向きを引き継ぐようにした理由（2026-08-07）

**横書きの箇条書きを作っている最中、セリフを1つ置くたびに縦書きへ戻っていた。** マンガのセリフは縦書きが普通なので既定は縦書きだが（→ 6.11）、書式だけ引き継いで向きが戻ると、使う側からは**「勝手に縦にされる」**としか映らない。実際「ダブルクリックで縦書きに切り替わる」という形の報告になった（本人の指摘 2026-08-07）。

**既にあるセリフの向きが書き換わることは無い。** 横書き5行を順に訂正する流れ（選ぶ → 入力 → 確定を5回）をQtの事象の順どおりに叩いて確認済みで、`direction` は最初から最後まで `horizontal` のまま。**縦書きが出てくるのは「新しく作ったとき」だけ**だった——直すべきはそちらで、既存のセリフを守る処理ではない。

##### セリフを選んでいなくても書式を決められる

「フォントを選ぶ...」は**セリフを選んでいなくても押せる**。そのときは今あるセリフに触らず、「次に作るセリフの書式」だけを決める（`EditorState.set_next_text_font`）。

**以前はここで「先にセリフを選んでください」と断っていた。** 引き継ぎが入ったことで、これは**次の書式を決めるためだけに要らないセリフを1つ置け**という意味になってしまう（本人の指摘 2026-08-07）。

- 窓に出す値は**今の「次の書式」**。選択中のセリフのときと同じで、無関係な既定を見せない
- **作品を1文字も変えないので履歴には積まない。** 決めただけで未保存の印が付かない
- そのぶん `changed` が飛ばないので、状態表示は `MainWindow._refresh_hint` で自分から出し直す
- セリフのメニューで**この項目だけ常に押せる**。他（入力・整列・大きさ・太字）は選んでいる1つへの操作なので、従来どおり選ぶまでグレー

##### 次に何で置かれるかを状態表示に出す

引き継ぐようにすると、**次に置かれる書式は「最後に何を指定したか」で決まる**ため、画面のどこにも出ていないと置いてみるまで分からない。置いてから直すのは選び直すのと同じ手数になる。

| いつ | 状態表示 |
|---|---|
| セリフの道具（`T`）を持っている | `セリフを追加: UD デジタル 教科書体 N 42px（約 20pt） 縦書き` |
| 何も選んでいない | `コマ未選択 / 次のセリフ: ...` |
| セリフを選んでいる | 従来どおり**そのセリフ**の書式（`セリフを選択中: 3 行 / ...`） |

#### 置いたばかりのセリフが空のまま残るなら、追加ごと取り消す（2026-08-07 に追加）

置いたばかりのセリフが**中身の無い枠のまま残る**なら、そのセリフを消し、履歴の「セリフの追加」も捨てる（`History.discard_last`）。抜け方と打った内容の組み合わせで決まる。

| 抜け方 | 打った内容 | どうなる |
|---|---|---|
| 確定（Ctrl+Enter・欄の外を押す） | 空 / 空白だけ | 追加ごと取り消す |
| 取り消し（Esc） | 空 / 空白だけ | 追加ごと取り消す |
| 取り消し（Esc） | 打ってある | **追加ごと取り消す**（2026-08-08 に追加。下記） |
| 確定 | 打ってある | 残る（普通の追加） |

押し間違いで道具を持ったまま画面を押しただけのときに、中身の無い枠が残る。**枠は選んだときしか描かれないので、残っていること自体に気づけない**（本人の指摘 2026-08-07）。書き出してから空のセリフに気づく形になる。

- **確定（Ctrl+Enter・欄の外を押す）と取り消し（Esc）の両方で消す。** どちらで抜けても中身が空なのは変わらず、片方だけ残すと「Esc だと残る」という覚え直しが要るだけになる
- **空白だけの入力も空として扱う**（全角空白を含む）。描いても何も出ないので、枠だけが残るのと区別が付かない
- **やり直し（Redo）には積まない。** 置いた覚えの無いものを呼び戻せても仕方がない。`undo()` と分けて `discard_last()` を用意したのはこのため。**直前の1手の名前が「セリフの追加」でなければ何もしない**——間に別の編集が挟まっていたときに、そちらを消さないための保険
- **対象は置いた直後だけ**（`begin_text_edit(is_new=True)`）。**既にあるセリフを空にして確定した場合は消さない。** 一度消して打ち直す操作を塞ぐことになる
- 先に作ってから入力欄を開いているのは、入力欄が `TextObject` の位置・大きさ・書式を見て開くため。作る前に開くと、どこへどの大きさで出すかを決められない

###### 打ってから Esc で抜けた場合も取り消す（2026-08-08 に追加）

**打った文字が消えるのは仕様どおり**（Esc は取り消し）。問題は、そのあと**追加そのものが取り消されず、中身の空のセリフが1つ残っていた**こと。上の「空のまま閉じたら取り消す」が避けたかったものが、この経路だけ素通りしていた。

| 出口 | 置いたばかりのセリフに「あいうえお」と打った場合（2026-08-08 に実機で実測） |
|---|---|
| `Ctrl+Enter` | 残る／中身 `'あいうえお'` |
| 入力欄の枠外をクリック | 残る／中身 `'あいうえお'` |
| `Esc` | **残る／中身 `''`** ← 直した点 |

**Esc は「置く前へ戻す」と読むのが自然。** 既にあるセリフを `F2` で打ち直して Esc すると元の文へ戻る（＝編集を始める前の姿）。置いたばかりのセリフにとって「始める前の姿」は**セリフが無い状態**なので、追加ごと消すほうが同じ規則の当てはめになる。

- **これで失われるものは無い。** 打った内容は Esc の時点でどのみち捨てられており、残っていたのは空の枠だけ。**取り消しの対象を増やしても、消える中身は増えない**
- 判定は `finish_text_edit` の1か所で `is_new and (not commit or not content.strip())`。「新しく置いたもの」＋「空のまま残るなら」の2条件だけで、経路ごとの分岐を増やしていない
- **状態表示の文言は分ける。** 空のままなら「空のままだったので、セリフは作りませんでした」、打ってから Esc なら「取り消したので、セリフは作りませんでした」。前者を出すと、打った本人には「打ったはずなのに空と言われた」と映る
- **既にあるセリフ（`F2` から）は従来どおり。** Esc で元の文へ戻るだけで、セリフ自体は消えない（`is_new` が偽）
- 履歴の扱いも従来どおり。`discard_last` は**直前の1手の名前が「セリフの追加」でなければ何もしない**。間に別の編集が挟まっていた場合は消さずに素通りし、空のセリフが残る——**消せないほうを取る**（消し間違えるより軽い）

**「枠は選んだときしか描かれない」は正確ではない**（上の 2026-08-07 の記述、および `finish_text_edit` の説明）。空のセリフの点線枠は `PageRenderer.aids` が真なら**選択と関係なく描かれる**（`_draw_text`）ので、画面にもページ一覧のサムネイルにも出ている。切れるのは PNG / PSD の書き出しだけ。ただし薄い灰色の細い点線（`PLACEHOLDER`）で、**書き出して初めて気づく**という結論は変わらないため、判断は動かしていない。

###### 入力中にメニューの項目が押されたら、先に確定する（2026-08-08 に追加）

**入力中でもメニューのショートカットは発火する。** 画面側（`PageView.keyPressEvent`）の「入力中は1つも横取りしない」は、キーがその部品まで届いた場合にしか効かず、`QAction` のショートカットはそれより手前で解決される（→ [PySide6の落とし穴.md](PySide6の落とし穴.md) の 5）。

実測すると、このアプリのショートカット 38 件のうち **24 件が入力中に素通りしていた**（2026-08-08）。`F7` `Ctrl+B` `Ctrl+[` `Ctrl+]` のような書式の項目、`PgUp` `PgDown` `Ctrl+Shift+N` のようなページを移す項目、さらに `Ctrl+S` `Ctrl+N` `Ctrl+O` `Ctrl+W` `Ctrl+Q` まで含む。1文字キー（`V` `P` `B` など）と `Ctrl+Z` `Ctrl+V` `Del` は入力欄へ譲られていた。

放っておくと2つの形で壊れる。

| 入力中に押されたもの | 起きること |
|---|---|
| ページを移す項目 | **入力欄は開いたままページだけが変わる。** 確定時の書き戻しは今のページからセリフを探すので見つからず、**打った文字が黙って捨てられる**。元のページには中身の無い枠が残る |
| 書式の項目 | 履歴に1手挟まるので、上の「置いたばかりのセリフを Esc で追加ごと取り消す」の照合が外れ、同じく空の枠が残る |

**先に確定してから項目を実行する**形にした（`MainWindow.run_action`）。`QAction` は全て `MainWindow._act` で作る決まりなので、塞ぐ場所は1つで足りている。

- **キーを効かなくする方向では直さない。** 入力中に `Ctrl+S` を押すのは自然な操作で、塞ぐと「効かないキー」を覚えることになる
- **画面を触ったとき・右クリックのときと同じ形に揃えた。** どちらも既に「そこで確定してから次の操作へ移る」（→ `contextMenuEvent`）。残っていたのはショートカットの道だけ
- **空のまま項目を押した場合は、閉じたときと同じ扱いになる**（追加ごと取り消す）。確定を挟むので、経路ごとの分岐は増えていない
- 書き戻す先が見つからない場合の受け皿も足した。**今は通れない道**だが、黙って捨てるのが元の壊れ方だったので、状態表示で知らせる

#### その場編集はシーンに置いた項目で行う

画面に重ねた別の部品ではなく、シーンの中に入力欄を置いている。拡大縮小や画面移動に自動で付いてくるので、位置合わせを自分でやらずに済む。入力中は下地の文字を描かない（二重に見えるため）。折り返しも切ってあり、入力中と確定後で改行位置が変わらない。

#### 検証環境にフォントが無い

テストは表示装置なし（offscreen）で動かしているが、**この環境には使えるフォントが1つも無い**（`QFontDatabase.families()` が空）。そのため、描いた画素で文字を確かめる検証は自動テストに入れていない。文字の大きさ・整列・改行・入力中の見え方は、フォントのある環境で別途確かめた（上の実測値がそれ）。

### 6.6 保存・読込 → **実装済み（2026-08-05）**

- フォルダ形式（`project.json` + `assets/`）
- **アトミック保存**: 一時ファイルに書き切ってから本体を置き換える。保存中の異常終了でデータを失わない
- 保存のたびに、それまでの `project.json` を `backup/project.N.json` へ退避（5世代）
- **自動バックアップ**: 5分ごとに作業中の内容を `backup/autosave.N.json` へ退避（3世代）
- `format_version` を見て、将来の形式変更に備える

#### 自動バックアップは本体（project.json）を書き換えない（2026-08-05）

タイマーが書くのは `backup/` の中だけ。**押していない保存を起こさない。**

起こすと、「保存していない変更があります」の確認と食い違う。利用者から見れば保存していないのにディスクの中身は新しく、**Undo で戻せる範囲とディスクの内容がずれる**。保存を押す操作が何を意味するのかが曖昧になる。

同じ理由で、退避しても**未保存の印は消さない**。消すと閉じるときの確認が出ず、本体が保存されないまま黙って終わる。

#### 手で保存した世代とは別の系列にする

`backup/` の中に2系列を置く。

| 名前 | 中身 | 世代 |
|---|---|---|
| `project.N.json` | 過去の**保存済み**の姿（保存のたびに退避） | 5 |
| `autosave.N.json` | まだ**保存していない**今の姿（タイマーが退避） | 3 |

**混ぜてはいけない。** 中身の性質が違うので、復元しようとしたときにどれが保存済みでどれが作業中か見分けられない。さらに世代は数個しかないので、混ぜると**タイマーが回るたびに過去の保存済みの姿が押し出されて消える**。既にある機能の劣化になる。

#### 変化が無ければ書かない — `is_dirty` では判定できない

**「前回自動バックアップした時点」を、「前回保存した時点」とは別に覚える**（`History._autosaved`）。

上のとおり退避では `mark_saved()` を呼べないので、`is_dirty` は退避した後も真のまま残る。それを目印にすると、**1文字も変えていなくてもタイマーが回るたびに書く**ことになる。

比較は保存形式そのものなので（`is_dirty` と同じ仕組み）、**「変えて元に戻した」場合も正しく書かない**。保存した時点では両方の印を進める。保存した内容は `project.json` にそのまま入っており、同じものを写しても増えないため。

#### 一度も保存していない作品は対象外

判定は「中身が空か」ではなく「**保存先が決まっているか**」で行う。

- 退避先の `backup/` を置く場所が無い
- その状態で貼った画像は**まだディスクに無い**（`PendingAssets` がメモリで預かっている → 6.3）。JSON だけ書いても、参照先の無い退避になる
- 逆に保存先さえ決まっていれば、画像は貼った時点で `assets/` に入るので、この問題は起きない

この線引きだと「新規で 10 コマ描いたがまだ保存していない」も対象外になる。救うには**作品フォルダの外に置き場所を用意する話が丸ごと増える**ので、要求が立ってから考える。

#### 失敗しても作業を止めない

窓は出さず、状態表示に出すだけにする。利用者が押した操作ではないので、書けなかったからといって手を止めさせる理由がない。

**失敗したときは印を進めない**ので、次の回にまた試す。保存先が外付けドライブで抜かれている間は5分ごとに知らせ続けることになるが、**退避されていない事実は伝わったほうがよい。**

#### 間隔は `settings.json` で変えられる（2026-08-05 に追加）

`autosave_interval_sec`（秒。既定 300、5〜3600 の外は既定に落とす）。

**設定に出したのは、好みのためというより動きを確かめる手段として。** 5分は待って確かめるには長い。かといって**コードを 30 秒に書き換えて確かめると、戻し忘れがそのまま残る**。設定に出しておけば、確かめたい間だけ短くして、あとで消せる。

読むのは起動時の1回だけ。変えたらアプリを開き直す（窓の初期位置のように「使う直前に読み直す」形にはしていない。タイマーは起動時に1度積むものなので、読み直す場所が無い）。

#### 動かなかったときに切り分けられるようにする（2026-08-05）

**タイマーは黙って回るので、「動いていない」と「動いているが退避する理由が無い」の区別が画面から付かない。** 実際に「5分待っても何も起きない」という報告があり、どちらなのか分からなかった。

`data/autosave.log` に記録を残す（git 管理外）。

| 記録する内容 | 毎回書くか |
|---|---|
| 起動（間隔つき） | 毎回 |
| 退避した（書いたパス） | **毎回** |
| 退避しなかった理由（保存先が未定／変更なし） | 内容が変わったときだけ |
| 失敗した理由 | 内容が変わったときだけ |

**同じ内容が続く間は1行にまとめる。** 保存先の決まっていない作品を1日開きっぱなしにしても1行で済む。一方**退避できた回は毎回残す**——いつの時点の内容が `backup/` に入っているかは、記録の目的そのものなので落とせない。

読み方は次のとおり。

- **記録が空** → タイマーを積んだアプリが動いていない（起動し直していない）
- **起動の行だけ** → タイマーは回っているが、退避する理由が無い（→ 続く行に理由が出る）
- **起動＋退避の行** → 正しく動いている

#### バックアップからの復元（2026-08-06 に追加）

「ファイル」＞**「バックアップから復元...」**。`backup/` の世代を1つ選んで戻す。

##### 戻しても project.json は書き換えない

**画面の中身だけ入れ替え、「未保存」にする。** 確定は利用者が保存を押したときで、そのとき今の `project.json` は自動で `backup/project.1.json` へ退避される（→ `_rotate_backups`）。

自動バックアップと同じ線引き（上記「本体を書き換えない」）に揃えてある。書き換えてしまうと、**Undo で戻した内容とディスクの中身がずれる**——画面は復元前の作業に戻っているのに、`project.json` は復元後のまま、という状態になる。

この決め方の副産物として、**「戻す前に今の内容を退避する」の重みが下がった**。今の `project.json` はそもそも触られないので、失われるのはメモリ上の未保存分だけで、それは Undo 1回で戻る。退避は「アプリごと落ちた場合」の備えとして残している。

##### Undo で取り消せる

履歴に**1手として積む**。6.8 がスナップショット方式なので、まるごとの差し替えでも普通の1手と扱いが変わらない。

**作品を開く経路（`EditorState.reset`）は通さない。** あちらは `History` ごと作り直すので、通した瞬間に戻れなくなる。

**中身が今と同じなら積まない**（`commit` と同じ約束）。同じ内容の世代を選んだだけで履歴が1手埋まることがなく、このとき選択もページ番号も動かさない。何も起きていないのに画面が揺れる理由がない。

##### 一覧は日時順に混ぜる

2系列（保存済み5世代・作業中3世代）を**系列ごとに分けず、日時の新しい順に混ぜて**並べる。分けて並べると、保存済みと作業中のどちらが新しいのかが読み取れない。

```
2026-08-06 14:32  保存済み（1つ前）  5ページ・23コマ
2026-08-06 14:05  作業中（1つ前）    5ページ・25コマ
```

**中身の手がかり（ページ数・コマ数）まで出す。** 日時と種別だけでは、どの作業をしていた頃の世代なのかを思い出せない。数えるのは辞書のままで、`Project` は組み立てない——一覧を出すだけのために8件ぶんの検証を通すのは重く、しかも**直せる乱れのある世代が一覧から消える**。

**読めない世代も一覧に残す**（選べない状態で「読めません」と出す）。黙って消すと、5世代あるはずのものが4つしか出ない理由が分からない。

###### 日時は分単位まで。秒は出さない（2026-08-06 に見送り）

同じ分に作られた世代は、一覧上で日時が同じに見える（実機で4行が `06:34` で並んだ）。それでも**分で止める。**

- **秒を足しても人の記憶とは繋がらない。** 「どちらが数秒新しいか」は、どの作業をしていた頃かを思い出す手がかりにならない
- **選び分けは既にできている。** 並びは日時順で、各行に「（Nつ前）」が付くので、日時が同じでも別の行だと分かる

##### 順序が要 — 読むのが先、退避が後

**選んだ世代を先に読み切ってから、今の内容を退避する。**

逆にすると `write_autosave` が世代を1つずつ繰り下げるため、`autosave.2.json` を選んだつもりが別の中身になり、`autosave.3.json` に至っては**最古として削除されたあとを読む**ことになる。見立ての段階では「戻す前に1つ退避しておく」としか書いておらず、この順序に気づいていなかった。

##### 窓を開けている間は自動バックアップを止める（2026-08-06 に実機で発見）

同じ繰り下げが、**タイマー経由でも起きる**。窓は modal だが Qt のタイマーは止まらないので、開けたままにしていると裏で `autosave.N.json` が繰り下がる。一覧が控えているのはパスなので、**一覧に出ていた行とは別の中身**が入る。

実測では「2ページ・15コマ」と出ていた行を選んで、18 コマの内容が入った。画面を見れば違うと分かり「元に戻す」で戻せるが、**黙って違うものが入る**のが問題。

窓を閉じている間に世代を動かせるのはこのタイマーだけ（保存は modal の裏では押せない）なので、開けている間だけ止めれば塞がる。**閉じたら必ず動かし直す**——止めたままにすると、以降まったく退避されなくなる。

上の「順序が要」を守っても防げない。あちらは自分で呼ぶ退避の話で、こちらは裏で回るタイマーの話。**繰り下げという同じ原因に、入り口が2つある。**

##### 使えないときは、押せるまま理由を伝える

「一度も保存していない」「`backup/` が空」のいずれもメニューを灰色にせず、押したうえで状態表示に理由を出す。灰色にすると、なぜ使えないのかを伝える先が無くなる（→ 6.15 の複製と同じ線引き）。

**未保存の確認は出さない。** 戻しても失うものが無く（`project.json` は書き換わらず、今の作業は Undo 1回で戻る）、聞く理由がない。

**退避しなかった回は状態表示には出さない。** 5分ごとに「何もしませんでした」と出ても邪魔なだけで、しかも見ていないうちに消える。

#### タイマーが実際に発火することをテストで押さえる

最初に入れたとき、テストは `autosave()` を直接呼ぶものばかりで、**「タイマーの合図が処理につながっているか」を1つも確かめていなかった**。結線が切れていても全部通る状態だったので、間隔を短くして実際に発火させるテストを足した。

#### 「名前を付けて保存」は専用の窓にする（2026-08-03 に直した）

作品が1個のファイルではなく**フォルダ**なので、普通の「ファイルを保存」の窓は使えない。かといって「既にあるフォルダを選ぶ」窓（`QFileDialog.getExistingDirectory`）にすると、**名前を打つ欄が無い**。フォルダを選んだ瞬間にそこへ書き込まれ、作品に名前を付けられなかった。

そこで「置き場所」と「作品名」を分けて受け取る窓を用意した（`ui/saving.py`）。フォルダを作る操作は結果が目に見えないので、**出来上がるフォルダの場所を1行で見せる**。

作れない名前は**押す前に断る**。作ってから OS に断られると、直す場所（名前の欄）が既に閉じていて打ち直しが遠くなる。断るのは次の4つ。

| 断るもの | 理由 |
|---|---|
| 空の名前 | フォルダを作れない |
| `\ / : * ? " < > \|` を含む | Windows がフォルダ名に許さない |
| 末尾のピリオド | Windows が黙って落とすため、打った名前と違うフォルダができる |
| `CON` `NUL` `COM1` など | Windows が装置の名前として押さえていて、フォルダにできない |

行き先については、**中身のある「作品でないフォルダ」に書かない**。元から入っていたファイルと混ざり、あとからどちらの持ち物か分からなくなる。既にある作品への上書きは、赤字で示したうえで確認を出してから行う。

#### 「開く」は project.json を選ばせる（2026-08-03 に直した）

保存と対になる問題で、「開く」も**フォルダを選ぶ窓**にしていた。作品はフォルダ単位なので内部で使うのはフォルダのほうだが、**利用者から見れば「ファイルを開く」操作**で、目当ての `project.json` が一覧に出てこない。選べないのか場所を間違えたのかが分からず、「開けない」という結論になる。

`project.json` を選ばせ、その親フォルダを作品フォルダとして扱う（`storage.project_dir_of`）。ファイルとフォルダのどちらを渡しても同じ場所に行き着くので、呼ぶ側が受け取ったものの種類を気にせずに済む。

##### `project.json` を `assets/` の中に入れない理由

`assets/` は「未使用ファイルを整理」が中を走査して、参照されていないものを `_unused/` へ移す場所。作品の本体をそこへ置くと、**整理の対象と同じ場所に混ざる**。また `assets/` の中身はファイル名が内容のハッシュで人が触らないものなので、本体は1つ上に置いて見つけやすくしている。

#### 窓が始まる場所の初期値は `settings.json` で決める

置き場所の初期値は **`data/settings.json`**（作業フォルダの中）の `default_parent_dir` に書く。次の順に、**実在する最初のもの**を使う。

1. 開いている作品の親フォルダ（2作目は1作目の隣に作ることが多い）
2. `default_parent_dir`（新しい作品のとき）
3. ドキュメントフォルダ

**「名前を付けて保存」「作品を開く」「画像を選ぶ」で同じ場所を使う**（`MainWindow._default_parent`）。窓ごとに分けると、同じ作業の途中なのに始まる場所が変わり、そのたびに辿り直すことになる。特に「画像を選ぶ」は場所を渡していなかったため、**アプリを起動したフォルダ**から始まっていた（2026-08-03 に直した）。下書きは作品の隣に置かれることが多いので、保存先と揃えるのが一番近い。

##### 設定は使う直前に読み直す（2026-08-03 に直した）

起動時に一度だけ読んでいたため、**`settings.json` を書き換えてもアプリを開き直すまで効かなかった**。手で書き換える前提のファイルなのに、書き換えた本人から見ると「設定した場所にならない」としか見えず、しかも効かない理由が画面に出ない。設定の書き方（JSON のバックスラッシュ、ドライブの有無）を疑うことになり、原因から遠ざかる。

窓を開く瞬間に数百バイト読むだけなので、待たされることはない。読み込みに失敗しても既定値で進む点は変わらない。

##### 置き場所を `%LOCALAPPDATA%` から `data/` へ移した（2026-08-03）

当初は `%LOCALAPPDATA%\MANGA_layout\settings.json` に置いていた。2台の PC で git 同期しており、保存先には `F:` のような片方の PC にしか無いドライブが入るため、リポジトリの中に置けないという判断だった。

**この条件は `data/` でも満たせる。** `data/` は `.gitignore` の `/data/` で管理外なので、同期で持ち込まれない。

移した理由は、`%LOCALAPPDATA%` だと**「誰が見ているファイルなのか」が分からなくなる**こと。パッケージ版アプリ（MSIX）の中から触ると `%LOCALAPPDATA%` は `...\Packages\<アプリ>\LocalCache\Local\` へ転送されるが、**パス表示は元のまま変わらない**。同じパスなのに実体が別で、片方には設定が入っていて片方は空、という状態になる。

2026-08-03、まさにこれで「設定した場所にならない」が起きた。確認すると毎回「設定は正しく入っている」と出るのに、アプリは既定のドキュメントを開く。食い違いに気づけたのは**更新時刻のずれ**（一方 05:11、他方 05:55）からで、それまで Qt のファイル窓・パスのハードコード・PC の取り違えを疑って回り道をした。作業フォルダの中なら実体が1つしかなく、この取り違えが起こらない。

**代わりに失うもの:** clone し直すと設定も消える。手で書き直す数行なので、取り違えの分かりにくさとは釣り合わないと判断した。

実在を毎回確かめるのは、外付けドライブが繋がっていない PC で開いたときに、無い場所を出しても選び直す手間が増えるだけだから。

設定は**人が手で書き換える前提**なので、壊れていても起動を止めない（既定値で進む）。知らない項目は読み飛ばす。1個の綴り間違いでアプリが開かなくなるほうが困る。起動時に雛形を置くのは、実物が無いと「どこに何を書けばいいのか」が分からないため。

### 6.7 書き出し → **実装済み（2026-08-03、JPG 対応は2026-08-05）**

- ページ単位で PNG / JPG 出力（現在ページ / 全ページ）
- 画像サイズ指定（117% / 100% / 75% / 50%、**既定 100%（原寸）**）。**dpi を数字で指定させることはしない**（→ 3章）
- 紙に置き換えた大きさは参考として1行出すだけ
- 画面表示と同じ描画経路（`PageRenderer`）を通るので、見た目と一致する
- 書き出し先は**作品フォルダの中の `export/`**。ファイル名は `p01.png` / `p01.jpg`（総ページ数に合わせて桁を揃える）

#### 書き出し先を作品フォルダの中に固定した理由

アプリ側の決まった場所（`outputs/` のようなもの）に集めると、作品を別の PC へ持っていったときに書き出しだけが置き去りになる。`export/` を作品フォルダに置けば、作品と一緒に移動・複製でき、クリスタから開くときも作品フォルダを辿るだけで済む。

**毎回ダイアログで選ばせるのもやめた。** 書き出しは同じページを何度もやり直す操作なので、そのたびに場所を選ぶと置き場所が散らばり、「最新がどれか」を見失う。別の場所へ出したければ、書き出してから移せばよい。

保存前の作品には置き場所が無いので、そのときは断って先に保存してもらう。画像の貼り付け（`PendingAssets`）のように預かって後で書く形にはしない。書き出しは「いま欲しいファイルを作る」操作で、後回しにすると何のために押したのか分からなくなる。

#### 画面と変えているのは3点だけ

書き出し用に描き直すのではなく、`PageRenderer` の引数で切り替える。別々に書くと「画面で見た通りに出ない」が起き、しかもクリスタで開くまで気づけない。

| 変える点 | 理由 |
|---|---|
| 画像は原寸（`FullImages`） | 画面用の縮小版（長辺 1600px）を 600dpi へ引き伸ばすと、書き出しだけがぼやける。画面で確かめても気づけない |
| 用紙の縁・目安線・影を描かない | 置き場所の目印であって作品ではない。特に縁の線は、用紙そのものが画像の範囲であるぶん四辺に残る |
| 画面でだけ要る補助表示を描かない（`aids=False`） | コマの下地（薄い灰色）、空のセリフの点線枠、欠けた画像の×印。下記 |
| ラフ（下敷き）を描かない（`rough=False`） | なぞる相手であって作品の中身ではない（→ 6.23）。**`aids` とは別のスイッチ**にしてある——`aids` を落とすのは書き出しだけだが、ラフは一覧のサムネイルには出したいため |

##### コマの下地を書き出しに出さない

紙の上ではコマの中は白で、画面の薄い灰色は「どこがコマか」を見分けるための色でしかない。下敷きに敷いたときに灰色が乗ると、**絵と紙の白の境目が分からなくなる**。コマの範囲は枠線が示すので、下地が無くても困らない。

空のセリフの点線枠・欠けた画像の×印も同じ理由で切る。どちらも編集中に見失わないための目印で、作品ではない。欠けた画像は目印の代わりに、書き出す前の警告で数を知らせる。

原寸は**1ページ書き出すごとに読み直して捨てる**。全ページぶんを持ち続けると、2048×2048 が何十枚も同時にメモリへ載る。

#### 画像サイズ（%）は画素数をそのまま増減させる

座標系が px なので、**ページの寸法そのものが原寸**。100% は換算を挟まない。

A4 相当（1240 × 1754px）での実測（2026-08-03。117% は 2026-08-05 に追加）:

| 画像サイズ | 画素数 | ファイル |
|---|---|---|
| 117% | 1447 × 2046 | — |
| 100% | 1240 × 1754 | 26.9 KB |
| 75% | 930 × 1316 | 18.0 KB |
| 50% | 620 × 877 | 10.7 KB |

書き出される画素数は窓の中に常に1行で出す。用紙の大きさは表示中のページの実物を使う（A4 相当の決め打ちにすると、B5 相当やカスタムの作品で数字が合わない）。

##### 既定は 100%（原寸）

**ページの px 寸法そのままで出す。** 座標系が px なので、画面に出ている数字と書き出したファイルの画素数が一致し、「なぜ指定していない大きさで出たのか」を考えずに済む。

**2026-08-06 に 117% から戻した。** 下の「117% を選べるようにした理由」は今も成り立つが、それは**下敷きとしてクリスタへ持っていくときの話**で、書き出し全部がそうとは限らない。毎回効かせる既定ではなく、必要なときに選ぶものとして置く。

##### 117%（175dpi 相当）を選べるようにした理由

ページの px は 150dpi 換算なので、A4 相当の原寸は長辺 1,754px にしかならない。一方**貼り込む画像素材の原寸は長辺 2,048px 程度**あり、原寸で書き出すとその細かさを捨てることになる。150 → 175dpi 相当（倍率 7/6）まで上げると長辺が約 2,048px になり、素材の細かさをちょうど使い切る。

そこから上へは伸ばさない。素材にそれ以上の細かさが無いので**絵は細かくならず、待ち時間とファイルサイズだけが増える**（当初は 350dpi まで上げる案だったが、この理由で 175dpi に決め直した。2026-08-05）。

**倍率は用紙によらず一律 7/6。** 用紙ごとに「長辺 2,048px」へ合わせる案もあったが、ページの大きさで倍率が変わると書き出しの倍率がページ依存になる。B5 相当は長辺 1,771px と 2,048 に届かないが、素材を使い切れないぶんは僅かで、仕組みを単純に保つ方を採った。

この 175dpi を**設定で変えられるようにする案は 10.2 に置いた**（2026-08-05 の時点ではスコープ外）。

##### 一度やって取り消した設計: 倍率を dpi に折り込む

座標系が mm だった頃、「150dpi の 75%」を 112.5dpi として扱い、PNG に書き込む解像度も 112.5 に下げて、**紙の上の大きさが A4 のまま変わらない**ようにしていた。印刷を前提にすれば筋は通るが、**この道具では印刷しないので誰の役にも立たない**。「サイズを変えたのに変わっていない」ように見えるだけで、要求と逆の結果になっていた（2026-08-03、利用者の指摘で撤回）。

**印刷を前提にした作法を、印刷しない道具に持ち込まないこと。** その後、座標系そのものを px に変えたことで、この取り違えが起きる余地も消えた（→ 3章）。

#### 紙の大きさは参考表示だけ

「1240px」と言われてもピンとこないので、150dpi 換算での紙の大きさを1行添える（`paper_hint`）。**出力には一切影響しない。**

PNG には `setDotsPerMeterX/Y` で 150dpi を書き込む。入れておかないと**クリスタが 72dpi の画像として開き**、原稿用紙に対して極端な大きさで貼られて毎回縮める羽目になる。

##### 拡大したときだけ dpi も一緒に上げる

117% では 175dpi を書き込む（`export_dpi`）。拡大は「**大きさは変えずに、きめを細かくしたい**」操作なので、150dpi のままだと同じ絵がクリスタで 1.17 倍に膨れ、毎回縮めることになる。

**縮小側は 150dpi に据え置く。** 50% で 75dpi と書くと、クリスタは A4 のまま粗く貼り、「小さくしたのに小さくならない」になる。それは下の「取り消した設計」そのもの。**縮小はウェブ用に小さいファイルが欲しい操作、拡大は原稿に貼る下敷きを細かくしたい操作**で、求めているものが逆なので、扱いも逆にする。

#### 書き始める前に3回止まる

上書きしてしまったものは戻らないので、断る場所はすべて書き込みの前に置く。

1. 保存前の作品（書き出し先が決まらない）→ 先に保存するか聞く
2. 使えない画像がある → その場所が白く抜けることを伝える（判定は点検と共通 → 6.29）
3. 同じ名前のファイルがある → 件数と名前を出して上書きの可否を聞く

書き込み自体も、保存と同じく**別名で書き切ってから置き換える**。上書きの途中で落ちても、前回の書き出しが壊れた状態で残らない。

#### 書き出し中はページごとに進捗と中止ボタンを出す（2026-08-08 に追加）

全ページ書き出しは GUI スレッド上で1ページずつ直列にこなす。以前はフィードバックが待機カーソルだけで、PSD（→ 10.1）は1ページ 10〜30MB あり全ページで数百MBになり得るため、**数十ページの書き出しでは Windows が「応答なし」と表示し、落ちたのか進んでいるのか区別が付かなかった**（本人の指摘 2026-08-08）。

- **ページを1枚書き終えるごとに、進捗窓の値を進める。**「書き出しています…（3/12）」のように、今どこまで進んだかを数字で出す
- **中止ボタンを押せば、その時点で打ち切る。** 途中まで書けた分は残す（失敗して止めたときと同じ扱い）。消すほうが親切に見えて、「どこまで進んだか」が一切分からなくなるほうが困る
- **中止した場合は、何枚まで書けたかを状態表示に出す。** 「書き出しました」ではなく「中止しました」と、成功時と紛れない言い方にする
- **1ページだけ・小さいページなど、一瞬で終わる書き出しでは窓を出さない。** Qt の既定の見積もり（`QProgressDialog.setMinimumDuration`）に任せており、書き出しの主動線（表示中のページ1枚）を邪魔しない

完了メッセージの画素数は、**書き出したページの寸法から出す**（2026-08-08 に修正）。以前は表示中のページの寸法を使っていたため、ページごとに大きさが違う作品（→ 6.1）で全ページ書き出すと実際とは違う数字が出ることがあった。書き出したページの大きさが割れていれば「画素数はページごとに異なる」と言うだけにし、根拠の無い数字は出さない。

#### 押した直後と、書き終えたことを、待たされる書き出しにだけ出す（2026-09-05 に追加）

上の進捗窓には**両端に穴があった**（本人の指摘 2026-09-05）。

**始まり: PSD の1ページ目のあいだ、窓が1つも出ていなかった。** `setMinimumDuration` の 500ms は Qt がイベントを処理する隙がないと数えられないが、書き出しが `processEvents()` を呼ぶのは1ページ書き終えた後だけで、1ページ目には隙が無い（実測 → PySide6の落とし穴.md 9）。PSD は1ページ 1.0 秒（→ 6.28）あり、**押した直後のいちばん「効いたのか」と思う時間帯が、そのまま無反応だった。**

**終わり: 完了は画面下の1行（6秒で消える）にしか出ていなかった。** 数十ページの PSD は数分かかるので、そのあいだに席を立つと、**戻ったときに終わったことを示すものが何も残らない。**

- **PSD のときは、押した直後に進捗窓を出す**（`forceShow`）。「書き出しの準備をしています…（全12ページ）」と、これから何ページ書くかを先に言う
- **PNG / JPG には出さない。** 1ページ 0.2 秒で隙が毎秒5回でき、500ms の見積もりがそのとおりに働く。「一瞬で終わる書き出しでは窓を出さない」（上の4番目）をそのまま保つ
- **待たされる書き出しでは、完了も消えない窓で出す。** 条件は **PSD であること・複数ページであること**のどちらか（`_takes_a_while`）。1枚だけの PNG は今までどおり画面下の1行だけで、**書き出しの主動線にクリックを1回足さない**
- **かかった時間そのものは測らない。** 測って決めると「速く終わったときだけ窓が出ない」ことになり、同じ操作の見え方が回ごとに変わる。**押す前から分かる2つ**で決めれば、利用者は結果を予想できる

**残る穴は承知のうえ。** 1枚が極端に大きい PNG では、始まりの穴が同じように起きる。そこは倍率で決まるので、形式で分けるこの切り方では拾えない。**「今何ページ目を書いているか」を書く前に出す案は見送った**（書き出し関数に引数を1つ足すことになり、PNG 側も一緒に検証が要る。1ページあたりの待ちが気になったら足す）。


#### JPG 書き出しと品質の設定化（2026-08-05）

書き出しダイアログの「形式」で PNG / JPG を選べる。既定は PNG（これまでどおりの動き）。JPG は用紙を白で塗ってから描く既存の下地（`PAGE_BG`）にそのまま乗るので、透過を気にする必要はない。

**JPG の品質はダイアログには出さず、`AppSettings.jpg_quality`（`settings.json`、既定90）から取る。** 書き出し dpi を設定に出す案（→ 10.2）と同じ位置づけで、選択肢を増やすよりダイアログを単純に保つほうを採った。範囲外・数でない値は既定の90に落とす（`autosave_interval_sec` と同じ検証の形）。設定は使う直前に読み直すので（→ `default_parent` と同じ理由）、書き出しのたびに手で書き換えた値が効く。

PNG と JPG を同じ `export/` に混在させて書き出しても、別形式の古いファイルは消さずに放置する。上書きの確認も拡張子込みで判定するので、形式を変えて書き出し直しても別形式のファイルは上書き対象に挙がらない。

### 6.8 Undo / Redo → **実装済み（2026-08-02）**

**スナップショット方式**（操作確定ごとにプロジェクト全体の状態を積む）。
操作ごとに「何をどう戻すか」を書くコマンド方式に比べ、操作を1つ足すたびに戻す処理も書く必要がなく、書き忘れによる「戻したはずなのに戻っていない」が起きない。

- 履歴上限 50 手
- 画像の実体は `assets/` にあり JSON には含まれないため、1回のスナップショットは軽い
- 状態は**保存形式を経由して持つ**。つまり保存されない項目は Undo でも戻らない。この2つを意図的に一致させてあるので、項目を足したときに `to_dict()` への追加を忘れれば Undo のテストでも落ち、抜けに気づける

#### 実測（30ページ・オブジェクト360個の作品）

| 項目 | 値 |
|---|---|
| 保存形式（非圧縮） | 87 KB |
| 1手あたり（圧縮後） | 4.6 KB |
| **50手ぶんの合計** | **0.23 MB** |
| 1手の確定 | 2.1 ms |
| 1手の巻き戻し | 6.6 ms |

辞書のまま持つと1手あたり数 MB になり50手で数百 MB に達するため、保存形式の文字列にしてから圧縮している。上の数値なら履歴の重さは考えなくてよい。

#### 実装上の決めごと

- **`History` がプロジェクトの唯一の持ち主になる。** Undo すると `Project` の実体は別のものへ差し替わるため、画面側は必ず `history.project` を読み直す。古い `Page` や `Panel` を掴んだままにすると、戻した後の編集が反映されない
- **編集は `with history.edit("コマの移動") as project:` でまとめる。** 途中で例外が出た場合は直前の確定状態へ戻してから送出するので、半分だけ適用された状態が残らない
- **変化が無ければ履歴に積まない。** ドラッグして元の位置に戻した場合など
- **`merge_key` で連続入力をまとめる。** セリフを1文字ずつ積むと Undo 1回で1文字しか戻らないため、同じ鍵で連続した確定は1手に吸収する。入力欄から離れたときに `break_merge()` で区切る
- **`is_dirty` で未保存状態を持つ。** 保存した地点まで Undo で戻れば、正しく「変更なし」に戻る

### 6.9 コマ追加は「1回きり」 → **実装済み（2026-08-02）**

**コマ追加の道具は、1つ作ったら選択の道具へ自動的に戻る。**

| 操作 | 結果 |
|---|---|
| コマ追加の道具にする（`P` またはボタン） | 追加できる状態になる。何も無いところではカーソルが十字になる |
| 何も無いところをクリック | 既定の大きさ（基本枠の約 1/3）のコマを、クリック位置を中心に置く |
| 何も無いところをドラッグ | ドラッグした大きさのコマを作る |
| 上記どちらかで1つ作った直後 | そのコマが選択され、**道具は選択に戻る** |
| 既にあるコマ・つまみの上を押す | 追加ではなく、移動・大きさ変更を行う |
| 次のコマを作りたいとき | もう一度コマ追加の道具にする |

#### なぜ「1回きり」にするか

コマを置いたあとは、ほぼ必ず位置と大きさを整える。連続して置くことのほうが少ない。

道具を追加のままにしておくと、整えようとした操作がそのまま次のコマの追加になってしまう。実際に、**コマの枠を掴んでも編集できない／空白をクリックすると意図せずコマが増える**という状態になっていた。追加より「追加した直後の編集」を優先するほうが、実際の手の動きに合う。

内部の状態遷移も、追加できる状態 → コマを作る → 選択して編集、と一方向で終わる形になり、誤クリックで増え続けることがなくなる。

#### 決めたこと

- **クリックとドラッグの両方を残す。** クリックだけなら既定の大きさ、ドラッグなら好きな大きさ。「置いてから整える」流れと「最初から狙った大きさで作る」流れのどちらも塞がない
- **ドラッグと呼べない小さな動きはクリックとみなす。** 以前はここで何も作らずに終えていたが、押したのに反応が無いように見えるため、既定の大きさで置く
- **既定のコマは用紙の中へ収める。** はみ出したまま作ると、つまみが画面の外に出て掴めなくなる

### 6.10 斜めの縦割り → **実装済み（2026-08-03）**

コマを**斜めの縦線**で2枚に割る。割った2枚は `SlantPair` として結び付き、以後は1つの矩形のように振る舞う。

| 操作 | 内容 |
|---|---|
| 斜めに割る | `K` でコマの上をなぞると斜めの分割線が追従し、クリックで確定 |
| 向きの反転 | 「コマ > 斜めの向きを反転」。`/` と `\` を入れ替える。外側の矩形は変わらない |
| 境界をずらす | 境界の中点に出る左右向きのつまみをドラッグ。掴める範囲は印より広い（50px 四方）。外側の矩形は変わらない。細いほうが最小幅を割る手前で止まる |
| 移動 | どちらか一方を掴めば、**2枚まとめて動く** |
| サイズ変更 | つまみは組の外側の矩形に付く。8方向すべて使える。細いほうが最小幅を割る手前で止まる |
| 削除 | 片方を消すと組が解け、**残った1枚は斜めの辺を持ったまま**残る |
| 再分割 | できない。斜めが欲しければ「先に矩形として割る → その1枚を斜めに割る」 |

#### 形の決まり方

外側の矩形と、次の3つのパラメータだけで2枚の形が決まる。描くたびに作り直すので、形を作る経路は1本しかない。

| パラメータ | 決まる時期 | あとから変えられるか |
|---|---|---|
| 位置（`ratio`） | 分割時にクリックした位置 | **つまみでずらせる**（外側の矩形に比例して付いてくる） |
| 角度（`angle`） | 分割時の設定値 12° | 変えられない |
| 向き（`direction`） | 既定は `/` | 反転できる |

- **位置は絶対座標ではなく割合で持つ。** 長さで覚えると、外側を縮めたときに境界が矩形の外へ飛び出す
- **角度はペアごとに保存する。** `LayoutSettings.slant_angle` を変えても、すでに作ったコマは変形しない
- **隙間は垂直距離で `gutter` になるよう、角度で割り戻してずらす。** 真横に `gutter/2` ずらすだけだと、傾けたぶん見た目の隙間が細くなる
- **境界をずらしても中の画像は動かさないし、所属も変えない。** 境界を絵の向こう側へ送ると、その絵はコマの外に出て切り抜かれ、見えなくなる。「窓の大きさを変えても中身は付いて回らない」という既存の方針（→ 4章）と揃えてある。絵が勝手に隣のコマへ飛ぶより読みやすい
- **境界のつまみは小さく描いて広く拾う。** 印は他のつまみと同じ 9px、掴める範囲は 50px 四方。左右にしか動かないうえ、境界の周りは隙間で他に掴むものが無いため、狙いを外しても拾えるほうが扱いやすい。代わりに**角と辺のつまみより後に判定する**。先に見ると、縮小したときや細いコマでそれらを覆い隠し、大きさを変えられなくなる
- **ドラッグ中はモデルに触らない。** 下見を描くだけにして、離した時点で1手として積む。Undo 1回で元に戻る（しっぽの付け根と同じ流儀）
- **押し戻す値は最小幅ちょうどになる。** 判定側は `EPS` ぶん緩めてある。厳密に比べると浮動小数の丸めで弾かれ、限界までドラッグした瞬間に落ちる

#### なぜ「ペア単位」に縛るか

境界線を自由にドラッグできる設計も検討したが、**既存のコードが7箇所とも軸並行の矩形を前提にしていた**（当たり判定・つまみ・リサイズ・吸着・分割・移動）。ペアの外側を常に矩形に保つと、そのうち5つはコードを触らずに済む。

代償として、**左右とも斜めの平行四辺形のコマは作れない**。1枚のコマが入れる組を1つに限ることが、上の簡単さを支えているため。必要になったら、そこを緩める設計変更として別に扱う。

#### 角度を 12° にした理由

角度を固定にすると、コマの大小によらず傾きが揃って見える。一方で角度が大きいほど、縦に長いコマで偏った分割ができなくなる（斜めの振れ幅が高さに比例して増え、細いほうが最小幅を割るため）。

| 高さ | 横ズレ幅 | A4 相当の基本枠の幅 1062px で作れる左右比 |
|---|---|---|
| 945px（通常のコマ） | 201px | ほぼ制限なし |
| 1576px（ページ全高） | 335px | 約 1:4 〜 4:1 |

（この表は mm 時代の実測 160/267mm・34.0/56.8mm を 150dpi で換算したもの。比なので px にしても変わらない）

20° で試したところ、全高のコマでは 1:2 程度までしか偏らせられなかった。12° はその制限を緩めつつ、斜めと分かる傾きが残る値。

#### 当たり判定を外接矩形から多角形へ変えた

`panel_at()` と `image_at()` は外接矩形で代用していた。斜めのコマは**隣同士で外接矩形が重なる**ため、斜めに削られた三角形の部分を押すと隣のコマが選ばれる。`Polygon.contains()`（辺の上も内側とする ray casting）に置き換えた。

同じ理由で `set_panel_rect()` も書き換えた。以前は矩形で上書きしていたので、斜めのコマは**大きさを変えた瞬間に静かに平らになっていた**。外接矩形の比で頂点を移す形にすると、軸並行の矩形では結果が変わらず、斜めは傾きを保つ。

### 6.11 縦書き → **実装済み（2026-08-03）**

MVP 範囲外としていたが（→ 10章）、**下見で前提が変わったため前倒しした**。当初の見積もり「3〜4日」は句読点・小書き文字の位置調整と、長音符「ー」・括弧の回転を自前で持つ想定だった。実際には**その全部をフォント側が持っていた**。

`Text.direction` を `"vertical"` にすると縦書きで描く。**セリフごとに持つ**。1ページの中に縦書きのセリフと横書きの効果音が混ざるのが普通なので、作品全体の設定にはしない。

#### 新しく作るセリフは縦書き（2026-08-03）

マンガのセリフは縦書きが普通なので、**横書きのほうを選ぶ形**にした（`DEFAULT_TEXT_DIRECTION`）。

**これは「作るとき」の既定であって、「読み込むとき」の既定ではない。** 保存形式に `direction` が無いファイルは**横書きとして読む**。この項目が無いファイルは縦書きがまだ無かった頃に書かれたものなので、縦書きとして読むと**既にある原稿の見た目が開いた瞬間に変わる**。2つの既定はわざと食い違わせてあり、`TextObject.from_dict` 側にその旨を書いてある。

#### Qt には日本語の縦書きが無い

`QTextOption` にあるのは `setTextDirection`（アラビア語などの右横書き）だけで、縦書きの指定はどこにも無い。`QTextDocument` にも `QFont` にも無い（2026-08-03、Qt 6.11.1 で確認）。**文字列をまとめて渡す方法が使えないので、1文字ずつ位置を計算して描く。**

#### 字形はフォントに任せる

句読点を右上へ寄せる、長音符「ー」と括弧を 90 度回す、小書き文字をずらす——これらは OpenType の縦書き字形（`vert`）を有効にすれば**フォント側がやる**。`QFont.setFeature(QFont.Tag("vert"), 1)` の1行で足りる。

手元の日本語フォント9種（游ゴシック・游明朝・MS ゴシック・MS 明朝・メイリオ・BIZ UD 系）すべてで効くことを実測した。欧文フォントを選んでも日本語部分は日本語フォントに代替されるため効く。**文字ごとの例外表を自前で持つ必要は無い。**

#### 置き場所の計算は Qt から切り離す（`manga_layout/vertical.py`）

日本語の縦書きは字の大きさと同じ一辺の正方形を単位に組むので、**字ごとの送り幅を測らずに位置が決まる**。そのため計算を Qt から完全に分離できた。

分離は好みではなく必要だった。**テストは offscreen で動いており、その環境には使えるフォントが1つも無い**（`QFontDatabase.families()` が空、どの文字も送り幅が同じ、`boundingRect` が 100000 を返す）。字の寸法に依存した検証はそもそも成立しない。

描画は `PageRenderer` の中で分岐するので、画面・サムネイル・PNG 書き出しの3つが同時に縦書きになる（→ 6.1、6.7）。

#### `align` は項目を増やさず読み替える

「行の始まりに寄せる」が `left`、「終わりに寄せる」が `right` と読むと、縦書きにもそのまま渡せる。横書きの行は左から始まり、**縦書きの列は上から始まるので `left` は上寄せ**になる。表示上の呼び名だけ言い換える。

項目を増やさないのは、増やすと保存形式が変わるうえ、**向きを往復したときにどちらの値を使うのか決められなくなる**ため。

#### 入力欄は横書きのまま。**枠の中に確定後の姿を出す**（2026-09-05 に追加）

**縦書きのセリフでも、その場編集の入力欄は横書きで出る。** Qt に縦書きの入力欄が無く、`QGraphicsTextItem` も縦書きにできないため。ここは今も変わらない。

これは「入力中と確定後で見た目が食い違わない」という 6.5 / 9章の方針に反する。2026-08-03 には次の3案から**「割り切って案内で断る」を選んだ**。

| 案 | 当時の採否 | 理由 |
|---|---|---|
| 入力中は横書きのまま。案内で断る | 採用 | 食い違うのは入力中の一時的な見た目だけで、確定結果は正しい。実装は案内文1つ |
| 縦書きの入力欄を自作する | 不採用 | カーソル移動・選択範囲まで作ることになり、機能全体より高くつく |
| 別の小窓で入力する | 不採用 | 方針は守れるが「画面上でその場編集」（6.5）を失う |

**この3案は「入力欄をどうするか」しか並べていなかった。** 断りを入れても、打っている間に**列が何本になるか・どこで改行されるか・枠からはみ出すか**は確定するまで分からないままで、使い勝手の不満はそこに残った（本人の指摘 2026-09-05）。

そこで**入力欄はいじらず、確定後の姿を枠の中へ実時間で出す**ことにした（第4の案）。

- 描くのは**確定後とまったく同じ経路**（`render._draw_text_editing` → `_draw_text_vertical`）。色も字形も変えない。**別の描き方にすると「下見では収まっていたのに確定したらはみ出した」が起こり、下見を見る理由そのものが無くなる**
- **入力欄のほうを枠の下へ逃がす**（`TextEditorItem._place_in`）。重ねると二重になって両方読めない。**横書きは今までどおり枠に重ねたまま**——あちらは入力中と確定後で字が 1px も動かないのが一番よい。逃がした先で枠にくっつけないよう、札と同じ間隔だけ空ける
- 打っている内容は `DragPreview.editing_text_content` に載せて渡す。しっぽや回転と同じ形で、**モデルには確定するまで触らない**。サムネイルと書き出しは `NO_PREVIEW` を受け取るので、下見が焼き付くことはない
- **1文字も入っていない間は点線枠を出す。** 入力欄が枠の外へ逃げているので、ここに何も描かないと**どこへ字が入るのかが画面から消える**

案内は「横書きで入る」の断りではなく、**どこを見れば仕上がりが分かるか**を指す形にした（`TEXT_EDIT_HINT_VERTICAL`）。長さは書き換え前と同じ 14 文字に収めてある（→ 下の幅の話）。

#### 【テキスト入力モード】の札（2026-09-05）

入力中は、入力欄のすぐ下に **【テキスト入力モード】と書いた札**を出す（`ModeLabelItem`）。上から **入力欄 → 札 → 確定の目印**（`ConfirmHintItem`）**→ 文字の大きさのキー**（`SizeKeysHintItem`）の順に並ぶ。

**札そのものが書体の見本を兼ねる。** これから打つ書体・大きさ・太字を、そのまま札の字に使う。空のセリフを打ち始めるときは入力欄に1文字も無く、**「どの書体の何 px で入るのか」が画面のどこにも出ていなかった**（本人の指摘 2026-09-05）。状態表示に書体名を文字で足す手もあるが、**名前を読んでも大きさは分からない**。

- **表示倍率を無視しない。** 見本である以上、拡大したら札も同じだけ大きくならないと「実際の大きさ」を示したことにならない。**すぐ下の `ConfirmHintItem`（確定の目印）とはここが逆**で、あちらは作品ではなく画面の道具なので倍率を無視する。同じ入力欄に付く2つの札で扱いが違うので、テストで両方を1つにまとめて見ている
- 余白も間隔も px 固定にせず、**字の高さに対する割合**で決める。px 固定にすると大きい書体で札だけ詰まって見える
- 押しても自分では受け取らない（`NoButton`）。クリックは下の画面へ素通りし、「画面を触ったら確定」という既にある道に乗る——`ConfirmHintItem` と同じ線引き
- **札は入力欄の上に置かない。** 上へ出すと**フキダシの輪郭と重なりやすい**（本人の指摘 2026-09-05）。セリフはフキダシの中に置くのが普通なので、入力欄の真上は輪郭が通っている確率が高い。確定の目印も札より下へ回した
- **札と目印は長さの単位が違う。** 札は書体と同じ px（倍率で伸び縮みする）、目印は画面の画素（倍率を無視する）。目印の位置に札の高さを足せるのは**足す側が px だから**で、目印は自分の内側で画面画素ぶんの間隔をさらに空ける
- 縦書きで入力欄を枠の下へ逃がす幅も、**札と同じ間隔**を使う（`ModeLabelItem.gap`）。書体の大きさで変わるので定数では決められない——px 固定にすると大きい書体で詰まって見える

**この案内は「文字を入力してください」を置き換える形で、足していない。** 状態表示のうち案内に使えるのは約 560px しかなく（右の常設表示が 696px を占める）、横書き用の 445px に足すと `Ctrl+Enter で確定、Esc で取り消し` が**切れて消えた**。操作キーが読めなくなるほうが、説明が足りないより困る。上限を `TEXT_EDIT_HINT_MAX_CHARS` に置いて歯止めをかけたが、**文字数は幅の代用でしかない**（日本語 13px / 英数字 7px でほぼ倍違う）。書き換えるときは実機で幅を測り直す。

#### 入力欄の外に「確定」の目印を出す（2026-08-03）

**Enter は改行なので、押しても入力から抜けられない。** 確定は Ctrl+Enter だが、それが分からないと「閉じる手段が無い」と感じてしまう。押し方を入力欄のすぐ下に出す（`ConfirmHintItem`）。

- **押しても確定するが、マウスの操作は自分で受け取らない。** クリックは下の画面へ素通りし、「画面を触ったら確定」という既にある道に乗る。自分で受け取ると、確定処理の途中で自分がシーンから外されることになって危うい
- **表示倍率を無視して常に同じ大きさで描く**（`ItemIgnoresTransformations`）。作品の一部ではなく画面の道具なので、拡大しても太らせない。コマ枠に `cosmetic_pen` を使わないのと同じ線引き

#### 残っている小さな課題

- **英数字**は枠内に立ったまま出る。本来2桁の数字は縦中横（数字だけ寝かせて1マスに収める）にする。マンガのセリフでは頻度が低いので後回し
- **`；`（セミコロン）**だけ縦書き字形を持たないフォントがある。セリフでは使わないため対処しない

#### 文字の大きさのキーの看板（2026-09-05）

確定の目印の下に、**薄い橙で `文字拡大 Ctrl+>　文字縮小 Ctrl+<` と書いた看板**を積む（`SizeKeysHintItem`）。確定の目印（青）と色を分けてあるのは役目が違うため——あちらは「入力から抜ける」、こちらは「入力の外にある書式の操作」。

**看板の記号と実際の割り当ては、わざと字が違う**（`canvas.KEY_FACE`）。指しているキーは同じで、看板は **Shift 側の刻印**を借りている。

| 看板に出す字 | 実際の割り当て | 同じキーか |
|---|---|---|
| `Ctrl+>` | `Ctrl+.` | JIS 配列では `.` のキーに `>` が並んで刻印されている |
| `Ctrl+<` | `Ctrl+,` | 同じく `,` のキーに `<` |

- **句読点は点が小さすぎて `,` と `.` を見分けられない**（本人談 2026-09-05）。見分けられない記号は、看板に出しても案内にならない
- **割り当てそのものは句読点のまま。** 山括弧へ移すと Shift を押しながらになり、押す手数が増える。**見やすさは看板で足りるので、押し方は変えない**——2026-09-05 に一度は割り当てごと移したが、手数が増えると分かって戻した
- **看板を読んだとおりに Shift を押して打つと効かない。** 割り当ては `Ctrl+.` なので、Shift の付いた形は別のキーとして扱われる（`QTest` で実測）。**承知のうえで採っている**——見つけられないキーは、そもそも押されない
- ショートカットキーの一覧（メニュー・`docs/ショートカットキー一覧.md`）には**割り当てのまま `Ctrl+.` / `Ctrl+,` が出る**。あちらは `QAction` から自動で作るもので、手を入れると嘘をつき始める（→ `tools/dump_shortcuts.py`）

- **看板の字は、メニューの割り当てから対応表を通して引き当ててテストしてある。** 割り当てを変えれば表に無い綴りになって落ちる。**拡大と縮小を取り違えても看板の文字は正しく見える**ので、目視では捕まらない
- 確定の目印の**子**にしてある。あちらは表示倍率を無視するので、その子は倍率の掛かっていない座標系に並ぶ。**間隔を px と画面画素で混ぜずに積める**
- **押すと入力から抜ける。これは仕様として許容する**（2026-09-05 判断）。メニューのショートカットは入力欄より手前で解決されるので、項目の実行前に必ず確定する形に揃えてある（`MainWindow.run_action`、2026-08-08）。**打った内容は捨てられない**が、看板を読んで「打ちながら変えられる」と期待すると外れる。**この差はテストに残してある**（`test_大きさのキーは入力を確定してから効く`）

  打ちながら変えられるようにするには、大きさのキーだけを「先に確定」の対象から外し、入力欄と札の書体を追従させることになる。**やめたのは、それが `run_action` の穴を1つ開け直すことだから。** 履歴に書式の1手が挟まると、**「置いたばかりのセリフを Esc で追加ごと取り消す」が効かなくなる**（→ 6.5）。空のセリフは画面に薄い点線しか出さず、書き出しでは何も描かれないので、**残っていること自体に気づけない**。**打ちながら大きさを変えられないことは、押し直せば済む。気づけない空の枠が残ることは、押し直しでは済まない**

### 6.12 右クリックのメニュー → **実装済み（2026-08-04）**

画面の上で右クリックすると、**押した場所に応じたメニュー**が出る。

| 押した場所 | 出るもの |
|---|---|
| 何も無いところ | ここにコマを追加 / ページ全面にコマを作る / ここに3種のフキダシ・セリフを追加 / 元に戻す・やり直す |
| コマの上 | ここで横・縦・斜めに割る / 斜めの向きを反転 / 集中線（→ 6.16。畳んで出す）/ ここに3種のフキダシ・セリフを追加 / 画像の貼り付け・読み込み / この画像を差し替え・削除（カーソルの下に画像があるときだけ）/ コマを削除 |
| 画像の上（ダブルクリックで踏み込んだあと） | コマにフィット / 回転をリセット（傾いているときだけ）/ この画像を差し替え / ファイル画像 読み込み / 画像を削除 |
| フキダシの上 | フキダシメニューの中身（種類・しっぽ・紐づけ）/ ここにセリフを追加 / フキダシを削除 |
| セリフの上 | セリフメニューの中身（入力・縦書き・整列・大きさ・太字・フォント）/ セリフを削除 |

#### 押した場所のものを選び直してから出す

Windows の慣例に合わせ、**右クリックした対象を選択してからメニューを出す**。選び直さないと「セリフを太字に」が直前に選んでいた別のセリフに効く。狙ったものと操作の対象がずれるのは、メニューの中身が正しくても取り返しにくい間違いになる。

判定は左クリックと**同じ道**（`PageView._pick_at`）を通す。別々に書くと、右クリックで選ばれるものと、そのまま引いたときに動くものが食い違う。

#### 項目は既存の QAction を写して並べる。作り直さない

有効・無効の切り替えと文言の書き換え（「しっぽを消す／出す」など）は `MainWindow._refresh` が1か所でやっている。右クリック用に別の項目を立てると同じ処理をもう1組書くことになり、片方だけ直し忘れる。道具箱をメニューと同じ `_tool_actions` から作っているのと同じ線引き。

例外は「ここに〜」「ここで〜」の項目だけ。**押した場所を持てるのは右クリックだけ**で、メニューバー側には対応する項目が無い（場所が決まらないため、道具に持ち替える形になっている）。この2つは押されたメニューと一緒に捨てる。

写す元として持つのは **QAction の一覧であって、メニュー（QMenu）そのものではない**。メニューを持つと、誰かがメニューバーを辿った拍子に右クリックのメニューが組めなくなる（2026-08-04 に修正 → [PySide6の落とし穴.md](PySide6の落とし穴.md) の 1）。

#### 「ここに〜」はドラッグの無い経路として、クリックと同じ道を通す

メニューから作るときはドラッグが無いので大きさが決まらない。**大きさ0の矩形を渡し、「クリックだけで置いた」ときの扱い**（既定の大きさ・用紙内への寄せ・6.9 の道具の戻し方）をそのまま使う。既定値や案内文を書き写すと2か所を直すことになる。

「ここにセリフを追加」だけは、置いたあとそのまま入力欄が開く。**ここは検証環境で確かめられない。** メニューが閉じるときの焦点の戻り方（入力欄が焦点を得る前にメニューへ奪われると `focusOut` で閉じてしまう）が絡み、offscreen のテストでは焦点の動きを追えないため。**2026-08-04 に実機で確認済み**——メニューから作った直後に入力欄が開いたまま残り、そのまま打てる。

#### 分割はその場で1回きり実行する。道具は持ち替えない

メニューバー側の「横に分割」は道具の切り替え（選んでから、割る場所を押す）だが、右クリックは**押した場所が既に分かっている**ので、その場で割って終わる。ここで道具に持ち替えると、次に画面を押したときに割るつもりのない場所が割れる（6.9 と同じ理由）。

#### 「削除」は対象を名前に出す（2026-08-04 に直した）

項目名を「削除」ではなく「**コマを削除**」「**画像を削除**」「**セリフを削除**」「**フキダシを削除**」にする。Delete キーの結果も同じ判定なので、メニューバーの編集メニューにも同じ名前が出る。

**画像を消すつもりでコマを消した、という取り違えが実際に起きた。** コマの中の画像を選ぶにはダブルクリックで一段踏み込む必要があり（6.3）、右クリックしただけではコマが選ばれるため、メニューには「削除」と書かれたコマの削除だけが並んでいた。

ホバーで案内を出す形も考えたが、**カーソルは押そうとしている項目の上にある**ので、名前そのものを変えるほうが早く気づける。ステータス表示の右側には既に「コマを選択中: …」が常時出ており、それを見て気づけていない実績もある。

呼び名と実際に消えるものは `MainWindow.delete_target` で**まとめて決める**。別々に持つと、「削除」と書いてあるのに違うものが消える状態を作れてしまう。

#### コマの上でも、カーソルの下の画像を消せる

名前を変えても「では画像はどうやって消すのか」は残る。ダブルクリックで踏み込む約束を知らないと辿り着けないので、**コマのメニューに「この画像を削除」を出す**（カーソルの下に画像があるときだけ）。踏み込まずに消せることがこの項目の目的なので、選択はコマのままで、画像を id で名指しして消す。

同じ理由で「**この画像を差し替え...**」も並べる（2026-08-05 → 6.3）。どちらもカーソルの下の1枚に効くので、名前の形を揃えた。何をするのか（前の絵が消えること）は名前を伸ばさず、カーソルを乗せた間の説明に逃がしている。

#### 入力中の右クリックは、まず確定する

Qt 標準の入力欄メニュー（コピー・貼り付け）には**任せない**。メニューへ焦点が移った時点で入力欄の `focusOut` が走り、メニューを開いたまま入力欄がシーンから外れる。左クリックと同じ「画面を触ったら確定」に乗せる（→ 6.11 の確定の目印と同じ考え方）。

画面移動の最中（スペース押し・中ボタン）も出さない。掴んだままメニューが割り込むと、離しても移動が終わらない。

### 6.13 ふわふわ_フキダシ（不安表現） → **実装済み（2026-08-04）**

輪郭がなめらかに波打つ3つ目の種類（`style` が `"wavy"`）。**弱った声・動揺・不安**に使う。

道具の1文字キーは持たない（`W` は 2026-08-07 に雲_フキダシへ移した → 6.22）。「フキダシ > ふわふわ_フキダシを追加」、または右クリックの「ここにふわふわ_フキダシを追加」から置ける。既にあるフキダシを「ふわふわ_フキダシにする」で変えることもできる（3種は相互に変更できる）。

#### ギザギザで代用すると意味が逆になる

丸い（通常）とギザギザ（叫び）の2種では、**弱っている声を表す形が無い**。ギザギザで代用すると叫びに読めてしまい、伝わる内容が逆になる。形を1つ足すだけで、この取り違えが起きなくなる。

#### 作り方はギザギザと同じ経路に乗せる

楕円の半径を角度ごとに増減させる（→ 6.4）。違いは増減のさせ方だけで、**交互ではなく余弦波でなめらかに**動かす。追加したのは形を作る関数1つと、`balloon_outline` の分岐1行。

- **山で半径 1.0、谷で 1 - depth。** 始点（角度 0）が必ず山になるので、一周してちょうど山へ戻る
- **頂点数は波の数の整数倍にする。** 半端だと最後の波だけ途中で打ち切られ、始点との継ぎ目に角が出る。ギザギザで「必ず偶数」にしているのと同じ理由
- **しっぽの付け根は波の谷より内側**（`1 - wavy_depth`）。輪郭の上に置くと、谷にあたる位置で本体と三角形が離れて隙間が空く（→ 6.4）

#### 1波あたりの線分数に下限を置く

**なめらかさがこの形の意味そのもの**なので、`WAVY_MIN_SEGMENTS_PER_WAVE`（8本）を下回らせない。楕円の分割数（72）を波の数（16）で割ると1波あたり5本になり、山と谷の間が直線になって**角が立つ＝ギザギザとの差が消える**。分割数の設定より下限を優先する。

#### 既定値は描いて決めた

**波 16・深さ 0.09**（`wavy_waves` / `wavy_depth`）。ギザギザ（山 14・深さ 0.33）より**多く・浅く**してある。

深くしすぎると花びらや雲に見えて、叫びとの差ではなく別の物になるおそれがある（注意点）。3種を並べて描き、2026-08-04 に目で確かめて決めた。**数では確かめられない性質**なので、自動テストには「山と谷の間に収まる」「隣同士が跳ねない（ギザギザとの差）」までしか入れていない。

### 6.14 マーク（ビックリマーク） → **実装済み（2026-08-04）**

**背景を透明にした PNG を、ページの上に直接置く。** アプリ側で描き文字を生成はしない（→ 10.2。描き文字そのものは 2026-08-06 に範囲外と決めた）。

最初に入れるのは2種。どちらも**アプリに組み込んだ素材**（`manga_layout/stickers/`）で、道具は `M`、または「マーク > ビックリマークを追加」。右クリックの「ここにビックリマークを追加」からも置ける。

| 画面の呼び名 | 保存形式の `kind` | 素材ファイル | 寸法 | 傾き |
|---|---|---|---|---|
| ビックリマーク | `exclaim` | `exclaim.png` | 224 × 297 | **素材の側で傾けてある** |
| ビックリはてなマーク | `exclaim_question` | `exclaim_question.png` | 214 × 240 | なし（当面このまま使う） |

#### コマの子にすると要求そのものを満たせない

いまの画像（`Image`）はコマの子なので**コマ枠で切り抜かれる**（→ 4章）。マークは**コマからはみ出して置くのが普通**なので、この置き方では使えない。

**フキダシと同じ形にする。** ページ直下（`floating`）に置き、コマへは `attached_panel_id` で紐づける（→ 4章「フキダシをコマの子にしない」）。コマを動かせば付いてくるが、切り抜かれない。紐づけ先の決め方・コマ削除で巻き添えにしない扱いも、フキダシで既に動いているものをそのまま使う。

#### 重なりは段を1つ増やす（フキダシの上・セリフの下）

重なりの順は `z` ではなく**種類で決まる段**で持っている（→ 4章）。ここに1段足す。

    用紙 → コマ（と中の画像）→ フキダシ → **マーク** → セリフ

- **フキダシより上**。マークはフキダシに重ねて置くもので、下に潜られると用が無い
- **セリフより下**。上に置くと、位置を誤ったときに**セリフが読めなくなる**。フキダシを後から足すとセリフが消えていた不具合（→ 4章）と同じ形の事故で、段の順で最初から起こらないようにする
- 段は**種類から毎回求めるもので、保存形式には持たない**。だから段を1つ増やしても既にある作品に影響しない
- 当たり判定を見る順（`layout` 側）も同じ順に合わせる。**描く順と拾う順がずれると、見えているものを掴めなくなる**

#### 傾きは素材に焼き込む。マークはアプリで回転しない

10.1 では「ここで回転が要求として立つ」と見ていたが、**傾けた PNG を用意すれば回転は要らない**と分かった。`rotation` は枠だけ持ったまま常に 0 とする。

これで**当たり判定・つまみ・吸着・はみ出しの判定が全部矩形のまま**で済む。斜めコマで「ペア単位に縛る」と決めたのと同じ線引き（→ 6.10）。

角度違いが要るようになったら、**素材を1枚足すほうが安い**。素材は2枚しかなく、増やす操作も1クリックなので、この交換は成立する。

**2026-08-05 に、コマの中の画像（`Image`）だけ回転できるようにした（→ 6.3）。この節はマークについては変えない。** 画像は利用者が持ち込むもので枚数も角度も決まっておらず、「素材を1枚足す」で代えられないため、線引きが違う。

#### 素材は組み込みだが、貼った実体は作品の中に入る

`manga_layout/stickers/` の PNG を読み、**既存の経路で `assets/` へ SHA1 で入れる**（→ 5章）。作品は自己完結するので、あとで素材を差し替えても**既にある作品の見た目は変わらない**。逆に、素材を新しくしても**既に貼ってあるマークは古いまま**になる。

置き場所を `data/` にしなかったのは、`data/` が git の管理外だから（使い捨ての置き場 → 11章）。組み込み素材はアプリの一部なので、コードと一緒に管理下に置き、2台のPCで同じものが揃うようにする。

#### 当たり判定は矩形で始める

傾けてある分、外接矩形の四隅は必ず透明になる。**矩形で判定すると、何も見えていない角をクリックしてもマークが掴める。**

それでも矩形で始める。フキダシを楕円で判定した（→ 6.4）のは輪郭が塗りと一致していたからで、マークの形はファイルごとに違い、輪郭を式で表せない。透明度を見て判定する手はあるが、**掴みにくさが実際に出てから入れる**。

**下の余白削りで、この弱点はかなり小さくなった。** 残るのは傾いた記号の四隅だけ。

2026-08-05、「フキダシの中に置いたマークが掴めない」が実際に出たが、**原因はマーク側ではなくセリフ側だった**（枠の空いた所までクリックを吸っていた → 6.5）。そちらを直せば掴めるので、ここは矩形のまま据え置く。**掴みにくさが出たからといって、出た場所と原因が同じとは限らない。**

#### 素材は解像度を上げ、透明の余白を削って取り込む

最初の素材は 48×48 しかなく、A4（1240×1754 px）に大きく貼ると 5〜8倍に拡大されて輪郭がぼけた。作り直して 360×360 / 381×381 になった（8倍ちょうどではないが、拡大率としては十分）。

作り直したものを測ると、**2枚で余白の割合が違った**。

| | 画像 | 記号の実寸 | 画像に占める割合 |
|---|---|---|---|
| ビックリマーク | 360 × 360 | 224 × 297 | 幅 62% / 高 82% |
| ビックリはてなマーク | 381 × 381 | 214 × 240 | 幅 56% / 高 63% |

このまま同じ 240px 角で置くと、！は高さ 198px、!? は 151px にしか見えない。**同じ大きさで置いたつもりが3割ちがって見える。**

そこで**取り込むときに完全に透明な画素だけを削る**（`tools/import_sticker.py`）。絵そのものは1画素も変えない。これで3つ片づいた。

- 2枚の見かけの大きさが揃う
- **当たり判定の矩形が記号に密着する**（上の弱点がほぼ消える）
- 置いた直後の大きさ（長辺 240px）が素材の原寸あたりに来るので、置いた瞬間からくっきり出る

削り忘れると静かに元へ戻るので、**四辺それぞれに不透明な画素が接していること**をテストで止めている（`tests/test_sticker.py`）。

#### そのほかの決め

| 論点 | 決め |
|---|---|
| 書き出しに出すか | **出す。** 作品の内容そのもの。コマの下地・空セリフの点線枠のような補助表示（`aids`）とは違う（→ 6.7） |
| リサイズ | **常に縦横比を保つ。** 画像なので、比を崩すと記号の形が壊れる。`Shift` で切り替える余地は作らない（→ 6.3 の等比リサイズとは扱いを変える） |
| 置いた直後の大きさ | 縦横比を保ったまま**長辺 240 px**。A4 の幅の 2 割弱で、「大きな！」として置いてから縮めるより足す回数が少ない |
| Undo で戻るか | **戻る。** 保存形式に載る以上そうなる（→ 6.8） |
| 削除 | `Delete`、または右クリックの「マークを削除」。確認は出さない（フキダシと同じ） |

---

### 6.15 複製 → **実装済み（2026-08-05）**

**選んでいるものを1つ写して、少しずらして置く。**「編集 > 複製」と右クリックから。**キーは割り当てない**（理由は 7章）。

同じ形のコマ・同じ形のフキダシを並べる場面が多く、置き直して大きさを合わせる手間がそのまま減る。

#### 写せるもの

| 選んでいるもの | 写るもの |
|---|---|
| コマ | コマと、**中の画像も一緒に**。画像の実体は増えない（同じ SHA1 を指すだけ → 5章） |
| コマの中の画像 | 画像だけ。同じコマの中に増える |
| フキダシ | フキダシと、**上に乗っているセリフも一緒に** |
| マーク | マークだけ |
| セリフ | セリフだけ |
| 斜めに割ったコマ | **写さない。** 理由を状態表示に出して何もしない |

#### 決めたこと

- **右下へ隙間1つ分（`gutter` 35px）ずらす。** 真上に重ねると、写せたのかどうか見て分からない
- **写したほうを選択状態にする**（他の「追加」と同じ）。続けて複製すると階段状に増えるので、2回押して同じ場所に2枚重なることがない
- **コマへの紐づけは引き継ぐ。** 元と同じコマに紐づく
- **フキダシと一緒に写したセリフは、写したフキダシに紐づけ直す。** 元のフキダシに紐づけたままにすると、元を動かしたときに**写したセリフだけが飛んでいく**（→ 6.5）
- **id は振り直す。** 写しは別のものなので、元と同じ id を持たせない
- **Undo で1手で戻る。** ラベルは「コマの複製」「フキダシの複製」のように対象を名前に出す（削除と同じ → 6.12）
- **ページからはみ出しても直さない。** 端のコマを写せば外へ出るが、位置は利用者が決めるもの（ページサイズ変更と同じ → 6.1）
- **画像は、写しがコマの外へ完全に出るなら写さない。** ずらす量（隙間1つ分）ぶんコマの縁へ寄せた画像を複製すると、写しがコマと1pxも重ならず選べなくなることがある（→ 6.3「画像とコマが重ならなくなる操作は弾く」と同じ理由、2026-08-08 に追加）。理由を状態表示に出して何もしない

#### 斜めに割ったコマを写さない理由

斜めのコマは**2枚1組**で形が決まっている（→ 6.10）。片方だけ写すと、相方のいない平行四辺形が1枚できる。組に属さないコマとして扱うと、外側の矩形も向きの反転も求められない。

組ごと写す道もあるが、それは**組の管理まで触る**話になる。使いたくなってから考える。

**押せる状態のまま断る。** グレーにすると「写せない」ことは伝わるが、**理由が伝わらない**。斜めのコマは選んだときの見た目が普通のコマと変わらないので、出所が斜めであることに気づけない。削除ではロックしたコマをグレーにしたが（→ 6.17）、あちらは状態表示に「ロック中」が常に出ているので気づける。ここには対になる表示が無い。

#### 写したものは、写した時点の姿を持つ（実装して決めたこと）

書き写す形ではなく、**保存形式を1往復させて写す**（`_detached_copy`）。`Project.copy()` が Undo のスナップショットで使っているのと同じ理屈で（→ 6.8）、`to_dict()` に載らない項目は複製にも載らない。手で項目を書き並べると、あとから項目を足したときに**写し忘れても静かに通る**。

この形にしたことで、下の2つは書かずに済んだ。

- **集中線はそのまま乗る。** 中心も空きも割合なので（→ 6.16）、ずらしても直すところが無い
- **画像の傾き（`rotation`）もそのまま乗る**（→ 6.3）

#### しっぽの先端も一緒にずらす（実装して決めたこと）

フキダシを動かしたときは**先端を置いていく**（→ 6.4。先端はしゃべっている人物を指すページ座標なので、置き場所を変えても指す相手は変わらない）。**複製はこれと逆にする。**

ここは「同じ形のフキダシを並べる」操作なので、先端を置いていくと**写しだけしっぽが伸びて別の形になる**。指す相手を引き継ぐより、形を引き継ぐほうが目的に合う。

#### 写しはロックしない（実装して決めたこと）

ロックしたコマも複製できる。**ただし写しはロックしない**（→ 6.17 の「新しく作ったコマはロックしない」と同じ）。

複製が止まらないのは、ロックが止めるのが**形と削除**だけだから（→ 6.17）。元のコマは1px も動かないので、止める理由が無い。写しのほうは置き場所を決めるために作ったものなので、いきなり動かせないと写すたびに解除の手間が挟まる。

#### 呼び名は削除と同じものを使う（実装して決めたこと）

「コマを複製」「フキダシを複製」の呼び名は、削除（→ 6.12）と**同じ1つの関数**（`ui/state.py` の `object_label`）から取る。書き分けると、「コマを複製」と出ているのに「画像の複製」で履歴に積まれる、といった食い違いを作れてしまう。マークの呼び名だけが `kind` ごとに違うので、そこも同じ関数の中で引く。

### 6.16 集中線 → **実装済み（2026-08-05）**

**コマの中に放射状の線を引く。図形として生成し、コマの属性として1つ持つ。**

「コマ > 集中線 > 入れる」と、コマの上での右クリックから。**キーは割り当てない**（→ 7章）。

| 操作 | 内容 |
|---|---|
| 入れる / 消す | コマを選んで、メニューまたは右クリックから |
| 中心を動かす | コマを選んでいる間に出る**十字**のつまみをドラッグ |
| 内側の空きを変える | 空きの縁の右に出る**四角**のつまみを左右にドラッグ |
| 本数・太さ | メニューで増減 |
| 形を振り直す | `seed` を引き直して、ばらつきだけを作り直す |

#### 貼るのではなく生成する

10.1 で先に決めるとしていた論点。**生成を採る。**

貼る側（マークと同じ経路 → 6.14）はほぼ流用で済むが、**置いたあとに中心をずらせない**。マークで「角度違いは素材を1枚足すほうが安い」と言えたのは角度が数種類で済むからで、**中心の位置は連続量**なので同じ交換が成立しない。素材が際限なく増える。

生成にすると、**コマ枠の切り抜きが実装を安くする**。線を中心から「外接矩形のどの頂点よりも遠く」まで伸ばしておけば、端の処理を書かずに済み、斜めのコマ（→ 6.10）でも隙間なく埋まる。

#### コマの子ではなく、コマの属性にする

10.1 では「コマの子にするのが自然」と見ていたが、**独立したオブジェクトにはしない**。

集中線は**コマの中に収まるもの**で、コマと別の大きさを持つ意味が薄い。矩形を持たせると、コマを縮めたときに集中線だけ取り残される状態を作れてしまう。**矩形を持たず、常にコマいっぱいに広がる**ことにすれば、その食い違いが最初から無い。

代償として **1コマに1つまで**になるが、2つ重ねる場面が思い当たらない。要求が立ってから考える。

この決めで、選択・移動・リサイズ・削除・複製・右クリックの一式に触らずに済む。**選択できる種類を増やさない**（コマ・画像・フキダシ・セリフ・マークの5つのまま）。マークを足したときは種類を1つ増やしたので一式に手が入ったが、あれは**コマからはみ出して単独に置くもの**だったため必要だった。集中線は違う。

#### 描く順は画像より手前・コマ枠より奥

切り抜きの中で、コマの子の画像を全部描いたあとに描く。z は持たない（1つしか無いので順序を決める相手がいない）。

コマ枠より奥にするのは、線が枠線を覆うと**コマの輪郭が途切れて見える**ため。画像の描画がコマ枠より先なのと同じ理由（→ `PageRenderer.draw_panel`）。

#### 内側は白く塗らない。線を引かないだけ

10.1 では「内側の空き（**白く抜く**楕円）」と書いていたが、**塗らない**。

このアプリでは集中線が画像の**上**に乗るので、白で塗ると**下の絵が消える**。ネームで中心に置くのは見せたい人物なので、要求と逆の結果になる。線を引かない領域を空けるだけにすれば、下の絵はそのまま見える。

塗りが要るようになったら、**塗る/塗らないの切り替えとして後から足せる**（形の計算は変わらない）。

**空きの形は真円**（短辺基準）。コマの縦横比に合わせた楕円にする案もあったが、実際の抜きは人物の輪郭に沿うもので、楕円1つで合わせきれる性質ではない。合わせ込みは清書側の仕事（→ 1章）。

#### 線は棒ではなく楔形

**外周が太く、中心に向かって細くなる細長い三角形**を塗る。幅一定の棒を放射状に並べると、中心付近で線が詰まって黒く潰れ、集中線に見えない。

三角形の頂点は**内側の空きの縁**、底辺は外周側。頂点を中心そのものに置くと、空きの中へ食い込む。

#### ばらつきの乱数は自前で持つ

`seed` から線の角度・太さ・空きの縁を揺らす。**Python の `random` は使わない。**

`random` の出す並びは処理系の実装に属するもので、**将来それが変われば、同じ `seed` から違う形が出る**。保存した意味が消え、書き出した PNG と画面が食い違う——`seed` を保存する目的そのものが崩れる。数行の擬似乱数（線形合同法。前の値から次の値を決める単純な計算式）を自前で持てば、この危うさが丸ごと消える。2台の PC で同じ形が出ることも同時に保証される。

**種は掛け算1回では混ざらなかった。** 線形合同法は下位の桁の質が悪く、隣り合う種から始めると1歩進めても近い値のままになる。実測で、種 100 と 101 の最初の値が **0.02 しか違わず**、1本目と2本目の向きが揃って見えた。シフトと掛け算を交互にかけて、1ビットの違いを 32 ビット全体へ散らしてから回している（`_Noise._scramble`）。

**乱数が出す値そのものをテストで固定してある。** ここが落ちたときは、既にある作品の形が変わっているということなので、乱数の作り方を変えてよいかどうかから考える。

`seed` は入れた時点で1つ決めて焼き付ける。「形を振り直す」だけが引き直し、**1手として履歴に積む**（Undo で前の形に戻る）。

#### つまみを見る順

コマの**角と辺のつまみより後、本体のドラッグより先**に判定する。

- 角・辺より後にするのは、中心がコマの隅に寄っているときにそれらを覆い隠さないため（斜めの境界のつまみと同じ → 6.10）
- 本体より先にするのは、中心のつまみがコマの真ん中あたりに出るため。後にすると、掴んだつもりがコマの移動になる

つまみの形は既にある意味に合わせる。**四角＝大きさを変える**なので空きの縁に使い、中心には空いている**十字**を当てる（丸は画像の回転、ひし形はしっぽの付け根で使っている → 6.3、6.4）。

#### 既定値は描いて決める

本数・太さ・空きの大きさの既定は `FocusSettings`（`focus.py`）に置く。コマ割り（`LayoutSettings`）や吹き出し（`BalloonSettings`）と別にしてあるのは、調整したい場面が違うため。

**本数 72・太さ 0.018・空き 0.15** に決めた。8通りを並べて描いて選んでいる（2026-08-05）。本数 36 では線の間が空いて集中に見えず、120 では紙面が黒くなって下の絵が読めない。太さ 0.035 は外周で塊になり、0.008 は下書きとしては細すぎた。**数では確かめられない性質**なので、目で見て決めている（ふわふわ_フキダシの既定値と同じ流儀 → 6.13）。

コマごとの値は入れた時点で焼き付けるので、**設定を変えても既にあるコマは変わらない**（斜めの角度と同じ → 6.10）。

#### そのほかの決め

| 論点 | 決め |
|---|---|
| 書き出しに出すか | **出す。** 作品の内容そのもの。`PageRenderer` を通るのでサムネイルにも自動で出る（→ 6.1、6.7） |
| Undo で戻るか | **戻る。** 保存形式に載る以上そうなる（→ 6.8） |
| 線の色 | **黒のみ。** 白集中線（黒地に白い線）は要求が立ってから → **2026-08-05 に実装（6.19）** |
| 斜めに割ったコマ | **入れられる。** 中心はそのコマ自身の外接矩形が基準。実形より外接矩形が大きいが、切り抜きで正しく出る |
| コマを消したら | 一緒に消える。コマの属性なので、紐づけを外して残す相手がいない（フキダシ → 6.2 とは違う） |
| コマを割ったら | **前半（上または左）に残る。** 分割は元のコマを前半として id ごと残すので、そのコマの属性も残る。新しくできた側には付かない |
| 流線（平行に流れる効果線） | ~~今回は範囲外~~ → **2026-08-06 に実装（→ 6.26）。** 「`focus_lines` に `kind` で混ぜず、別の項目にする」はそのまま採った。生成の仕組みのうち共用したのは**乱数だけ**で、線の形（楔ではなく紡錘）と端の処理は共用できなかった |

### 6.17 コマの位置ロック → **実装済み（2026-08-05）**

**完成したコマを、誤って動かさないようにする。**「コマ > ロックする」と、コマの上での右クリックから。**キーは割り当てない**（→ 7章。6.15 複製と同じ判断）。

コマ割りが決まったあと、絵やセリフを入れている最中にコマ本体を掴んで動かしてしまう事故を防ぐ。フキダシやセリフを掴み損ねた指が、下に敷いてあるコマを動かしてしまう場面がそのまま無くなる。

| 操作 | 内容 |
|---|---|
| ロック / 解除 | コマを選んで、メニューまたは右クリックから。ロック中は「🔒ロックを解除」に文言が変わる（→ 6.12 と同じ仕組み） |
| ページ全部をロック / 解除 | 「コマ > このページのコマをすべてロック／すべて解除」。コマ割りが決まった時点でまとめて掛ける |

#### 止めるのは形と削除。中の画像は今まで通り

ロック中のコマで**できなくなる**のは、本体の移動・大きさ変更・分割・コマの削除の4つ。

**できる**ままなのは、選択・中の画像の貼り付け／読み込み／移動／大きさ変更／回転／削除、フキダシ・セリフ・マークの追加、集中線の増減や形の振り直し。「コマの箱は確定、中身はこれから」という状態に合う線引き。中の画像まで止めると、差し替えるたびに解除する手間が挟まる。

斜めの境界のつまみ（→ 6.10）も本体の形を変える操作なので、移動・大きさ変更と同じ扱いで止める。

#### 選んだときだけ分かる。常時は出さない

ロックしていても、他のコマと同じ見た目のまま置いておく。**選んだときだけ**、ステータス表示に「ロック中」と出す。掴もうとして動かなければ気づける、集中線の空きの縁の点線と同じ考え方（→ 6.16）。

つまみ（8方向の四角、回転の丸）は**出さない**。出したまま効かないのが一番たちが悪い。掴めないことが見た目で先に分かる。

#### メニューの項目そのものに、今ロック中かを出す（2026-08-06 追加）

項目は「ロックする」↔「🔒ロックを解除」で入れ替わる。**ロック中のときだけ頭に錠前が付く。**

文言だけでも状態は読み取れるが、「解除と出ている＝今ロック中」という**裏返しの読み方**を一度挟む。錠前は今の状態をそのまま見せるので、その一手間が消える。**押した結果を名前に出す**（→ 6.19「入れる／消す」）という決まりはそのままで、印を足しただけ。

「ロック」を「ロックする」にしたのも同じ理由。「ロック」は名詞にも命令にも読めて、**状態を書いているのか押した結果を書いているのか紛れる。** 相方が「ロックを解除」という動作の名前なので、こちらも動作に揃える。

上の「常時のアイコンは付けない」とは別の話。あれは**キャンバスに置きっぱなしのアイコン**を指していて、こちらはメニューを開いた間だけ見える。画面が常時うるさくなることはない。

#### 錠前は絵文字（U+1F512）をそのまま文言に入れる（実装して決めたこと）

アイコン画像（`QAction.setIcon`）を用意する道もあるが、**素材と描画の面倒を1つ増やすだけで見えるものは同じ**。文言の一部にすれば、メニューバーと右クリックが同じ QAction を共有している今の形（→ 6.12）に何も足さずに済む。

**絵文字を避ける理由は無かった。** 心配されたのは Python での文字化けだが、確かめたところ次の2点だった。

- **画面に出るぶんには問題無い。** Qt は Segoe UI Emoji で色付きの錠前を描く（実測 2026-08-06）。
- **危ないのは CP932 のパイプ・ファイルへ「印字」したときだけ。** このアプリは `print` もログ出力も持たないので、その経路自体が無い。しかも**レ点（U+2713）でも同じように落ちる**ので、絵文字だから危ないという話ですらない。

そのうえで、テストでは**錠前そのものを assert に書かない**。落ちたときの pytest の出力をリダイレクトすると、上の唯一の危ない経路を自分で作ってしまうため。文字数と「ロック」で始まらないことを見る。

#### 選択はできる。掴めなくするだけ

選べなくすると、解除するための入り口（メニュー・右クリック）へたどり着けなくなる。**選択はできて、動かす・変える・消す操作だけ弾く。**

#### 吸着の相手にはなる

ロックしたコマは、他のコマや画像が寄ってくる基準として今まで通り使える。**動かないコマこそ、位置を揃える基準にちょうど良い。**（→ 6.2 の吸着）

#### 斜めに割った組は、片方がロックなら組ごと止まる

斜めのコマは2枚1組で形が決まっている（→ 6.10）。片方だけロックを解いて動かせる状態は、相方との整合が崩れる。**組のどちらかがロックなら、境界のドラッグも含めて組ごと動かせない。**

解除も同様に、**組のどちらかを解除すると両方解除**になる（片方だけ解いても動かせないままでは意味が無いため）。

#### 新しく作ったコマはロックしない

`P` で追加した直後のコマ、分割で新しくできたコマは、いずれもロックなし。**ロックは自分で掛けるものであって、既定では掛からない。**

コマを割ったときの継承は集中線と同じ形（→ 6.16 の「コマを割ったら」）：**前半（上または左）に残る側**が元のロック状態を引き継ぎ、新しくできた側は常にロックなし。分割自体はロック中のコマではできない（上の「止めるのは形と削除」）ので、割った直後の新しいコマは常に無条件でロックなしになる。

#### 決めたこと

| 論点 | 決め |
|---|---|
| 止める範囲 | **形と削除だけ。** 中の画像・紐づく吹き出し／セリフ／集中線は今まで通り触れる |
| 見せ方 | **選んだときだけ**ステータス表示に出す。キャンバスに常時のアイコンは付けない |
| メニューの文言 | **「ロックする」↔「🔒ロックを解除」。** ロック中だけ頭に錠前を付ける（2026-08-06） |
| 一括操作 | **入れる。** ページのコマをまとめてロック／解除できる |
| キー割り当て | **しない。** 押し間違いで固まると、原因に気づきにくい（→ 6.15 と同じ） |
| Undo で戻るか | **戻る。** 保存形式に載る以上そうなる（→ 6.8） |
| 保存形式 | `Panel.locked`。**入れていない（false）コマでは項目ごと省く。** ロックを使っていない作品の project.json はこの機能の追加前と同じ内容のまま（→ `focus_lines` と同じ線引き → 6.16） |
| 削除確認との関係 | 別の仕組み。ロックは「掛け忘れなければ守られる」、削除確認ダイアログは今まで通り付けない（→ 6.2） |

### 6.18 付箋 → **実装済み（2026-08-05）**

**サムネイル一覧の各ページに、3色の付箋を1つ貼れる。**「ここから再開」「あとで手直し」といった**一行のメモ**を添えられる。

ネームは何日かに分けて描くので、次に開いたとき**どこまで進んでいて、どこを直すつもりだったか**が分からなくなる。ページ数が増えるほど、一覧を上から見返す時間が増える。

| 操作 | 内容 |
|---|---|
| 貼る / 色を変える | 一覧の項目を右クリック → 色を選ぶ |
| メモを書く | 右クリック → 「メモ...」で一行の入力欄 |
| はがす | 右クリック → 「はがす」 |
| 見る | 一覧の縮小画像の上に色が重なる。メモはホバーで全文 |

#### 色に意味を持たせない

**3色に「要修正」「保留」のようなラベルを割り当てない。** 色は同じ意味の付箋をまとめて目に入りやすくするための**識別**であって、意味は一行メモに書く。

10.1 では「自由に選べると色ごとの意味が定まらず、翌日には自分でも読めなくなる」として色を絞ることにしたが、**絞るだけでは意味は定まらない**。定めようとすると、今度は決めた意味に合わない使い方がしづらくなる。ネームを描く途中で必要になる区別は回ごとに違うので、**意味はその場でメモに書けるほうが合う**。

色は3色。区別のために2色では足りず、4色以上になると「どの色にしたか」を思い出す手間が、色を見て分かる利得を上回る。

#### メモは任意

**色だけ貼れる。** 右クリックから色を選んだ1手で貼り終わり、入力欄は出さない。メモは後から別の項目で書く。

貼るたびに入力を促す道もあるが、「ここが最後」を印すだけの用途が一番多い。**その用途に毎回 Enter を1回足すのは、機能を使わない理由になる。**

#### 1ページに1つ

複数貼れるようにすると、一覧に何個並ぶか分からなくなり、**幅を広げない**（→ 6.1）という前提と噛み合わない。1つで足りないと分かってから考える。

#### サムネイルの指紋から外す

付箋は**保存形式に載るが、用紙の絵には出ない**。一覧のサムネイルは「ページの保存形式そのものを指紋にして、変わっていなければ描き直さない」仕組みで動いている（→ 6.1）が、その根拠は「**保存に載らないものは見た目にも出ない**」だった。付箋はこれを裏返す初めての項目になる。

そのままにすると、付箋を貼るたびにそのページの**絵をまるごと描き直す**。しかも指紋が変わった時点で前の絵は捨てられるので、付けて外して戻しても再利用されない。

**指紋を作るときだけ付箋を除く。** そのうえで、付箋は**サムネイルの絵には焼き込まず**、一覧が1行を描く段で縮小画像の上に重ねる。絵と付箋を別々に持てば、付箋を変えても絵は使い回せる。

**除外は指紋の側の1箇所に閉じる。** 保存形式・Undo・読み込みはどれも今まで通り付箋を含む。

#### 本画面と書き出しには出さない

作品の内容ではなく**作業の覚え書き**。コマの下地・空セリフの点線枠と同じ線引き（→ 6.7 の `aids`）。用紙の上に置くと作品と混ざり、書き出した PNG に残れば渡した相手にも見える。

**一覧でしか見えない**ことが、そのまま「消し忘れても害が無い」という性質になる。

#### 付箋のあるページを探す手段は入れない

「次の付箋へ飛ぶ」キーは**今回は作らない**。一覧をスクロールして色を目で拾う形で始め、ページ数が増えて足りなくなってから考える。

#### 決めたこと

| 論点 | 決め |
|---|---|
| 何に持たせるか | `Page.note`（`{"color": "...", "text": "..."}`）。1ページに1つ |
| 色 | **3色。意味は固定しない**（→ 上） |
| メモ | **任意。** 色だけでも貼れる |
| 入り口 | **一覧の右クリックのみ。** 行に常時出すボタンは付けない（一覧の幅を圧迫しない → 6.1） |
| 見せ方 | **縮小画像に重ねる。** 番号欄の脇に列を増やさない |
| 本画面に出すか | **出さない** |
| 書き出しに出すか | **出さない** |
| Undo で戻るか | **戻る。** 保存形式に載る以上そうなる（→ 6.8） |
| 保存形式 | 付箋の無いページでは**項目ごと省く。** 使っていない作品の project.json はこの機能の追加前と同じ内容のまま（→ `slant_pairs`・`focus_lines` と同じ線引き） |
| 古い作品 | 項目が無ければ「付箋なし」（→ 5章） |
| サムネイルの指紋 | **付箋を除く**（→ 上） |
| 探す手段 | **今回は入れない** |

### 6.19 集中線の白（色違い） → **実装済み（2026-08-05）**

**集中線を白にできる。** 形の計算・つまみ・本数と太さの調整は何も変わらない、**単純な色違い**。6.16 で「白集中線（黒地に白い線）は要求が立ってから」としていた分を、要求が立ったので入れた。

暗い背景（夜・影・墨ベタ）に集中線を重ねると、黒い線は背景に沈んで見えない。白にすればそのまま使える。

| 操作 | 内容 |
|---|---|
| 切り替え | 集中線メニュー（コマの右クリックからも）の「白にする／黒に戻す」。入れる／消すと同じく1項目の文言を入れ替える |

#### `FocusLines` に持たせる。独立した項目にはしない

色は集中線そのものの属性で、位置や本数と別の場所に置く理由が無い。`white: bool`（既定 False＝黒）を足しただけで、`locked`（→ 6.17）と同じく**立っていない作品では保存形式から項目ごと省く**。この機能を使っていない project.json は追加前と一字一句変わらない。

#### 塗り分けだけ。形は共有する

`focus_triangles`（`focus.py`）は色を知らない。三角形の並びは今までどおり1か所で作り、**描く側（`render.py`）が最後に色を選ぶだけ**にした。形と色を1つの関数に混ぜると、色を増やすたびに形の計算まで書き直す羽目になる。

#### メニューに色選びは作らない

黒と白の2択で、しかも「単純な色違い」という要求そのものが2値。色を選ぶダイアログやパレットは、選べる色が2つしか無い場面では選ぶ手間のほうが大きい。増やす要求が立ってから考える。

### 6.20 四角_フキダシ（ナレーション・地の文） → **実装済み（2026-08-05）**

輪郭が矩形の4つ目の種類（`style` が `"rect"`）。**ナレーション・地の文**に使う。
誰かのセリフではないので、**しっぽを消した状態で置く**。

道具の1文字キーは持たない。「フキダシ > 四角_フキダシを追加」と、右クリックの「ここに フキダシ を追加」から置く。既にあるフキダシを「四角_フキダシにする」で変えることもできる（4種は相互に変更できる）。

#### 角は丸めない。設定項目も持たない

丸めると半径の設定が1つ増えるうえ、ナレーションの枠は尖っているほうが普通。輪郭は矩形の4隅そのままで、他の3種と違って `BalloonSettings` を1つも見ない（分割数も谷の深さも効く余地が無い）。

足したのは形を作る関数1つと `balloon_outline` の分岐1行で、ふわふわ（→ 6.13）と同じ形。画面・サムネイル・PNG 書き出しの3つが同時に新しい形になる。

#### 当たり判定だけは、楕円のままにできなかった

**6.4 で解いた問題が、ここで裏返る。**

丸い3種を楕円で判定しているのは、矩形だと四隅の何も無い場所で掴めてしまい、下のコマが選べなくなるため。四角ではその四隅が**形の一部**なので、楕円のまま残すと**見えている箱の角を押しても判定から漏れ、下のものが選ばれる**。

`balloon_contains` に種類の分岐を1つ足した。**「押しても掴めない」という気づきにくい形で出る**ので、四隅が当たること・種類を変えると判定も入れ替わることをテストで固定してある。

#### しっぽは「置いたときだけ」消す。種類を変えたときは触らない

`BALLOON_STYLES_WITHOUT_TAIL` に載っている種類は、置いた時点で `tail.enabled` が `false` になる。**禁止ではない**ので、あとから「しっぽを出す」で出せる。

**種類を変える操作では消さない。** 消すと、しっぽを付けた既にあるフキダシを四角にしただけで、それが黙って失われる。「置いたときの既定」と「あとから変えたとき」は別の話として分けてある。

先端の位置は、しっぽの無い四角にも他の種類と同じに入れておく。入れないと、あとで「しっぽを出す」を選んだときに向きの決まらないしっぽが出る。

出したときの付け根は**内接楕円の 0.95** のまま（他の種類と同じ計算）。四角に内接する楕円はどの向きでも箱の内側にあるので、付け根はいつも本体に隠れ、三角形が辺を突き抜けて出る。辺に沿った厳密な付け根は作らない——**破綻しなければ足りる**という線引き。

#### メニューは種類を畳む

種類が3→4になったのに合わせ、**「◯◯にする」と、右クリックの「ここに◯◯を追加」をそれぞれ下位のメニューへ畳んだ**。集中線を畳んでいるのと同じ扱い（→ 6.12、6.16）。畳まないと、フキダシを置かない場面でも「ここに」の組だけで縦に4行取り、コマ・マーク・セリフが下へ押し出される。

畳むときに **PySide6 の地雷を1つ踏んだ**。`addMenu()` の戻り値（見出しの QAction）を有効・無効の一覧に控えたところ、メニューバーを辿られた時点で実体ごと消え、以後の画面更新が落ちるようになった。見出しは控えず、中身だけを控える形に直してある → [PySide6の落とし穴.md](PySide6の落とし穴.md) の 1。

### 6.21 丸い飛びしっぽ（心の声・独り言） → **実装済み（2026-08-05）**

しっぽの2つ目の形。付け根から先端へ向かって、**小さくなる円が3つ**並ぶ。声に出していない言葉——**心の声・独り言**を表す。

「フキダシ > しっぽを丸くする」、右クリックからも同じ項目で切り替える。三角へはいつでも戻せる。

#### フキダシの種類とは別に持つ

`Tail` に `shape`（`"triangle"` / `"bubbles"`）を足した。種類の側に焼き込むと、丸い・ふわふわ・雲のそれぞれに「飛びしっぽ版」が要って**種類が倍に増える**。別に持てば組み合わせで表せる。

**三角のときは項目ごと省く。** 飛びしっぽを足す前に保存した作品と、三角のままの作品の project.json が一字一句同じになる（`locked` → 6.17、`white` → 6.19 と同じ形）。

**ギザギザ・四角との組み合わせは禁じない。** 禁じると、メニューの分岐・読み込みの検証・「保存済みの組み合わせをどう扱うか」の3つが増える。意味の薄い組み合わせは、選ばない限り出てこない。

#### 6.4 の「1つの輪郭に合成する」規則が、ここだけ逆になる

三角のしっぽを本体と合成しているのは、継ぎ目に枠線が残るのを防ぐため。**飛びしっぽは離れていること自体が「口に出していない」という意味**なので、合成しない。円と円の間も空ける。

ただし `QPainterPath`（Qt が図形をまとめて持つ入れ物）は離れた図形を1つのパスに持てるので、**塗り1回・線1回という描き方は変えずに済んだ**。円を足す2行だけ。

起点は**輪郭のいちばん外側**（割合 1.0）に取る。ギザギザや波形は谷で内へへこむが、外側から測っておけば隙間はその分だけ広がるので、どの種類でも本体に食い込まない。

#### 円の数は3つで固定する

長さで増減させると、**先端を引いている最中に円が生えて形が跳ねる**。数が変わらなければ、引いた量がそのまま間隔の広がりとして目に見える。

#### 大きさはしっぽの長さから決める（描いて分かった）

当初は `Tail.width`（三角のときの付け根の幅）を先頭の円の直径として読み替える設計だった。**描いてみて2つ問題が出た。**

- 既定の長さ（フキダシの高さの 0.375）に対して円が小さすぎ、**鎖ではなく点が散らばった絵**になった
- 先端を引いて伸ばしても円は動くだけで、**引いた量が絵に出なかった**

そこで、隙間を半径に対する割合で持ち（`TAIL_BUBBLE_GAP_RATIO`）、**鎖の全長が起点から先端にちょうど収まるよう半径を逆算する**形にした。伸ばせば円も大きくなり、操作した量が見える。

遠くまで引いたときのために上限を置く（`TAIL_BUBBLE_MAX_RATIO`＝フキダシの短辺の 0.25）。当たった分だけ鎖は先端に届かなくなるが、向きは変わらない。

**`Tail.width` は三角のときだけ効く**ことになった。保存形式は変わらないので、既にある作品には影響しない。

縮み方（0.72）と隙間（0.45）は 6.13 と同じく**並べて描いて目で決めた**。縮めすぎると3つめが点になって数えられず、1.0 に近づけると数珠つなぎに見えて意味が消える。

#### 掴み方は三角と揃える

先端の丸・付け根のひし形・40度の上限（`TAIL_ROOT_MAX_GAP`）は**そのまま使える**。向きと付け根の高さは三角と同じ計算（`tail_base_angle`）を通る。

「見えている形の内側ならどこでも掴める」（→ 6.4）も引き継ぐ。三角と円で2通り書くと片方を直し忘れて挙動が食い違うので、`tail_body_contains` の1箇所で吸収する。

### 6.22 雲_フキダシ（心の声・回想） → **実装済み（2026-08-05）／形を作り直した（2026-08-06）**

輪郭が丸い膨らみの連なりになる5つ目の種類（`style` が `"cloud"`）。**心の声・回想**に使う。置くと**しっぽが丸い飛びしっぽ**（→ 6.21）になる。

道具は `W`、または「フキダシ > 雲_フキダシを追加」。右クリックの「ここに フキダシ を追加」からも置ける。

**`W` は 2026-08-07 にふわふわ_フキダシ（→ 6.13）から付け替えた**（本人の指示）。並ぶ順を使う頻度に合わせたとき（→ `BALLOON_STYLE_LABELS`）雲は2番目に上がったのに、キーの付き方だけが前の頻度のまま残っていた。**キーの本数は増やしていない**——足すこと自体に値打ちは無く、元から通っていたものを塞ぐ副作用は必ず付く（→ 7章）。

#### 狙いは綿ではなく「楕円と楕円のつながり」（2026-08-06 に作り直した）

最初の形（膨らみ9・深さ 0.22）は**綿・雲に寄りすぎていた**。この種類を実際に使うのは**登場人物のふつうのセリフやつぶやき**で、ホラーや不安の場面ではない。求める絵は「少しモコモコした楕円と楕円のつながり」であって、雲そのものではなかった。

作り方（楕円の半径を増減させる → 6.13）は変えず、3つだけ変えた。

| 変えたところ | 前 | 後 | なぜ |
|---|---|---|---|
| 膨らみの並べ方 | 角度で等分 | **弧の長さで等分** | 角度で割ると、縦長のフキダシでは曲がりのきつい上下に膨らみが密集し、長い左右の辺がのっぺり空く |
| 山の頂 | `\|sin\|`（尖った頂） | **`sin` を 0.55 乗**（平たい頂） | 頂が広がるぶん膨らみ1つの半径が大きくなり、円弧に近づく |
| 膨らみの数 | 9 | **5** | 1つ1つを大きく見せる |

**「円を実際に重ねてつなぐ」は試して捨てた。** 楕円の平らな側に大きな円を当てると、その円が上下へ大きくはみ出す。円を大きくすることと楕円の形に沿わせることが両立しないので、半径の増減に戻した。

#### 手描きのゆらぎ（`cloud_jitter`）

膨らみ1つ1つの**幅**とくびれの**深さ**を 0.30 の割合でばらす。全部同じ形だと型で抜いたように見え、手描きの気配が出ない。**高さだけ振っても効かない**——幅が揃っている限り型抜きに見える。

**種はフキダシの ID から作る**（`noise.seed_from_text`）。保存する項目は増えず、開き直しても同じ形に戻る。`hash()` は実行のたびに値が変わるので使えない。

複製すると ID が変わるのでゆらぎ方も変わる。**同じ形が2つ並ばないほうが手描きとしては自然**なので、これは直さない。

#### 既定値は描いて決めた

**膨らみ5・深さ 0.12・頂の平たさ 0.55・ゆらぎ 0.30**（`cloud_lobes` / `cloud_depth` / `cloud_roundness` / `cloud_jitter`）。

2026-08-06 に、既定のフキダシ（縦長）と横長の両方で、数×深さの組み合わせを並べて描いて決めた。

| 外すと | どう見えるか |
|---|---|
| 膨らみ4以下 | 落花生の殻に見える。つながった楕円に読めない |
| 膨らみ7以上 | 綿・雲へ戻る |
| 深さ 0.24 以上 | 彫りが深く、心の声側へ寄る |
| ゆらぎ 0.80 以上 | デコボコが目立ち、輪郭が汚れて見える |

1つの膨らみあたりの線分数の下限（`CLOUD_MIN_SEGMENTS_PER_LOBE`）は 12 から **24** へ増やした。膨らみが大きくなったぶん、12 本では頂に角が立つ。

**数では確かめられない性質**なので、自動テストには「山と谷の間に収まる」「くびれの数が膨らみの数と一致する」「縦長でも上下に偏らない」「ゆらいでも外接矩形に収まる」「同じ種なら同じ形」までしか入れていない（→ 6.13 と同じ線引き）。

#### 置いた時点で飛びしっぽにする

雲もしっぽも心の声を表すので、**三角と組み合わせることがまず無い**。`BALLOON_STYLES_WITH_BUBBLE_TAIL` に載せて、置いたときの既定にした。

**禁止ではない。** あとから「しっぽを三角にする」で戻せる。四角のしっぽ（→ 6.20）と同じく**種類を変えたときには触らない**——既にあるフキダシのしっぽが黙って変わるのを避けるため。

### 6.23 ラフ（下敷き） → **実装済み（2026-08-06）**

**紙に描いたラフを、ページの一番下に敷いてなぞる。** 写真でもスキャンでもよい。コマを置いた部分から順に隠れていくので、どこまで起こしたかが見たまま分かる。

ネームを描く前に紙でコマ割りを試すことがあり、そこで決めた配置を画面で作り直すとき、**紙を横に置いて目分量で写す**ことになっていた。同じ絵の上に重ねられれば、写す手間も目分量もなくなる。

| 操作 | 内容 |
|---|---|
| 敷く | ファイル → ラフ → 「読み込む...」で画像を選ぶ |
| 色を変える | 同じメニューの「ラフを青くする／ラフの色を戻す」。右クリックからも |
| 位置と大きさを直す | 同じメニューの「ラフを調整」で道具に持ち替える。右クリックからも |
| 外す | 同じメニューの「外す」 |

#### `Page.rough` に持たせる。画像でもマークでもない

置き場所は**ページの属性**（1ページに1枚）。付箋（→ 6.18）とまったく同じ形で、敷いていないページは項目ごと省くので、**使っていない作品の project.json はこの機能の追加前と一字一句変わらない**。

`ImageObject` や `StickerObject` として置く道もあったが、そちらにすると「選べる・掴める・複製できる・重なり順がある」が全部付いてくる。ラフは**なぞる相手**であって作品の中身ではないので、どれも要らないどころか邪魔になる（コマを選んだつもりでラフが動く）。

実体は他の画像と同じく `assets/` に SHA1 で入れる。**`referenced_assets()` に数える**のを忘れると「未使用ファイルを整理」が実体を `_unused/` へ移す。マーク（→ 6.14）で一度踏んだ論点だが、ラフは**欠けたときの×印すら出さない**ぶん、こちらのほうが気づきにくい。

#### 用紙のすぐ上、コマより奥に描く

描く順は **用紙 → ラフ → 目安線 → コマ → …**。

- **目安線より奥。** 基本枠がどこかはラフを敷いても変わらないので、上に残す
- **コマより奥。** 画面ではコマの下地（薄い灰色）が不透明に塗られるため、**コマを置いた部分は自動で隠れる**。裏返すと、コマの中を見ながらラフをなぞることはできない。置き終わった場所から消えていく見え方のほうを取った

#### 書き出しには出さない。ただし `aids` には相乗りさせない

書き出しでは描かない（`PageRenderer(rough=False)`）。作品の中身ではないので、渡した相手に見えては困る。

**「画面でだけ要る補助表示」（`aids`）とは別のスイッチにした。** `aids` を落とすのは書き出しだけで、**サムネイル一覧は落とさない**。一覧にもラフを出したいので、相乗りさせると出しようがなくなる。

**一覧にはそのまま出す。** 「一覧は本画面と同じ経路で描く」（→ 6.1）を崩さずに済み、指紋（`Page` の保存形式そのもの）にも自然に乗るので、付箋のような除外（→ 6.18）が要らない。ラフを敷いたページは一覧でも薄く青く見えるので、**まだ起こしていないページがどれか**が一覧で分かる。

#### 青くするのと薄くするのは、切り替えの1手にまとめる

**色を捨ててから青を乗せる**（`images.to_blue_pencil`）。手順は Grayscale に落として、その上から青を Screen 合成するだけ。Screen は「白はそのまま、黒は乗せた色になる」ので、紙の白は白のまま残り、鉛筆の線だけが青くなる。濃淡もそのまま階調として残る。

- **色を先に捨てる**のは、青ボールペンや色鉛筆のラフでも同じ濃さに揃えるため。捨てずに青を乗せると、赤い線だけが黒く沈む
- **灰色ではなく青**にしたのは、鉛筆のラフがもともと灰色だから。灰色に落としてもコマ枠と見分けが付かない。青にすれば、濃さを上げても「これは下敷き」と一目で分かる
- **濃さ（既定 40%）は `settings.json` の `rough_opacity`**（→ 下）。青くするかどうかとは別の話だが、**切り替えは1手にまとめた**。「ラフの色を戻す」は写真の細かいところを確かめるための状態なので、そこで薄いままだと確かめられない

#### 濃さは `settings.json` に出す

`rough_opacity`（0.05〜1.0、既定 0.4）。**作品ではなく、この PC での見やすさの話**なので `project.json` には入れない（→ `settings.py` の冒頭）。焼き込むと、同じ作品を別の PC で開いたときに相手の好みを上書きする。

濃いラフはなぞる線がよく見える代わりにコマ枠と紛れ、薄いラフはその逆。どちらが良いかは元のラフ（鉛筆かペンか、写真かスキャンか）で変わるので決め打ちにできない。JPG 品質（→ 6.7）と同じく**窓には出さず、必要な人が手で書き換える**。

**読み込む直前に読み直す。** その値が初めて画面に出る瞬間なので、書き換えてからアプリを開き直させない（→ `ProjectIO.default_parent` と同じ扱い）。

#### 掴めるのは、道具を持ち替えている間だけ

位置と大きさは**「ラフを調整」の道具**（`TOOL_ROUGH`）に持ち替えて直す。持ち替えている間は**ラフしか掴めない**。

一番下に敷いてあるものなので、普段の選択に載せると必ず上のもの（コマ・フキダシ）に取られる。取られないようにすると今度は「コマを選んだつもりでラフが動く」経路ができる。**道具で切り分ければ、その取り違えが構造的に起こらない。**

- **カーソルは標準の手（`OpenHandCursor`）。専用の絵は持たない。** 掴めるものが1種類しか無いモードなので、コマやセリフと取り違えようがない。**取り違えようが無いところに印を足しても、増えるのは作る手間だけ**（着手時は鉛筆を描いて入れたが、2026-08-06 に本人の指摘で外した）
- **つまみの上だけは、伸びる向きの形に戻す。** 手のままだと、どちらへ伸びるつまみか分からない
- **縦横比は常に保つ。** 写真なので、歪めた下敷きをなぞっても使いものにならない（マーク → 6.14 と同じ扱いで、Shift で外せる余地も作らない）。写真に余白が写り込んでいる場合は、拡大して外へ追い出す
- **吸着しない。** 吸着はコマの縦横の線に揃える仕組みで、下敷きを合わせる相手は絵の中身であってコマの辺ではない
- **調整中は選択枠もつまみも出さない。** 掴めないものにつまみが付いていると、掴めるつもりで押して何も起きないことになる
- **ラフを外すと選択の道具へ戻る。** 外した時点でこの道具は何もできなくなり、項目も押せなくなるので、戻る手段がメニューからしか残らない
- **同じ項目をもう一度押すと調整をやめる**（2026-08-06 に追加。トーンの範囲と揃えた → 6.27）。状態表示にも出口を出し（「ラフ調整中: … / **もう一度、メニュー「ラフを調整」を押すと解除**」）、**畳んだ親の名前も「ラフ」→「ラフ調整中」に差し替える**

#### 決めたこと

| 論点 | 決め |
|---|---|
| 何に持たせるか | `Page.rough`。**1ページに1枚** |
| 実体 | `assets/` へ SHA1（画像・マークと同じ）。**`referenced_assets()` に数える** |
| 描く位置 | 用紙のすぐ上、**目安線とコマより奥** |
| コマの下 | **隠れる**（コマの下地が不透明なため。狙った挙動） |
| 書き出し | **出さない**（`rough=False`。`aids` とは別のスイッチ） |
| サムネイル一覧 | **出す**（指紋の除外は不要） |
| 色 | **青**（青鉛筆の下書き）。色を捨ててから Screen で乗せる |
| 濃さ | **`settings.json` の `rough_opacity`**、既定 0.4 |
| 色と濃さの切り替え | **1手にまとめる**（「ラフを青くする」↔「ラフの色を戻す」） |
| 位置・大きさ | **道具に持ち替えている間だけ**掴める。縦横比は常に保つ |
| 回転 | **持たない**（傾いた写真は撮り直せる。マークの `rotation` と同じ線引き） |
| Undo で戻るか | **戻る。** 保存形式に載る以上そうなる（→ 6.8） |
| 保存形式 | 敷いていないページでは**項目ごと省く**（→ `note`・`focus_lines` と同じ） |
| 古い作品 | 項目が無ければ「ラフなし」（→ 5章） |
| 1文字キー | **割り当てない**（→ 7章） |

#### 今回やらないこと

- **複数枚のラフ／全ページ共通のラフ。** 1枚で足りないと分かってから考える（付箋の「1ページに1つ」と同じ）
- **ラフの回転。** 傾いた写真は撮り直せる。素材を差し替えられるという点でマークと同じ側にある
- **ラフの上に直接描く。** それは別の道具（お絵かき）で、この道具の範囲ではない（→ 1章の「やらないこと」）

### 6.24 コマの重なり順 → **実装済み（2026-08-06）**

**重なっているコマの、どちらを手前に描くかを入れ替える。**「コマ > コマを手前へ／　　　奥へ」と、コマの上での右クリックから。**キーは割り当てない**（→ 7章。6.15 複製・6.17 ロックと同じ判断）。

コマを重ねて置いたとき、後から作ったほうが必ず手前になる。あとで「下のコマを上に出したい」となったとき、それまでは**作り直すしかなかった**。

#### 土台は最初からあった

コマは `z`（重なりの順を表す数値）を最初から持っており、描く順（奥→手前）もクリックの当たり順（手前→奥）も元から `z` を見ている。**この機能で足したのは `z` を書き換える操作だけ**で、見た目と当たり判定は自動で付いてくる。

逆に言えば、片方だけ直すことはできない。「手前に出したのに掴めない」という壊れ方が起きないのは、2つが同じ順を見ているため。

#### 「1つ手前へ」ではなく「最前面へ」

段階的に上げる形（Photoshop の「前面へ」）にはしない。**重なっていないコマを1つ跨いでも見た目が変わらず、押しても何も起きないように見える。**

漫画のコマで重なるのは多くて2〜3枚なので、端まで動かせば1回で済む。段階を数えさせる利点がない。

#### 「奥へ」があるので、隠れたコマにも届く

クリックは必ず手前のコマに当たる（→ 6.2）。そのため**完全に隠れたコマは選べず、「手前へ」だけでは救えない。** 手前のコマは必ず選べるので、そちらを選んで「奥へ」送れば同じ結果になる。2項目あって初めて、どの重なり方でも入れ替えられる。

#### 斜めに割った組は2枚一緒に動く

ロック（→ 6.17）と同じ扱い。片割れだけ手前に出すと、1枚を割って作ったはずの2枚の間に別のコマが挟まる。**組のどちらを選んでも、2枚まとめて動く。** 組の中の前後関係は動かす前後で変わらない。

#### 2項目めは「コマを」を書かず、空白で頭を揃える

```
コマを手前へ
　　　奥へ
```

「コマを手前へ／コマを奥へ」と並べると、読む字数が倍になるうえ、同じ語を2回読ませる。かといって「手前へ／奥へ」だけでは、**何が手前へ行くのか**が項目名から分からない。

**上の行にだけ書き、下は消したぶんを全角空白で埋める。** 2項目めが上の続きとして読め、字数は最小のままになる（→ 6.12 の「読む字数を減らす」）。空白の数は「コマを」の字数と同じ3つ。

#### 端に居るときは押せない

既にいちばん手前のコマで「手前へ」は押せない（グレー）。押しても変化の無い操作で Undo の一手を使わせない（→ 「このページのコマをすべてロック」と同じ）。コマが1枚だけのページでは両方ともグレーになる。

**右クリックでは、押せない側を出さない。** メニューバー側は項目の位置が動かないほうがよいのでグレーで残すが、右クリックは開くたびに読む場所なので、読み飛ばす行を作らない（→ 6.12）。

#### 決めたこと

| 論点 | 決め |
|---|---|
| 動かす量 | **端まで（最前面／最背面）。** 段階的な「1つ手前へ」は入れない |
| 項目の数 | **2つ（手前へ／奥へ）。** 隠れたコマに届かせるのに両方が要る |
| 斜めの組 | **2枚一緒に動く**（→ 6.17 と同じ） |
| 押せないとき | メニューバーは**グレー**、右クリックは**出さない**（→ 6.12） |
| キー割り当て | **しない**（→ 7章） |
| Undo で戻るか | **戻る。** 保存形式に載る以上そうなる（→ 6.8） |
| 保存形式 | **変更なし。** `Panel.z` は最初からある |

#### 今回やらないこと

- **重なり順の一覧（レイヤーパネル）。** コマが重なる場面自体が多くないので、一覧を常時置くほどの情報量がない
- **画像・フキダシ・セリフの重なり順。** フキダシとセリフの前後は種類で決まっており（→ 4章）、`z` では覆せない。ここを触れるようにすると、その決まりと衝突する

### 6.25 重なったものを順に選ぶ → **実装済み（2026-08-06）**

**同じ場所を何度もダブルクリックすると、そこにあるものが1つずつ順に選ばれる。** 大きなコマや大きな絵の下に埋もれて、クリックでは届かないものを拾い上げるための操作。

クリックは必ず手前のものに当たる（→ 6.2）。そのため**完全に隠れたコマや、他の絵の下に回った絵は、それまで選ぶ手立てが無かった**。6.24 の「奥へ」でコマは救えるようになったが、あれは**重なり順そのものを書き換える**操作で、作品が変わる。「見たいだけ」「触りたいだけ」のときに順を崩すのは行きすぎている。

#### 巡る順は「コマ → その中の画像 → 次のコマ → …」

```
手前のコマ → そのコマの中の画像（手前から）→ 次のコマ → その中の画像 → …
```

末尾まで行ったら先頭へ戻る。

**重なりの順（z）そのままではない。** 画像はコマの上に描かれるので、z の順に並べれば画像がコマより先に来る。ここでコマを先に置いているのは、**1つめの段を「コマ → 中の画像」にするため**で、そうすると**ダブルクリック1回の動きが今まで（→ 6.3）と1ミリも変わらない**。足したのは2回目から先だけになる。

この順にしたおかげで、規則は「押すたびに次を選ぶ」の一文で済んでいる。1回目だけ例外、という但し書きが要らない。

#### 選択が移るだけで、作品は変わらない

**z は書き換えない。** Undo に一手も積まないし、保存される内容も変わらない。順を変えたいときは 6.24（コマを手前へ／奥へ）を使う。**「拾い上げる」と「並べ替える」を別の操作にしてある**ということで、ダブルクリックで順が変わると、覗いただけのつもりが作品を書き換えてしまう。

#### 見えていないものも並びに入る

隠れたものに届かせるのが目的なので、**そこに隠れているという理由で除かない。** 選択枠は最前面に描いてある（`PageView.drawForeground`）ので、隠れたコマを選んでも枠は必ず見える。何が起きたのか画面から分かる。

コマの中の画像だけは今までどおり**コマの形で切り抜いた範囲**でしか当たらない（`layout.image_at` と同じ）。あれは絵の描かれていない場所を掴めてしまう話で、重なりとは別の理由。

#### 押される前の選択から数える

**ダブルクリックの手前には、必ず普通の押下が入る。** Qt はダブルクリックを「押下 → 離す → ダブルクリック」の順で送るので、2回目のダブルクリックの直前に押下が挟まり、そこで**手前のコマに選び直されてしまう**。今の選択から数えると、巡回は2つめと3つめを往復して**隠れたコマに永久に届かない**。

そこで押下の時点で**選び直す前の選択を控えておき**（`PageView._selected_before_press`）、ダブルクリックはそちらから数える。

この壊れ方は、テストからダブルクリックの事象だけを直接呼ぶと**再現しない**。押下と組にして送って初めて出る（→ `tests/test_pick_cycle.py` の `click_pair`）。

#### 状態表示は、コマのときだけ何番目かを添える

コマを選ぶのはこの巡回だけなので（1クリックで選ぶ経路とは別物）、そちらには「コマを選びました（2 / 3）。ダブルクリックで次のものへ」と出す。**隠れたコマを選ぶと枠しか手がかりが無い**ため、位置が分かると迷わない。

**画像のときは今までの文のまま。** 踏み込んだ直後に読みたいのは「これから何ができるか」（ドラッグ・つまみ・Esc）で、そこに巡回の話を混ぜると長くなる。

#### 決めたこと

| 論点 | 決め |
|---|---|
| 何が変わるか | **選択だけ。** z は書き換えない（順を変えるのは 6.24 の役目） |
| 操作 | **ダブルクリック。** 今の「踏み込む」の続きにする。既存の動きは1つも変えない |
| 巡る対象 | **コマと、コマの中の画像。** フキダシ・マーク・セリフは入れない |
| 巡る順 | コマ → その中の画像 → 次のコマ → …。**z の順そのままではない** |
| 末尾 | **先頭へ戻る。** 行き止まりを作らない |
| 隠れたもの | **並びに入れる。** それが目的 |
| Undo で戻るか | **戻らない。** 作品が変わらないので積むものが無い |
| 保存形式 | **変更なし** |

#### フキダシ・マーク・セリフを入れなかった理由

種類をまたぐ前後は **z では覆せない決め**になっている（→ 4章。以前 z だけで重ねていたら、セリフの上に後から作ったフキダシが載って文字が消えた）。選択を移すだけなら段をまたいでも壊れないが、**巡る順をどう決めるかという別の話が立つ**。

そもそも困っていたのは「大きさ的に埋もれている」場合で、フキダシやセリフは小さく、下に回っても端が出ていることが多い。**要求が立っているのはコマと画像の側だけ**なので、そこに絞った。要ることが分かってから広げる（6.3 の回転をマークに広げなかったのと同じ線引き）。

#### 「次のオブジェクトを選択」を見送った判断を、ここで覆した

7章に「`Tab`（次のオブジェクトを選択）はやらない」（2026-08-05）がある。理由は「重なって掴みにくいものは右クリックで選び直せており、**順に巡る操作が要る場面が出てきていない**」だった。

**場面が出てきたので入れた。** ただし当時の判断のうち「キーを足すと元から通っていたものを塞ぐ」という部分は今も生きているので、**キーは割り当てていない**。ダブルクリックは既にこの用途（踏み込む）に使われていた操作で、新しく塞ぐものが無い。

#### 今回やらないこと

- **キーでの巡回（Tab など）。** 上のとおり、塞ぐものが出る
- **重なっているものの一覧を出す。** 右クリックに一覧を足す案もあったが、メニューは開くたびに読む場所なので字数を増やしたくない（→ 6.12）。ダブルクリックなら読む字数はゼロ
- **逆回り（Shift+ダブルクリック で1つ戻る）。** 重なるのは多くて2〜3枚で、行き過ぎてももう1〜2回押せば戻る

### 6.26 流線 → **実装済み（2026-08-06）**

**コマの中に平行な効果線を引く。** 集中線（→ 6.16）と同じく図形として生成し、コマの属性として1つ持つ。

「コマ > 流線 > 入れる」と、コマの上での右クリックから。**キーは割り当てない**（→ 7章。集中線・ロック・複製と同じ判断）。

| 操作 | 内容 |
|---|---|
| 入れる / 消す | コマを選んで、メニューまたは右クリックから |
| 向きを変える | コマを選んでいる間に出る**丸**のつまみをドラッグ。`Shift` で 15 度刻み |
| 本数・太さ・長さ | メニューで増減 |
| 形を振り直す | `seed` を引き直して、ばらつきだけを作り直す |

#### 集中線から引き継ぐもの・引き継げないもの

6.16 で「足すときは `focus_lines` に `kind` で混ぜず、別の項目にする」と決めてあった。**そのとおりにする**（`Panel.flow_lines`）。混ぜると、集中線にしか無い `center` / `hole` と、流線にしか無い `angle` / `length` が1つの型に同居し、**どの組み合わせが正しいのかを型が語れなくなる**。

| 集中線 | 流線 | |
|---|---|---|
| `center`（中心の位置） | `angle`（向き） | **置き換わる。** 位置ではなく向きで決まる |
| `count` / `width` / `seed` / `white` | 同じ | そのまま持つ |
| `hole`（内側の空き） | **持たない** | → 下 |
| — | `length`（線の長さ） | **足す。** 集中線は常に外周まで伸びるので要らなかった |
| 楔形（中心へ向かって細る） | **紡錘形**（両端が細る） | → 下 |

**集中線の側は1行も変えない。** コマの属性が1つ増えるだけなので、流線を使っていない作品の project.json はこの機能の追加前と同じ内容のままになる（付箋・ロックと同じ線引き）。

#### 抜き（主役を残す領域）は持たない

集中線の `hole` にあたるものを**持たない**。コマ全面を平行線が貫く。

集中線に空きが要るのは、線が1点へ集まるため**中心が必ず黒く潰れる**から。**流線には潰れる場所が無い**ので、同じ必要が立たない。ネームでは走る人物の絵が線の上に乗り、そちらで隠れる。

要求が立ったら足せる。「この領域に入る線を描かない」という後置きの条件で、線の並べ方そのものは変わらない（集中線で「白く塗る」を後から足せるようにしてあるのと同じ形）。

#### 線は棒ではなく紡錘形

**中央が太く、両端に向かって細くなる**細長い形を塗る。両端のそれぞれ 25% をテーパーに使い、真ん中の 50% は幅一定。

菱形（中央だけが太い）にすると葉に見え、幅一定の棒にすると定規で引いた線に見える。漫画の流線は**端が抜ける**のが要で、そこだけを作る。

**この決めが、集中線には無かった手間を1つ持ち込む。** 集中線は線を「外接矩形のどの頂点よりも遠くまで」伸ばし、はみ出しをコマの形で切り抜くことで**端の処理を1行も書かずに済ませていた**（→ 6.16）。流線は端が画面に見えるので、その手が使えない。長さを値として持ち、線ごとに揺らすことになる。

#### 長さの基準だけ、短辺ではなく対角線にする

`length` の基準は**外接矩形の対角線**。集中線の `hole` / `width` が短辺基準なのと違えてある。

短辺基準だと、`1.0` にしても横長のコマを半分しか走らない。長辺基準だと、斜めに流したとき（`angle` が 45 度前後）に貫かない。**矩形の中でいちばん長い弦は対角線**なので、そこを基準にすると `1.0` が「向きによらず必ず貫く」と一致する。

基準が2つになるが、**揃っていることより、上限の意味が読めることを採る**。`width` の側を対角線基準に揃えない理由は逆で、太さは短辺基準のほうが「コマの大小によらず同じ見え方」になる（→ 6.16）。

#### 線の並べ方

向きに**直交する方向**へ等間隔に並べ、`seed` から4つを揺らす。

| 揺らすもの | 振る向き | 理由 |
|---|---|---|
| 直交方向の位置 | 両側 | 等間隔のままだと縞模様に見え、流れに見えない |
| 長さ | 短くする側だけ | 長くすると、決めたはずの `length` を超える |
| 線に沿った位置 | 両側 | 端が一直線に並ぶと、切った紙の縁に見える |
| 太さ | 細くする側だけ | 太くすると隣と重なって塊になる（集中線と同じ） |

並べる範囲は**対角線ぶんの帯**。向きが斜めのとき、外接矩形の幅ぶんだけ並べると端に線の無い三角形が残る。帯からはみ出た線はコマの形で切り抜かれて消えるので、多めに並べて構わない。**ここは集中線の「遠くまで伸ばして切り抜く」がそのまま効く**（効かないのは線に沿った方向だけ）。

#### 乱数は集中線のものをそのまま使う

自前の線形合同法をそのまま使う。**`random` は使わない**——理由は 6.16 と同じで、並びが処理系に属していると、将来それが変われば同じ `seed` から違う形が出る。

**乱数は `manga_layout/noise.py` へ出した**（実装して決めたこと）。当初は `focus.py` に置いたまま `flow.py` から借りるつもりだったが、そうすると**「流線は集中線の一部」という関係が import に現れる**。集中線を消したら流線まで動かなくなるので、どちらにも属さない場所へ出した。移したのは乱数だけで、`focus.py` の他は1行も変えていない。

形を作るコードは `flow.py` に分ける（Qt も描画も知らない。`focus.py` と同じ）。**共用するのは乱数だけ**で、それ以外は形も基準も違う。

#### つまみは丸。記号の在庫が尽きているので回転から借りる

つまみの形は、四角＝大きさ、丸＝画像の回転、ひし形＝しっぽの付け根、十字＝集中線の中心で**在庫が尽きている**（→ 6.3、6.4、6.16）。

**丸を借りる。** 借りて混乱しないのは、画像の回転と**やることが同じ**（掴んで回す）ため。意味の重複ではなく一致なので、新しい記号を作るより読み替えが要らない。`Shift` のときだけ 15 度刻みになるのも回転と揃える（→ 6.3）。

出る場所はコマの外接矩形の中心から `angle` の向きへ、短辺の 35%。**距離は使わない**（掴んだ点の角度だけを見る）。流線は中心を持たないので、つまみの出る中心は常に外接矩形の中心で動かない。

見る順は **コマの角・辺 → 集中線の十字・四角 → 流線の丸 → コマ本体**。

- 角・辺より後にするのは集中線と同じ理由（隅に寄ったときに覆い隠さない）
- **集中線より後にする**のは、あちらの中心が**動かせる＝どこにでも来る**ため。流線の丸は決まった場所にしか出ないので、重なったときに逃げられるのは集中線の側だけ。逆順にすると、集中線の中心を丸の位置へ持っていった瞬間に掴めなくなる
- コマ本体より先にするのは集中線と同じ（つまみがコマの内側に出る）

#### 集中線と同時に入れられる。順は流線が奥

別の属性なので、止めなければ両方入る。**止めない**（止めるほうがコードが増える）。

重なったときの順は**流線が奥・集中線が手前**で固定。z は持たない（各1つずつなので、順を決める相手が最初から1つしかいない）。流線は面を流す背景側、集中線は一点へ集める前景側なので、集中が上に乗るほうが意味と合う。

描く層そのものは集中線と同じで、**画像より手前・コマ枠より奥**（→ 6.16）。

#### 既定値は描いて決める

`FlowSettings`（`flow.py`）に置く。`FocusSettings` と別にするのは、集中線と流線で調整したい場面が違うため（`LayoutSettings` / `BalloonSettings` を分けたのと同じ）。

**本数 48・太さ 0.006・長さ 0.60** に決めた。9通りずつ2回、実際に描いて選んでいる（2026-08-06）。**数では確かめられない性質**なので、目で見て決めている（集中線・ふわふわ_フキダシと同じ流儀 → 6.16、6.13）。

- **本数 24 では線が1本ずつ数えられて流れに見えず、96 では紙が黒くなって下の絵が読めない。** 48 と 60 の差は小さく、少ないほうを採った（増やす操作はあるが、減らして初めて気づくほうが遅い）
- **太さは集中線（0.018）の3分の1。** 0.020 は塊になり、0.010 でもまだ重い。集中線が太くてよいのは中心へ向かって細るからで、全長が同じ太さの流線に同じ値は使えない
- **長さ 0.45 は紙が空きすぎ、0.75 以上は線どうしが重なって密になる。** 0.60 は端の抜けが見えたまま流れが読める

角度の既定は **0 度（水平・右向き）**。横に流すのがいちばん多く、迷う要素が無い（集中線の「中心はコマの真ん中」と同じ考え方）。

コマごとの値は入れた時点で焼き付ける。**設定を変えても既にあるコマは変わらない**（斜めの角度・集中線と同じ → 6.10、6.16）。

#### メニューは集中線と同じ形で畳む

「コマ > 流線」に畳む。項目は8つ（入れる／消す・線を増やす／減らす・太く／細く・長く／短く・白にする／黒に戻す・形を振り直す）で、集中線の7項目と同じ並べ方にする。

コマのメニューの下に**畳みが2つ並ぶ**ことになるが、どちらも入れていないコマでは「入れる」1つしか使えないので、開いたときに読む字数は増えない（→ 6.16）。

「向きを 15 度回す」のメニュー項目は**作らない**。つまみで足りるうえ、項目が2つ増えて読む字数が増える（→ 6.12）。

#### 決めたこと

集中線と同じ扱いにすることで、以下は追加の設計が要らない。

| 論点 | 決め |
|---|---|
| 書き出しに出すか | **出す。** 作品の内容そのもの。`PageRenderer` を通るのでサムネイルにも自動で出る |
| Undo で戻るか | **戻る。** 保存形式に載る以上そうなる（→ 6.8） |
| 線の色 | **黒と白の2値。** `FlowLines.white` で切り替える（→ 6.19 と同じ形） |
| 斜めに割ったコマ | **入れられる。** 基準はそのコマ自身の外接矩形。切り抜きで正しく出る |
| コマを消したら | 一緒に消える。コマの属性なので、紐づけを外して残す相手がいない |
| コマを割ったら | **前半（上または左）に残る。** 新しくできた側には付かない |
| 複製 | 保存形式を1往復させるので**自動で写る**（→ 6.15）。長さを1つも絶対値で持たないので、写した先で形も崩れない |
| 選択できる種類 | **増やさない。** コマ・画像・フキダシ・セリフ・マークの5つのまま |

#### 今回やらないこと

- **抜き（主役を残す領域）。** 上のとおり、流線には潰れる場所が無い
- **1コマに2つ以上。** 向き違いを重ねる場面が思い当たらない。要求が立ってから考える（集中線と同じ判断）
- **曲がる流線。** 直線だけ。曲線にすると、向き1つでは形が決まらなくなり、つまみも道具立ても別物になる
- **集中線と流線を1つの型にまとめること。** 上のとおり、型が正しい組み合わせを語れなくなる

### 6.27 画像の黒をトーン（斜線・灰色・白）に置き換える → **実装済み（2026-08-06）**

**貼り込んだ画像の暗い部分を、トーンに置き換えて描く。** ペイントソフトや生成 AI で作った絵の黒ベタが、ネームの段階で「ここはトーンにする」と読めるようになる。

これは README の「できないこと」に挙げてある**トーンの、ごく一部だけ**。**トーンを塗る道具は作らない**（範囲を選ぶ・貼る・削る）。入るのは「暗い所を斜線に置き換える」という置換だけで、どこを塗るかの指定は元画像の側で済ませる。

「画像 > トーン > 入れる」と、**右クリックの畳み**から。**キーは1つも割り当てない**（→ 7章。集中線・流線・ロック・複製と同じ判断）。

| 操作 | 内容 |
|---|---|
| 入れる / 消す | 絵かコマを選んで、メニューまたは右クリックから |
| 見た目 | **斜線 / 灰色 / 白抜き** をレ点で選ぶ（→ 下） |
| 濃さ | **`Shift+.` / `Shift+,` で増減**（メニューにも同じ項目がある → 下） |
| 目の細かさ・向き | メニューで増減（向きは 15 度ずつ） |
| どこまでを黒と見るか | **`Shift+]` / `Shift+[` で増減**（メニューにも同じ項目がある → 下） |
| 細い線を守るかどうか | メニューで増減（`thin`） |
| 範囲を絞る | 道具「トーン範囲を調整」に持ち替えて、×のつまみをドラッグ |

**増減する項目には、今が何段目かを添える**（「細い線を残す（2/20）」→ 下）。

#### 4つの決め

| 論点 | 決め | 理由 |
|---|---|---|
| 何を黒と見るか | **しきい値以下の暗い画素すべて** | 真っ黒（`#000000`）ちょうどに限ると、JPG の圧縮や生成画像の階調で黒ベタがわずかに浮いた時点で拾えなくなる |
| 斜線の大きさの基準 | **画像基準**（画像と一緒に伸縮・回転する） | 紙（ページ）基準にすると、描く経路の作り替えと**拡大率ごとの作り置き**が要る。画像基準なら画像を展開する所で焼き込むだけで、`PageRenderer` は1行も変わらない |
| 見た目 | **斜線（線トーン）が出発点** | 網点は粒が細かいので、画面の縮小表示でも書き出しの縮小でもモアレ（細かい模様どうしが干渉して出る縞）が出る。斜線は粗いぶん出にくい。**灰色と白抜きは後から足した**（→ 下） |
| 貼る単位 | **画像ごと**（`ImageObject.tone`） | 「この絵だけ黒ベタのまま残したい」が普通にある。コマの属性にすると1枚だけ外せない |

#### 見た目を3つから選ぶ（2026-08-06 に追加）

**斜線 / 灰色 / 白抜き**（`TONE_KINDS`）。本人の要望で足した（「トーンの種類の増加／均一パターンの灰色を追加」2026-08-06）。

**どこを置き換えるかは3つとも同じ**（しきい値・細さ・矩形）。違うのは**置き換えた先に何を敷くか**だけで、合成の側（マスクで抜く）は種類が増えても触らない（→ `tone._pattern`）。

| 種類 | 何を敷くか | 何のためにあるか |
|---|---|---|
| 斜線（既定） | 白地に黒い斜線 | ネームで「ここはトーン」と読める |
| 灰色 | むらの無い灰色 | 斜線がうるさいとき。**網点と違いモアレが出ない**ので、却下した網点の代わりになる |
| 白抜き | 紙の白 | **クリスタで本物のトーンを貼る前提**。斜線や灰色を敷いたままだと、貼った網点の隙間から下が透ける（→ 6.28「トーン範囲」） |

| 論点 | 決め | 理由 |
|---|---|---|
| 濃さの持ち方 | **`density` を斜線と共用する** | 斜線の `density` は「線の太さ ÷ 間隔」＝**そのままインクの占める割合**なので、灰色の濃さに読み替えると**種類を切り替えても濃さの見た目が揃う**（0.35 なら 35% の黒）。項目を別に持つと、「濃く」がどちらを動かすかを種類ごとに覚えることになり、メニューにも2組並ぶ |
| 効かない値 | **消さずに持ち続ける** | 灰色では `angle` と `pitch` が絵に出ないが、捨てると斜線へ戻したときに調整も消える。メニューでグレーにして「今は効かない」とだけ示す |
| メニューの形 | **3つ並べてレ点。名前は書き換えない** | 流線の色（「白にする／黒に戻す」→ 6.26）は2つだから成り立つ形で、3つあると押すまで次がどれか分からない |
| 置き場所 | **濃さより前** | 何を敷くかが決まらないと、濃さや目が効くかどうかも決まらない |
| 白抜きを濃さ 0 の灰色にしない | **別の種類として持つ** | `density` の下限は 0.05＝5% の灰。クリスタでトーンを貼るなら、わずかな灰色も網点の隙間から見える |
| 出発点 | **斜線のまま** | 灰色・白抜きは貼り直す前提の絵。ネームとして読むためのものを既定に置く |
| 保存形式 | `kind` を**省いたら斜線**として読む | 種類を足す前に保存した作品がそのまま開ける（セリフの `direction` と同じ扱い → 6.11） |

**焼いた1枚の鍵（`Tone.key`）にも `kind` を混ぜる。** 混ぜないと、種類を切り替えても前の1枚が引かれて絵が変わらない。

#### 持ち主は画像だが、**コマを選んだ状態でも効かせる**（2026-08-06 に直した）

最初は「絵そのものを選んでいるときだけ有効」にしていたが、**メニューがグレーのまま**という指摘を受けた（本人談 2026-08-06）。

**絵を選ぶにはコマをダブルクリックして中へ入る必要がある**（→ 6.3）。集中線・流線はコマの属性なので、同じつもりでコマを選んだ利用者には、トーンだけが理由の分からないグレーに見える。**保存形式は画像のままにして、操作の入り口だけ広げた。**

| 選んでいるもの | どの絵に効くか |
|---|---|
| 絵 | その絵 |
| コマ（絵が1枚） | その1枚 |
| コマ（絵が2枚以上） | **決めない。** 押したら状態表示で「絵を選んでから」と断る |
| コマ（絵なし） | 項目をグレーにする |

**絵が2枚以上のときにグレーにしないのは、理由が伝わらないから。** 背景の上にキャラを重ねる使い方があり、どちらに掛けるかは黙って選べない。押せるままにして、押したときに断る（斜めのコマを複製できないと伝えるときと同じ線引き → 6.15）。

#### 右クリックには、絵の上とコマの上の両方に出す

**集中線・流線と同じ畳み方**で、右クリックのメニューにも「トーン」を並べる（本人の要望 2026-08-06）。コマの上では**集中線・流線のすぐ隣**に出る。

**絵の入っていないコマでは出さない。** 押しても何も起きない項目を並べると、読む手間だけが増える（→ 6.12。重なり順の2項目と同じ扱い）。

#### 範囲の2項目は「入れる」のすぐ下に置く（2026-08-06 に直した）

畳みの中の並びは、**入れる／消す → 範囲を調整・範囲を全体に戻す → 濃さと目 → 拾い方**。

最初は範囲を調整する道具を「道具」メニューにだけ置いていたが、**入れた直後に遠くのメニューへ移ることになり繋がりが見えない**という指摘を受けた（本人談 2026-08-06）。**ラフが調整の道具を自分の畳みに置いているのと同じ形**にした（→ 6.23、`FileMenu._build_rough_menu`）。実体は同じ QAction なので、「道具」メニュー側の選択の印も一緒に付く。

**右クリックにも、この道具だけは写す。** 道具の切り替えを右クリックへ写さない決まりがあるのは、「右クリック側は押した場所が分かっているので『ここに〜』を別に出しており、同じことが2通り並ぶ」ため。**この道具には『ここに〜』の相手がいない**ので、外すと右クリックからトーンを入れた直後に行き先が消える。

#### 中は4段。どの段も画素を1つずつ Python で触らない

`tone.py` に置く（`images.py` と同じく Qt に依存する非 UI 層）。

| 段 | やること | 使うもの | 実測（2048×2048） |
|---|---|---|---|
| 1 | 白地に載せてから灰色にする | `QPainter` → `Format_Grayscale8` | 22ms |
| 2 | しきい値で切る | **`bytes.translate`** | **4.6ms** |
| 3 | 細いものを落とす | `QImage.scaled` を2回 | 10〜13ms |
| 4 | 矩形で絞る | `setClipRect` | 1.7ms |

**依存パッケージは1つも増やしていない。** numpy も Pillow も OpenCV も要らない。`Format_Grayscale8` は1画素1バイトで並ぶので、生バイト列を 256 個の対応表に通すだけでマスクになる。`translate` は C 側の1ループなので、Python の for 文（205ms）とは桁が違う。

**収縮・膨張も専用の道具を使わない。** 縮小（`SmoothTransformation`）は周りの画素を混ぜるので細い線は薄まり、大きなベタは濃いまま残る。そこをもう一度切り直して拡大すれば、収縮 → 膨張と同じ結果になる。

**1段目に「白地に載せる」があるのは、透明の扱いを保存の仕方に委ねないため。** `Format_Grayscale8` はアルファを捨てるので、背景が「透明かつ黒」で保存された画像では背景一面がトーンになる。先に白へ倒しておけば、透明かどうかだけで決まる。

#### 細さで落とすのが、この機能の要

**ペン入れした線も真っ黒なので、黒ベタと明るさでは区別が付かない。** しきい値だけで置き換えると、ワンピースの輪郭・脚・靴の線がすべて破線になる（実画像で確認済み）。

`thin` より細いものをマスクから落とすと、**線は元の黒のまま残り、ベタだけがトーンになる**。同じ絵で線が完全に元通りになることを確かめてある。

- `thin` は**画像の短辺に対する割合**。px で持つと、画面用の縮小版（長辺 1,600px）と書き出しの原寸（2,048px 程度）で落ちる線の太さが変わる
- `0` にすると何も落とさない。線画を持たない絵ではこちらのほうが輪郭が素直に出る
- **明るさの下限を持つ案（「8〜64 の間だけ」）は入れなかった。** これだけは「ペイント側でベタを少し明るい黒で塗る」という運用と一組でしか効かず、細さと矩形で足りた
- **面積で数える案も入れなかった。** 連結成分を数える処理が要るのに、細さで足りている

**白抜きハイライトは何もしなくても残る**（髪の中の白い筋はトーンの中でも白いまま抜ける）。**埋めない**——抜きは描いた人が意図して置いたものなので、勝手に埋めるほうが乱暴。

##### 拾う黒と濃さはキーで連打できる（2026-08-06 に追加／濃さは 2026-08-07）

**`Shift+]` で拾う黒を増やし、`Shift+[` で減らす。** 調整できる値は4つあるが、最初にキーを添えたのはここだけだった（本人の要望 2026-08-06）。目の細かさ・細さは決め打ちで足りるのに対し、**拾う黒は絵ごとに違い、行き過ぎたか足りないかを見ながら往復する**。メニューだと1回ごとに開き直すことになり、往復が成立しない。

**濃さにも `Shift+.` / `Shift+,` を足した**（本人の指示 2026-08-07）。拾う黒を決めたあと、敷いた斜線が濃すぎ／薄すぎに見えるのはその場で分かるので、**同じ絵を見ながら続けて詰められる**ほうがいい。記号は `>` `<` で、右が増える側という向きは拾う黒・セリフの大きさと同じ。

**白抜きのときは素通りさせる。** 白く塗るだけなので濃さは効かず、メニューでもグレーにしてある。ここで値だけ動かすと、状態表示に段数が出るのに絵が変わらず、変わらない理由を探すことになる（→ 6.12）。

| 論点 | 決め | 理由 |
|---|---|---|
| キー | **拾う黒は `Shift+]` / `Shift+[`、濃さは `Shift+.` / `Shift+,`。`+` / `-` は使わない** | あちらは拡大縮小で、**拾い具合を見るときこそ拡大したい**。意味を差し替えると、いちばん要るときにズームが使えなくなる（→ 7章） |
| 向き | 右が増える側、左が減る側 | セリフの大きさ（`Ctrl+.` / `Ctrl+,`）と揃える。修飾キーが違うので取り合いにならない |
| 効く場面 | **トーンの入った絵かコマを選んでいるとき。道具は問わない**（濃さは白抜きを除く） | しきい値は道具の持ち物ではない。入っていなければ素通りするだけで、他の操作を塞がない |
| **`setShortcut` を使わない** | 画面（`PageView`）側で拾う | **Qt はメニューのショートカットを入力欄より先に処理する。** 付けるとセリフを打っている最中の `}` を奪う（→ 7章の `+` / `-` と同じ扱い） |

**連打ぶんは履歴に1手しか積まない**（`merge_key` → `History.commit`）。1回ずつ積むと、20回押した調整を戻すのに Undo を20回押すことになる（セリフの入力と同じ扱い）。**鍵に項目の名前を混ぜてある**ので、濃さを触ってからしきい値を触れば別の手として積まれる。

**配列によって `Shift+[` が `{` にならないことがある**ので、`{` `}` と `[` `]` の両方を拾う（`+` に対して `=` も拾っているのと同じ手当て → 7章）。

**2026-08-06、実機で動作を確認済み**（本人の環境）。**offscreen のテストでは測れない**種類のものなので、ここは実際に押して確かめている（→ 7章「この種のことは offscreen のテストでは測れない」）。

#### 増減する項目には「今 何段目か」を出す（2026-08-06 に追加）

**「細い線を残す」が一回きりの操作に見えた**（本人談 2026-08-06）。実際には3回押してちょうどよくなったが、**重ねてよいことがどこにも出ていなかった**。押した後に状態表示へ「トーンにしない細さ: 0.3% 未満」とは出していたものの、**割合は「設定値を1つ決めた」と読める**ので、もう一度押す合図にならない。

項目名を「**細い線を残す（2/20）**」の形にし、押すたびに数字が動くようにした。**動くこと自体が「もう一度押してよい」の合図**になる。上限も同時に見えるので、あと何回押せるかも分かる。

| 論点 | 決め | 理由 |
|---|---|---|
| 数字の出どころ | **今の値から逆算する**（`tone.level_of`）。押した回数は数えない | 数えると、Undo で戻したとき・別の絵に移ったとき・保存して開き直したときに、**数字だけが実際の値とずれる**。逆算なら、どの経路で変わっても必ず合う |
| 数え始め | **下限を 0 段目**とする。入れた直後は `thin` なら 2 段目 | 「1回目」から数えるには基準が要るが、既定値は 0 段目ではない。下限を 0 にすれば基準が値の側だけで決まる |
| どの項目に付けるか | **増減する4つすべて**（濃さ・目・拾う黒・細さ）。向き（15度回す）には付けない | 1つだけに数字が付くと、**残りが一回きりに見える**。向きはぐるぐる回るだけで端が無く、「あと何回」に意味が無い |
| キーの付く項目 | **括弧を重ねず、数字を前に置く**（「拾う黒を増やす 3/20（Shift+]）」） | 「（Shift+]）（3/20）」は括弧が2つ続いて読みにくい |
| 対になる項目 | **両方に同じ数字を出す**（「細い線を残す（3/20）」「細い線も塗る（3/20）」） | 片方だけだと、2つの項目が**同じ1つの値**を上げ下げしていることが伝わらない |
| 状態表示 | 割合をやめ、段数に揃える。**拾う黒だけ元の値も添える**（「拾う黒: 3/20（明るさ 30 以下）」） | 拾う黒は絵ごとに合わせる値で、前に上手くいった絵と見比べられるほうがよい |

**段数は全項目とも「下限から何回押したか」の1つの式で出る**（`round((値 − 下限) ÷ 1回ぶん)`）。拾う黒だけは下限（4）が刻み（10）の倍数でないが、実際に押して届く値は 4／10／20／…／200 の21段で、同じ式で 0〜20 に収まる。0.001 を足し続けたときの誤差（0.006000000000000001 のような値）も、丸めるので表に出ない。

**これは「項目名は短く保つ」（→ 6.12、6.16）の例外ではない。** 添えているのは説明ではなく**押した結果そのもの**で、押すたびに動く。「入れる／消す」がラベルを書き換えているのと同じ筋（→ 6.19）。

**右クリック側には何も足していない。** 写しているのが同じ QAction そのものなので、書き換えは自動で両方に出る（→ `items_to_copy`）。

#### 矩形で範囲を絞る

しきい値と細さで拾いきれないもの——服の模様・目の黒・小物のベタ——を、位置で外す。

| 論点 | 決め | 理由 |
|---|---|---|
| 座標系 | **画像に対する割合**（`Tone.area`） | 画像と一緒に動き・伸縮し・回る。ページ座標で持つと、画像を動かすたびに囲い直すことになる |
| いくつ | **1つだけ** | 2つ以上が要る場面が思い当たらない（集中線・流線と同じ判断） |
| 省いたとき | **画像全体**（＝絞らない） | 項目を持たない作品が今までどおりに読める |
| 外側 | **元の黒のまま。** 白く抜いたりしない | 絞るのは「トーンにしない」であって「消す」ではない |
| はみ出した矩形 | **弾かず、直さない** | 0〜1 の外は絵が無いだけで、画像の縁で自然に切れる |
| **1画素も掛からない矩形** | **弾いて、理由を伝える**（→ 下） | 離れたコマを触ったつもりのドラッグがここへ落ちる。結果が「トーンが消えた」に見え、原因の見当が付かない |
| 縁 | **ぼかさない** | 設定が1つ増えるうえ、「ここで切った」と分かるほうがネームでは扱いやすい |

##### 離れたコマとの行き来（2026-08-06 に追加）

**上下のコマにそれぞれトーンを入れて、範囲も別々に絞る使い方**（本人の要望 2026-08-06）。持ち主が画像ごとなので、**データとしては最初からできる**——足したのは事故を1つ塞いだだけ。

**この道具を持っている間は選び直せない**（→ 下「道具から出る条件」、6.23）。そのため**離れたコマを触ったつもりのドラッグが、選んでいる絵の範囲を絵の外へ飛ばす**。絵は元のままで**トーンだけが消えたように見える**ので、原因の見当が付かない。実際に再現することをテストで確かめてある（塞ぐ前は 200 × 200 px の範囲が入った）。

**重なりが 1px 未満のドラッグは捨てて、状態表示で断る**（「選んでいる絵から外れています。別の絵を絞るには、選択の道具（V）で選び直してから」）。潰れた矩形を捨てるのと同じ場所・同じ 1px の線引き（`_apply_tone_area`）。**黙って何も起きないのは避ける**——道具が壊れていると受け取られる（→ 6.15）。

**はみ出し自体は今までどおり通す。** 弾くのは重なりが1つも無いときだけで、絵に少しでも掛かっていれば縁で切れる（上の表）。

**コマを移る手数（道具を戻す → 選び直す → 道具を持ち直す＝3手）はそのままにした。** 道具を持ったまま他の絵へ移れるようにする案もあったが、**囲い直すドラッグは絵の外から引き始めるのが普通**なので、「絵の外を押したら選び直し」にすると囲い直せなくなる（絞る前の枠が画像いっぱいで移動に取られた件と同じ形 → 下）。手数は面倒でも間違いにはならないので、**実害のある事故だけを先に塞いだ**。

#### つまみは道具に逃がし、記号は×にした

つまみの記号は**四角＝大きさ・丸＝回転・ひし形＝しっぽの付け根・十字＝集中線の中心**で在庫が尽きている（→ 6.26 で既に困っている）。矩形の4隅にもう1種類足すと、画像を選んだだけで**つまみが9個**並ぶ。

**「トーン範囲を調整」を道具として1つ足した**（`TOOL_TONE_AREA`）。**ラフの調整（`TOOL_ROUGH` → 6.23）と同じ形**で、持っている間は選択枠も他のつまみも全部消し、トーンの範囲だけを出す。

| 論点 | 決め | 理由 |
|---|---|---|
| 記号 | **×（斜めのクロス・太め）** | 集中線の十字とは 45 度違うだけだが、**同時に画面に出ない**。あちらはコマを選んでいるとき、こちらは道具を持っている間だけ |
| 出る場所 | **隅の4つだけ。辺には出さない** | 8個出すと×が並んで枠そのものが読めなくなる |
| 当たり判定 | **×の線ではなく、今までどおり 9px 四方の正方形** | 線そのものを判定にすると掴みにくい。×は見た目だけ |
| 色 | **シアン `#00BCD4`。赤系は使わない**（→ 下） | 色は「どの種類を選んでいるか」を指しており、役割には使っていない |
| 絞る前の枠 | **画像いっぱいを点線で出す** | 何も出さないと、道具を持ったのに掴めるものが無いように見える。実線と点線で「今は全体が対象」を分ける |
| 道具から出る条件 | 別のものを選んだとき、トーンが消えたとき、**同じ項目をもう一度押したとき**（→ 下） | 掴めるものが1つも無い道具を持ったまま残さない（ラフと同じ → 6.23） |

##### 調整の道具は、入口と出口を同じ場所にする（2026-08-06 に追加）

**調整モードに入っていることが分からなかった**（本人の指摘 2026-08-06）。**「選択」を押せば戻れる**と後から気づいたが、初見では**モードに入っている事実そのものが見えなかった**——この道具では何も選べないので、**「クリックしても選べない」としか見えない**。

| 論点 | 決め | 理由 |
|---|---|---|
| 出口 | **同じ項目をもう一度押すとレ点が外れ、選択の道具へ戻る** | **入った場所を辿れば出られる。** 別の場所（「選択」）を知っている必要があると、知らない人は出口そのものを探せない |
| どの道具に | **調整の2つだけ**（ラフ・トーンの範囲 → `ADJUST_TOOLS`） | 作る側の道具は押すたびに1つ作るので、2回目が「やめる」に化けると意味が変わる |
| 名乗り | **状態表示に「トーン範囲を調整中: 今は画像全体 / もう一度、メニュー「トーン範囲を調整」を押すと解除」** | モードに入っていることと出口を、同じ1行で伝える。**ラフの側にも出口を足して揃えた**。文言は 2026-08-27 に差し替え——以前は「同じ項目をもう一度押すと戻る」だったが、**どの項目のことかを書いていなかった**（入り口を覚えていない人は、その「同じ項目」を探せない）。道具の名前は `TOOL_LABELS` から引く |
| **畳んだ親の名前** | **調整中は「トーン」→「トーン調整中」に差し替える**（→ 下） | **レ点は畳みを開いた先にある。** 閉じたままでは状態が読めない |

**畳んだ親の名前でも名乗る**（本人の要望 2026-08-06）。レ点だけだと**サブメニューまで開かないと状態が分からない**。

- **名前は短いまま。** 「説明は状態表示へ逃がす」（→ 6.12）は変えていない。状態表示のほうは長い1行（「トーン範囲を調整中: 今は画像全体 / …」）を出したままにする
- **名前は1か所に持つ**（`TONE_MENU_LABEL`、`ADJUSTING_MENU_LABELS`）。親の「トーン」は**メニューバーと右クリックの2種類（絵の上・コマの上）で別々に書かれていた**ので、書き分けたまま片方だけ直す形にしない
- **メニューバーと右クリックで作りが違う。** あちらは同じ QMenu を持ち続けて `refresh()` で名前を差し替え、こちらは右クリックのたびに組み直すので組む時点の道具を見る（`ContextMenu._tone_label`）
- **ラフ（→ 6.23）も同じ形にした。** 「ラフ」→「ラフ調整中」
- **`tool_changed` を `_refresh` に繋いだ。** 道具の持ち替えは選択も編集も伴わないので、繋がないと**次に何かを選ぶまで前の文言が残る**。名乗り（状態表示）を足したときに漏れており、**畳んだ親の名前を足した時点で表に出た**。テストは `_refresh()` を自分で呼ばずに確かめる

**ラフ（→ 6.23）にも同じ形を入れた。** 構造が同じものを片方だけ変えると、**予見可能性がかえって下がる**（本人の言葉）。ラフの右クリックにある「調整をやめる」はそのまま残す——あちらは畳みの中に出口を1つ増やしているだけで、競合しない。

**2026-08-06、実機で確認済み**（本人の環境）。レ点の付き外れ・畳んだ親の名前・状態表示の3つとも。**offscreen のテストでは「その文字列になっているか」までしか測れない**種類のものなので、見え方は実際に開いて確かめている（→ 7章）。

**色を赤にしなかった理由は2つあり、どちらも既に埋まっているから。**

- **赤 `#E53935` はセリフを選んでいるときの色**（`canvas.TEXT_ACCENT`）。しかも「以前ここが漏れていてコマの青に落ちており、セリフを選んでいるのかコマを選んでいるのか枠の色では区別できなかった」という経緯つきで足されている
- **赤系 `#D9534F` は欠けた画像の目印で、しかも矩形に対角線2本＝すでに「赤い×」**（`render.MISSING_IMAGE`）。トーンの範囲も矩形なので、隅に赤い×を出すと**「この画像は壊れている」と同じ語彙**になる。この目印は選択と無関係に用紙へ描かれるので、隣のコマに欠けた画像が1枚あればトーン調整中でも画面に出る

**この道具の間は他のつまみが消えるので、他の6色と見分ける必要は無い。** 避けるのは**下の絵**（黒い斜線と白）のほうだけなので、黒・白・灰色以外の彩度のある色ならよい。

##### 見えている枠と、実際に持っている範囲は別に持つ

**絞る前の枠は画像いっぱい**なので、「内側を押したら移動」を素直に書くと**どこを押しても移動**になり、**新しく囲い直せなくなる**（実装して踏んだ）。

掴んだときに覚えるのは2つ。**枠**（隅を掴んだときの出発点。絞っていなければ画像いっぱい）と、**範囲**（実際に持っているもの。絞っていなければ `None`）。移動に入るのは範囲を持っているときだけで、持っていなければ常に囲い直しになる。

**押しただけで動かさなかったときは何も起きない。** 幅 0 の範囲が入ると、トーンが丸ごと消えたように見えて戻し方が分からなくなる。

#### 焼いた1枚は覚えておく。鍵に設定を混ぜる

画像基準を採ったので**描画は変えなくてよい**。展開した1枚に斜線を焼き込み、覚えておけば、本画面・サムネイル・書き出しの3つとも自動で新しくなる（ラフの `to_blue_pencil` と同じ形）。

**ただし `ImageCache` の鍵が足りない。** 鍵が画像の参照（`assets/` の相対パス）だけだと、**同じ画像を2枚貼ってトーンだけ変えた**ときに後から引いたほうが先の1枚を掴む。

- **入れ物を分けた**（`ToneCache`）。鍵に混ぜるだけの案もあったが、それだと同じ画像をトーン違いで使うたびに **PNG の展開が2回**走る。分ければ展開は画像ごとに1回で、こちらは焼く手間だけを持つ
- **上限を持つ**（既定 24 枚）。`ImageCache` の鍵は作品の画像数で頭打ちになるが、こちらは設定も鍵に入る。メニューで「濃く」を10回押せば10通りの鍵ができ、1枚 10MB 前後あるので放っておくとメモリを食い潰す
- **描く側に渡すのは参照文字列ではなく画像そのもの**にした（`PageRenderer.images`）。同じ `asset` でもトーン次第で別の絵になるため
- **書き出しは原寸に対して焼き直す。** 画面用に焼いた1枚を引き伸ばすと、書き出したものだけ斜線がぼやける。焼き直しても見た目が変わらないのは、間隔と太さを画像の短辺に対する割合で持っているため

**引いている最中はモデルを触らない。** 触ると1動きごとに焼き直すことになり、1枚 40ms がフレームごとに乗る（他のドラッグと同じ流儀）。

#### 実装で踏んだ所: `Alpha8` と `Grayscale8` を混ぜると黙って壊れる

マスクを合成に使うには、明るさを透明度として読み替える必要がある。**`convertToFormat` でこれをやってはいけない。** `Format_Alpha8` の画像を `Format_Grayscale8` へ変換すると**全画素 255 になり**、マスクが「画像全部」を指す。エラーは出ず、絵が一面トーンになって初めて気づく。

正しくは、**マスクは `Grayscale8` のまま持ち回り、合成の直前だけ生バイト列を `Alpha8` として持ち直す**（1画素1バイトで並びが同じなので、バイト列は使い回せる）。→ [PySide6の落とし穴.md](PySide6の落とし穴.md) の 3

#### 決めたこと

集中線・流線と同じ置き方をしているので、以下は追加の設計が要らなかった。

| 論点 | 決め |
|---|---|
| 書き出しに出るか | **出る。** 作品の内容そのもの。`ImageCache` を通るのでサムネイルにも自動で出る |
| Undo で戻るか | **戻る。** 保存形式に載る以上そうなる（→ 6.8） |
| サムネイルの指紋 | `page.to_dict()` から作っているので**自動で含まれる**。付箋（→ 6.18）と違い、絵に出るものなので除く必要が無い |
| 複製 | 保存形式を1往復させるので**自動で写る**（→ 6.15）。長さを1つも絶対値で持たないので、写した先で見た目も崩れない |
| 画像を回したら | 斜線も範囲も一緒に回る。画像基準の帰結で、向きを揃えたければ `angle` で直せる |
| 画像を消したら | 一緒に消える。画像の属性なので、紐づけを外して残す相手がいない |
| 選択できる種類 | **増やさない。** コマ・画像・フキダシ・セリフ・マークの5つのまま |
| 元の画像 | **1バイトも変えない。** 焼くのは展開した写しのほうで、`assets/` の実体はそのまま |

#### 今回やらないこと

- **トーンを塗る道具。** 範囲を選ぶ・削る・複数種類を重ねるは作らない。どこを塗るかの指定は元画像の側で済ませる
- **網点。** モアレが出る。**灰色（→ 上）がこの代わりになる**——粒が無いのでモアレの出ようが無く、クリスタで貼り直すなら白抜きのほうが向いている
- **クロス（斜線を直交でもう1回引く）。** 種類の枠ができたので数行で入るが、要求に挙がっていない（2026-08-06 本人確認済み）
- **線数（60L・80L）でトーンを選ばせること。** 線数は「1インチあたり何本」なので、**dpi を持たないこのアプリでは指定できない**（→ 3章）
- **トーンを素材の画像として持つこと。** 斜線は生成する。素材にすると `assets/`・タイルの継ぎ目・解像度違いの3つが増える（集中線を生成にした 6.16 と同じ判断）
- **トーンの種類に集中線を並べること。** 集中線は 6.16 で**既に別の機能**として入っており、しかも中心を後からずらせるよう図形として生成している
- **面積で線とベタを分ける。** 「細さで落とす」と「矩形で絞る」で足りた
- **明るさの下限を持つこと。** 同上。運用と一組でしか効かない
- **白抜きの穴埋め**（Flood Fill）。目の白目まで潰れる
- **矩形を2つ以上。** 1つ（→ 上）
- **矩形の縁をぼかすこと。** 硬く切れるほうがネームでは扱いやすい
- **紙（ページ）基準の斜線。** 画像を大きく引き伸ばすと斜線も粗くなるが、ネームの下敷きとしては読める。粗さが気になった時点で考える
- **明るい側の置き換え**（白い所にトーン）。要求が立っていない
- **色の指定。** 敷けるのは黒・灰・白だけで、色は選べない。集中線・流線のような「白にする」を持たないのは、ここが黒ベタの置き換えなので、**白い斜線**にすると「黒を白で塗る」ことになって元の絵の意味が消えるため（**白抜き**は斜線をやめて一様に白くするもので、これとは別）

### 6.31 設定を調整する道具 → **実装済み（2026-08-06）**

`data/settings.json` を窓から書き換える。リポジトリ直下の **`settings.bat`**（中身は `tools/settings_editor.py`）。

#### 手書きのどこで詰まっていたか

設定は**手で書き換える前提**で作ってある（→ 6.6）。その前提は変えないが、実際に書き換えると次の5つで詰まる。**この道具はこの5つだけを引き受ける。**

| 詰まる所 | なぜ起きるか |
|---|---|
| フォルダの場所で `\` を2つ重ねる | JSON の決まり事。間違えるとファイルごと読めなくなり、**設定が丸ごと効かなくなる** |
| 書き間違えても黙って既定値に戻る | 「1文字の間違いでアプリを開けなくするほうが困る」という判断（→ 6.6）の裏返し。**効かない理由が画面に出ない** |
| 何を書けるか覚えていられない | README を横に置く必要がある。項目は4つで、`export_dpi` を足す案も控えている（→ 10.2） |
| 効くタイミングが項目ごとに違う | `autosave_interval_sec` だけはアプリを開き直す。他は使う直前に読み直すので即効く |
| 実在しないフォルダを書いても分からない | 外付けドライブが繋がっていない日は、正しく書いても効かない |

#### 画面

小さな窓1つに4項目を縦に並べ、**入力欄の真下に説明を置く**。説明を右や別の窓（ヘルプ）に逃がさないのは、README を開き直さずに済ませるのがこの道具の目的の1つで、離すと目的が半分になるため。

| 行 | 入力の形 | 出す補足 |
|---|---|---|
| 最初に開くフォルダ | テキスト欄＋「選ぶ…」＋「空にする」 | 実在しなければ「今この PC からは見えません」。**ただし保存は止めない**（外付けが繋がっていない日にも先に設定しておける） |
| 自動バックアップの間隔 | 数値（5〜3600秒） | 「この項目だけは、変えたらアプリを開き直します」を常時 |
| JPG の品質 | 数値（1〜100） | 既定90。「90 と 100 は見分けがつかず容量だけ増える」（→ 6.7） |
| ラフの濃さ | 数値（0.05〜1.00、0.05刻み） | 既定0.40。「薄いとコマ枠と紛れず、濃いとなぞる線がよく見える」（→ 6.23） |

**範囲は入力欄が守る。** 入れられない値をそもそも入力できないので、「保存したのに既定に戻っている」が起きない。`\` を2つ重ねる書き方は `json` 側が受け持つので、利用者は普通のパスを渡すだけでよい。ファイル窓が返す `/` 区切りと、エクスプローラーの「パスのコピー」が付ける前後の `"` は、書き込む前に落として揃える。

#### 触っていない所を壊さない

**この道具の一番の役目はここ。** 手で書き換える前提のファイルに、後から機械が触るという立場なので、書き換えの正しさより「触っていない所を壊さないこと」を優先する。

- **知らない項目を消さない。** 読んだ JSON を丸ごと持ったまま4項目だけ差し替えて書き戻す（`update_settings_file`）。本体の `save_settings` は全項目を書き直す作りなので、そのまま使うと手で書き足したものが黙って消える。**本体側は変えていない**（雛形を置くときはむしろ全部書いてよい）
- **壊れた設定は、開いたうえで先に知らせる。** 「読めませんでした。保存すると今の中身は置き換わります」を窓の上に出す。黙って上書きすると、手で書いた中身が消えたことに気づけない。消したくない人は保存せずに閉じて、元のファイルを直せる
- **書けなかったときは閉じない。** 閉じると「設定した」と思い込んだまま進む。設定は次にアプリを使うまで結果が見えないので、ここで止めないと気づく機会が無い

#### 採用されなかった値を、開いたときに知らせる

範囲外・型違いは既定値に落ちる。**落ちること自体は正しい動き**だが、書き換えた本人からは「効かない」としか見えない。設定ファイルに書いてあるのに採用されていない項目を拾って、窓の上に出す（「次の項目は、書かれている値を使えなかったので既定に戻しています: 自動バックアップの間隔」）。

区別が2つ要る。

- **書いていない項目は知らせない。** 既定で動くのが当たり前で、知らせても雑音になる
- **`null` / `""` は打ち間違いではない。** 「最初に開くフォルダ」を空にするのは「ドキュメントから始める」という意図の指定

`true` は Python では `1` として通ってしまうため、値の一致だけを見ると「採用された」に見える。真偽値は値によらず落ちたものとして扱う（設定側の検証と同じ理由 → 6.6）。

#### アプリのメニューには入れない

設定を触るのは年に数回で、**そのために本体のメニューを1行増やす釣り合いにならない**。独立していればアプリ本体に手を入れずに済み、本体を壊す心配も無い。アプリを開いたままでも使えて、フォルダ・JPG品質・ラフの濃さは使う直前に読み直すので即効く（→ 6.6）。

置き場所は `tools/` ではなく**リポジトリ直下**。`tools/` の中だと、他の3つ（`check_clipboard.py` など開発中の切り分け用）と同じ「開発用」に見えて、日常的に使うものだと分からない。

#### 判断の記録

- **`settings.bat` は `run.bat` と同じ形（`python.exe`＋失敗したら `pause`）にした。** 黒い画面を出さない `pythonw.exe` も考えたが、**それだと起動に失敗したときに何も出ないまま終わる**。本体も同じく黒い画面が出るので、見た目も揃う
- **「すべて既定に戻す」ボタンは付けない。** 既定値は各行の説明に出ているので手で戻せる。押す機会が年に数回のボタンで画面を1行増やさない
- **「やめる」に確認を出さない。** 設定は数行で、間違えても開き直して入れ直せる（コマ削除に確認を付けていないのと同じ判断 → 6.2）

#### 今回やらないこと

- **設定の項目そのものを増やすこと。** `export_dpi` を足すかは別の判断（→ 10.2）。この道具は「今ある項目を書きやすくする」だけ
- **作品に属する設定**（ページの大きさ、綴じ方向）。あれは `project.json` 側で、混ぜると作品を渡した相手の好みを上書きする（→ 6.6）
- **設定の履歴・取り消し。** 数行のファイルで、間違えれば開き直して入れ直せる
- **手書きを塞ぐこと。** この道具は必須ではない。メモ帳で開く道はそのまま残る

### 6.28 PSD 書き出し（第1段階） → **実装済み（2026-08-06）**

ページを**1枚に潰さず、種類ごとのレイヤーに分けて**書き出す。クリスタ側で「コマ枠だけ残す」「絵だけ薄くする」「セリフを消して打ち直す」ができるようになる。着手前の見立てと、決めたことは 10.1 にある。

#### 動線は増やさない

**書き出しダイアログ（`Ctrl+E`）の「形式」に PSD を足しただけ。** 範囲・画像サイズはそのまま効き、増える項目は無い。レイヤーの分け方は決め打ちなので、選ばせるものが無い。

書き出し先も PNG と同じ `export/` で、`p01.psd` のように**拡張子だけが違う**（→ 6.7）。同じ場所に並ぶので、どのページを出したか探し直さずに済む。

#### コマはフォルダ、それ以外は種類ごと1枚（下が奥）

    セリフ / マーク / フキダシ
    [フォルダ] コマN ── 絵・トーン範囲（非表示）・集中線と流線・コマ枠
      …（コマの数だけ、奥から手前の順に）
    ラフ / 用紙

**並びは `render.PageRenderer.draw` の描く順そのまま。** ここで独自の順を持つと、画面と書き出しで重なりが食い違ったときに、どちらが正なのか決める相手がいなくなる。

フキダシ・マーク・セリフは**種類ごとに1枚**。ページ直下にあってコマの子ではない（→ 4章）ので、コマに紐づけようがない。

**中身の無いレイヤー・フォルダは出さない。** フキダシを1つも置いていないページに空の「フキダシ」が残っても、クリスタでは邪魔になるだけ。

**ラフは非表示で入れる**（→ 6.23）。なぞる相手であって作品の中身ではないので、開いた直後に見えていては困る。PNG では表現できず捨てるしかなかったが、PSD は非表示と不透明度を持てる。

**ラフだけは原寸を焼き直す。** 画面用は長辺 1,600px に縮めてあるが、PSD のラフはクリスタでなぞる相手なので、引き伸ばして入れると入れた意味が薄れる（→ `images.full_rough_from_bytes`）。

#### トーンは4枚に分けて渡す（2026-08-06 に追加）

**クリスタのトーンは種類がはるかに多く、こだわるなら結局そちらで貼り直すことになる**（本人の要望 2026-08-06「マスク画像をそのまま用意」）。**そのとき1手で差し替えられる形にして渡す。**

    トーン       ← 消して、自分のトーンに差し替える
    トーン範囲   ← 非表示。選択範囲を作るための目印
    白ベタ       ← 元の絵の黒ベタを隠す
    絵           ← **トーンを焼く前**

##### マスク1枚では足りなかった（同日に作り直した）

**最初はマスク1枚だけを足したが、それでは差し替えられなかった**（本人の指摘 2026-08-06）。「絵」にトーンが焼き込まれたままなので、その上に網点を貼っても**隙間から下のトーンが透けて干渉する**（濃くなる・モアレの元になる）。

**必要なのは、下敷きを断つ白ベタと、消せる形で分かれたトーンの2枚。** マスクは選択範囲を作るための目印でしかなく、それだけでは「貼る場所が分かる」ところまでしか行かない。

| 論点 | 決め | 理由 |
|---|---|---|
| 動線 | **増やさない。** PSD のコマのフォルダに3枚増えるだけ | 書き出しの選択肢を増やすと「どちらで出したか」を覚えることになる（→ 上「動線は増やさない」） |
| 「絵」の中身 | **トーンを焼く前**（`psd_export.BareImages`） | ここが今回の要。焼いたままでは何を上に置いても透ける |
| 表示 | **トーン範囲だけ非表示**（ラフと同じ）。白ベタとトーンは表示 | 目印は作品の中身ではない。白ベタとトーンは**開いた時点の見た目を作っている**ので消せない |
| 並び | **白ベタ → トーン範囲 → トーン** | トーン範囲を選んだまま新しいレイヤーを作ると、**白ベタより手前・コマ枠より奥**に入る。白ベタの下に入ると隠れ、一番上だと枠線を覆う |
| 描き方 | **絵と同じ経路**（`PageRenderer` の画像を引く所だけ差し替える → `psd_export._Painters`） | コマの形での切り抜き・画像の位置・回転・矩形の絞りが自動で揃う。別に描くと、斜めのコマや回した絵で**トーンだけがずれる**。ずれても画面には出ないので、**クリスタで貼ってから気づく**ことになる |
| 3枚の作り方 | **1回のマスクから3枚とも作る**（`tone.tone_pieces`） | 焼くのは1枚 40ms 程度。別々に呼ぶと同じ計算を3回する |
| 白ベタの縁 | **中間の濃さを落とす**（`tone._fully_masked`） | マスクの縁は 0 と 255 のあいだの値を持つ（細さで落とす段が拡大を通るため）。そのまま白を敷くと、縁で**2回混ざって**焼いた1枚より白く浮く |
| 元の絵 | **`FullImages` と同じ入れ物から引く**（`base`） | 別に持つと、同じ絵を1ページで2回展開する |
| 出さない場合 | **トーンの入った絵が1枚も無いコマ** | 空のレイヤーはクリスタで邪魔になるだけ（→ 上） |
| 別 PNG で出す案 | **採らない** | クリスタで位置を手で合わせることになる。位置合わせが要らないことがこの機能の中身 |

**種類の「白抜き」（→ 6.27）と組にすると、貼ったあとの見た目まで揃う。** 白ベタが下を断つので斜線・灰色のままでも干渉はしないが、**トーンを消した時点の見た目**が白抜きなら白、斜線なら斜線で残る。

##### 分けると、トーンの境目 1px が PNG 書き出しとずれる

**これまでは「合成済みの1枚が PNG と完全に一致する」ことを保っていた**（→ 下、第2段階）。**トーンを分けたページではそこが崩れる**（最大 34/256、実測）。

理由は、**絵とトーンが別々に縮んでから重なる**ため。焼き込んであれば合成してから1回だけ縮むので、混ざる順番が入れ替わらない。**避けるには焼き込んだままにするしかなく、それでは分ける意味が無い**（2026-08-06 本人確認済み）。

- ずれるのは**トーンの範囲の境目 1px だけ**。面の中も外も一致する
- **クリスタで見える絵と、PSD の中の合成済みの1枚は一致する**（同じレイヤーを重ねたもの）。食い違うのは PNG 書き出しとの間だけ
- 白ベタの縁を落としてあるのは、このずれを最小にするため（上の表）
- トーンを分けないページは**今までどおり完全に一致する**（テストも 1 までのまま → `test_psd_export.Test重ね直すとPNGと一致する`）

##### 絵を重ねているコマだけは分けない

分けると、**コマの中の絵が全部「絵」レイヤーへ、トーンが全部その上のレイヤーへ**まとまる。絵が1枚なら順番は変わらないが、**トーンの入った絵の上に別の絵を重ねている**場合、上の絵より手前にトーンが出てしまう（背景の上にキャラを重ねる使い方 → 6.27）。

そのコマだけは今までどおり**焼いた1枚とトーン範囲**を出す（→ `psd_export._splittable`）。

**重なっているかどうかまでは見ない。** 見るには回転を含む形の判定が要るうえ、**外したときの結果が「絵の上にトーンが乗る」という気づきにくい間違い**になる。トーンの入った絵の上に別の絵があるかどうかだけで決める。

#### コマをフォルダにまとめたのは、重ねたコマで枠線が貫いたから（第2段階）

**第1段階では種類ごとの8枚だった。** 枠線を1枚にまとめたため、コマを重ねたページで**下のコマの枠線が上のコマの絵を貫いて出た**（線が二重に描かれたのではなく、隠れるべき線が隠れなかった）。集中線・流線でも同じことが起きていた。

コマ1つ＝フォルダ1つにすると、上のコマの絵が下のコマの枠線より手前に来る。**6.24 が持っている重なり順をそのまま並びに移すだけで、幾何の計算は要らない。**

外から見ると「隣り合うコマが境界線を共有して二重に描かれる」に見えるが、**このアプリではその状態が作れない**——コマとコマの間には隙間（`LayoutSettings.gutter`、既定 35px）が入る。したがって「共有される辺を1本にまとめる」類の手当ては効かない。**覆われる辺は相手の内側を通っている**ので、重なりをまとめる操作では消えない。

これで**合成済みの1枚（レイヤーを重ねた結果）が PNG 書き出しと完全に一致する**ようになった。第1段階で唯一残っていた食い違いが消えている（**トーンを4枚に分けたページだけは境目 1px がずれる** → 上）。

##### フォルダの名前も並びも読み順で コマ1〜コマN

番号は**上端が上のものから、同じくらいなら右から**（縦の差が `gutter` より小さければ同じ段と見なす → `psd_export.reading_order`）。**このアプリはコマに番号を持っておらず、ここで初めて振る。** 番号はラベルでしかないので、変わったコマ割りで直感と合わなくても中身は入れ替わらない。

**一覧の並びも同じ読み順にする**（上から コマ1、コマ2……）。PSD には並び順と重なり順の区別が無く、一覧の並びがそのまま重なり順になるが、**コマが重ならないなら前後は絵に出ない**。出ないものを絵に合わせる必要は無く、探しやすい向きを採る。

**1組でも重なっていれば重なり順のまま**（→ `psd_export._stacking`）。そちらでは並びが絵に出る。読みやすさのために、下のコマの枠線が上のコマの絵を貫く状態へ戻したら本末転倒。

重なりの判定は、コマの形を**枠線の太さぶん外へ膨らませて**から見る（→ `psd_export.panels_overlap`）。枠線は形の線の上に中心を置いて引かれるので太さの半分だけ外へはみ出しており、形だけで見ると隣り合っただけのコマが「触れていない」ことになる。

**フォルダは閉じた状態で入れる。** コマの数だけ並ぶので、開いていると一覧が縦に伸びて「どのコマか」を探すのに流し読みが要る。

##### 枠線を1枚のまま隠れる部分だけ落とす案は採らなかった

各コマの枠線を描くときに手前のコマの形を切り抜く手もあり、十数行で済むうえ**枠線をまとめて編集できる**という利点がある。**将来ユーザーが選べるようにする可能性は残す**が、今回はスコープ外（2026-08-06 本人確認済み）。

この案は PNG と厳密には食い違う。**上のコマに絵が入っていない場合**、PNG では下の枠線が見えるが、この案では消える。

#### 外部のライブラリを足さずに書いた

書き込みができるものは numpy と C 言語の拡張を要求し、更新が止まっている。**出来合いのパッケージが無ければその場でコンパイルが要り、2台のPCで同じ環境を作れなくなる。** 必要な範囲（8bit RGB・ラスターレイヤー・透明度）だけなら標準機能で書ける。**依存パッケージは1つも増えていない**（トーン 6.27 で numpy を足さずに済んだのと同じ形）。

圧縮は PackBits（同じ値の連続をまとめる方式）。**同じ値が3つ以上続く場所を 0x00 と 0xFF だけ探す**という割り切りで書いてある。長く続くのは透明な所（0）と白・黒で塗った所しかなく、絵の部分は元々ほとんど縮まないので、そこを丁寧に探しても待ち時間が増えるだけになる。

#### 実測（2026-08-06、A4 相当・117%・1447×2046px）

| | 大きさ | かかった時間 |
|---|---|---|
| PSD（4コマ・9レイヤー、絵・集中線・ラフ入り） | 18.9 MB | 1.0 秒 |
| PNG（同じページ） | 0.3 MB | 0.2 秒 |

コマごとのフォルダにしても**大きさも時間も変わらなかった**。増えるのは大きさ0の目印だけで、画素は同じだけしか無い。

**トーン範囲を足したぶんは測っていない**（2026-08-06 時点）。**トーンを入れたコマにだけ**1枚増え、黒と透明しか無いので PackBits でよく縮む見込みだが、確かめてはいない。増えるとすればマスクを焼く手間（1枚 40ms 程度 → 6.27）と、その1枚ぶんの大きさ。

**PSD は PNG の約60倍。** 絵のレイヤーがほとんど縮まないため。全ページ書き出すとページ数ぶん積み上がる。

#### 検証のしかた

**レイヤーを重ね直した結果が PNG 書き出しと一致することを、画素で突き合わせている**（`tests/test_psd_export.py`）。分け方を間違えても PSD としては開けてしまうので、形式の検証だけでは捕まらない。ここが通れば**クリスタを開かずに分解が正しいと言える**。

食い違いの上限は 1。重ねる回数が1つ増えるぶん、半透明の所で 256 段階への丸めが1回多く掛かる。**2以上になったら丸めではなく分け方の間違い。**

形式そのもの（見出し・解像度・レイヤー名・画素）は、書いたものを読み返して確かめている（`tests/test_psd.py`）。**読む処理はテスト側に持つ。** 書く処理をもう一度呼んでも、決まりを守れているかは分からない。

#### クリスタで開けることを実機で確認した（2026-08-06）

自動テストで言えないのはここだけだったので、A4 相当・117%（絵・集中線・ラフ入り、18.9MB）を実際に開いて確かめた。**レイヤー名・並び・非表示のラフ・セリフの本文とも、そのまま出た。**

この確認で**重ねたコマの枠線が貫く**のが見つかり、第2段階（コマごとのフォルダ）に進んだ。**第1段階を小さく出した狙いがそのまま働いた形。**

#### 確認用のスクリプトを画面なしで動かすと、セリフが豆腐（□）になる

上の確認の途中で1度引っ掛かった。**PSD の問題ではなく、書き出しより前のフォント解決で起きる。**

自動テストは画面なし（`QT_QPA_PLATFORM=offscreen`）で動かしている（→ `tests/conftest.py`）。この状態では Windows の日本語フォントが引けず、`Yu Gothic UI` を要求しても CJK を持たない別のフォントに落ちる。**同じページを PNG で書き出しても同じ豆腐が出る**ので、PSD かどうかとは関係ないと切り分けられる。

テストの中の突き合わせは両側とも同じ経路を通るので、**位置の検証（2px ずれを見つけたもの）には影響しない**。引っ掛かるのは、書き出したものを目で見る確認用スクリプトのほう。**そういうスクリプトは `QT_QPA_PLATFORM` を外して動かす。**

#### 踏んだ落とし穴

- **レイヤー名の欄は2つある。** 古いほうは1バイト文字しか入らないので、日本語は `luni` のほうへ UTF-16 で書く。古いほうだけに書くと**クリスタで文字化けする**。古いほうには役割が分かる英字（`paper` / `art` / `text`）を入れてある
- **`Format_ARGB32_Premultiplied` に描いた文字は、`Format_ARGB32` より 2px ずれる。** 透明な紙に描くので速い形式を選んだところ、**セリフだけが左上へ 2px 寄った**。塗りつぶしの色とは無関係で、形式だけで決まる（→ [PySide6の落とし穴.md](PySide6の落とし穴.md) の 4）。上の画素の突き合わせが無ければ見つけられなかった

#### 今回やらないこと

- **枠線を1枚のままにして、隠れる部分だけ落とす形**（上記。将来ユーザーが選べるようにする可能性は残す）
- **PSD の読み込み。** 「クリスタで作ったものをこの道具へ戻す」動線そのものが無い（→ 1章）
- **編集できるテキストレイヤー・調整レイヤー・レイヤーマスク・ベクター。** クリスタ側で作れるものをこちらで持つ意味が無い
- **トーンを別レイヤーにすること。** 画像に焼き込む作り（→ 6.27）で、分けるとコマの中の画像ごとにレイヤーが増える。仕上げはクリスタでやり直す前提なので見合わない
- **グレースケール・モノクロ2階調での書き出し。** 8bit RGB だけ

### 6.29 抜けチェック（書き出す前の点検） → **実装済み（2026-08-06）**

**全ページを見回して、直し忘れを出す。**「ファイル → 抜けチェック...」で走り、**自動では何も直さない**。着手前の整理は 10.1 に残してある。

| 何を | どこに出るか |
|---|---|
| **何が問題か** | 結果の窓にテキストで。閉じるまで残り、選んでコピーできる |
| **どのページか** | サムネイルの**左上に紫の印**。窓を閉じたあとも残る |
| **件数** | 画面下の1行（6秒で消える） |

#### 拾うもの

重い順。**この順で窓に並ぶ**（`check.KIND_ORDER`）。

| 種類 | 何を見るか |
|---|---|
| 使えない画像 | `assets/` に無い、または実体はあるが画像として開けない。マークも数える（コマの子ではないが同じように白く抜ける） |
| 何も置いていないページ | コマ・フキダシ・セリフ・マークが1つも無い。**ラフは数えない**（→ 下） |
| フキダシからはみ出したセリフ | `layout.text_ink_bands` の帯が `layout.balloon_contains` の内側か |
| 空のままのセリフ | 文字が空（空白だけも空とみなす） |
| 絵の入っていないコマ | 画像が1枚も無い。**集中線・流線だけのコマは空ではない** |
| 付箋の残っているページ | `Page.note`（→ 6.18） |

窓では**「直し忘れ」と「消し忘れ」に分けて**出す。付箋は自分で貼った目印であって、絵が白く抜ける画像の欠けと同じ列に置かない。

##### 「無い」と「開けない」を分けない（2026-08-09 に統一）

画像については、**実体が無い場合と、実体はあるが画像として開けない場合を1つの項目にまとめる。** 書き出した結果はどちらも同じ——そこが白く抜ける——で、利用者に伝えたいこともそこだから。名前を「実体の見つからない画像」から**「使えない画像」**に改めたのは、実体がそこにあるのに「見つからない」と出ると、無いファイルを探しに行かせてしまうため。

**書き出し前の警告（`export.missing_assets_in`）も同じ判定を使う**（`EditorState.has_asset`）。分かれていた頃は、点検が実体の有無だけを見て「問題なし」と答える一方、書き出し前の警告は展開できるかを見ていたため、**壊れた画像1枚に対して2つの機能が違う答えを返していた。**

- **判定はヘッダーだけを読む**（`images.readable_file`）。点検は全ページを巡るので、展開まで試すとその場で固まる（2026-08-08 の指摘と同じ理由）
- **ヘッダーが正しく、画素の途中で切れているファイルは取りこぼす。** そこまで見るには結局すべて展開するしかない。画面には従来どおり×印が出るので、気づけないわけではない

#### 何も置いていないページ（2026-08-06 に追加）

**判定が逆立ちしていた。** 空のコマ1つは拾うのに、コマが1つも無い白紙のページは素通りしていた。白紙のほうが直し忘れとしては明らかで、そのまま書き出せば白いページが1枚出る。

**ラフ（→ 6.23）は中身に数えない。** 書き出しでは切られる（`PageRenderer` の `rough=False`）ので、敷いてあっても**書き出した結果は白紙のまま**。「書き出すと白いページになる」をそのまま判定にしている。

**付箋も中身ではない**（→ 6.18。本画面にも書き出しにも出ない）。付箋を貼っただけの白紙ページは、白紙と付箋の2件として出る。

#### 用紙からのはみ出しは拾わない（2026-08-06 に外した）

10.1 の候補には入れていたが、**入れた翌日に外した**（本人確認済み）。**紙の端まで絵を出すコマ（断ち切り）は普通の使い方**なので、拾うと窓が常に埋まる。「一覧が常に埋まると読まれなくなる」（→ 10.1）は、しきい値の話としてセリフのはみ出しにだけ書いていたが、**項目そのものを入れるかどうかにも同じことが言える**。

**`layout.outside_page` は消していない。** あちらは**ページの大きさを変えた直後**に「今この操作で外へ出た」ことを知らせるもの（→ 6.1）。同じ計算でも、**その場で起きた変化**と**ずっとそうだった状態**では意味が違う。前者は知らせる値打ちがあり、後者は本人が選んでそうしている。

#### 何も書かない

**作品ファイルを1文字も変えない。** 紫の印は画面の状態（`EditorState.check_marks`）だけに持ち、`Page` には持たせない。結果として、押しても**未保存にならず・Undo の履歴も伸びず・サムネイルの指紋も変わらない**。

**印は付箋ではない**（→ 10.1 に理由）。付箋はページに1つだけなので、同じ場所へ紫を入れると自分で貼った黄・桃・青が上書きで消える。描き方だけを借りて（`ui/pages.py` の `_paint_badge`）、置き場所を左上に分けてある。

| いつ | 印はどうなるか |
|---|---|
| もう一度押した | **前の結果を捨てて付け直す。** 直っていれば消える |
| 編集しただけ | **消えない。** 直ったかどうかは点検し直して確かめる |
| 窓を閉じた | 残る。窓を閉じたあとも残りを追えるのが印の役目 |
| 別の作品を開いた | 消える（→ `EditorState.reset`） |

#### セリフのはみ出しは、フォント無しでも判定できた

10.1 では「**この項目だけ実機での確認**になる」と見ていた（検証環境にフォントが1つも無い → 6.5・6.11）が、**自動テストで確かめられた。**

`text_ink_bands` は**字送りの計算だけ**で帯を出しており、実際の字形を測っていない（→ `vertical.layout`）。測っていないものは、フォントが無くても同じ答えになる。

**判定は厳しくしすぎない**（`check.OVERFLOW_SLACK` ＝ 字の大きさの 25%）。縦書きの帯は列送りぶんの幅を持つので、楕円のフキダシでは角が少し外へ出るのが普通。ここを 0 にすると窓が常に埋まり、埋まった窓は読まれなくなる。

- **縦書き**は帯の四隅で見る。列送りの余り（字の 1.33 倍ぶん）を先に削ってから、見逃す幅ぶん内へ寄せる
- **横書きは左右で見ない。** Qt が行を組むので字送りが分からず、帯の幅が枠のまま（→ `layout.text_ink_bands`）。左右で判定すると、字が短いだけのセリフまで全部はみ出しになる
- **フキダシに紐づいていないセリフは対象外。** コマの外へ置くナレーションは正常な使い方（→ 6.5）

#### 見立てから変わった点

- **セリフのはみ出しがテストできた**（上記）。10.1 で「先に潰すと決めた点」に挙げていた唯一の穴が塞がった
- **窓から飛ぶ機能が要らなくなった。** 紫の印が付いたサムネイルを押せばページは切り替わる（→ 6.1）ので、移動手段は既にある
- **拾うものを入れ替えた**（実装当日）。**用紙からのはみ出しを外し**（断ち切りと区別が付かない）、**何も置いていないページを足した**（空のコマは拾うのに白紙は素通りしていた）。どちらも 10.1 の候補表からは読み取れず、**動くものを1度通してから**でないと出てこなかった
- **規模は 500行前後の見立てに対し、およそ 440行**（検査 255 / 窓 89 / 印 70 / メニュー 30）。テストは 51件・570行で、見立て（200行）より厚くなった——判定のしきい値まわりに「拾わない」ことを確かめる例が要るため

### 6.30 メニューを探す（ヘルプ） → **実装済み（2026-08-06）**

**打ち込んだ言葉を含むメニュー項目を、道順と一緒に並べる。**「ヘルプ → メニューを探す...」（F1 / Ctrl+F）。項目は 124 件あり、10 本のメニューに分かれている。**どこから手が届くかが分からない**という躓きに対して、メニューを1本ずつ開いて回る以外の手を1つ持たせる。

| 出すもの | 例 |
|---|---|
| 道順 | `ファイル → ラフ → 読み込む...` |
| ショートカット | `［Ctrl+O］`（無い項目には出さない） |
| 説明 | 下の段に字下げして（カーソルを乗せた間だけ画面下に出るあの文） |

#### 項目名だけでなく、説明からも探す

**項目名は読む字数を減らす側に倒してある**（→ 6.12）ので、名前だけを対象にすると当たらない言葉が多い。「backup」で「バックアップから復元...」に、「断ち切り」で該当なしと分かるのは、説明を見ているため。道順も対象に入れてあり、「表示」と打てば「拡大」も出る（項目名にも説明にも「表示」は無い）。

#### ここから実行はしない。出すのは場所だけ

窓から直に押せるようにすると、**選んでいるものによって押せない項目**が並んだときに、押せない理由をこの窓でももう一度説明することになる。メニュー側は灰色でそれを示している（→ 6.12）ので、行き先だけ出して開き直してもらう。窓は相手を止めない（開いたままメニューバーへ手が届く）。

#### 押すと、そのメニューを画面上端で四角く囲む

行を1回押すと、いちばん上のメニュー（「画像」）の見出しに枠が付き、3秒で消える。**最初に開く場所だけを指す**もので、押しても実行はしない。

**メニューを実際に開いて項目まで光らせることはしない。** 外からメニューを開かせる操作は環境で挙動が割れやすく、**表示装置なしの自動テストでは確かめられない**（ポップアップが出ない）。加えて、開いたメニューは押せてしまうので、上の「実行はしない」の裏口になる。囲む位置はメニューバーに聞けば座標で返るため、**どこを囲むかは自動テストで押さえられる**——確かめられる形に寄せた。

枠は `QRubberBand`（範囲を示すためだけの Qt の部品）を借りる。自前で描くと、重ねる順番と再描画の面倒を見ることになる。

**窓は初めて開くとき、親の窓の右下へ寄せる。** 上端に重なると、囲んだ枠が窓の下に隠れて用をなさない。**2回目からは動かさない**（置き直した場所を開くたびに引き戻さない）。

#### 言い換えは、先回りして足す（2026-08-07 に方針を変えた）

**元は逆の決まりだった。** 「先回りで表を作ると、使われない言い換えまで抱えたうえ、項目が増えるたびに足す手間が残る」ため、**実際に打って出てこなかった言葉が分かってから足す**としていた。

**入れた翌日に、その決まりのほうを捨てた**（本人の判断 2026-08-07）。理由は、**このアプリが時間を無駄にしないためのものだから**。定番の呼び名で0件になって探し回る時間がいちばん大きな無駄で、そこに「使われるまで待つ」を掛けると、待っている間ずっとその無駄が出続ける。外したときに払うのは「使われない行が1つ残る」だけで、**釣り合っていない**。

ただし闇雲には増やさない。足すのは、**他のソフトで通っている呼び名で打たれる見込みがあり、かつ今このアプリで0件になる**言葉に限る。

**最初の1件は入れた当日に来た**（本人の要望 2026-08-06）。「フキダシ」に対して**「吹き出し」「吹きだし」「ふきだし」**の3つ（`menu_search.SYNONYMS`）。表に無い言葉は今も単純な文字の一致だけで探す（「こま」では「コマ」は出ない）。

**翌日に2件目が来た**（本人の要望 2026-08-07）。「セリフ」に対して**「テキスト」**、「ビックリマーク」に対して**「びっくり」**。

**同じ日に3件目。「コマ」に対して「コマ割」「コマ割り」「コマわり」。** これは言葉を知らなかったのではなく、**日本語IMEが「コマ割り」までをひとまとまりで確定する**ため、「コマ」だけを打つつもりでも送り仮名が付いた形が入ってしまう。文字の一致だけでは、長いほうを打った人が短い項目に辿り着けない。**送り仮名を落とすだけの読み替え**として表に入れた。

- **語の一部としても効く。**「ふきだしを追加」でも読み替わる
- **長い言い換えから先に当てる**（`read_as`）。短いほうが先だと、長いほうの前半だけ持っていかれる（「コマ割り」に「コマ割」が先に当たると「コマり」になり、どこにも当たらない）。表に書いた順に引きずられないよう、引くときに並べ替える
- **読み替えた先は、途中まででよい。**「びっくり」の行き先は「ビックリマーク」ではなく**「ビックリ」**にしてある。最後まで伸ばすと「びっくりマーク」と打たれたときに「ビックリマークマーク」になって外れる。項目名に含まれるところで止めれば、どちらの打ち方でも当たり、「ビックリはてなマーク」にも一緒に届く
- **アプリ側の表記は増やさない。** 表に出る「フキダシ」は `BALLOON_STYLE_LABELS` と `BALLOON_PLACE_HERE_NAME` の2箇所だけという決まり（→ 6.12）はそのまま。ここは**探すときだけの読み替え**で、画面に出る文言には触らない
- 表を膨らませない側の歯止めとして、**表に無い言葉が読み替わらないこと**を確かめるテストを1件置いてある

方針を変えたときに足したのが次の群（いずれも足す前は0件だった）。

| 打った言葉 | 行き先 | なぜ0件だったか |
|---|---|---|
| png / jpg / jpeg / psd / エクスポート / 出力 | 書き出し | **拡張子で打たれる。** 項目名は「画像で書き出し...」で、形式名がどこにも出ない |
| アンドゥ / リドゥ / 取り消し / セーブ | 元に戻す / やり直す / 保存 | 項目名をやまとことばに寄せてある（→ 6.12） |
| コピー / 複写 | 複製 | このアプリに `Ctrl+C` は無く、同じ用が足りるのは「複製」 |
| ズーム / 表示倍率 / 書体 / 検索 | 拡大 / フォント / 探す | 同上 |
| スピード線 / 尻尾 | 流線 / しっぽ | マンガ用語のゆれ |

- **拡張子は大文字で打たれる。** `read_as` は casefold 済みの言葉を受け取る形にし、表の鍵は小文字で書く決まりにした（「PNG」「Png」でも同じ行に当たる）

#### 一覧が0件になる問いには、一覧の外から答える

言い換え表が「同じものの別の呼び名」を結ぶ表なのに対し、ここから下の2つは**一覧では答えられないこと**に答える表（本人の要望 2026-08-07）。どちらも打つと一覧は0件になるが、**答えは正反対**なので表も分ける。

| 表 | 打った言葉が指すもの | 返す答えの向き |
|---|---|---|
| `MISSING_FEATURES` | このアプリに無い | 「ありません」 |
| `NOT_IN_MENU` | ある。ただしメニューバーには出ない | 「そこにあります」 |

出しかたは共通で、`guide_notes` が両方を引き、一覧の上に並べる。**「ある」ほうを先に出す**（行き先のある答えのほうが、探している人が次にやることに直に繋がる）。

##### 無い機能には、無いと答える

このアプリはコマ割りとフキダシの配置だけを受け持ち、**絵は描かない**。そう書いてあるのは README だが、**READMEを読まずに触った人は、まず「ペン」を探す**。文字の一致だけだと「見つかりません（別の言葉で探してください）」としか返らず、**無いのか、呼び名が違うだけなのかが分からない**まま探し続けることになる。言い換え表では答えられない問い（読み替える先が無い）なので、別の表にした。

| 打った言葉 | 返す答え |
|---|---|
| ペン | ペン入れ機能はありません。ペイントソフト側で行ってください |
| 消しゴム | 消しゴム機能はありません。（同上） |
| 擬音 / オノマトペ / 描き文字 / 効果音 | 擬音機能はありません。（同上） |
| ブラシ / レイヤー効果 / レイヤー結合 / AI生成 / ベクター / フィルター / 色調補正 / 印刷 / グラデーション / 図形 / ぼかし / シャープ | その機能はありません。（同上） |
| psd（PSD・ｐｓｄ も同じ） | PSD 機能は、【書き出し】のみとなっています |

- **文の中に混じっていても拾う。**「psdを入力するには？」のように、探す言葉ではなく質問の形で打たれることを見込む。大文字小文字・全角半角の違いは潰す（`missing_notes`）ので、PSD は**表に1行あれば足りる**
- **同じ答えは1回しか出さない。**「効果音のオノマトペ」で同じ文が2行並ばないよう、文面で重複を落とす
- **案内を出しても、一覧は消さない。** 案内は上に足すだけで、文字がたまたま一致した項目はそのまま並べる（「PSD」は書き出しの説明に出てくる）
- **無いと答えたときは「別の言葉で探してください」と言わない。** 言い直しても出てこないものを探し続けさせることになる
- **案内は一覧の上に出し、灰色にしない。** 下に置くと、0件の一覧を見た時点で窓を閉じられて読まれない
- **「トーン」は入れていない。** 依頼には挙がっていたが、`画像 → トーン` に16項目あり、**入れると窓が嘘をつく**。実在する言葉が表に入っていないことを確かめるテストを1件置いてあるので、後から足そうとすれば落ちる

**同じ日に、「定番なのにない、どこかにあるはず」という思い込みへ先回りする形で広げた。** 答えの文面は4つに分ける。

| 群 | 言葉 | 返す答え |
|---|---|---|
| 描く・加工する | ペン / 消しゴム / 擬音 / ブラシ / 塗りつぶし / 不透明度 / 縁取り / 白フチ / 手ぶれ補正 / パース定規 / トリミング / 左右反転 / AI生成 / ベクター / フィルター / 色調補正 / グラデーション / 図形 / ぼかし / シャープ / 印刷 | 〜はありません。**ペイントソフト側で行ってください** |
| 印刷・製本 | トンボ / 裁ち落とし / タチキリ / ノンブル / ページ番号 / 見開き / 表紙 / 入稿 / dpi / 解像度 | **印刷向けの機能はありません**（ウェブ漫画用です） |
| 文字組み | ルビ / 縦中横 / 行間 / 字間 / 禁則 | **セリフの書式は、大きさ・太字・寄せ・縦横だけです** |
| 置き場所の補助・その他 | グリッド / スナップ / ガイド / 定規 / 整列 / 等間隔 / テンプレート / 素材 / スタンプ / 置換 / プラグイン / マクロ / 一括 / クラウド / 同期 / 動画 / gif | **その機能はありません**（誘導は付けない） |

- **「ペイントソフトで」と言えないものがある。** 定規やグリッド、一括処理は向こうでやっても解決しない。**行き先を書けないときは、余計な誘導を付けずに無いとだけ言う**
- **「レイヤー」だけは、無いで済ませない。** 編集中には無いが、**PSD には分けて書き出す**（→ 10.1）。無いとだけ答えると、クリスタへ渡す道があること自体を知らないまま帰らせる。答えは「編集中にレイヤーはありません。PSD で書き出すとレイヤーに分かれます」
- **「切り抜き」は表から外した**（2026-08-27）。自動領域選択として実際に入ったので、
  一覧のほうが道順を答える（→ 10.3）。**「マスク」はレイヤーと同じ扱いで、無いでは済ませない**
  ——中身としては持っているが、画面に出る呼び名が違う。答えは「マスクという名前の項目は
  ありません。絵の一部を消すなら【画像 → 切り抜き】です」
- 「レイヤー結合」のように**2つの行に当たる語**は、答えを2行とも出す。片方を消す仕組みは入れていない（どちらも読む値打ちがあるため）

##### あるが、メニューには無いものは、行き先を答える

この窓の一覧は**メニューバーを辿って作る**（→ 下）。だから**右クリックからしか出ない機能は、どう探しても0件になる**。無い機能とまったく同じ見え方をするのに、正しい答えは正反対。

最初の1件は**付箋**（→ 6.18）。`NOT_IN_MENU` に「付箋」「ふせん」を置き、**「付箋は、サムネイルを右クリックしてください」**と返す。同じ日に3件足した。

| 打った言葉 | 返す答え | なぜメニューに無いか |
|---|---|---|
| 付箋 / ふせん | 付箋は、サムネイルを右クリックしてください | ページ一覧の右クリック限定（→ 6.18） |
| スクロール / 手のひら / 画面移動 / パン | 画面の移動は、スペースキーを押しながらドラッグ（中ボタンのドラッグでも動きます） | ホイールは拡大・縮小に取ってあり、上下移動は別の手（→ `MangaCanvas.wheelEvent`）。メニューには1つも出ていない |
| 設定 / オプション | 設定は settings.bat から変えられます | 設定の窓はメニューから出さず、`settings.bat` に分けてある（→ 6.31） |
| 右クリック | 多くの操作は、対象を右クリックすると出ます | **このアプリは右クリックが本体**（→ README 冒頭）。メニューバーだけを見ている人への近道 |

- **`MISSING_FEATURES` に混ぜない。** あちらには「メニューにも実在しないこと」を確かめる歯止めのテストが掛かっており、実在する付箋を入れると落ちる。2つの表に重なりが無いことも、テストで押さえてある
- 逆向きの歯止めとして、**この表の言葉が一覧では本当に0件になること**も確かめる。メニューから届くようになったら（＝案内が要らなくなったら）そこで落ちる

#### 一覧はメニューバーを毎回辿って作る

**作り置きしない。** 文言は状態で変わる（畳んだ親が「トーン調整中」になる → 6.27、「前回のファイルを開く（xxx.json）」にファイル名が入る → 6.6）ため、作り置きは古い名前で出る。124 件を読むだけなので、開くたびに作っても待たされない。

**`QAction.menu()` は使わない**（→ `PySide6の落とし穴.md` の 1）。メニュー側から `menuAction()` で引く逆向きで対応表を先に作り、それを見ながら下位のメニューへ降りる。逆向きにしないと、辿ったこと自体がアプリの持っている `rough_menu` などを後から無効にする。**症状が出るのは辿った場所ではなく、無関係な別の場所**なので、歯止めのテストを2件置いた（辿ったあとに `_refresh` が通ること／右クリックのメニューが組めること）。

#### 道具の項目は2か所に出る

「セリフを追加 (T)」のように、道具メニューと担当のメニューの両方に出る項目は、**一覧にも2行出る**。同じ項目が2つあるのではなく、**入口が2つある**のが実際なので、まとめずにそのまま出している。

#### 規模

窓と検索で 300行、メニューへの接続と枠の表示が 55行、テストが 31件・260行。

### 6.32 トゲトゲ_フキダシ（叫び・曲線版） → **実装済み（2026-08-07）**

**ギザギザ_フキダシと同じ叫びを、直線ではなく曲線で描いた6つ目の種類**（`style` が `"spiky"`）。頂は尖ったまま、山と谷の間が反り、谷が丸く底を打つ。

道具は「フキダシ > トゲトゲ_フキダシを追加」、または右クリックの「ここにトゲトゲ_フキダシを追加」。既にあるフキダシを「トゲトゲ_フキダシにする」でも変えられる。

#### ギザギザは残して、6種目として足した

**意味ではなく画風の違い**なので、ふわふわ（6.13）のように「無い表現を足す」話ではない。それでも差し替えにしなかったのは、**差し替えると保存済みの作品のギザギザまで黙って曲線に変わる**ため。どちらを使うかは作品ごとに選べばよく、選ばなければ出てこない（`kind` を自由に読む 6.14 と同じ緩さ）。

**ショートカットキーは割り当てない。** ギザギザと二者択一で、画風を決めたあとは片方しか使わない。キーを足すこと自体に値打ちは無く、元から通っていたキーを塞ぐ副作用は必ず付く（→ 7章）。

#### 作り方はふわふわ・雲と同じ経路に乗せる

楕円の半径を角度ごとに増減させる（→ 6.4、6.13）。足したのは形を作る関数1つと、`balloon_outline` の分岐1行、しっぽの付け根の分岐1行。

山1つの中の位置 `s`（頂で 0.0、谷で 1.0）に対して、半径を `1 - depth × sin(π s / 2) ^ sharpness` にする。

- `s → 0`（頂）で傾きが残るので、**頂は角のまま**。ここが丸まるとふわふわ（不安）に読めて、意味が逆になる
- `s = 1`（谷）で傾きが 0 になるので、**谷はなめらかに底を打つ**。ふわふわが余弦で頂も谷も丸めるのと、ここが違う
- **しっぽの付け根は谷より内側**（`1 - spiky_depth`）。ギザギザ・ふわふわと同じ理由（→ 6.4）

#### 1山あたりの線分数に下限を置く

`SPIKY_MIN_SEGMENTS_PER_SPIKE`（28本）。ふわふわ（8本）より多い。楕円の分割数（72）を山の数（12）で割ると1山あたり6本になり、**山と谷の間が直線に落ちてギザギザとの差が消える**。曲線であることがこの形の意味そのものなので、分割数の設定より下限を優先する（ふわふわ・雲と同じ線引き → 6.13、6.22）。

#### 既定値は描いて決めた

**山 12・深さ 0.33・鋭さ 0.7**（`spiky_spikes` / `spiky_depth` / `spiky_sharpness`）。

**深さはギザギザと同じ 0.33**。同じ叫びの直線版・曲線版なので、深さまで違うと「曲線にした」以外の差が混ざり、どちらを選ぶかの判断がぶれる。

**山だけはギザギザ（14）より減らして 12 にした。** 曲線にするとトゲ1本が太くなるぶん、同じ数では密に見える。最初はギザギザと同じ 14 で作ったが、**セリフ（「待って！」）を入れて 10 / 12 / 14 を並べたところ、違いはトゲの太さより「セリフの周りに残る余白」として出た。** 10 では1本が大きすぎ、14 では余白が詰まる。2026-08-07 に目で見て 12 にした。

鋭さは頂の近くの落ち方だけを変える。1.0 より小さいと急に落ちて鋭く、大きいと頂の近くが平らになって角そのものが消える。曲線の当て方を4案（直線のまま / 内へえぐる / 頂は鋭く谷は丸く / 外へ膨らむ）並べて描き、2026-08-07 に目で見て「頂は鋭く谷は丸く」を選んだ。**数では確かめられない性質**なので、自動テストには「山と谷の間に収まる」「頂の隣の刻みが谷の隣の10倍以上ある（＝頂は角・谷は丸い）」までしか入れていない。

## 7. 操作（ショートカット）

| キー | 動作 | 状態 |
|---|---|---|
| `V` / `P` / `H` / `J` / `K` | 道具の切り替え（選択 / コマ追加 / 横に分割 / 縦に分割 / 斜めに縦割り） | 実装済み |
| `B` / `W` / `G` / `T` | フキダシ（丸い / 雲 / ギザギザ）/ セリフ。トゲトゲ・ふわふわ・四角はメニューと右クリックから（→ 6.13、6.20、6.32） | 実装済み |
| `M` | ビックリマークを置く（→ 6.14）。ビックリはてなはメニューと右クリックから | 実装済み |
| `N` | 次のコマを提案する（→ 10.5）。**押すたびに次の案へ差し替わる**ので1打で繰り返せるキーにした | 実装済み |
| F2 / Enter | 選択中のセリフを入力する | 実装済み |
| Ctrl+B | セリフを太字にする | 実装済み |
| F7 | セリフを縦書き / 横書きに切り替える（2026-08-05、Ctrl+T から変更） | 実装済み |
| Ctrl+. / Ctrl+, | セリフを大きく / 小さく（2026-09-05 に `Ctrl+]` / `Ctrl+[` から変更。**角括弧はキーの刻印を見ても何という記号か分からず、探すのに時間がかかる**——押しにくさではなく、**記号を知っているかどうか**が効いていた） | 実装済み |
| Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z | 元に戻す / やり直し（Y と Shift+Z の両方が「やり直す」） | 実装済み |
| Ctrl+S / Ctrl+Shift+S | 保存 / 名前を付けて保存 | 実装済み |
| Ctrl+N / Ctrl+O | 新規作成 / 開く | 実装済み |
| Ctrl+E | 画像で書き出し（PNG / JPG / PSD → 6.7、6.28） | 実装済み |
| F1 / Ctrl+F | メニューを探す（→ 6.30）。ヘルプの慣習と「探す」の慣習が割れているので両方通す | 実装済み |
| Alt+F / Alt+E / … | メニューを開く（`ファイル(F)` の括弧の文字）。**メニューバーの右端に「＋ Alt キー」と灰色で出している**——括弧が何なのかは初めて見た人には分からない。**項目ではなく隅に置く部品**（`setCornerWidget`）。押せない項目として置くと、`SH_Menu_AllowActiveAndDisabled` が 1 の見た目（`windowsvista`・`windows`）で **Alt → → と進んだときに反転表示で止まる**（2026-09-05 修正。このPCの既定 `windows11`・`fusion` では起きず、手元では見えない） | 実装済み |
| Delete | 選択中のコマ／画像を削除 | 実装済み |
| Ctrl+Shift+A | ページ全面にコマを作る | 実装済み |
| Ctrl+Shift+N | ページを追加（末尾） | 実装済み |
| Ctrl+Shift+I | ページを挿入（表示中のページの前） | 実装済み |
| PageUp / PageDown | 前後のページへ | 実装済み |
| Ctrl+Shift+PageUp / Ctrl+Shift+PageDown | 表示中のページを前後へ動かす（並べ替え） | 実装済み |
| ホイール | 拡大縮小（マウス位置が軸） | 実装済み |
| `+` / `-` | 拡大縮小（画面中心が軸）。Ctrl+ 併用も可 | 実装済み |
| Ctrl+1 / Ctrl+0 | 原寸で表示 / ページ全体を表示 | 実装済み |
| スペース+ドラッグ／中ボタン+ドラッグ | 画面移動 | 実装済み |
| Ctrl+V | 画像を貼り付け | 実装済み |
| Ctrl+Shift+F | 画像をコマにフィット（埋める） | 実装済み |
| ダブルクリック | 押した場所のものを順に選び直す。1回目はコマに入って画像を選ぶ（→ 6.25）。**選んでいるセリフの上なら打ち始める**（→ 6.5） | 実装済み |
| 右クリック | 押した場所のものを選び、その場のメニューを出す（→ 6.12） | 実装済み |
| Esc | 画像からコマへ戻る／選択解除 | 実装済み |
| Shift+] / Shift+[ | トーンで拾う黒を増やす / 減らす。**メニューのショートカットにはしない**（→ 6.27、下記の `+` / `-` と同じ理由） | 実装済み |
| Shift+. / Shift+, | トーンを濃く / 薄くする（白抜きでは効かない）。こちらもメニューのショートカットにはしない（→ 6.27） | 実装済み |
| Shift+ドラッグ | 等比リサイズ（画像の角のつまみ）／15度刻みの回転（画像の回転つまみ） | 実装済み |
| 丸いつまみをドラッグ | 画像を回転（→ 6.3） | 実装済み |

### この表を画面からも出す（ヘルプ → ショートカットキーの一覧...） → **実装済み（2026-08-07）**

**この表は要件定義の中にしか無く、使う人には届いていなかった。**「ヘルプ → ショートカットキーの一覧...」で、メニューごとに区切ったキーの一覧を出す。キーは付けない（**覚えていない人が開く窓に、キーで入る道を作っても使われない**。足せばそのぶん元から通っていたものを塞ぐ → 下記）。

**一覧は手で書かず、メニューを辿って作る。** キーはメニュー項目に付いているので（`MainWindow._act`）、窓の側に表を持つと**キーを1つ変えた日から一覧が嘘をつき始める**。道具箱を `_tool_actions` から作っているのと同じ理由。

| 出どころ | 中身 | 件数 |
|---|---|---|
| メニューを辿って作る | ファイル・編集・コマ・画像・フキダシ・マーク・セリフ・道具・ページ・表示・ヘルプ | 39 |
| 手で書く（`EXTRA_GROUPS`） | 画面（メニューに無いキー）／セリフを打っている間／マウス | 12 |

- **手で書くのは QAction が無いものだけ。** 素の `+` / `-` と `Shift+]` は、セリフの入力中に横取りしないため画面側（`PageView`）で拾う決まりで、メニュー項目が無い（→ 下記2つの節）。ホイール・右クリック・ダブルクリックも同じ
- **書き写しはここだけなので、歯止めのテストを1件置く。** 手で書いた行のキーがメニュー側と重なっていたら落ちる（重なった時点で、片方が古くなる余地ができる）
- **2本通してあるキーは両方出す。** `Ctrl+Y / Ctrl+Shift+Z`、`F1 / Ctrl+F`。片方だけ出すと、もう片方を知らないまま帰る。`QAction.shortcut()` は1本目しか返さないので `shortcuts()` を読む
- **同じキーは二度出さない。** 道具の項目は道具メニューと担当のメニューの両方に置いてあり（同じ QAction の使い回し）、素直に並べると同じキーが2行出る。先に出たほうを残すので、道具のキーは「コマ」「フキダシ」…の側に並び、「道具」には `V`（選択）だけが残る
- **「メニューを探す」窓（→ 6.30）とは扱いが逆になる。** あちらは**入口が2つある**ことを見せる窓なので2行そのまま出す。こちらは**キーが何をするか**を見る窓なので、同じキーが2行あると数え直すことになる
- 押しても実行しない（→ 6.30 と同じ線引き）。窓は相手を止めないので、出したまま実際に押して確かめられる

#### 1行おきに背景を変える（2026-08-07 に追加）

**キーの長さが `Ctrl+Shift+PgDown` から `V` まで開いているため、左の列と右の列が離れた行では、目が横に渡る途中で1行ずれる**（本人の指摘 2026-08-07）。地の色を**暗い側へ 8 段（0〜255）下げた色**を、1行おきに敷く。暗い配色なら `#2d2d2d` → `#252525`。

- **薄い灰色を直に書かない。** 窓は使う人の配色（明るい／暗い）の上に出る。決め打ちすると、暗い配色では地より明るく浮いて**縞ではなく白い帯**になる。地を基準に動かせば、どちらの配色でも地の隣に落ちる
- **明るい側へは寄せない**（2026-08-07 に向きを反転）。最初は「文字の色へ 9%」で作ったが、暗い配色では縞のほうが地より明るくなり、**読む先である文字より背景が目立ってうるさい**（本人の指摘）。**どちらの配色でも暗い側へ下げる**
- **割合ではなく段数で持つ。** 暗い側へ向けると割合が使えない——地が `#2d2d2d` なら黒までの余白は 45 段しかなく、5% では 2 段しか動かずに縞が消える。段数なら、明るい配色でも暗い配色でも同じ見え方の差になる。8 段は 4 / 6 / 8 / 12 を実際に出して選んだ（差を小さくしたいという要望に対して、消えない範囲でいちばん静かな側）
- **真っ黒に近い地のときだけ、明るい側へ逃がす。** 下げる先が無く、下げたつもりで縞が消えるため
- **Qt の `setAlternatingRowColors` にも任せない。** あれが使う `AlternateBase` は、このPCの暗い配色では地が `#2d2d2d` なのに **`#ffffff` が入っている**（2026-08-07 実測）。任せた場合に出るのは、まさに上で避けたい真っ白な帯。**配色が壊れていても、自分で混ぜた色なら必ず地の隣に落ちる**
- **窓の側に色を持たない（`setPalette` で差し替えない）。** 差し替えると**その部品はそこで配色の継承が止まり**、使う人が明るい／暗いを切り替えても古い色のまま残る。実際に一度そう書いて、明るい配色を被せても一覧だけ暗いままになることを確かめた。**行ごとの背景として敷き、開くたびに引き直す**（窓は作りっぱなしで使い回すため）
- **縞は見出しごとに数え直す。** 通しで数えると、項目数が奇数のメニューの次から縞の向きが裏返り、「見出しの次は地の色」という手掛かりが消える

#### 規模

窓と一覧の組み立てで 259行、メニューへの接続が 20行、テストが 26件・230行。

### ホイールは上下移動ではなく拡大縮小に割り当てる

セリフを扱うようになると、**文字が小さすぎて読めない**ため倍率を変える回数が一気に増える。一方で画面の上下移動はスペース+ドラッグと中ボタン+ドラッグで足りている。デザイン系の道具の慣習にも合う。

倍率の軸は操作で変える。**ホイールはマウスの位置**、**キーは画面の中心**。キーでカーソルの下を軸にすると、画面の隅にカーソルがあるときに見ていた場所が外へ飛んでいく。

上下限（`MIN_VIEW_SCALE` 0.3 〜 `MAX_VIEW_SCALE` 40）で止める。際限なく動かせると、行き過ぎたときに戻ってこられない。

### 素の `+` / `-` はメニューのショートカットにしない

**キー入力は画面（`PageView`）が最初に受け取る。** 素のキーをメニュー項目のショートカットに割り当てると、セリフの入力中にも先に反応してしまい、`-` を打っただけで画面が縮む。そのため素の `+` / `-` は画面側で拾い、**入力中は1つも横取りしない**という規則にした。メニューには修飾キー付き（Ctrl++ / Ctrl+-）を出す。

同じ理由で、入力中は Esc も画面側で拾わない。以前ここで拾っていたため、**入力中の Esc が取り消しにならず選択だけ変わる**不具合になっていた（2026-08-02 修正）。

### 複製に `Ctrl+D` を割り当てない。`Tab`（次のオブジェクトを選択）はやらない（2026-08-05 に決めた）

**この道具はマウスで使うもので、キーを覚えて速くする道具ではない。** 表に並んでいるキーは「あれば使う」程度の位置づけで、**キーを足すこと自体に値打ちは無い**。一方、キーを足すと**元から通っていたものを塞ぐ**副作用は必ず付いてくる。この2件はどちらも、得るものより塞ぐもののほうが大きいと判断した。

複製（→ 6.15）は「編集 > 複製」と右クリックから使う。**キーが無いので、入力中に暴発する経路も最初から無い。**

「次のオブジェクトを選択」はやらない。重なって掴みにくいものは右クリックで選び直せており（→ 6.12）、順に巡る操作が要る場面が出てきていない。

#### 実測した3点（2026-08-05）

キーの取り合いは読んで分かるものではないので、実際に押して確かめた。

| 押したもの | 結果 |
|---|---|
| セリフ入力中の `Ctrl+V` | **画像の貼り付けは動かない。**入力欄が先に受け取り、文字の貼り付けになる |
| セリフ入力中の `Ctrl+D` | **メニュー側が反応してしまう。**入力欄が Ctrl+D を使わないので素通りする |
| `Tab` | いまはサムネイル一覧へ宛先（フォーカス）が移るキー。入力中に押すと**入力がそこで確定して終わる** |

読み取れること。

- **メニューに付けたキーは、入力中でも一律に横取りするわけではない。** 入力欄が使うキー（`Ctrl+V`・`Delete`・`Ctrl+C` など）は入力欄が先に取るので、定番の操作は守られている
- **守られるかどうかは「入力欄がそのキーを使うか」で決まる。** `Ctrl+D` は使われていないため守られない。もし割り当てるなら、**入力中は複製を無効にする**という決めが別に要る
- `Tab` を割り当てると、**いまサムネイル一覧へ移れている経路が消える**。得るものと引き換えにするには釣り合わない

#### この種のことは offscreen のテストでは測れない

テストは表示装置なしで動かしている（`QT_QPA_PLATFORM=offscreen` → conftest.py）。この状態ではウィンドウが「有効」にならず、**メニューのキー（QAction のショートカット）が1つも鳴らない**。既存のテストがキー操作を `keyPressEvent` の直接呼び出しで書いているのはそのためで、**キーの取り合いそのものは通っていない**。

上の実測は `QT_QPA_PLATFORM=windows` を指定して本物のウィンドウを一瞬開いて取った。**「テストが通っている＝キーが取り合いにならない」ではない**ので、キーを足す判断のときは同じやり方で測る。

### 他ソフトと意味が食い違うキーは避ける（2026-08-05、公開を見据えて変更）

**このアプリは公開を目指すので、書き出した先で不特定多数が触る。** 開発中の自分だけが使う前提なら覚え直せば済むが、公開後は「他のソフトで身についた動きを無意識に期待する」利用者が大半になる。**キーの意味が他ソフトと食い違っていると、押した本人が損をする**（例: セリフを打っている最中に Ctrl+T を押すと新しいタブではなく縦書きに切り替わる）。

主要ソフト（ブラウザ、Office、Adobe 系）と意味が正面から食い違っていたキーを2つ直した。

| キー | 直す前 | 直した後 | 理由 |
|---|---|---|---|
| Ctrl+T | セリフの縦書き / 横書き切り替え | **F7** へ移した | ブラウザ・多くのアプリで「新しいタブ」。一覧の中で唯一、正面から食い違っていた |
| Ctrl+Y | やり直しのみ | **Ctrl+Shift+Z も併用**（Ctrl+Y は残す） | ソフトによって「やり直す」が Ctrl+Y と Ctrl+Shift+Z のどちらか割れている。両方通せば、どちらの流儀で覚えていても迷わない |

Ctrl+V・Ctrl+Z・Ctrl+S・Ctrl+N・Ctrl+O・Delete・F2 など、他ソフトとの意味が一致しているキーはそのまま残す。表に無いが検討した候補（Ctrl+A・Ctrl+F・Ctrl+W など）はこのアプリで使っておらず、空いたままにする。

道具の1文字キー（`V` `P` `H` `J` `K` `B` `G` `W` `M` `T`）は今回の見直しの対象外。**修飾キー無しの1文字はどのソフトでも固有機能に使う枠**で、他ソフトとの取り合いを気にする対象がそもそも無い。

## 8. 30日スケジュール

| 日 | 内容 |
|---|---|
| 0〜1 | 環境構築（venv / PySide6）、**クリップボード貼り付けの検証**、ウィンドウ雛形 |
| 2〜4 | データモデル、project.json の読み書き、assets の SHA1 管理 |
| 5 | **Undo/Redo**（スナップショット方式） |
| 6〜12 | **コマレイアウト**（追加・削除・移動・リサイズ・分割・枠線・選択ハンドル・吸着）← 最大の山 |
| 13〜17 | 画像（Ctrl+V / D&D / ファイル読み込み、クリッピング、移動・拡大縮小、コマにフィット） |
| 18〜21 | フキダシ（楕円・ギザギザ、しっぽ、コマへの紐づけ） |
| 22〜24 | テキスト（横書き、フォント設定、その場編集） |
| 25〜26 | ページ管理（追加・削除・並べ替え、サムネイル一覧、サイズ指定） |
| 27 | PNG 書き出し（作品フォルダの `export/` へ） |
| 28〜30 | **実際にネームを1本描いて、詰まった所を直す** |

### スケジュールの意図

- **Undo を Day 5 に前倒し**した。後半に置くと、それまでに書いた全ての操作コードに手を入れ直すことになる
- **Day 28〜30 を削らない。** ここが「毎日使えるツール」と「動くけど使わないツール」を分ける。実際に1本描くまで、致命的な使いにくさは絶対に見つからない
- ページ管理を後ろに置いたのは、単ページで完結するものを先に完成させて早く触れるようにするため。データ形式にはページ配列が最初からあるので、UI だけ後回しにできる

---

## 9. リスクと対策

### ★ Day 1 に潰すべき最大のリスク: クリップボード → **解消済み（2026-08-02）**

**クリスタから Ctrl+V した画像は、透明度（アルファチャンネル）を完全に保ったまま渡ってくる。設計変更は不要。**

ワークフロー全体がこの1点に乗っており、30日目に発覚すると設計ごと破綻するため最優先で確定させた。
測定は [tools/check_clipboard.py](tools/check_clipboard.py)（クリスタから 2048×2048 の絵をコピーして実行）。

| alpha | 画素数 | 割合 |
|---|---|---|
| 0（完全透明） | 3,522,227 | 83.98% |
| 1〜254（半透明） | 8,156 | 0.19% |
| 255（不透明） | 663,921 | 15.83% |

判定の決め手は **中間 alpha の種類数が 235 / 254** だったこと。アンチエイリアス（線の縁のなめらかな階調）が1段も失われていない。

**`hasAlphaChannel()` が真でも合格とは限らない。** 器だけ残って中身が1ビットのマスクに潰れる失敗の形があり、サマリーだけ見ると成功と誤読する。階調の種類数まで数えて初めて判定できる。

半透明画素が `(255,255,255,5)` のように **alpha が下がっても色の値が下がらない**（ストレートアルファ）ことも確認済み。ここを取り違えると、コマに合成したとき縁に黒い縁取りが出る。

実装は Qt 任せの取得経路（`QClipboard::image()`）をそのまま使えばよい。追加の変換処理は要らない。

**代替案（「クリスタ側で PNG 書き出し → ドラッグ&ドロップ」を主動線に変更する）は不要になった。** ドラッグ&ドロップ自体は 6.3 の機能として残す。

### その他

| リスク | 対策 |
|---|---|
| 大きい画像で描画が重い | **対処済み（2026-08-02）。** 表示用の縮小版（長辺 1600px）を持ち、画面には縮小版、書き出し時のみ原寸を使う。**上の実測でクリスタからの貼り付けは 2048×2048（419万画素）だったため、後回しにできる項目ではない。** 縮小版はメモリ上に持ち、ディスクキャッシュは作らない → 6.3 |
| コマ分割の操作設計が難しい | MVP は「選択中のコマを水平／垂直に2分割」だけに絞る。任意の線での分割はやらない |
| ギザギザのフキダシの形が汚くなる | **対処済み（2026-08-02）。** 楕円の輪郭を角度で分割し、半径を交互に増減させて生成。山の数と谷の深さを `BalloonSettings` で調整できる。頂点数は必ず偶数にする → 6.4 |
| セリフの改行位置が思い通りにならない | **方針どおり実装（2026-08-02）。** MVP は手動改行のみ。自動折り返しは後回し。入力欄でも折り返さないので、入力中と確定後で改行位置が変わらない → 6.5 |

---

## 10. 将来の拡張（MVP範囲外）

### 10.1 追加予定機能（2026-08-04 に記録）

**やる前提で書き留めるが、実装時期・順序は未定。** 実際にネームを描く中で出てきた要求なので、10.2 の「構造だけ用意する」とは区別する。

ここに書いてあるのは**要求と、先に決めておかないと後戻りする論点**まで。仕様として確定させるのは着手時とする。

#### 叫び声のマーク（未着手・2026-08-07 に記録）

**素材だけ用意してある**（`data/叫び声_アアァー.png`）。v1.0 には**入れていない**。

マーク（→ 6.14）の3つ目として足す。既存の2つ（`exclaim` / `exclaim_question`）と同じ道を通るので、
新しい仕組みは要らない。

必要な作業は4か所。

- `tools/import_sticker.py` で `manga_layout/stickers/` へ取り込む（余白は自動で落ちる）
- `stickers/__init__.py` の `STICKER_KINDS` に種類を足す。**既存の値は変えない**（保存済みの作品が指しているため）
- `ui/state.py` の `STICKER_TOOLS` と `STICKER_KIND_LABELS` に画面の呼び名を足す
- キーの割り当て（→ 6.14）。**空いているキーが少ないので、割り当てない選択もある**

先に決めておく論点が1つ。**`kind` に何という名前を使うか。** 既存は `exclaim` / `exclaim_question` と
記号の名前で揃えているが、叫び声は記号ではない。ここだけ日本語由来の名前（`shout` など）になると
並びが崩れる。**保存形式に書かれる値なので、あとから変えられない。**

#### ~~点検（書き出す前の見回り）~~ → **2026-08-06 に実装（→ 6.29）**

**決めた形はそのまま通った**（知らせるだけ／結果は窓と紫の印の2か所／印は付箋ではない／書き出しでは自動で走らせない）。

**見立てから変わったのは2点。** 「セリフのはみ出しだけは実機での確認になる」としていたが**自動テストで確かめられた**（字送りの計算だけで帯を出しており、字形を測っていないため）。もう1点は**窓から「そのページへ飛ぶ」が要らなくなった**こと——紫の印が付いた時点で、サムネイルを押す既存の動きが移動手段になる。規模は 500行前後の見立てに対し、およそ 440行。

以下は着手前の記録。

**全ページを見回して、直し忘れを出す。** 何が問題かは**結果の窓に文で**、どのページかは**サムネイルに紫の印で**出す。押したときだけ走り、**自動では何も直さない**。

##### 何が欲しいのか

空のままのセリフ、フキダシからはみ出した字、消し忘れた付箋——**どれも「そのとき画面を見ていれば気づけた」ものだが、ページが増えるほど目視は落ちる。** いまは書き出してから、あるいは人に見せてから分かる。

##### 新しい考え方ではなく、既にある方針を機械に移すだけ

このアプリは**「直さないが、気づけるようにする」**を一貫して選んでいる。

- 枠に収まらない字は隠さずに出す。隠すと、はみ出していることに気づけない（→ 6.5）
- 小さい用紙に変えても、はみ出したものを自動で詰め直さない。代わりに数を出す（→ 6.1）

**点検は自動で直す機能ではない。** 同じ判断を目視から総当たりへ移すだけで、直し方は利用者が決める。ここを取り違えて「収まるように縮める」を入れると、上の2つと真っ向から食い違う。

##### いま同じ性質の数え上げが散っている

| いまあるもの | いつ出るか | 足りないところ |
|---|---|---|
| 用紙からはみ出した数（`layout.outside_page` → `window.py`） | **ページの大きさを変えたときだけ** | 他のページは見ない |
| 実体の見つからない画像の数（`export.missing_assets_in`） | **書き出すときだけ** | 数だけで、どれのことか分からない |
| 枠に収まらない字（→ 6.5） | 画面を見ていれば | 数えていない |

**どれも「そのとき画面を見ていれば」の条件付き。** 呼び出す場所を1つにまとめ、いつでも押せるようにする。

##### 拾うもの（候補。絞るのは着手時）

| 項目 | 何を見るか | 使える既存の部品 |
|---|---|---|
| 空のままのセリフ | 文字が空 | — |
| **フキダシからはみ出したセリフ** | 字の帯が輪郭に収まるか | `layout.text_ink_bands` / `layout.balloon_contains` |
| 用紙の外にあるもの | ページの矩形の外 | `layout.outside_page`（**全ページへ回すだけ**） |
| 絵の入っていないコマ | コマの画像が空 | — |
| 実体の見つからない画像 | `assets/` に無い | `export.missing_assets_in`（数える形→1つずつ返す形へ） |
| 付箋の残っているページ | `Page.note`（→ 6.18） | — |

**幾何計算を新しく起こさなくてよい。** はみ出し判定に要る2つ（字の帯・フキダシの輪郭）は、掴み所の判定のために既にある（→ 6.5・6.4）。

##### 決めておくこと

| 論点 | 決め |
|---|---|
| 直すか / 知らせるだけか | **知らせるだけ**（上記） |
| 出し方 | **結果の窓にテキストで出す ＋ 見つかったページのサムネイルに紫の印**（→ 下。2026-08-06 本人確認済み） |
| そのページへ飛ぶか | **窓からは飛ばない。** 紫の印の付いたサムネイルを押せば、それがそのままページ送りになる（→ 下） |
| 動線 | **「ファイル → 書き出し」の隣。** 書き出す直前に通る場所に置かないと、点検そのものを忘れる。ショートカットは足さない（→ 7章） |
| メニューの名前 | 本人の呼び方は**「抜けチェック」**。メニューに出す文言は着手時に決める（読む字数の短い方を優先する） |
| 書き出しのときに自動で走らせるか | **走らせない。** 黙って止められると困る。押したときだけ |
| 重さの違い | 「実体の無い画像」と「付箋が残っている」を同じ列に並べない。**窓の文で分ける**（前者は絵が白く抜け、後者は自分で貼った目印）。**印の色では分けない**（→ 下） |
| 範囲 | 全ページか、今のページも選べるか（着手時） |

##### 結果は2か所に出る（2026-08-06 に本人と確定）

**窓とサムネイル。役割が違うので、どちらか一方では足りない。**

| どこ | 何が分かるか | なぜ要るか |
|---|---|---|
| **結果の窓**（テキスト） | 「3ページ: 空のままのセリフ」のような行が並ぶ | **何が問題かは文でしか言えない。** 閉じるまで消えないので読み返せる。選んでコピーもできる |
| **サムネイルの紫の印** | どのページか | **一覧を上から見れば一目で分かる。** 窓を閉じたあとも残るので、直しながら残りを追える |

**画面下の1行（`state.message` → 6秒で消える）には出し切れない。** ここに出せるのは「4ページで見つかりました」までで、**読み返せる**という要求（本人 2026-08-06）を満たさない。件数だけをここに出すのは構わない。

**常設のログ欄は作らない。** 過去の点検が積み上がる利点より、点検以外に使う予定の無い欄が作業領域を常時削る不利のほうが大きい（→ 6.1 の「一覧の幅を広げない」と同じ線引き）。

**窓から「そのページへ飛ぶ」は作らない。** 紫の印が付いたサムネイルを押せば、ページ送りは既にある動きでそのまま起きる。飛ぶ経路を新しく作らないという方針（→ 下）が、ここでは**何も作らない**形で満たせる。

##### 紫の印は、付箋ではない（2026-08-06 に本人と確定）

**見た目は付箋と同じ丸だが、別物として持つ。** 本人の要望は「使われていない紫で、問題の見つかったサムネイルに付箋を貼る」だったが、**付箋そのものにすると壊れる**。

| 付箋にすると起きること | なぜ |
|---|---|
| **自分で貼った付箋が消える** | 付箋は**1ページに1つ**（→ 6.18）。紫を貼った時点で、そのページの黄・桃・青は上書きで失われる |
| 直したのに紫が残り続ける | 保存に載るので、開き直しても残る。はがすのは手作業になり、**一覧が常に紫**になって読まれなくなる |
| Undo の履歴が点検で埋まる | 10ページに貼れば履歴に乗る（→ 6.8）。「戻す」を押すと点検が剥がれる |
| 点検が自分の印を見つける | 拾うものに「付箋の残っているページ」がある（→ 上）。自分で貼った紫を次の点検が拾う |

**別の印にすれば、4つとも起きない。** 作品ファイルは一字も変わらず、Undo にも指紋（→ 6.1）にも乗らない。

**作れることは既存のコードで確かめてある。** 付箋の丸は**サムネイルの絵に焼き込んでおらず、一覧が1行を描く段で上から重ねている**だけ（`ui/pages.py` の `_paint_note`）。同じやり方で、保存形式に載らない印がそのまま描ける。**この土台は付箋のために既に作ってある**ので、新しい仕組みは要らない。

| 論点 | 決め |
|---|---|
| どこに持つか | **画面の状態として持つ**（`EditorState`）。`Page` には持たせない＝保存形式は変わらない |
| 置き場所 | **縮小画像の左上。** 付箋は右上（`_paint_note`）なので、**重ならず、隠しもしない** |
| 色 | **紫1色。** 項目ごとに色を分けない——分けると色の意味を覚えることになり、6.18 の「色に意味を持たせない」と正面から食い違う |
| いつ消えるか | **次に点検を押したとき付け直す**（直っていれば消える）／作品を開き直したとき・新規のときに消える |
| 編集したら消えるか | **消さない。** 直したかどうかは点検し直して確かめる。編集のたびに消すと、印を付けた側から消えていく |
| Undo に乗るか | **乗らない**（→ 6.8 は作品の変更だけを積む） |
| 一覧の指紋 | **無関係。** 絵に焼き込まないので、付箋のような除外（→ 6.18）も要らない |

##### 先に潰すと決めた点

- **セリフのはみ出し判定は自動テストで確かめられない。** 検証環境にフォントが1つも無い（→ 6.5・6.11）ため、字の大きさを測る判定はテストが書けない。**この項目だけ実機での確認**になる。他の項目（用紙外・空・欠け）は数値だけなので普通にテストできる
- **判定を厳しくしすぎない。** 縦書きの帯は列送りぶんの幅を持つ（→ 6.5）ので、楕円のフキダシでは角が少し外へ出るのが普通。**一覧が常に埋まると読まれなくなる**ので、どこから「はみ出し」と呼ぶかのしきい値を先に決める
- **フキダシに紐づいていないセリフは対象外。** コマの外へ直接置くナレーションは正常な使い方であって、はみ出しではない（`Text.attached_balloon_id` の有無で分ける → 6.5）
- **結果を保存しない。**「保存しない」とは**作品ファイル（project.json）に書かない**という意味であって、**押した結果が消えるという意味ではない**（2026-08-06 に本人と確認）。窓は閉じるまで残り、紫の印はアプリを開いている間ずっと残る。次に押したときに数え直して付け直す。**作品に書くと、直したあとも古い結果が残り続け、しかも 6.1 のサムネイルの指紋と 6.8 の履歴に噛み合わせが要る**——「保存に載るのに絵には出ない項目」は、付箋で一度踏んでいる（→ 6.18）
- **飛ぶ経路を新しく作らない。** ページの切り替え（id で追いかける → 6.1）と選択は既にある。ここで別経路を作ると、Undo との食い違いを作れてしまう

##### 見積もり（着手前）

| 対象 | 規模 |
|---|---|
| 検査（純 Python） | 150行前後。**新しい保存項目は増えない** |
| 結果の窓（テキストを並べて出すだけ） | 80行前後 |
| 紫の印（状態に持つ・一覧に重ねて描く） | 40行前後 |
| メニュー | 20行 |
| テスト | 200行前後 |

合計 500行前後。**PSD 第2段階（およそ400行）と同じくらい。** 描画・保存形式・履歴には触らない。

**当初は 600行前後と見ていた**（一覧の窓と「そこへ飛ぶ」で 180行）。**紫の印を付けたことで、窓から飛ぶ必要が無くなった**のが差の中身——サムネイルを押せばページが変わる動きは既にあるので、印が付いた時点で移動手段が揃う。

#### ~~PSD 書き出し 第2段階：コマごとのフォルダ~~ → **2026-08-06 に実装（→ 6.28）**

**決めた5つ**（コマ1つ＝フォルダ1つ／読み順の番号／フキダシ類は種類ごと1枚／中身の無いコマは出さない／案Bはスコープ外）は**そのまま通った**。「描画には触らない」も見立てどおりで、`render.py` は1行も変えていない。

**見立てから変わったのは2点。**

- **フォルダと普通のレイヤーを、同じ入れ物で扱う形にした。** 見立てでは「0×0 を書ける手当てが要る」までしか書いていなかったが、素直に書くと**書き出しの側がフォルダを知る**ことになり、面の圧縮・見出しの組み立てが2通りに割れる。フォルダを**先に「大きさ0のレイヤー2枚」へほどいてしまう**（`psd._records`）と、書く側はフォルダを一切知らずに済んだ
- **大きさも時間も変わらなかった。** 見立てには無かった観測。増えるのは大きさ0の目印だけで、画素は同じだけしか無い（18.9MB / 1.0秒のまま → 6.28）

**規模は見積もりどおり**（300〜400行と見込んで、新規はおよそ 400行）。

以下は着手前の記録。

第1段階（→ 6.28）をクリスタで使って**実際に困った**ので着手する。書き留めた時点で「コマごとに割る」とだけ決めていたが、**割らないと直らない不具合が先に出た。**

##### 何が起きたか（実測）

コマを重ねて置いたページで、**下のコマの枠線が、上のコマの絵を貫いて出る。**

PNG では「コマ1の絵 → コマ1の枠線 → **コマ2の絵** → コマ2の枠線」の順に描くので、コマ2の絵がコマ1の枠線を覆い隠す。PSD では枠線を1枚にまとめたため**枠線が全部の絵より手前**に来て、隠れるはずの線が残る。

**線が二重に描かれているのではなく、消えるべき線が消えていない。** 6.28 に「重ね方が変わる」と書いた制約が、実際に絵として出た形。

**集中線・流線でも同じことが起きる**（下のコマの線が上のコマの絵を貫く）。まだ目に見えていないだけで、原因は同じ。

##### 一般論の「共有境界線を1本にする」は、このアプリでは効かない

外から見ると「隣り合うコマが境界線を共有していて二重に描かれる」に見えるが、**このアプリではその状態が作れない。** コマとコマの間には隙間（`LayoutSettings.gutter`、既定 35px）が入り、分割してできた2枚は必ず離れている。

したがって次の2つはどちらも効かない。**問題は「共有される辺」ではなく「他のコマに覆われる辺」**で、覆われる辺は相手の**内側**を通っているため、重なりをまとめる操作では消えない。

- 枠を線ではなくリング状の面（外側の矩形 − 内側の矩形）にして union する
- ページ全体の辺を集めて、同じ位置の線分を1本だけ残す

##### 採る形：コマごとのフォルダ（案A）

    PSD（上が手前）
     ├ セリフ / マーク / フキダシ
     ├ [フォルダ] コマ2 ── 枠線・集中線・絵
     ├ [フォルダ] コマ1 ── 枠線・集中線・絵
     └ ラフ（非表示）/ 用紙

**幾何計算は要らない。** 6.24 で持っている重なり順をそのまま PSD の並びへ移すだけで、コマ2の絵がコマ1の枠線より上に来る。**隠れる線はレイヤーの重なりが自動で隠す。**

「分離すると人手で削ることになる」という見方があるが、それは**分離したものを全部同じ高さに置いた場合**の話。フォルダに入れて順を保てば手作業は要らない。**「表示されない枠を計算して除外する」という理想に、計算なしで届く。**

PNG と完全に一致するので、6.28 の突き合わせ（レイヤーを重ねた結果 ＝ PNG 書き出し）が**重なりのあるページでもそのまま通る**ようになる。第1段階で唯一残していた食い違いが消える。

##### 見送る形：枠線1枚のまま、隠れる部分を落とす（案B）

各コマの枠線を描くときに、手前のコマの形を切り抜いてから描く。Qt がパスの引き算を持っているので十数行で済み、8枚構成のまま**枠線をまとめて編集できる**という案Aに無い利点がある。

**今回はスコープ外**（2026-08-06 本人確認済み）。**将来ユーザーが選べるようにする可能性はゼロではない**ので、案Aの実装で「フォルダに入れる／入れない」を切り替えられる形を塞がないようにはしておく。

なお案Bは PNG と厳密には食い違う。**上のコマに絵が入っていない場合**、PNG では下の枠線が見えるが、案Bでは消える。「見えている部分だけを描く」という本来の姿にはそちらのほうが近いが、突き合わせの検証は書き分けが要る。

##### 成立しない形

- **枠の内部を白で塗る。** 絵が乗らなくなる
- **全部を1枚に結合する。** レイヤーに分ける意味が無くなる（PNG と同じ）

##### 決めたこと

| 論点 | 決め |
|---|---|
| フォルダに入れる単位 | **コマ1つ＝フォルダ1つ。** 中身は枠線・集中線・流線・絵 |
| フォルダの名前 | **読み順で コマ1〜コマN。** 上端が上のものから、同じくらいなら右から（縦の差が `gutter` 未満なら同じ段と見なす） |
| フキダシ・マーク・セリフ | **これまでどおり種類ごと1枚。** ページ直下にあってコマの子ではない（→ 4章） |
| ラフ・用紙 | 変えない |
| 中身の無いコマ | フォルダごと出さない（空レイヤーを出さないのと同じ線引き → 6.28） |

**番号はラベルでしかない。** このアプリはコマに番号を持っておらず（読み順を計算する処理が無い）、ここで初めて振る。変わったコマ割りで直感と合わないことはありうるが、**中身が入れ替わるわけではない**ので実害は無い。

**次のコマの提案が数える順とは違うことがある**（段の切り方が違うため。約3割のページ、および4コマ2列のページ）。**揃えない理由と実測は 10.5 にある。**

##### 見積もり（着手前）

| 対象 | 規模 |
|---|---|
| `psd.py` にフォルダを足す | 80行前後。区切り用の**大きさ0のレイヤー**を2枚はさむ形式。第1段階で「空レイヤーは出さない」としたぶん、0×0 を書ける形にする手当てが要る |
| `psd_export.py` の組み立て・読み順 | 80行前後 |
| テスト | 200行前後 |

合計 300〜400行。**描画（`render.py`）には触らない。** 分けられる単位（中身・効果線・枠線）は第1段階で作ってあるので、コマごとに呼び分けるだけ。第1段階より小さく、回帰リスクのある場所には触れない。

#### ~~PSD 書き出し（クリスタへレイヤーごと渡す）~~ → **2026-08-06 に第1段階を実装（→ 6.28）**

**決めた5つ**（段階的に進める／ラフは非表示レイヤー／セリフはラスター化のみ／画像サイズは 6.7 を流用／動線は「形式」に足すだけ）は**そのまま通った**。ライブラリを足さない判断も通り、**依存パッケージは1つも増えていない**。

**見立てから変わったのは4点。**

- **回帰リスクの場所を読み違えた。** 「唯一のリスクは `render.py` の改修」と書いたが、そこは既定値を足すだけで済み、実際に踏んだのは**レイヤーを描く先の `QImage` の形式**だった。`_Premultiplied` にすると**セリフだけが 2px ずれる**（→ [PySide6の落とし穴.md](PySide6の落とし穴.md) の 4）。**画素で突き合わせる検証を先に用意していなければ見つけられなかった**——見た目では 2px は見えない
- **ラフだけ原寸を焼き直す経路が要った。** 見立てには無かった論点。画面用のラフは長辺 1,600px に縮めてあり、**「書き出しには出ないので原寸を返す経路は要らない」と `images.py` に書いてあった**。PSD に入れると決めた時点でその前提が崩れる（→ `full_rough_from_bytes`）
- **コマが重なっているページでは、重ね方が PNG と変わる。** 見立てには無かった。種類でまとめる以上避けられず、第2段階まで残る（→ 6.28）
- **合成済みの1枚は `render_page` を呼ばずレイヤーから作った。** 見立てでは「merged image も書く」までしか決めていなかったが、**呼び直すとファイルの中で食い違いうる**（重なりのあるページで、レイヤーを重ねた結果と一致しない）。レイヤーから作れば食い違いようがなく、同じ絵を2回展開せずに済む

**規模は見積もりのやや上。** 1,000〜1,300行と見込んで、実際は 1,564行（`psd.py` 420 / `psd_export.py` 211 / テスト 754 / 既存の改修 179）。増えたぶんは主にテスト側で、**PSD を読み返す処理をテストに持った**ため。

以下は着手前の記録。

1章の「やらないこと」に **PSD入出力** と書いてあるのは MVP の話。ここは MVP 範囲外の追加予定を書く場所なので矛盾しない。**実装したら 1章と README の「できないこと」から外す。**

##### 何が欲しいのか

いまの PNG / JPG 書き出し（→ 6.7）は**1枚に潰した絵**なので、クリスタ側では「下敷きとして敷く」ことしかできない。コマ枠だけ残す・セリフだけ残す・絵だけ薄くする、がどれもできない。

PSD にすると、**この道具が既に持っている「種類ごとの重なり」（用紙 → コマ → フキダシ → マーク → セリフ）を、そのままクリスタへ渡せる。** 新しく情報を作る話ではなく、**潰さずに渡すだけ**の話。

##### 本人と決めたこと（2026-08-06）

| 論点 | 決めたこと |
|---|---|
| レイヤーの分け方 | **段階的に進める。** 第1段階は種類ごとの固定枚数。コマごとに割るのはそのあと |
| ラフ（下敷き → 6.23） | **非表示レイヤーとして入れる。** PNG では表現できず捨てるしかなかったが、PSD は非表示と不透明度を持てる |
| セリフ | **ラスター化のみ。** 編集可能なテキストレイヤーは作らない（クリスタで打ち直す） |
| 画像サイズ | **6.7 の 117% / 100% / 75% / 50% を流用。** PSD 専用の仕組みは作らない |
| 動線 | **書き出しダイアログの「形式」に PSD を足す**（`Ctrl+E`）。新しいメニュー項目もショートカットも足さない |

##### 第1段階のレイヤー構成（下が奥）

    セリフ / マーク / フキダシ / コマ枠 / 集中線・流線 / 絵 / ラフ（非表示）/ 用紙

**8枚で固定。** 並びは `render.PageRenderer.draw` の描く順そのままで、**新しい順序の決まりを作らない**。ここで独自の順を持つと、画面と書き出しで重なりが食い違ったときに、どちらが正なのか決める相手がいなくなる。

第2段階では「絵 / 集中線・流線 / コマ枠」をコマごとのフォルダに割る。**第1段階を先に出すのは、確かめる手段が本人の手作業しかないため**（下記）。

##### 実体は「PSD を書く処理」ではなく「分けて描けるようにする」こと

内部データは既に足りている。位置・回転・不透明度・重なり順はすべて持っている（4章）。**足りないのは「レイヤー」という入れ物**で、`PageRenderer` は1枚に描き切る作りになっている（→ 11章の「部品を持たない」）。

したがって作業の重心は `psd.py` ではなく **`render.py` の側**にある。`draw_panel` は下地・中身・枠線を続けて描くので、枠線だけを別の1枚へ描けるようにする必要がある。**ここは画面・サムネイル・PNG 書き出しの3つが通る場所**なので、この機能で唯一の回帰リスクはここに集まる。

##### 一般的な「PSD 出力」の説明と、この道具の食い違い3点

外から見た構成案は「コマ / 吹き出し / 擬音 / トーン / 集中線」の形になりがちだが、**3つはこの道具の持ち物と噛み合わない。**

- **擬音（描き文字）は持っていない。** 2026-08-06 に範囲外と決定済み（→ 10.2）。あるのはマーク（→ 6.14）だけ
- **トーンは画像に焼き込む作り**（→ 6.27）。「トーンだけのレイヤー」は存在しない。`build_mask` があるので分けること自体は可能だが、**コマの中の画像ごとにレイヤーが増える**うえ、仕上げはクリスタでやり直す前提なので見合わない。**焼いた1枚をそのまま出す**
- **集中線・流線はコマの属性で、コマの形に切り抜かれる**（→ 6.16、6.26）。ページ直下の独立した物ではないので、第1段階では複数コマぶんが1枚に同居する

##### ライブラリを足さず、自前で書く

**トーン（6.27）で numpy を足さずに済んだのと同じ判断。**

書き込みができるライブラリ（pytoshop）は numpy と C 拡張を要求し、更新が 2019 年頃で止まっている。**出来合いのパッケージが無ければその場でコンパイルが要り、2台のPCで同じ環境を作れなくなる**（requirements.txt はバージョン固定で運用している）。

必要な範囲は **8bit RGB・ラスターレイヤー・透明度・レイヤーフォルダ**だけで、圧縮（PackBits＝同じ値の連続をまとめる単純な方式）も標準機能で書ける。**依存パッケージは1つも増やさない見込み。**

##### 先に潰すと決めた点

- **レイヤー名の日本語は `luni` に書く。** PSD はレイヤー名の欄を2つ持ち、古いほうだけに書くと**クリスタで文字化けする**。知らずに書けば必ず踏む
- **合成済みの1枚（merged image）も書く。** これが無いと開けないソフトがある
- **重ね直して PNG と一致することをテストで確かめる。** 各レイヤーを順に重ねた結果が `export.render_page` と一致すれば、分解が正しいことは**クリスタを開かずに言える**。半透明の丸めがあるので完全一致ではなく許容差で見る
- **それでも「クリスタが期待どおり開くか」は本人が開くまで分からない。** 自動テストで言えるのは PSD の構造として正しいところまで。第1段階を小さく出す理由がこれ
- **ファイルは重い。** 1ページ 10〜30MB 程度（PNG の5〜10倍）。全ページだと数百MBになる。**「応答なし」に見える問題自体は 6.7「書き出し中はページごとに進捗と中止ボタンを出す」で解決済み**（2026-08-08。ただし**1ページ目のあいだは窓が出ていなかった**ので 2026-09-05 に押した直後から出す形へ直した → 6.7）——進めば進捗窓の数字が動き、待ちすぎると感じたら中止ボタンで打ち切れる。書き終えたことも、複数ページなら消えない窓で出る（→ 6.7）。**書き始める前に総容量を見積もって警告する案はまだ入れていない**（ページ数と平均サイズからの概算はできるが、実測してみないと精度が分からない）
- 1辺の上限は PSD 側が 30,000px。いまの `MAX_SIDE_PX`（8,000）のほうが厳しいので触らなくてよい

##### 見積もり（着手前）

| 対象 | 規模 |
|---|---|
| 新規 `psd.py`（PSD を書く） | 500〜600行 |
| 新規 レイヤーへの分解 | 200行前後 |
| **既存 `ui/render.py` の改修** | 50行程度。**唯一の回帰リスクがここ** |
| 書き出しダイアログに PSD を足す | 20行 |
| テスト | 300〜400行 |

合計 1,000〜1,300行。**トーン（6.27）より一回り大きく、集中線（6.16）よりだいぶ大きい。**

#### ~~画像の黒をトーンに置き換える~~ → **2026-08-06 に実装（→ 6.27）**

**先に決めた4つ**（何を黒と見るか＝しきい値／斜線の大きさは画像基準／見た目は斜線／貼る単位は画像ごと）は**そのまま通った**。着手前に潰すと決めた2点も両方とも通っている。

**見立てから変わったのは4点。**

- **numpy を足すかどうかは、足さずに済んだ。** 「Python のループでは数秒かかる」と見込んでいたが、**実測は 205ms**で桁を1つ読み違えていた。しかも `bytes.translate`（256 個の対応表に通す Python の標準機能）なら **4.6ms** で、収縮・膨張も `QImage.scaled` 2回で書けた。**依存パッケージは1つも増えていない**（→ 6.27）
- **重さの見立ての順が逆だった。** 「実装コストの大半はキャッシュの作り替え」と書いたのは当たっていたが、**もう1つ重いものがあった**——絞る矩形のドラッグで、**見えている枠と実際に持っている範囲を別に持つ**必要が出た（→ 6.27）。絞る前の枠は画像いっぱいなので、素直に書くとどこを押しても移動になり、**新しく囲い直せなくなる**
- **「マスクを別に取っておくか」は採らなかった。** 焼いた1枚だけを覚える形で足りた。マスクまで持つと入れ物が3つになり、得るのは「しきい値を変えずに向きだけ変えるとき展開し直さない」だけ
- **つまみの色は、本人の案（赤）から変えた。** 赤はセリフの選択色で、赤系は欠けた画像の目印——しかも**あちらは矩形に対角線2本＝すでに「赤い×」**。形（×）は案どおり採り、色だけシアンにした（→ 6.27）

**`Alpha8` と `Grayscale8` の取り違えは、見立てで書いたとおり実際に踏んだ。** 書いておいたので原因の特定は早かった（→ [PySide6の落とし穴.md](PySide6の落とし穴.md) の 3）。

**「画面と書き出しで判定結果が完全には一致しない」は、まだ確かめていない。** 細い線の所で1px ずれうる、という見立てのまま残っている。実際に使って差が見えたら考える。

#### ~~付箋（サムネイルに貼る）~~ → **2026-08-05 に仕様確定（→ 6.18）**

置き場所（`Page.note`・1ページに1つ）、書き出しと本画面に出さないこと、Undo で戻ること、一覧の幅を広げないことは見立てどおり。**変わったのは3点。**

- **色に意味（ラベル）を割り当てない。** 「数を絞れば意味が定まる」と書いていたが、**絞るだけでは定まらない**。定めると今度は決めた意味に合わない使い方がしづらくなる。色は識別に使い、意味は一行メモに書く（3色）
- **一行メモは任意にした。** 貼る動作を右クリック1手で終わらせないと、「ここが最後」を印すだけの用途に毎回入力が挟まる
- **サムネイルの指紋から付箋を外す必要が出た。** 見立てには無かった論点。付箋は**保存に載るのに絵には出ない**初めての項目で、6.1 のキャッシュがその前提の上に立っている（→ 6.18）

#### ~~波形のフキダシ（不安表現用）~~ → **2026-08-04 に実装（→ 6.13）**

見立てどおり、ギザギザと同じ経路（楕円の半径を角度ごとに増減させる）に乗り、**足したのは形を作る関数1つと分岐1行**だった。保存形式は `style` の値が増えただけで、既にある作品には影響しない。

見立てから増えたのは**1波あたりの線分数の下限**。楕円の分割数をそのまま波の数で割ると1波が5本になり、角が立ってギザギザとの差が消えることが描いてみて分かった（→ 6.13）。

#### ~~大きな「！」マーク（透明 PNG を貼る）~~ → **2026-08-04 に仕様確定（→ 6.14）**

置き場所（ページ直下・`attached_panel_id` で紐づけ）と実体の扱い（`assets/` へ SHA1）は見立てどおり。**変わったのは2点。**

- **「素材を毎回ファイル窓から選び直すのは手数」への答えは、素材をアプリに組み込むことだった。** よく使う2枚をメニューに並べ、1クリックで置く。汎用の「好きな画像を貼る」は作らない
- **マークでは回転は要求として立たなかった。** 傾けた PNG を用意すれば済むので、`rotation` は 0 のまま（→ 6.14）。ただし**要求は画像の側から立った**（2026-08-05 → 6.3）。素材は差し替えれば済むが、利用者が持ち込む画像は差し替えられない

#### ~~集中線~~ → **2026-08-05 に実装（→ 6.16）**

**生成する側を採った。** 貼る側は中心をずらせず、中心の位置は角度と違って連続量なので、「素材を1枚足す」（→ 6.14）で代えられない。乱数の種を保存する見立てもそのまま採ったが、**種を保存するだけでは足りず、乱数そのものを自前で持つ**ことになった（Python の `random` は並びが処理系に属する）。

**変わったのは置き場所。** 「クリッピングの対象（コマの子）にするのが自然」と書いていたが、独立したオブジェクトにはせず**コマの属性として1つ**持たせた。矩形を持たずコマいっぱいに広がるので、選択できる種類を増やさずに済む（→ 6.16）。

もう1つ、**内側は白く抜かない**ことにした。ここでは集中線が画像の上に乗るので、白で塗ると下の絵が消える。

#### ~~バックアップからの復元~~ → **2026-08-06 に実装（→ 6.6）**

**見立てから変わったのは3点。**

- **「Undo で取り消せるようにするか」は、噛み合わせを見るまでもなかった。** 6.8 がスナップショット方式（1手ごとに作品全体を積む）なので、**作品まるごとの差し替えは普通の1手と何も変わらない**。履歴が入れ替わるのではなく、復元後の姿が今の履歴の上に乗るだけ。`History` に差し替え用のメソッドを1つ足すだけで済んだ（→ `History.replace`）
- **一覧に「中身の手がかり」（ページ数・コマ数）を足した。** 見立てでは日時と種別だけだったが、日時だけでは「どの作業をしていた頃か」を思い出せない
- **「戻す前に1つ退避する」には順序がある**（下記）。見立てのまま素直に書くと壊れる

以下は着手前の記録。

**`backup/` に貯まった世代を、アプリから選んで戻せるようにする。**

自動バックアップ（→ 6.6）で退避先はできたが、戻す手段は「`project.json` へ手で置き換える」しかない。**貯めるだけでは使われない**ので、いずれ要る。

先に決めるのは**戻したものを Undo で取り消せるようにするか**。作品まるごとの差し替えを1手として履歴に積めるなら扱いは楽だが、履歴そのものが入れ替わる話なので、6.8 の「保存と Undo を一致させる」との噛み合わせを見る必要がある。

- 一覧には**日時と、何の世代か**（保存済み／作業中）を出す。番号だけでは選べない
- 戻す前に、今の内容を1つ退避しておく。戻す操作自体で今の作業を失わせない

#### ~~丸い飛びしっぽ・雲_フキダシ・四角_フキダシ~~ → **3つとも 2026-08-05 に実装（→ 6.20・6.21・6.22）**

以下は着手前の記録と、そこから何が変わったかの控え。**見立てから変わったのは3点**で、それぞれ該当する見出しの直後に書いてある。

しっぽの新しい形として**丸い飛びしっぽ**（心の声・独り言）、フキダシの新しい種類として**雲_フキダシ**・**四角_フキダシ**（ナレーション・地の文）を追加する。**着手は1つずつ**にする。

3つとも保存形式は「値を増やす／項目を1つ足す」だけで、**既にある作品の project.json は変わらない**（6.13・6.14 と同じ形）。輪郭を作る分岐（`balloon_outline`）は画面・サムネイル・PNG 書き出しの3つが通るので、形を足せば3つとも同時に新しくなる。

##### ~~飛びしっぽはフキダシの種類とは別に持つ~~ → **2026-08-05 に実装（→ 6.21）**

置き場所（`Tail.shape`）・円3個の固定・組み合わせを禁じないこと・描画の作りを変えずに済むことは見立てどおり。**変わったのは1点。**

- **円の大きさを `Tail.width` から取るのをやめ、しっぽの長さから決めた。** 決め打ちで描いたところ、長いしっぽでは隙間だけが広がって**点が散らばった絵**になり、さらに先端を引いて伸ばしても円が動くだけで**引いた量が絵に出なかった**。隙間を半径に対する割合で持ち、鎖が起点から先端にちょうど収まるよう半径を逆算する形にした（→ 6.21）

以下は着手前の見立て。

##### 飛びしっぽはフキダシの種類とは別に持つ

`Tail` に `shape`（`"triangle"` 既定 / `"bubbles"`）を足す。種類の側に焼き込むと、丸い・ふわふわ・雲のそれぞれに「飛びしっぽ版」が要って種類が倍に増える。別に持てば組み合わせで表せる。

**円は3個で固定する。** 長さで増減させると、先端を引いている最中に円が生えて形が跳ねる。個数を固定すれば間隔が広がるだけで、引いた量がそのまま見える。

`Tail.width` は**先頭の円の直径**として読み替える。新しい設定項目を足さずに済み、「しっぽの太さ」という意味も変わらない。先端のつまみ・付け根のひし形・40度の上限（`TAIL_ROOT_MAX_GAP`）はそのまま使える。

**ギザギザ・四角との組み合わせは禁じない。** 禁じると、メニューの分岐・読み込みの検証・「保存済みの組み合わせをどう扱うか」の3つが増える。意味の無い組み合わせは、選ばない限り出てこない（`kind` を自由な文字列として読む 6.14 と同じ緩さ）。

##### 6.4 の「1つの輪郭に合成する」規則は、飛びしっぽでは逆になる

三角のしっぽを本体と合成しているのは、継ぎ目に枠線が残るのを防ぐため。**飛びしっぽは離れていることが意味そのもの**なので合成しない。

ただし `QPainterPath`（Qt が図形をまとめて持つ入れ物）は離れた図形を1つのパスに持てるので、**描画の作り（塗り1回・線1回）は変えなくてよい**。円を足すだけで済む。

##### ~~雲は「山は丸く、谷は尖らせる」~~ → **2026-08-05 に実装（→ 6.22）**

見立てどおり、`|sin|` を半径に乗せる関数1つと分岐1行で入った。置いた時点で飛びしっぽになるのも作る処理の1行。**変わったものは無い。**

描いて決めた既定は**膨らみ9・深さ 0.22**。6.13 に書いた「深くすると花びらに見える」は雲でも同じで、深さ 0.30 を超えると花に、膨らみ12を超えると歯車に見えた。**逆に浅いとふわふわと見分けが付かない**ので、両側から挟まれた狭い範囲になる。

以下は着手前の見立て。

##### 雲は「山は丸く、谷は尖らせる」

輪郭はギザギザ・ふわふわと同じ**半径の増減**で作る（→ 6.13）。ただし余弦波のまま深くすると花びらに見える（6.13 に書いた注意そのもの）。雲にするには増減のさせ方を変え、山を丸く・谷を尖らせる。足すのは形を作る関数1つと分岐1行で、ふわふわと同じコスト。

既定値（山の数・谷の深さ）は**並べて描いて目で決める**。数では確かめられない性質なので、自動テストは「山と谷の間に収まる」までしか置けない（→ 6.13）。

**雲を置いたときは、しっぽを最初から飛びしっぽにする。** 雲＋三角のしっぽはまず使わない。作る処理の1行で済む。

##### ~~四角では、当たり判定を楕円のままにできない~~ → **2026-08-05 に実装（→ 6.20）**

見立てどおり、輪郭は関数1つ＋分岐1行、当たり判定は分岐1つで入った。**変わったのは2点。**

- **メニューを畳む作り替えで、PySide6 の地雷を1つ踏んだ。** 畳んだ見出しの QAction を有効・無効の一覧に控えたところ、メニューバーを辿られた時点で実体ごと消えた。「畳む」は見た目の話だと見立てていたが、**持ってよいものの線引きが絡んでいた**（→ [PySide6の落とし穴.md](PySide6の落とし穴.md) の 1）
- **「しっぽを消す」を、置いたときと種類を変えたときで分ける必要があった。** 見立てには無かった論点。種類を変えたときも消すと、既にあるフキダシのしっぽが黙って失われる（→ 6.20）

以下は着手前の見立て。



いま当たり判定は**種類によらず楕円**で行っている。フキダシの四隅の何も無い場所で掴めてしまい、下のコマが選べなくなるのを防ぐため（→ 6.4）。

四角ではこれが裏返る。**見えている箱の四隅を押しても判定から漏れ、下のものが選ばれる。** `balloon_contains` に分岐が1つ要る。数行だが、取りこぼすと「押しても掴めない」という気づきにくい形で出る。

輪郭は矩形の4点そのままで、**角は丸めない**（丸めると設定項目が増えるうえ、ナレーション枠は尖っているほうが普通）。しっぽは**作った時点で消しておく**。出すこともできるが、付け根の計算は内接楕円のままなので厳密ではない（三角形が箱の辺を突き抜けて出るだけで破綻はしない）。使われるまで作り込まない。

##### メニューは畳む。ショートカットは足さない

種類が3→5になるので、右クリックの「ここに◯◯を追加」「◯◯にする」がそれぞれ5項目になり、しっぽの形の切り替えも増える。**畳んで（下位のメニューにして）出す。** 集中線を畳んでいるのと同じ扱い（→ 6.12）。

新しい1文字キーは足さない。**キーを足すこと自体に値打ちは無く、元から通っていたものを塞ぐ副作用は必ず付く**（→ 7章）。メニューと右クリックだけで出す。ビックリはてなマークと同じ位置づけ。

##### 着手の順は 四角 → 飛びしっぽ → 雲

**雲は飛びしっぽに依存する**（置いた時点で飛びしっぽにするため）ので、この2つの順は動かせない。四角は独立していて一番小さいので先に置き、**メニューを畳む作り替えをそこで済ませる**（3つとも同じ作り替えを必要とするため、最初の1つでやってしまう）。

### 10.2 構造だけ用意し、実装はしないもの

| 項目 | 準備しておくこと |
|---|---|
| ~~**縦書き**~~ | **2026-08-03 に実装済み（→ 6.11）。** 見積もりの前提だった「句読点・小書き文字の位置調整、長音符と括弧の回転」は、実測するとフォント側が全部持っていた。描画は前倒しで入った。入力欄は横書きのままだが、枠の中に確定後の姿を出すようにして、見えないまま打つ状態は 2026-09-05 に解消した |
| ~~**画像の回転**~~ | **2026-08-05 に実装済み（→ 6.3）。** 見込んでいた「！マーク」では要求されず（→ 6.14）、枠だけ残っていたが、**実際のコマに当てはめないと傾きを合わせられない**という形で画像の側から要求が立った。データ形式は最初から用意してあったため、project.json の変更は不要だった |
| 高解像度出力 | **2026-08-05 に 175dpi 相当（117%）まで対応（→ 6.7）。** これ以上は素材の原寸（長辺 2,048px 程度）を超えるので、ページの px 寸法を上げ、中身を選んで拡大する必要がある。mm 時代のように dpi を上げるだけでは済まない（→ 3章） |
| 書き出し dpi を設定で変えられるようにする | `settings.json` に `export_dpi`（150〜350、既定 175）を足し、書き出しの選択肢の一番上をその値から作る。**設定は知らない項目を読み飛ばし、範囲外を既定に落とす作りなので、項目を足すのに形式の変更は要らない**（`SETTINGS_VERSION` の更新も不要）。実装で要るのは `SCALE_CHOICES` / `DEFAULT_SCALE` を定数から「dpi を受け取って選択肢を作る関数」に変え、窓が持っている設定を `ExportDialog` へ渡すこと。**設定に出す値は px ではなく dpi**（px だと用紙ごとに書くべき数字が変わり、自分の用紙で何と書けばいいのか分からなくなる）。<br><br>ただし **350dpi にしても絵は細かくならない**。素材の原寸（長辺 2,048px 程度）は変わらず、そこから引き伸ばすだけで、ファイルサイズと待ち時間が約4倍になる（→ 6.7）。効くのは「350dpi の原稿に貼るとき、クリスタ側で拡大させずに済む」という手間の側だけ。**この釣り合いを承知したうえで必要な人が自分で書き換える**という位置づけなので、窓からは変えられなくてよい |
| 効果線・ステッカー・~~描き文字~~ | `EffectObject` / `StickerObject` / `SoundEffectObject` を Floating に追加する形。`type` フィールドで分岐する構造は既にある。**`StickerObject` は 2026-08-04 に `sticker` として実体化した**（→ 6.14）。**集中線は Floating ではなくコマの属性になった**（→ 6.16。コマの中に収まるもので、切り抜かれるほうが正しい）。**流線も同じくコマの属性**（→ 6.26。2026-08-06 に実装）。**描き文字は範囲外に決まった**（→ 下）。この行に残るものは無くなった |
| ~~**描き文字**~~ | **2026-08-06 に範囲外と決めた。** 描き文字は既に外部ツールで作れており、要求は「アプリと連携させる」ほうだった。だが**満足のいく同期を実装するとコストが大きくなりすぎると見込めた**ため、連携も断念した。アプリ側で描き文字を生成しないことは 6.14 で既に決めてある |

### 10.3 画像の切り抜き（マスク）（2026-08-27 実装）

**絵の一部を消して、残したい所だけをコマに出す。** 元画像は書き換えず、白黒のマスクを
別に持って掛ける。外せばいつでも元の絵に戻る。

出発点は SAM 3（言葉で指定する AI の切り抜き）の実験だったが、**そちらは中止した**（→ 10.4）。
残ったのは AI を知らない共通部分——マスクの形式・保存・描画・書き出しで、そこへ
**自動領域選択**という入り口を付けたものが、いま入っている機能。

#### 自動領域選択（塗りつぶし選択）で選ぶ

道具を持ち替えている間だけ効く（ラフ・トーン範囲と同じ切り分け → 6.23）。

1. 「画像 → 切り抜き」で道具を持つ（メニューの項目名も道具の名前も「切り抜き」）。
   **絵を選んでおく必要は無い**（押した所の絵が対象）
2. 消したい所を押す。指した1点から、似た濃さの続く範囲が広がって、その区画が消える
3. **Shift を押しながらなら「そこだけ残す」。** 背景が何区画にも割れている絵で速い
4. 違えば元に戻す（Ctrl+Z）。続けて押せば足せる

**選んでから確かめて確定する、という段取りを置かない。** 1回押すと1手として積む。
戻せる操作なら、確認を挟むよりやり直すほうが手数が少ない（本人の判断 2026-08-27）。
切り抜いてある絵をさらに押すと、**今のマスクから引く**（掛け直しではない）。押すたびに
前回の結果が消えると、区画ごとに消していけない。

似ていると見なす濃さの幅（許容差）は 0〜64 をメニューから4刻みで動かす。**上限を 64 で
止めてある**——陰影のある絵をそれ以上で押すと画面の半分が選ばれ、「区画を選ぶ」では
なくなる。選んだ割合が 97% を超えたときは「漏れたかもしれない」と知らせる。

得意・不得意がはっきりしている。**地が一様で、線が閉じていること**が前提で、
グラデーションで塗った絵では許容差をどう回しても収まらない。1画素の隙間があれば、
その区画は外へつながる。

**依存パッケージは増やしていない。** numpy も Pillow も OpenCV も使わず、行の中の
連なりを `bytes.find` に任せる（→ `manga_layout/wand.py`）。2048×2048 の絵で背景を
まるごと選んで 63〜71ms。

**続けて押す2回目からは、濃淡に直した1枚を使い回す。** 1回押してから画面が描き直る
までが 2048×2048 で 150ms、うち初回だけ 80ms 多い（展開と濃淡への変換）。
覚えるのは**押している絵1枚だけ**で、別の絵に移るか作品を開き直せば手放す
（→ `EditorState.wand_gray`）。

（根拠: `erase_region_at` と `image_preview` を続けて3回 / 確認日: 2026-09-06）

#### マスクの保存形式と描画

**マスクを作る道具を、保存形式から切り離す。** いま入っているのは自動領域選択だけだが、
別の入り口（手描き・別のモデル）を足すときに、画面・保存・描画を作り替えずに済ませる。

```
元画像 + 何を消すかの指示
  → マスクを作る道具（いまは自動領域選択）
  → ImageMask（アプリ共通のマスク）
  → 画面表示 / サムネイル / PNG・JPG・PSD書き出し
```

- アプリ共通の `ImageMask` は、**元画像と同じピクセル寸法の8bitグレースケール PNG** とする。
  自動領域選択が作るのは白（255）と黒（0）だけだが、保存形式は0〜255の濃淡を受け入れる。
  境界のぼかしは、実測して必要になってから足す
- **寸法が合わないマスクは断る。** 縮めて合わせない——合わせると、ずれた組み合わせが
  「輪郭がわずかにずれた絵」になるだけで、人が気づけない
- 適用したマスクは画像ごとの `mask_asset` として `assets/` に保存する。元の `asset` は残すので、
  マスクを外せば元の画像へ戻る。画像とマスクは別アセットで、**マスクを作った道具の名前を
  保存形式に書かない**
- マスクは画像の原寸座標に結び付ける。**元画像へマスクを適用して1枚の描画対象を作り、
  `ImageObject` の移動・拡大縮小・回転はその1枚へ1回だけ掛ける。**画像とマスクを別々に
  変換して輪郭がずれる経路は作らない
- 合成済みの画像は、元画像アセット・トーン設定・マスクアセットが変わったときだけ作り直して
  キャッシュする。画像やコマの移動・拡大縮小・回転・クリッピングでは、合成を作り直さない
- 画面用の縮小版と書き出し用の原寸の合成キャッシュは混ぜない。原寸側はページを書き出す間だけ持ち、
  複数ページ分を常駐させない
- 出力対象は画面・サムネイル・PNG・JPG と、**PSD**。PSD では「絵」レイヤーとトーンの3枚に
  **切り抜きを焼き込む**（→ `SAM3実装計画.md` 4.4）。焼き込まないと **PSD だけ切り抜き前の絵**が
  出るためで、画面と食い違ったことに気づくのがクリスタで開いたあとになる
- **レイヤーマスクとしては出さない。** クリスタ側で切り抜きを戻したいときは、アプリで
  マスクを外してから書き出す
- 掛け直し・外したあとに使われなくなったマスクの実体は、「未使用ファイルを整理」が拾う
  （→ 5章。画像の差し替え・削除と同じ扱い）

---

### 10.4 SAM 3 による切り抜き（実験・2026-08-27 着手 → **同日中止**）

> **この実験は中止した。安定版には入れていない。** 日本語の入力で候補が1件も返らず
> （英語では返る）、1回の待ちが 17 秒でその8割以上が準備だったため。実測と、
> 何が `feature/sam3` に残っているかは [SAM3実装計画.md](SAM3実装計画.md) の9章にある。
> **以下は、中止までに決めた内容の記録。** ここから残ったマスクの共通部分は 10.3 にある。

**目的は、画像に対して短い言葉を指定し、SAM 3 が返す候補から1つを選んで切り抜けるかを確かめること。**
対象は写真だけでなく、漫画・生成画像も含む。SAM 3 が漫画の線画や塗りで実用になるかは未確認なので、
この段階では安定版の機能にしない。

実装の進行と検証条件は [SAM3実装計画.md](SAM3実装計画.md) に記録する。

#### 考えていた操作

1. コマ内の画像を選び、メニューから「AI切り抜き...」を開く
2. 利用者が「人物」「髪」「右の人物」のように、切り抜きたいものを短く入力する
3. SAM 3 が返した候補を、元画像へ重ねたプレビューで比べる
4. 使う候補を1つ選んで適用する。元画像は書き換えず、切り抜きは取り消せる

**「何を切り抜くか」を自律的に考えるAIは、この実験の範囲外。** SAM 3 は言葉・枠・点などの
指示に基づいてマスクを返すモデルなので、最初は利用者の言葉をそのまま渡す。文章を考える別のAIを
前段に足すと、切り抜きの有用性と「何を選ぶか」の良し悪しを分けて確かめられなくなる。

#### SAM 3 側の境界

SAM 3 本体はアプリ本体の依存関係に入れず、専用の実行環境で動かす。アプリ側から見える入出力は
`ImageMask`（→ 10.3）だけに固定する。これにより、SAM 3 を外す／別モデルに取り替えるときに、
画面・保存・描画を一緒に作り替えずに済む。

- SAM 3 の起動と結果の読み替えは `manga_layout/ai/sam3/` に置いた。モデルの型・PyTorch・
  SAM 3 の import を、このフォルダの外へ出さない。**この実装は `feature/sam3` に残してあり、
  安定版には入れていない**（中止のため）

#### 導入と検証の順序

1. `feature/sam3` ブランチで、SAM 3 に依存しない型・結果の変換・テストを置く
2. SAM 3 専用の Python 環境を作り、公式の画像推論を単体で動かす
3. 白黒 PNG のマスクを1枚書き出し、画像寸法・候補数・信頼度を確認する
4. アプリの `ImageMask` へ読み替え、画像の表示と PNG 書き出しへ接続する
5. 漫画・生成画像・写真で、切り抜き精度、候補の選びやすさ、処理時間、GPUメモリ使用量を記録する。
   推論は最大1件だけ実行し、実行中の2件目は起動しない

標準テストはモデル本体をダウンロードせず、固定した白黒 PNG と偽の AI提供者で境界を確かめる。
SAM 3の元マスク、変換前の合成画像、変換・クリッピング後の出力を比べ、アプリ側がジャギー・
細線欠損・境界太りを増やしていないことも確かめる。実モデルのテストは、専用環境・モデル取得・
GPU がそろったときだけ別途実行する。モデル本体・チェックポイント・認証情報はリポジトリへ入れない。

---

### 10.5 次のコマの位置を提案する（定石方式・2026-09-04 着手）

**描きかけのページを見て、次に置くコマの位置と大きさを提案する。**

#### 何をして、何をしないか

| する | しない |
|---|---|
| 既存のコマの並びを**定石**と照合する | 絵の中身を読む |
| 成り立つ候補を**全部**出す | 物語を理解する |
| 幾何として正しいことを保証する | **どれが良いかを選ぶ** |

狙いは「AI がコマを創作する」ではなく、**既存の配置から型を推定して空白を補完する**こと。
この言い換えで**漫画を理解するモデルが要らなくなり**、後段は座標計算とルール判定だけで書ける。

#### 定石表（9件）

| 定石 | 当たる形 | 提案 | 優先 |
|---|---|---|---|
| 縦コマ列の左右反復 | 右半分に3個以上の揃ったコマ・左が空 | 左右反転して左半分へ（列ごと） | 4 |
| 細い右上コマの左隣 | 右上のコマが幅 1/3 以下・左と下が空 | 同じ幅・高さで左隣に1コマ | 3 |
| 見開きドン | このページが空白・直前が奇数ページ・**直前が描かれている** | 上部を横切る断ち切り | 3 |
| 同じ段の左を埋める | 最も下の段に左の空きがある | その段の左隣に1コマ（幅3案） | 3 |
| 三段構造の中段右 | 断ち切り無し・描かれているのが1段だけ | 中段のコマ・右端寄せ（幅4案） | 2 |
| 下段の右（断ち切りページ） | 断ち切りあり・下が空 | 下の帯のコマ・右端寄せ（幅4案） | 2 |
| 下段の右（断ち切り無し） | 断ち切り無し・下が空 | 下の帯のコマ・右端寄せ（幅4案） | 2 |
| 左半分を上下2等分 | 左半分が空 | 左に上下2コマ | 1 |
| 空白ページの描き出し | コマが1つも無い | 基本枠の右上に1コマ（幅4案＋断ち切り2案）＋4コマ×2列の雛形 | 1 |

**「幅3案」は 1/3・1/2・2/3・いっぱいの4案**を並べて出すという意味。
「いっぱい」は**帯をまるごと1コマにする**案（横帯で締める・場面を切り替える）。
**このときだけ隙間を引かない**——隙間は左隣と空けるためのもので、帯まるごとなら隣がいない。

**見開きドンの条件は4つ。** ①このページが空白 ②直前のページが存在し、奇数ページ
③**直前のページにコマが1枚以上ある** ④【提案】が押された。

**③を足したのは 2026-09-05**（それまでも実装は落としていたが、条件として名乗って
いなかった）。**動くものを実際に使ったうえで再確認済み。**この定石は「前ページを描き終えて次を開く」場面のものなので、
**白紙の扉ページを挟んだだけでは成り立たない。** `next_panel.context_for` は白紙の
ページでも「直前のページ」を作るため、**新規作品でページを足してすぐ `N` を押す**と
この状態になる。

**条件は名乗らせる。** ③を②の中に混ぜて書いていたころ、白紙のページで
**「× 直前のページがある（ページ 1）」**という報告が出ていた——存在するし奇数でも
あるのに ✗ で、**根拠と答えが食い違っていた。**

**空白ページの案は7つ。** 出す順は次のとおり。

| 案 | 中身 |
|---|---|
| 幅 1/3・1/2・2/3・いっぱい | 基本枠の右上に1コマ。高さは枠の 1/3 |
| 上と右を断ち切り（幅 1/2） | **上と右をページの外まで伸ばす。** 左辺と下辺は他の案と同じ位置 |
| 上半分を断ち切り | **左右と上を外へ出す。下辺だけが紙の中**（ページの半分の高さ） |
| 4コマ×2列 | **8コマまとめて1つの案。** 基本枠いっぱいに4段2列で敷く |

**小さすぎる案は落とす。案ごとに見る。** ページが小さくて余白が幅・高さの半分に
届くと、**基本枠そのものが潰れて幅も高さも 0 のコマができる**（178×178px・余白 89px
で実際に起きた。置いたあと次の提案で画面が固まった）。**面積の無いコマは何とも
重ならない**ので、重なりの判定では捕まらない——作った側で落とす（`MIN_ROOM` 未満は
提案しない。→ 定石9も同じ）。**断ち切りの案は枠を使わずページの外まで伸ばすので、
枠が潰れても残る。** 定石ごとにまとめて断らないのは、そのため。

**断ち切りは端ちょうどで止めない。** 端で止めると断裁のずれで白い筋が出る。
**越える量は 10px**（`next_panel.BLEED_OVERFLOW`）。線が出ない分だけあればよく、
**多いほど良いものではない**——越えた分は切り落とされ、画面では紙からはみ出して見えるだけ。

**この決まりは、断ち切りを作る3か所すべてに効く**（見開きドン／上と右を断ち切り／
上半分を断ち切り）。**越える量は1か所で決める**（`joseki._overflow`）——同じ規則を
3回書けば、いつか1つだけずれる。実際、**見開きドンだけ端ちょうどで止まっていた**
（2026-09-05 修正。テストがその姿を固定していた）。

紙に残るのは**ページの右半分**（左辺は「幅 1/2」の案と同じ位置に取る）。

**雛形は最後に出す。** 8コマまとめて敷く案なので、1コマずつの案を見てから。
隙間は `LayoutSettings.gutter`、番号は右列を上から下・続いて左列（4コマのページの
読み方に合わせる）。 実測した3ページで、最下段の右コマが
使える幅に占める割合は 33.7% / 55.3% / 67.9% と**3枚とも違った。1つに決めれば必ず2つ外す。**

#### 出す順

- **特殊なものから先**（優先度の降順）→ 同じなら定石表の並び
- **1つに絞らない。**【提案】を押すたびに、この並びを順繰りに見せる
- **余裕度（条件をどれだけ余裕をもって満たしたか）は順に関与しない。** 定石ごとに測って
  いるものが違い、**比べられない**（ばらつきの小ささ・空きの大きさ・細さが混ざる）

「どれが物語に合うか」は幾何では決まらない。**そこは決めずに、人に選ばせる。**

#### 提案を出さない場合

- **四角でないコマがあるページ**（定石は矩形を前提にしている）。**分割と同じ道具で
  判定する**（`Polygon.as_rect()`）——斜めのコマだけでなく、**わずかに傾いたコマ・
  頂点の順が崩れたコマ・面積の無いコマ**も断る。
  **お知らせに「斜め」と書かない。** 面積0のコマで「斜めのコマがあります」と出すと、
  探しても見つからない。以前はここに別の判定（4隅を整数に丸めて集合で比べる）を
  持っており、**「同じ線引き」と書いてありながら4つの形で答えが割れていた**
  ——提案は出せるのに、置いたコマを割ると断られる（2026-09-05 に1本化）
- **埋まっているページ**（置ける幅・高さが残らない）
- **その段のコマの半分より狭い空き。** 余白であって、コマを置く場所ではない

**押せなくはしない。** できない理由を伝えるほうが、グレーにするより分かる（→ 6.12 と同じ考え）。

#### 座標

**ページ幅・高さを 1.0 とした正規化座標**で計算し、px に戻して置く。
A4 でも B5 でも同じ式で通る（→ 3章）。

**断ち切りコマからは余白を借りない。** 断ち切りはページの端まで（外まで）伸びているので、
そこから借りると枠がページいっぱいに広がり、**次のコマまで断ち切りになる。**
基本枠が分かっているときは、そこで挟み込む（**断ち切りが続くページは多くない**）。

**挟み込むのは、借りた辺そのものが断ち切りのときだけ。** 断ち切りでなければ、
たとえ基本枠の外に描かれていても借りた値を使う。無条件に挟むと、**余白 40px で
描いたページの余白まで、アプリの余白 89px で上書きしてしまう**（2026-09-05 修正。
提案8件すべてが既存コマの右端より 49px 内側で終わり、段の左隣の案も上端が
右コマの y=40 ではなく枠の y=89 に揃っていた）。

**「2番目に右のコマ」へは移らない。** 欲しいのは最も右の辺＝右の余白で、2番目は
コマとコマの境目でしかない。**断ち切りなら余白が分からないのであって、別の値が
分かるわけではない。** 分からないときだけ基本枠へ落ちる。

**既存コマの上端・高さを写す定石も、上下を基本枠で挟む。** 「細い右上コマの左隣」と
「同じ段の左を埋める」がこれにあたる。写す相手が断ち切りだと、**そのまま写した隣の
コマまで紙の外へ出る**（2026-09-05 修正。前者だけ挟んでおらず、優先度3で最初に出る
案が y=-10px で置かれていた）。**挟んだあとの高さも確かめる**——面積が負のコマは
何とも重ならないので、重なりの判定ではすり抜ける。

**コマとコマの隙間は、縦と横で別の値を持つ。** px の隙間は正規化すると縦横で違う数に
なる（A4 の 35px は横 0.028・縦 0.020）。1本にまとめると、**横に置くコマの隙間に縦で
測った値を使う**ことになり、設定が 35px でも**横 22.3px・縦 31.6px に割れる**
（2026-09-05 修正。どちらも設定の値ではない）。

**縦の間隔は縦に、横の間隔は横に測る。** 測るのは並べた順の**隣どうし**だけ——総当たりに
すると、間にコマを挟んだ組まで隙間に数える（3コマの段で、右端と左端の距離が真ん中のコマ
ごと入る）。**測れないときだけ `LayoutSettings.gutter` の値へ落ちる。** コマが1枚しか
無いページでは縦も横も測れないので、以前はここで既定値 0.018 が出ていた。

**余白が入り切らないページでは、基本枠を「無い」ことにする。** ページの大きさは 50px まで
下げられる（→ 7章の下限）のに余白は既定 89px なので、**左右の余白だけでページ幅を超える**
ことがある。そのまま作ると幅も高さも負の枠になり、**裏返った枠を「置いてよい範囲」として
配り歩く**ことになる（150×150px で幅 -0.187 の枠を返していた。2026-09-05 修正）。
枠が無ければ既定の余白（ページ比 0.03）へ落ちるので、小さいページでも置ける案が出る。
**余白が入るうちは、薄い枠でも尊重する**（200×200px なら 89px が両側に入る）。

**空白ページに置くコマは、基本枠（`LayoutSettings.margin` の内側）にぴったり付ける。**
既にコマがあるページなら余白はそのコマから借りられるが、1枚も無いページには手本が無い。
**px の余白は正規化すると縦横で値が変わる**ので（A4 なら 89px が横 0.072・縦 0.051）、
1つの値で済ませると**右上が枠からはみ出す**（2026-09-04 に実機で発覚し、修正）。

#### 枠線

**置くコマの枠線は、読む順で直前のコマから写す。** 提案は「次に置くコマ」なので、
**直前のコマがいちばん近い手本**（分割が、割られたそのコマから写すのと同じ考え方）。
ページに1枚も無ければ既定のまま。

**`page.panels[0]` から写さない。** あれは**重なり順の先頭**であって、読む順の先頭でも
直前でもない。作った順と読む順が食い違うページ——1段目の右を先に作り、2段目の左を
あとで作ったページなど——では、**隣のコマではなく無関係なコマの枠線が付く**
（2026-09-05 修正。実測で 9.0 のところ 1.0 が付いていた）。

#### 置き場

| ファイル | 役割 |
|---|---|
| `manga_layout/joseki.py` | 定石表と照合。**画面もモデルも知らない**（正規化した矩形だけ） |
| `manga_layout/next_panel.py` | ページとの橋渡し（座標・読み順・ページの文脈・コマの生成） |

**読み順は `ui/psd_export.py` の `reading_order()` とは別物。** あちらは PSD のフォルダ名を
決めるためのもので行優先。提案は**縦4コマ列のページを列優先で読む**必要がある
（4コマ2列の8コマページは、右列を上から下、続いて左列。慣習で決まっている）。

**列優先で読むのは、ページが縦に切れて、どの列も4個以上あるときだけ。**
4コマ用の特殊な読み方なので、3段には広げない（3段は左右の列が揃っていても行優先）。

##### 2つの読み順は食い違う（利用者向けの注意・2026-09-05）

**PSD のフォルダ番号（コマ1〜コマN）は、提案が数えている順とは違うことがある。**
どちらも「上から下・右から左」だが、**段の切り方が違う。**

| | 段の切り方 | 使う場所 |
|---|---|---|
| 提案 | **縦に重なるコマは同じ段**（しきい値を持たない） | 次のコマの提案（→ この節） |
| PSD | **上端の差が隙間より小さいコマは同じ段** | 書き出しのフォルダ名（→ 10.1） |

**食い違うのは、段の高さが揃わないページ。** 右のコマだけ背が高いページでは、提案は
その背の高いコマを1つの段として先に読み、PSD は上端の近いコマを先に数える。
4コマ2列のページは**必ず**食い違う（提案は列優先、PSD は行優先）。

実測（コマ割りを機械的に 400通り作って照合。2026-09-05）:

| 材料 | 一致 | 食い違い |
|---|---|---|
| 割って作ったコマ割り 400通り | 275 | **125**（約3割） |
| 4コマ2列のページ | 0 | 全部 |

**それでも1本にまとめない。** まとめると PSD の出力が変わり、**既に書き出した作品を
再度書き出したときフォルダ名の順が変わる**（上の3割のページで）。どちらの切り方が
正しいかは測って決まる話ではなく、**出力を動かしてまで寄せる理由が無い**
（本人判断・2026-09-05。**動くものを実際に使ったうえで再確認済み**）。

**実害は無い。** 番号はラベルでしかなく、**中身が入れ替わるわけではない**（→ 10.1）。
クリスタ側で順に読み進めるときに、提案が「2番目」と言ったコマが「コマ3」に
なっていることがある、というだけ。

#### 画面

**「コマ(&M)」の「次のコマを提案」**（2026-09-04 実装）。「作る」の隣に置いてある。

- 押すと**提案どおりにコマが増える**。下見ではなく、実際に置く
- **もう一度押すと、直前の提案が次の案へ差し替わる**（増えない）
- **何回押しても Undo 1回で消える**（履歴は `merge_key` で1手にまとめる）
- **間に別の操作を挟んだら、その提案は確定したものとして扱う。** 次は差し替えず足す
- **「別の操作」には、コマを選ぶ・道具を持ち替える・ページを移るも入る。** 編集では
  ないが、履歴のまとめ扱いはそこで打ち切られる（`History.break_merge`）。
  **差し替えるかどうかは `History.merge_key` だけで決める**——履歴が1手にまとめる
  条件と同じものを見るので、**「差し替えたのに履歴は別の1手」が起こらない。**
  1手の名前（`undo_label`）で見ると打ち切りに気づけず、**Undo 1回で戻らなくなる**
  （2026-09-05 修正。コマをクリックしてもう一度押すだけで再現した）
- お知らせに「提案 2/7: 下段の右（断ち切り無し） / 幅 1/2」のように**何番目の案か**を出す

**キーは `N`**（→ 7章）。**押すたびに次の案へ差し替わる**操作なので、1打で繰り返せる
キーにしてある。`Alt+P` は「ページ(&P)」メニューが使っていて割り当てられない。

**メニューはグレーにしない。** 出せないときは理由をお知らせに出す（→ 6.12 と同じ判断）。

破線のプレビューと確定は入れていない。**`canvas.py` の描画とキー処理に手を入れる別仕事**で、
差し替え方式でどこまで足りるかを見てから決める。

**【提案】が押されたときだけ出す。** 空白ページを開いただけで大ゴマを提案し始める作りに
してはならない。これは条件として書けない（押されたかどうかは座標に現れない）ので、
**組み込む側の約束事**として守る。

## 11. 決定事項の記録

| 項目 | 決定 | 理由 |
|---|---|---|
| 技術 | Python + PySide6 | 画面表示と書き出しの描画経路が一本化される。既存環境も Python |
| 縦書き | ~~MVP 範囲外~~ → 2026-08-03 に実装（→ 6.11） | 「3〜4日かかる」の根拠だった字形の調整は、実測するとフォントの縦書き字形（`vert`）が全部やっていた。見送りの理由そのものが消えたので前倒しした |
| 縦書きの持ち方 | セリフごと。作品全体の設定にしない | 1ページの中に縦書きのセリフと横書きの効果音が混ざるのが普通（→ 6.11） |
| セリフの既定の向き | **作るときは縦書き。読み込むときの既定は横書きのまま** | マンガのセリフは縦書きが普通。ただし `direction` が無いファイルを縦書きで読むと、既にある原稿の見た目が開いた瞬間に変わる（→ 6.11） |
| 確定の目印 | 入力欄の外に出す。マウスは自分で受け取らない | Enter は改行なので押しても抜けられず、「閉じる手段が無い」と感じる。受け取ると確定の途中で自分が消えて危うい（→ 6.11） |
| 縦書きの計算 | `vertical.py` に分け、Qt も描画も知らせない | テストは offscreen で動きフォントが1つも無いため、字の寸法に依存した検証が成立しない。依存しない形にするしかなかった（→ 6.11） |
| 縦書き中の入力欄 | 横書きのまま出し、**枠の中には確定後の姿を実時間で出す**（2026-09-05） | Qt に縦書きの入力欄が無い。自作するとカーソル移動・選択範囲まで作ることになり機能全体より高くつく。入力欄をいじらずに「打ちながら仕上がりが見える」を満たせる（→ 6.11） |
| 入力中の文字の大きさ | **打ちながらは変えられない。仕様として許容する**（2026-09-05） | `Ctrl+.` / `Ctrl+,` は入力を確定してから効く。例外にすると履歴に1手挟まり、**「置いたばかりのセリフを Esc で追加ごと取り消す」が効かなくなる**。**押し直せば済むことと、気づけない空の枠が残ることを比べた**（→ 6.11） |
| 用途 | ネーム／下書き | 印刷精度（裁ち落とし・基本枠・600dpi）の管理を丸ごと外せる |
| コマ形状 | 保存はポリゴン | 追加コストゼロで斜めコマへの道を残した。2026-08-03 に回収（→ 6.10） |
| 斜めコマの操作 | ペア単位。角度は固定 | 自由に頂点を動かせると、既存の移動・リサイズ・吸着・分割がすべて矩形前提のまま壊れる（→ 6.10） |
| 斜めの境界をずらす | 割合を1つ差し替えるだけ。あとから追加した | 外側の矩形が矩形のままなので、ペア方式が買った5つ（移動・リサイズ・吸着・つまみ・分割ガード）に一切触れない純粋な追加になる |
| 斜めの角度 | 垂直から 12°（下辺から 78°）。ペアごとに保存 | 20° は全高のコマで等分近くしか割れなくなる。ペアに焼き付けるので既定値を変えても既存のコマは変形しない |
| フキダシの所属 | ページ直下 + コマへの紐づけ | コマの子にすると枠外にはみ出せない |
| マークの所属 | フキダシと同じ（ページ直下 + 紐づけ） | コマからはみ出して置くのが普通。コマの子だと切り抜かれる（→ 6.14） |
| マークの重なり | フキダシの上・**セリフの下**。段を1つ増やす | 上に置くと、位置を誤ったときにセリフが読めなくなる。段の順で最初から起こらないようにする（→ 6.14） |
| マークの傾き | アプリで回転せず、**傾けた PNG を用意する** | 回転を入れると当たり判定・つまみ・吸着が一斉に矩形の前提を失う。角度違いは素材を1枚足すほうが安い（→ 6.14） |
| マークの素材 | よく使う2枚をアプリに組み込む。汎用の画像貼りは作らない | 毎回ファイル窓から選び直すのは手数。貼った実体は `assets/` に入るので作品は自己完結する（→ 6.14） |
| 画像の回転 | **画像編集を持たない方針の唯一の例外として入れる。** 対象はコマの中の画像だけで、マークは対象外 | 傾きは実際のコマに当てはめないと合わせられず、他ソフトとの往復になる。明るさや色調と違い**傾きは配置の一部**（→ 6.3） |
| ドロップの落とし先 | **落としたコマ1つだけ。** 複数のコマへ配らず、コマもページも増やさない | 落とした枚数で構成が勝手にできると、それを消す作業が要る。コマ割りは作者が決めるものとして全体を組んである（→ 6.3） |
| ブラウザからのドロップ | 絵そのものが来ればそれを使い、住所だけなら取りに行く。**http / https に限り、転送先も見る** | ブラウザは両方を渡してくる。手元にある絵を捨てて取りに行くのは無駄。`file:` を通すと住所1つで手元のファイルが読める（→ 6.3） |
| 回転の持たせ方 | `Rect` も保存形式も変えず、描画・当たり判定・つまみの3か所でだけ角度を掛ける | 回転を全体に広げると当たり判定・つまみ・吸着が一斉に矩形の前提を失う。傾き 0 の作品が今までと同じ経路を通ることを保証したい（→ 6.3） |
| セリフの当たり判定 | 枠ではなく**字の並んでいる帯**。空のセリフだけ枠全体 | 枠の空いた所まで拾うと、フキダシの中のマークが見えているのに掴めない。フキダシを楕円で判定したのと同じ線引き（→ 6.5、6.4） |
| セリフの選択枠 | 当たり判定と同じ**字の並びの外接矩形**。大きさ変更はそれをそのまま枠に入れる | 枠のままだと、押しても掴めない場所まで枠とつまみが伸びて、何を掴んでいるのか分からない。描く範囲と掴める範囲は揃える（→ 6.5） |
| 集中線の作り方 | **貼るのではなく図形として生成する** | 貼ると置いたあとに中心をずらせない。中心は角度と違って連続量なので、「素材を1枚足す」で代えられない（→ 6.16） |
| 集中線の持ち方 | **コマの属性として1つ。** 独立したオブジェクトにしない | 矩形を持たずコマいっぱいに広がれば、コマを縮めたときに取り残される状態が無い。選択できる種類も増えない（→ 6.16） |
| 集中線の中心 | コマの外接矩形に対する**割合**。範囲は制限しない | 長さで覚えるとコマを縮めたとき中心が外へ飛び出す（`SlantPair.ratio` と同じ）。画面の外から集中させる置き方があるので上限は設けない（→ 6.16） |
| 集中線の乱数 | 種を保存し、**乱数そのものも自前で持つ**（線形合同法） | `random` の並びは処理系に属するので、変われば同じ種から違う形が出る。種を保存した意味が消える（→ 6.16） |
| 集中線の内側 | **白く塗らず、線を引かないだけ** | ここでは集中線が画像の上に乗るので、塗ると見せたい人物が消える（→ 6.16） |
| 集中線の白 | `FocusLines.white` を足すだけ。形の計算（`focus.py`）は色を知らない | 色を選べるのは黒／白の2値。形と色を1つの関数に混ぜると、色を増やすたびに形まで書き直すことになる（→ 6.19） |
| 流線の持ち方 | **`focus_lines` に混ぜず、`flow_lines` として別に持つ** | 集中線にしか無い `center` / `hole` と流線にしか無い `angle` / `length` が同居すると、どの組み合わせが正しいのかを型が語れなくなる（→ 6.26） |
| 流線の抜き | **持たない。** コマ全面を線が貫く | 集中線に空きが要るのは1点へ集まって中心が潰れるから。流線には潰れる場所が無い（→ 6.26） |
| 流線の長さの基準 | **外接矩形の対角線。** 太さの短辺基準とは違える | 短辺基準だと 1.0 でも横長のコマを半分しか走らず、長辺基準だと斜めのとき貫かない。矩形の最長の弦は対角線（→ 6.26） |
| 流線のつまみ | **丸を画像の回転から借りる。** 判定は集中線のつまみより後 | 記号の在庫（四角・丸・ひし形・十字）が尽きており、丸は「掴んで回す」で意味が一致する。集中線の中心は動かせるので、重なったとき逃げられるのはあちらの側（→ 6.26） |
| 描き文字 | **範囲外。** 外部ツールで作り、アプリとの連携もしない | 描き文字自体は既に外部で作れており、要求は連携のほう。満足のいく同期を実装するとコストが大きくなりすぎると見込めた（→ 10.2） |
| トーン | **画像の黒を斜線に置き換えるだけ入れる。塗る道具は作らない** | どこを塗るかの指定は元画像の側で済ませられる。塗る道具まで作ると、範囲の選択・削り・重ね順が丸ごと要る（→ 6.27） |
| トーンの持ち主 | **画像ごと**（`ImageObject.tone`）。コマの属性にしない | 「この絵だけ黒ベタのまま残したい」が普通にある。コマに付けると1枚だけ外せない（→ 6.27） |
| トーンの操作の入り口 | **保存形式は画像のまま、操作はコマを選んだ状態からも効かせる** | 絵を選ぶにはコマの中へ入る一手が要る。集中線・流線と同じつもりでコマを選ぶと、トーンだけが理由の分からないグレーに見える（→ 6.27） |
| 斜線の大きさの基準 | **画像基準。** 紙（ページ）基準にしない | 紙基準は描く経路の作り替えと拡大率ごとの作り置きが要る。画像基準なら展開する所で焼き込むだけで、`PageRenderer` は1行も変わらない（→ 6.27） |
| トーンの見た目 | **斜線のみ。網点は入れない** | 網点は粒が細かく、画面の縮小表示でも書き出しでもモアレが出る。線数（60L 等）は dpi を持たないこのアプリでは指定もできない（→ 6.27、3章） |
| 線画の線を守る手 | **細さで落とす**（縮小 → 切り直し → 拡大）。面積では数えない | 線もベタも真っ黒なので明るさでは分けられない。細さなら連結成分を数えずに済み、実画像で線が完全に戻った（→ 6.27） |
| トーンの依存パッケージ | **1つも増やさない。** numpy も Pillow も OpenCV も使わない | しきい値は `bytes.translate` で 4.6ms、収縮・膨張は `QImage.scaled` 2回で書けた（419万画素の実測 → 6.27） |
| トーンを焼いた1枚 | **`ImageCache` と別の入れ物に、上限付きで持つ**（`ToneCache`） | 鍵に設定が入るので増え続ける。混ぜると、同じ画像をトーン違いで使うたびに PNG の展開が2回走る（→ 6.27） |
| トーンの範囲のつまみ | **道具（「トーン範囲を調整」）に逃がし、記号は×** | 記号の在庫が尽きており、画像に足すとつまみが9個並ぶ。道具にすれば持っている間だけ出せる（ラフと同じ → 6.27、6.23） |
| トーンのつまみの色 | **シアン。赤系は使わない** | 赤はセリフの選択色、赤系は欠けた画像の目印で、あちらは既に「赤い矩形に対角線2本」＝赤い×。色は種類、形は役割という区分も崩れる（→ 6.27） |
| トーンの増減の見せ方 | **項目名に「2/20」を添える。押した回数は数えず、値から逆算する** | 「細い線を残す」が一回きりに見え、重ねてよいことが伝わらなかった。数えると Undo・絵の切り替え・開き直しで数字だけがずれる（→ 6.27） |
| フキダシの呼び名 | 画面の呼び名を1箇所に持ち、保存形式の値（`ellipse` など）とは切り離す | 呼び名は画面の都合で変わるが、保存形式が変わると既にある作品が開けなくなる（→ 6.4） |
| 波形の既定値 | 波 16・深さ 0.09。ギザギザより多く・浅く | 深いと花びらや雲に見え、叫びとの差ではなく別の物になる。3種を並べて描いて決めた（→ 6.13） |
| 波形のなめらかさ | 1波あたり 8 線分を下限にし、分割数の設定より優先する | なめらかさがこの形の意味そのもの。角が立つとギザギザとの差が消える（→ 6.13） |
| ギザギザの曲線版 | **6種目（トゲトゲ）として足す。** ギザギザは直線のまま残す | 差し替えると保存済みの作品のギザギザまで黙って曲線に変わる。意味ではなく画風の違いなので、作品ごとに選べばよい（→ 6.32） |
| トゲトゲの曲線の当て方 | **頂は鋭く・谷は丸く**（`sin(π s / 2) ^ 0.7`）。深さはギザギザと同値 | 頂まで丸めるとふわふわ（不安）に読めて意味が逆になる。4案を並べて描いて選んだ。深さまで変えると「曲線にした」以外の差が混ざる（→ 6.32） |
| トゲトゲの山の数 | **12。** ギザギザ（14）より減らす | 曲線にするとトゲ1本が太くなり、同じ数では密に見える。**セリフを入れて並べると、違いはトゲの太さより「セリフの周りの余白」として出た**（→ 6.32） |
| コマのロックで止める範囲 | **形と削除だけ。** 中の画像・紐づく吹き出し／セリフ／集中線は今まで通り触れる | 「コマの箱は確定、中身はこれから」の状態に合わせる。中身まで止めると差し替えのたびに解除する手間が挟まる（→ 6.17） |
| コマのロックの見せ方 | 選んだときだけステータス表示に出す。つまみは出さない。**メニューの項目にはロック中だけ🔒を付ける** | キャンバスに常時アイコンを置くと画面がうるさく、出したまま効かないつまみが一番たちが悪い。メニューは開いた間だけ見えるので、状態をそのまま出しても邪魔にならない（→ 6.17） |
| コマの重なり順 | **端まで動かす2項目（手前へ／奥へ）。** 段階的な「1つ手前へ」は入れない | 重なっていないコマを1つ跨いでも見た目が変わらず、押しても何も起きないように見える。完全に隠れたコマは選べないので、上を選んで「奥へ」送る経路が要る（→ 6.24） |
| 複製の写し方 | **保存形式を1往復させる**（`Project.copy()` と同じ理屈） | 項目を手で書き並べると、あとから項目を足したときに写し忘れても静かに通る。往復なら Undo と同じ1か所に抜けが集まる（→ 6.15、6.8） |
| 複製したフキダシのしっぽ | **先端も一緒にずらす。** 移動（→ 6.4）とは逆にする | ここは「同じ形を並べる」操作。先端を置いていくと写しだけしっぽが伸び、別の形になる（→ 6.15） |
| 複製した写しのロック | **ロックしない。** ロックしたコマ自体は複製できる | ロックが止めるのは形と削除だけで、元は 1px も動かない。写しは置き場所を決めるために作ったものなので、いきなり動かせないと解除の手間が挟まる（→ 6.15、6.17） |
| 斜めのコマを複製できないと伝える手段 | **項目は押せるままにして、押したら状態表示で断る** | グレーにすると「写せない」は伝わるが理由が伝わらない。斜めのコマは見た目が普通のコマと変わらず、ロック（常時ステータスに出る）と違って気づく手がかりが無い（→ 6.15） |
| クリップボード | 透明度は保たれる（2026-08-02 実測） | 最大リスクが解消。代替動線は不要になった（→ 9章） |
| ID の採番 | `Project` の 1 箇所に集約 | 重複した ID が生まれる経路を無くす。重複すると `attached_panel_id` の解決先が狂う |
| 未使用アセットの整理 | 保存時ではなく明示操作 | 保存時に動かすと Undo で戻した画像の参照が切れる（→ 5章） |
| 自動バックアップの書き先 | `backup/` だけ。**本体（project.json）は触らない** | 押していない保存が起きると、未保存の確認と食い違い、Undo で戻せる範囲とディスクの中身がずれる（→ 6.6） |
| 自動バックアップの系列 | 手で保存した世代（`project.N`）と分けて `autosave.N` に持つ | 「保存済みの姿」と「まだ保存していない姿」は性質が違い、混ぜると保存済みの世代が押し出されて消える（→ 6.6） |
| 自動バックアップの対象 | **保存先が決まっている作品だけ。** 空かどうかでは見ない | 一度も保存していないと退避先が無く、貼った画像もまだディスクに無い。JSON だけ書いても参照先の無い退避になる（→ 6.6） |
| 「変化なし」の判定 | 保存の印とは**別の印**を持つ（`_autosaved`） | 退避では `mark_saved()` を呼べないので `is_dirty` は真のまま残り、1文字も変えていなくても毎回書くことになる（→ 6.6） |
| ページの並び順 | 出所は `Project.pages` のみ。一覧は毎回そこから作り直す | 一覧側にも持たせると、Undo で戻したのに一覧だけ動いたまま、という食い違いが作れる（→ 6.1） |
| ページの新規作成 | 「追加（末尾）」と「挿入（次に）」の2項目に分ける | 一覧のカーソル位置を常に意識しているわけではなく、追加だけだと意図しない場所にページができる（→ 6.1） |
| ページ削除 | 確認ダイアログあり。メニューからのみ。最後の1枚は不可 | ページ1枚には積み上げた作業がまるごと乗る。0枚になると表示するものも置き場所も無くなる（→ 6.1） |
| ページサイズ変更 | はみ出したものを動かさず、数だけ知らせる | 位置は利用者が決めたもの。直し方も場面ごとに違う（→ 6.1） |
| ページの描画処理 | `PageRenderer` に集約し、本画面・サムネイル・書き出しで共有 | 分けて書くと片方だけ直したときに食い違い、しかも気づきにくい（→ 6.1、6.7） |
| コマの描画 | `QGraphicsItem` を持たず、その都度描く | Undo でモデルの実体が差し替わるため、部品を保持すると古い `Panel` を掴んだままになる。描き直しは1ページぶんなので素直に毎回描くほうが安全 |
| リサイズ時の追随 | しない（移動のみ追随） | リサイズは「絵に対する窓の大きさを変える操作」。中身が付いて回ると絵の位置を合わせ直すことになる（→ 4章） |
| Undo の持ち方 | 保存形式の文字列を圧縮して積む | 辞書のままだと50手で数百 MB。圧縮すれば 0.23 MB（→ 6.8） |
| プロジェクトの持ち主 | `History` に一本化 | Undo で実体が差し替わるため、画面側が古い参照を掴むと戻した後の編集が反映されない |
| 壊れた project.json | 読み込みを断る（黙って既定値に直さない） | 静かに既定値へ差し替えると、利用者のデータが気づかないうちに書き換わる。ただし「存在しないコマへの紐づけ」など直せる乱れは直して開き、内容を報告する |
| 画像の置き場 | 原本＝リポジトリ外 / キャッシュ＝`%LOCALAPPDATA%` / テスト画像＝git管理下 / 使い捨て＝`data/` | 寿命と持ち主が違うものを同じ場所に置かない。原稿がコード側の都合（ブランチ切替・クローンし直し）に巻き込まれるのを防ぐ |
| `assets/` の構造 | サブフォルダに分けずフラット | 1作品300枚程度では分割の利点が無い。JSON が相対パス全体を持つので後から変更できる |
