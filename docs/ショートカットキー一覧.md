# ショートカットキーの一覧

**この文書は自動で作られる。手で書き換えない。**
中身は「ヘルプ → ショートカットキーの一覧...」に出るものと同じで、
`tools/dump_shortcuts.py` がアプリのメニューを辿って書き出している。

キーを変えたら、次のコマンドで作り直す。

```
./venv/Scripts/python.exe tools/dump_shortcuts.py --write
```

作り直し忘れは `tests/test_ui_shortcuts.py` が見つける。

## ファイル

| キー | 動作 |
|---|---|
| `Ctrl+N` | 新規作成 |
| `Ctrl+O` | 開く... |
| `Ctrl+S` | 保存 |
| `Ctrl+Shift+S` | 名前を付けて保存... |
| `Ctrl+E` | 画像で書き出し... |
| `Ctrl+W` | 閉じる |
| `Ctrl+Q` | 終了 |

## 編集

| キー | 動作 |
|---|---|
| `V` | 選択の道具 |
| `Ctrl+Z` | 元に戻す |
| `Ctrl+Y / Ctrl+Shift+Z` | やり直す |
| `Del` | 削除 |
| `Ctrl+Shift+A` | ページ全面にコマを作る |

## コマ

| キー | 動作 |
|---|---|
| `P` | コマ追加 |
| `N` | 次のコマを提案 |
| `H` | 横に分割 |
| `J` | 縦に分割 |
| `K` | 斜めに縦割り |

## 画像

| キー | 動作 |
|---|---|
| `Ctrl+V` | クリップボードから画像を貼り付け |
| `Ctrl+Shift+F` | コマにフィット |

## フキダシ

| キー | 動作 |
|---|---|
| `B` | 丸い_フキダシを追加 |
| `W` | 雲_フキダシを追加 |
| `G` | ギザギザ_フキダシを追加 |

## マーク

| キー | 動作 |
|---|---|
| `M` | ビックリマークを追加 |

## セリフ

| キー | 動作 |
|---|---|
| `T` | セリフを追加 |
| `F2` | 文字を入力... |
| `F7` | 縦書き |
| `Ctrl+.` | 大きく |
| `Ctrl+,` | 小さく |
| `Ctrl+B` | 太字 |
| `F3` | 次のよく使う書体 |

## ページ

| キー | 動作 |
|---|---|
| `Ctrl+Shift+N` | ページを追加 |
| `Ctrl+Shift+I` | ページを挿入 |
| `Ctrl+Shift+PgUp` | ページを前へ移動 |
| `Ctrl+Shift+PgDown` | ページを後ろへ移動 |
| `PgUp` | 前のページ |
| `PgDown` | 次のページ |

## 表示

| キー | 動作 |
|---|---|
| `Ctrl++` | 拡大 |
| `Ctrl+-` | 縮小 |
| `Ctrl+1` | 原寸で表示 |
| `Ctrl+0` | ページ全体を表示 |

## ヘルプ

| キー | 動作 |
|---|---|
| `F1 / Ctrl+F` | メニューを探す... |

## 画面（メニューに無いキー）

| キー | 動作 |
|---|---|
| `+ / -` | 拡大 / 縮小（画面の中心が軸） |
| `Shift+] / Shift+[` | トーンで拾う黒を増やす / 減らす |
| `Shift+. / Shift+,` | トーンを濃く / 薄くする |
| `Enter` | 選んでいるセリフを打ち始める |
| `Esc` | 画像からコマへ戻る / 選択を解除する |

## セリフを打っている間

| キー | 動作 |
|---|---|
| `Enter` | 改行する |
| `Ctrl+Enter` | 確定する |
| `Esc` | 取り消す |

## マウス

| キー | 動作 |
|---|---|
| `ホイール` | 拡大 / 縮小（マウスの位置が軸） |
| `スペース+ドラッグ` | 画面を動かす（中ボタンのドラッグでも同じ） |
| `ダブルクリック` | 押した場所のものを順に選び直す（選んでいるセリフなら打ち始める） |
| `右クリック` | 押した場所のものを選び、その場のメニューを出す |
| `セリフの四隅をドラッグ` | 文字を大きく / 小さくする（Shift を押している間は枠だけ） |
| `Shift+ドラッグ` | 縦横の比を保って大きさを変える / 15度ずつ回す |

## 読み方

**1文字のキーは道具の切り替え。** 押すとその道具に持ち替わる。セリフを打っている
間は効かない（打った文字として入る）。

**「メニューに無いキー」は画面が直接拾う。** 連打して見た目を合わせる操作なので、
メニューを開き直さずに続けて押せるようにしてある。

**キーは変わることがある。** 覚え直しになる変更はしていないが、**探しにくい記号の
ものは移すことがある**（例: セリフの大きさは 2026-09-05 に `Ctrl+]` / `Ctrl+[` から
`Ctrl+.` / `Ctrl+,` へ移した。角括弧はキーの刻印を見ても何という記号か分からず、
探すのに時間がかかるため）。
