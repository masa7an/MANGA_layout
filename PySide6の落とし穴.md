# PySide6 の落とし穴

このアプリの画面は PySide6（Python 用の GUI 部品セット）で作っている。
PySide6 は Python の下で C++ の部品を動かしているため、**Python 側の変数はまだ
持っているのに、下の部品との結び付きだけが切れる**という壊れ方をすることがある。

普通に読んでも気づけない類のものだけをここに書き残す。実機で確かめた事実と、
そこから何をどう直したかをセットで残す（見立てだけのものは書かない）。

環境: PySide6 / shiboken6 6.11.1（shiboken6 は PySide6 が Python と C++ を
つないでいる土台の部分）、Python 3.12、Windows 11

---

## 1. `QAction.menu()` を呼ぶと、そのメニューが呼び出し側に引き取られる

記録日: 2026-08-04 ／ コミット `3e3b47c`

### 一言でいうと

```python
window.menuBar().actions()[0].menu()      # この1行が、メニューを壊す
```

メニューバーの項目からメニューを取り出す**だけ**の、何も変えていない1行。
これを通した後、アプリが持っていた `self.balloon_menu` が使えなくなった。

### 何が起きたか

テストの補助関数を `window.balloon_menu` を直に見る形に書き換えたところ、
6件が次のエラーで落ちた。

```
RuntimeError: libshiboken: Internal C++ object (PySide6.QtWidgets.QMenu) already deleted.
```

`QMenu`（メニュー1枚を表す部品）、`QAction`（メニューの項目1つを表す部品）、
`QMenuBar`（画面上端のメニューバー）。エラーは「下の部品はもう消えている」と
言っているが、**これは言葉のあやで、実際には消えていない**（→ 下の「実体は生きている」）。

### 実験で確かめたこと

同じウィンドウに対して、条件だけを変えて `self.balloon_menu` が使えるかを見た。

| やったこと | 結果 |
|---|---|
| メニューを作っただけ / 片付け（GC）を回しただけ | 使える |
| `action.menu()` を呼び、その `QAction` を**持ち続ける** | 使える |
| `action.menu()` を呼び、その `QAction` を**捨てて**片付けが走る | **使えなくなる** |
| `menu.menuAction()`（メニューから項目を取る、逆向き） | 使える |

`GC` は、誰も使わなくなった部品を Python が自動で片付ける仕組み。
いつ走るかは決まっていないので、**壊れるタイミングは読めない**。

### なぜ起きるか

PySide6 では `QAction.menu()` を呼ぶと、返ってきたメニューが
**呼び出し側の `QAction` の持ち物として登録される**。

`menuBar().actions()[0].menu()` のように 1行で書くと、途中の `QAction` は
その行が終わった時点で誰も使っていない状態になる。片付けが走ってこの `QAction` が
消えるとき、**持ち物として登録されていたメニューも道連れで無効化される**。

アプリが `self.balloon_menu` として持っていたのは、まさにそのメニューだった。

### 実体は生きている。壊れるのは Python 側の参照だけ

エラー文は「already deleted（もう消した）」と言うが、実測ではそうではなかった。

| 調べたこと | 無効化される前 | 後 |
|---|---|---|
| メニューの置き場所（アドレス） | `2436371566848` | **同じ** |
| メニューバーがぶら下げているメニューの数 | 9 | 9 |
| フキダシメニューの項目数 | 14 | 14 |

つまり **画面のメニューは普通に開けるし、中身も欠けない**。
無効になるのは、Python 側から掴んでいた変数だけ。

これは「メニューバーを辿り直すと生きたメニューが取れる」理由でもある。
生きている実体を、包み直しているだけ。

### どこまで実害があったか

- アプリ側（`manga_layout/`）に `QAction.menu()` の呼び出しは**1件も無い**。
  そのため、この不具合は**利用者の画面には出ていなかった**
- ただし一歩手前だった。誰かが1回でも呼べば、**右クリックのメニューが
  組めなくなる**（項目が減るのではなく、その場でエラーになって出ない）
- 呼んでいたのはテストだけ（メニューを名前ではなく中身で探すため）

### どう直したか

**壊れうるものを持たない形にした。** 右クリックのメニューが写す元を、
`QMenu` そのものではなく **`QAction` の一覧**に変えた。

```python
# 直す前: メニューを持つ → 上記の道連れに巻き込まれる
self.balloon_menu = balloon_menu

# 直した後: 写す項目だけを控える（→ MainWindow._items_to_copy）
self.balloon_copy_items = self._items_to_copy(balloon_menu)
```

持つのは `_act()` で作った `QAction` だけで、これはウィンドウが親なので
メニューの巻き添えにならない。

**区切り線には注意。** `menu.addSeparator()` で足した区切り線もメニューが親なので、
同じように道連れになる（実測で、控えた14件のうち区切り線の2件だけが無効になった）。
持ち帰らずに `None` の印だけ残し、右クリック側で引き直している。

### 書き方の指針

| 書き方 | 可否 |
|---|---|
| `menu = menubar.addMenu("...")` してメニューを変数に持つ | **持ち続けるなら避ける**。誰かが `QAction.menu()` を呼ぶと壊れる |
| `action.menu()` で取ったメニューを、その場で読んで捨てる | 可。テストはこれ |
| `action.menu()` で取ったメニューを保持する | 不可。取り出し元の `QAction` も一緒に持たないと壊れる |
| `QAction` を控えて後で使う | **可**。ウィンドウを親にして作れば（`QAction(text, self)`）安全 |

「呼ばないようにする」という申し合わせでは守れない。
**呼ばれても壊れないものを持つ**のが唯一の対処。

### 歯止め

`tests/test_ui_context_menu.py::TestSharedActions::test_メニューバーを辿られても組める`

メニューバーを `action.menu()` で一通り辿り、片付けを走らせた直後に
右クリックのメニューを組む。直す前の `window.py` に戻すと、この1件が
上と同じ `RuntimeError` で落ちる。

### 教訓（次に同じ形で迷わないために）

- **`RuntimeError: ... already deleted` は「消えた」と読まない。**
  「Python 側から掴めなくなった」と読む。実体が生きていることは珍しくない
- **どちらが壊れているかは、辿り直せば分かる。** 別ルート（メニューバー経由など）で
  取り直したものが使えるなら、実体は生きていて、参照だけの問題
- **「作った側」ではなく「読んだ側」を疑う。** 今回は
  `menuBar().addMenu()` の書き方をいくら見ても原因は出てこなかった。
  壊していたのは、まったく無害に見える読み取り1行のほう
