# PySide6 の落とし穴

このアプリの画面は PySide6（Python 用の GUI 部品セット）で作っている。
PySide6 は Python の下で C++ の部品を動かしているため、**Python 側の変数はまだ
持っているのに、下の部品との結び付きだけが切れる**という壊れ方をすることがある。

普通に読んでも気づけない類のものだけをここに書き残す。実機で確かめた事実と、
そこから何をどう直したかをセットで残す（見立てだけのものは書かない）。

環境: PySide6 / shiboken6 6.11.1（shiboken6 は PySide6 が Python と C++ を
つないでいる土台の部分）、Python 3.12、Windows 11

---

## 1. `QAction.menu()` を呼ぶと、そのメニューが呼び出し側に引き取られる

記録日: 2026-08-04 ／ コミット `3e3b47c`

### 一言でいうと

```python
window.menuBar().actions()[0].menu()      # この1行が、メニューを壊す
```

メニューバーの項目からメニューを取り出す**だけ**の、何も変えていない1行。
これを通した後、アプリが持っていた `self.balloon_menu` が使えなくなった。

### 何が起きたか

テストの補助関数を `window.balloon_menu` を直に見る形に書き換えたところ、
6件が次のエラーで落ちた。

```
RuntimeError: libshiboken: Internal C++ object (PySide6.QtWidgets.QMenu) already deleted.
```

`QMenu`（メニュー1枚を表す部品）、`QAction`（メニューの項目1つを表す部品）、
`QMenuBar`（画面上端のメニューバー）。エラーは「下の部品はもう消えている」と
言っているが、**これは言葉のあやで、実際には消えていない**（→ 下の「実体は生きている」）。

### 実験で確かめたこと

同じウィンドウに対して、条件だけを変えて `self.balloon_menu` が使えるかを見た。

| やったこと | 結果 |
|---|---|
| メニューを作っただけ / 片付け（GC）を回しただけ | 使える |
| `action.menu()` を呼び、その `QAction` を**持ち続ける** | 使える |
| `action.menu()` を呼び、その `QAction` を**捨てて**片付けが走る | **使えなくなる** |
| `menu.menuAction()`（メニューから項目を取る、逆向き） | 使える |

`GC` は、誰も使わなくなった部品を Python が自動で片付ける仕組み。
いつ走るかは決まっていないので、**壊れるタイミングは読めない**。

### なぜ起きるか

PySide6 では `QAction.menu()` を呼ぶと、返ってきたメニューが
**呼び出し側の `QAction` の持ち物として登録される**。

`menuBar().actions()[0].menu()` のように 1行で書くと、途中の `QAction` は
その行が終わった時点で誰も使っていない状態になる。片付けが走ってこの `QAction` が
消えるとき、**持ち物として登録されていたメニューも道連れで無効化される**。

アプリが `self.balloon_menu` として持っていたのは、まさにそのメニューだった。

### 実体は生きている。壊れるのは Python 側の参照だけ

エラー文は「already deleted（もう消した）」と言うが、実測ではそうではなかった。

| 調べたこと | 無効化される前 | 後 |
|---|---|---|
| メニューの置き場所（アドレス） | `2436371566848` | **同じ** |
| メニューバーがぶら下げているメニューの数 | 9 | 9 |
| フキダシメニューの項目数 | 14 | 14 |

つまり **画面のメニューは普通に開けるし、中身も欠けない**。
無効になるのは、Python 側から掴んでいた変数だけ。

これは「メニューバーを辿り直すと生きたメニューが取れる」理由でもある。
生きている実体を、包み直しているだけ。

### どこまで実害があったか

- アプリ側（`manga_layout/`）に `QAction.menu()` の呼び出しは**1件も無い**。
  そのため、この不具合は**利用者の画面には出ていなかった**
- ただし一歩手前だった。誰かが1回でも呼べば、**右クリックのメニューが
  組めなくなる**（項目が減るのではなく、その場でエラーになって出ない）
- 呼んでいたのはテストだけ（メニューを名前ではなく中身で探すため）

### どう直したか

**壊れうるものを持たない形にした。** 右クリックのメニューが写す元を、
`QMenu` そのものではなく **`QAction` の一覧**に変えた。

```python
# 直す前: メニューを持つ → 上記の道連れに巻き込まれる
self.balloon_menu = balloon_menu

# 直した後: 写す項目だけを控える（→ MainWindow._items_to_copy）
self.balloon_copy_items = self._items_to_copy(balloon_menu)
```

持つのは `_act()` で作った `QAction` だけで、これはウィンドウが親なので
メニューの巻き添えにならない。

**区切り線には注意。** `menu.addSeparator()` で足した区切り線もメニューが親なので、
同じように道連れになる（実測で、控えた14件のうち区切り線の2件だけが無効になった）。
持ち帰らずに `None` の印だけ残し、右クリック側で引き直している。

### 書き方の指針

| 書き方 | 可否 |
|---|---|
| `menu = menubar.addMenu("...")` してメニューを変数に持つ | **持ち続けるなら避ける**。誰かが `QAction.menu()` を呼ぶと壊れる |
| `action.menu()` で取ったメニューを、その場で読んで捨てる | 可。テストはこれ |
| `action.menu()` で取ったメニューを保持する | 不可。取り出し元の `QAction` も一緒に持たないと壊れる |
| `QAction` を控えて後で使う | **可**。ウィンドウを親にして作れば（`QAction(text, self)`）安全 |
| `menu.addMenu(sub)` の**戻り値の QAction** を控える | **不可**。これは `sub` が持っている QAction で、上と同じに壊れる（→ 下） |

「呼ばないようにする」という申し合わせでは守れない。
**呼ばれても壊れないものを持つ**のが唯一の対処。

### 続き: 畳んだメニューの「見出し」も同じ穴に落ちる（2026-08-05 に追記）

四角_フキダシを足すとき（要件定義 10.1）、種類を変える項目を下位のメニューへ
畳んだ。そのとき**見出しの QAction を有効・無効の一覧に入れた**。

```python
style_menu_action = balloon_menu.addMenu(self._build_balloon_style_menu())
self.balloon_actions.append(style_menu_action)   # ← これが壊れる
```

見た目は `_act` で作った項目を控えるのと同じだが、**この QAction の持ち主は
ウィンドウではなく下位の QMenu**。メニューバーを `QAction.menu()` で辿られると
一緒に引き取られ、以後 `_refresh` が毎回

```
RuntimeError: libshiboken: Internal C++ object (PySide6.QtGui.QAction) already deleted.
```

で落ちるようになった。**`addMenu()` の戻り値は「控えてよい QAction」に見えるが
違う**、というのがこの追記の要点。

対処は、見出しを控えないこと。畳んだ中身（`_act` で作った項目）だけを控え、
見出しは使えるままにする。中身がグレーになるので、押せない状態は伝わる。
集中線のメニューが最初からこの形だったため、**同じ形にすれば済んだ**。

同じことがテスト側でも起きる。メニューを辿って項目を持ち帰る書き方
（`tests/test_ui_balloon.py::balloon_menu_items`）は、**見出しを混ぜて返した
時点で呼んだ側が壊れる**。持ち帰る前に落とす。

`find(menu, label).menu()` の1行も同じ穴。**取り出し元の QAction を
使い捨てにすると、返ってきた QMenu がその場で消える**（上の表の3行目そのもの）。
中身を読み終わるまで QAction を生かしておく形にした
（`tests/test_ui_context_menu.py::_open_folded`）。

### 歯止め

`tests/test_ui_context_menu.py::TestSharedActions::test_メニューバーを辿られても組める`

メニューバーを `action.menu()` で一通り辿り、片付けを走らせた直後に
右クリックのメニューを組む。直す前の `window.py` に戻すと、この1件が
上と同じ `RuntimeError` で落ちる。

### 教訓（次に同じ形で迷わないために）

- **`RuntimeError: ... already deleted` は「消えた」と読まない。**
  「Python 側から掴めなくなった」と読む。実体が生きていることは珍しくない
- **どちらが壊れているかは、辿り直せば分かる。** 別ルート（メニューバー経由など）で
  取り直したものが使えるなら、実体は生きていて、参照だけの問題
- **「作った側」ではなく「読んだ側」を疑う。** 今回は
  `menuBar().addMenu()` の書き方をいくら見ても原因は出てこなかった。
  壊していたのは、まったく無害に見える読み取り1行のほう

---

## 2. 右クリックのメニューでは `setStatusTip` がステータスバーに届かない

記録日: 2026-08-05 ／ コミット `0775fd1` の直後

### 一言でいうと

**`setStatusTip` を付けただけでは、右クリックのメニューでは何も出ない。**
メニューバーから開いたメニューなら出る。同じ書き方で結果が変わる。

### 何が起きたか

右クリックのメニューの項目名を短くし（「ここに 丸い_フキダシ を追加」→
「　　　 丸い_フキダシ」）、落とした言葉をカーソルを乗せた間だけ
ステータスバーに出そうとした。メニューバー側と同じく `setStatusTip` を
付けたが、**ヒントは項目に入っているのに、ステータスバーは空のまま**だった。

### 実験で確かめたこと

`QT_QPA_PLATFORM=offscreen` で、宛先を変えながら送って切り分けた。

| やったこと | ステータスバー |
|---|---|
| ウィンドウへ `QStatusTipEvent` を直接送る | 出る |
| **メニューへ** `QStatusTipEvent` を送る | **空** |
| `action.showStatusText(menu)` | **空** |
| `action.showStatusText(window)` | 出る |

### なぜ起きるか

Qt は説明を「メニューを呼び出したウィジェット」へ送る。メニューバーから
開いたメニューにはそれがある（メニューバー自身）ので、そこから親を辿って
ステータスバーまで届く。

**右クリックのメニューには呼び出し元が無い。** その場合 Qt は QMenu 自身へ
送るが、`QStatusTipEvent` は親へ伝わらないため、誰も受け取らないまま終わる。
`QMenu(self)` と親を渡していても関係ない（親子であることと、イベントが
親へ伝わることは別）。

### どう直したか

宛先をウィンドウだと明示して繋ぐ（`MainWindow._show_tips_in_status_bar`）。

```python
menu.hovered.connect(lambda action: action.showStatusText(self))
menu.aboutToHide.connect(self.statusBar().clearMessage)
```

`hovered` はキーボードの上下でも鳴るので、マウスを使わない場合も同じに
なる。閉じたら消すのは、選び終わったあとに古い説明が残らないようにするため。

### 歯止め

`tests/test_ui_context_menu.py::TestStatusTips`

`menu.setActiveAction()` で本番と同じ経路（`hovered`）を通し、
ステータスバーの中身を見る。繋ぎを外すと3件とも落ちる。

### 教訓（次に同じ形で迷わないために）

- **例外が出ないので、画面を見るまで気づけない。** `setStatusTip` は
  黙って受け取られ、`statusTip()` で読み返すこともできる。
  「設定できたから効いている」とは言えない
- **メニューバーで動いたからといって、右クリックでも動くとは限らない。**
  同じ QMenu・同じ QAction でも、呼び出し元の有無で経路が変わる
- **親を渡してあることを、イベントが届く根拠にしない。**
  伝わるイベントと伝わらないイベントがある

---

## 3. `Format_Alpha8` を `Format_Grayscale8` に変換すると、全画素が 255 になる

### 一言でいうと

**明るさと透明度は、どちらも1画素1バイトだが、`convertToFormat` で行き来
できない。** 変換すると中身が捨てられ、マスクが「画像全部」を指すようになる。
**例外は出ない。**

### 何が起きたか

画像の暗い所を斜線に置き換える処理（→ [要件定義.md](要件定義.md) の 6.27）で、
「どこがトーンになるか」を1枚のマスク画像として持っている。合成に使うときは
**明るさを透明度として読み替える**必要がある。

素直に `convertToFormat` を挟んだところ、**絵が一面トーンになった**。マスクが
全画素 255（＝全部が対象）になっていた。

### 実験で確かめたこと

`Format_Alpha8` の画像に `convertToFormat(Format_Grayscale8)` を掛けると、
元の中身によらず全画素 255 が返る。逆向き（`Grayscale8` → `Alpha8`）も
中身は運ばれない。

### なぜ起きるか

Qt にとってこの2つは**別の意味を持つ形式**で、変換は「バイト列の読み替え」
ではなく「絵としての変換」になる。`Alpha8` は色を持たないので、色として
読み直せば白（255）になる。バイトの並びが同じことは、変換の根拠にならない。

### どう直したか

**マスクは `Grayscale8` のまま持ち回り、合成の直前だけ生バイト列を
`Alpha8` として持ち直す。** 1画素1バイトで並びが同じなので、バイト列は
そのまま使い回せる。

```python
# 直す前: 中身が捨てられ、マスクが「画像全部」になる
alpha = mask.convertToFormat(QImage.Format.Format_Alpha8)

# 直した後: バイト列を取り出して、別の形式として持ち直す
raw, w, h, bpl = _raw_of(mask)          # Grayscale8 のときだけ通す
alpha = QImage(raw, w, h, bpl, QImage.Format.Format_Alpha8).copy()
```

`QImage` は渡したバイト列を**参照したまま持つ**ので、`.copy()` で Python 側の
寿命から切り離す。しないと解放後の領域を読む。

### 歯止め

`manga_layout/tone.py` の `_raw_of` が、`Grayscale8` 以外を渡されたら
その場で例外を投げる。変換を挟んだ形に戻すと、ここで止まる。

`tests/test_tone.py` の一連の検証も、マスクが全画素 255 になれば
「選ばれなかった所は1画素も変えない」が落ちる。

### 教訓（次に同じ形で迷わないために）

- **1画素1バイトで並びが同じことは、変換できる根拠にならない。**
  Qt の変換は絵としての変換で、バイト列の読み替えではない
- **失敗が「全部が対象」の向きに転ぶのがたちが悪い。** 空になるなら
  何も起きず気づくが、全部になると**それらしい絵が出てしまう**
- **形式を跨ぐときは、変換ではなく持ち直しを疑う。** `QImage` は
  外のバイト列から作れるので、意味を変えたいだけならそちらが素直

---

## 4. `Format_ARGB32_Premultiplied` に描いた文字は、`Format_ARGB32` より 2px ずれる

### 症状

PSD 書き出し（→ 要件定義 10.1）で、ページをレイヤーに分けて描き、重ね直した
ものを PNG 書き出しと突き合わせたところ、**セリフの所だけが食い違った**。

コマ・画像・コマ枠・フキダシは1画素も違わない。セリフだけが**左へ2px・上へ2px**
寄っていた。文字の大きさも形も同じで、位置だけが動く。

### 原因

**描く先の `QImage` の形式**で決まる。塗りつぶしの色（白か透明か）は関係ない。

```
ARGB32   白        文字の外接矩形 (81, 61, 118, 98)
ARGB32   透明      文字の外接矩形 (81, 61, 118, 98)
ARGB32P  白        文字の外接矩形 (79, 59, 116, 96)   ← 2px ずれる
ARGB32P  透明      文字の外接矩形 (79, 59, 116, 96)   ← 2px ずれる
```

同じ `QPainter` の設定・同じ `QFont`・同じ矩形・同じ `drawText` の呼び方で、
`QImage` の形式だけを変えて測った実測値（2026-08-06、PySide6 6.11.1）。

`drawImage` や `drawPolygon` では起きない。**文字だけ**が動く。

### 直した形

**描き分ける場所すべてで、書き出し（`export.render_page`）と同じ `Format_ARGB32`
に揃える。**

```python
# 直す前: 描画が速い形式を選んだ。セリフだけが 2px ずれる
canvas = QImage(width, height, QImage.Format.Format_ARGB32_Premultiplied)

# 直した後: 書き出しと同じ形式にする
canvas = QImage(width, height, QImage.Format.Format_ARGB32)
```

`_Premultiplied`（透明度を色に掛け込んである形式）は描画が速いので、透明な紙に
描くときはこちらを選びたくなる。**速さのために形式を変えると、絵の内容が変わる。**

### 歯止め

`tests/test_psd_export.py` の `Test重ね直すとPNGと一致する` が、レイヤーを重ねた
結果と PNG 書き出しを画素で突き合わせる。形式を戻すと、セリフの所で食い違いが
255 まで跳ね上がって落ちる（丸めによる差は 1 まで）。

### 教訓（次に同じ形で迷わないために）

- **文字は「同じに描かれる」と思ってはいけない。** 図形は形式を跨いでも
  同じ位置に出るので、文字も同じだろうと考えると外す
- **同じ絵を2つの経路で作るなら、`QImage` の形式まで揃える。** 描画の
  設定（アンチエイリアス・倍率）を揃えるだけでは足りない
- **画素で突き合わせる検証を1つ持っておくと、この種は必ず捕まる。**
  見た目では 2px を見つけられない

---

## 5. メニューのショートカットは、入力欄の `keyPressEvent` より先に解決される

記録日: 2026-08-08

### 一言でいうと

**「入力中は1つも横取りしない」を部品の `keyPressEvent` に書いても、
`QAction` のショートカットには効かない。** キーが部品まで下りてくる前に
決着が付いているため。

### 症状

セリフのその場編集（→ 要件定義 6.5）の最中に `F7`（縦書き）や `PgDown`
（次のページ）を押すと、**入力欄を開いたまま項目が実行される**。

いちばん重いのはページを移す系で、次の順に壊れる。

1. 入力中に `PgDown` が発火し、**入力欄はそのままにページだけが変わる**
2. `Ctrl+Enter` で確定すると、書き戻しは**今のページ**からセリフを探す
3. 見つからないので**打った文字は黙って捨てられる**
4. 元のページには、中身の無いセリフの枠だけが残る

枠は薄い点線でしか描かれず書き出しにも出ないので、**残っていること自体に
気づけない**（この壊れ方は 6.5 に既出）。

`F7` や `Ctrl+B` が通った場合も、履歴に1手挟まるせいで「置いたばかりの
セリフを Esc で追加ごと取り消す」の照合（`discard_last_edit("セリフの追加")`）
が外れ、同じく空の枠が残る。

### 原因

`PageView.keyPressEvent` の防壁は、**キーがその部品まで届いた場合にしか
効かない**。Qt はキーを押されると、まず焦点のある部品へ `ShortcutOverride`
というイベントを投げ、そこで受理されなければショートカットとして処理する。
入力欄（`QWidgetTextControl`）が受理するのは**文字として打てるキーと、
標準の編集キーだけ**なので、それ以外は部品へ下りずにショートカットへ抜ける。

実測（2026-08-08、PySide6 6.11.1。焦点を持つ `PageView` へ `ShortcutOverride`
を送って `isAccepted()` を見た）。このアプリのショートカット 38 件の内訳:

| | 件数 | 例 |
|---|---|---|
| 入力欄へ譲られる | 14 | `V` `P` `B` `T` などの1文字、`Ctrl+Z` `Ctrl+Y` `Ctrl+V` `Del` |
| **ショートカットへ抜ける** | **24** | `F1` `F2` `F7` `PgUp` `PgDown` `Ctrl+B` `Ctrl+[` `Ctrl+]` `Ctrl+S` `Ctrl+N` `Ctrl+O` `Ctrl+W` `Ctrl+Q` `Ctrl+Shift+N` … |

**1文字キーが無事なのは偶然ではない**（文字として打てるので譲られる）。
逆に、**修飾キー付き・ファンクションキー・移動キーはまず抜ける**と考えてよい。

### 直した形

**項目の実行を1箇所に集め、その手前で入力を確定する**
（`MainWindow.run_action`）。`QAction` は全て `MainWindow._act` で作る
決まりになっていたので、塞ぐ場所は1つで足りた。

```python
# 直す前: スロットを直につなぐ。入力中でもそのまま実行される
action.triggered.connect(slot)

# 直した後: 実行の前に、開いている入力欄を確定する
action.triggered.connect(lambda *_, slot=slot: self.run_action(slot))

def run_action(self, slot) -> None:
    self.view.finish_text_edit(commit=True)
    slot()
```

**キーを押させない方向では直さない。** 入力中に `Ctrl+S` を押すのは自然な
操作で、塞ぐと「効かないキー」を覚えることになる。画面を触ったときと
右クリックのときが既に「先に確定してから次の操作へ移る」形だったので、
**そちらに揃えた**。

なお、トーンの連打キー（`Shift+]` など）が `setShortcut` を使わず画面側で
拾っているのは、この穴を**項目ごとに避けていた形**（→ `menus.ToneMenu`）。
穴の存在自体は気づかれていたが、1箇所ずつ避ける方針だったので他が残った。

### 歯止め

`tests/test_ui_text.py` に3つ置いてある。

- `Test入力中に項目が押されたとき` — 項目を `trigger()` で押して「入力欄が
  閉じている」「打った内容が残っている」を見る。確定の1行を外すと 5 件が
  落ちる（実測）
- `Test入力中のキーがどちらへ行くか` — **上の表の前提そのもの**。
  `ShortcutOverride` を自分で作って送り、譲られる／抜けるを確かめる。
  Qt の側が変われば防ぎ方の要否も変わるので、ここが落ちたら直す前に
  この文書を読み直す
- `Testキーを持つ項目の作られ方` — キーを持つ項目が `_act` と
  `_build_tool_actions` の2つの作り方だけで作られていることを数え上げる。
  **3つ目の作り方が現れると、その項目だけ静かに素通りする**

**測るのに窓を活性にする必要はない。** `ShortcutOverride` は自分で作って
`sendEvent` で送れるので、offscreen のまま答えが取れる。逆に、`MainWindow`
を組んで実際にキーを押させる形の確認用スクリプトは**その場で固まる**
（2026-08-08、120秒で打ち切られて背景タスクに回り、1行も出さなかった）。

書き戻し先を見失った場合の受け皿も `finish_text_edit` に足した。今は通れない
道だが、**黙って捨てるのが元の壊れ方**だったので、状態表示で知らせる。

### 教訓（次に同じ形で迷わないために）

- **`keyPressEvent` に書いた防壁は、ショートカットには効かない。**
  部品に届く前に決着が付く経路があることを、防壁の側からは見通せない
- **「1文字キーは大丈夫だった」を根拠に安全だと考えない。** 譲られるのは
  文字として打てるからで、`F7` や `Ctrl+B` には当てはまらない
- **項目ごとに避けると、避け漏れが必ず残る。** 実行の道を1つに束ねて
  そこで塞ぐほうが、項目を足したときに考えなくて済む
- **黙って消えるのがいちばん悪い。** 打った文字が捨てられても画面は
  何も言わなかったので、起きていること自体を報告できない

---

## 6. `QProgressDialog` は、値が上限に達すると中止フラグごと初期状態へ戻る

記録日: 2026-08-08

### 一言でいうと

**`setValue()` に渡した値が上限（`maximum()`）と同じだと、`QProgressDialog` は
既定で自分をまるごと初期状態へ戻す。** `wasCanceled()` も `False` に、
`value()` も `-1` に戻る。**中止ボタンを押した直後でも関係なく戻る。**

### 症状

書き出し（→ 要件定義 6.7・10.1）にページごとの進捗窓を足す際、依頼枚数
ぴったりまで `setValue()` を進めたところ、**直前に押した中止ボタンの
記録が消えていた**（実測）。

```python
dialog = QProgressDialog("書き出しています…", "中止", 0, 5, None)
for i in range(1, 6):
    dialog.setValue(i)
    if i == 3:
        dialog.cancel()
    print(i, dialog.value(), dialog.wasCanceled())
# 1 1 False
# 2 2 False
# 3 3 False   ← cancel() 直後
# 4 4 True    ← 中止フラグは正しく残っている
# 5 -1 False  ← 上限（5）に達した瞬間、値も中止フラグも消える
```

### 原因

`QProgressDialog` の既定は `autoReset=True`。ドキュメントに書いてある
挙動どおりだが、**「進捗を最後まで進めたら畳んで次の操作に備える」ための
仕組み**であって、中止ボタンの状態を守る仕組みではない。**上限にちょうど
達した瞬間だけ**起きるため、ページ数が少ない・処理が速いと再現せず、
気づきにくい。

### 直した形

**`setAutoReset(False)` を明示する。** ループの外で自分の判断（何枚
書けたか）で進捗窓の後始末をするなら、Qt に自動で畳ませる理由が無い。

```python
# 直す前: 既定のまま。上限ちょうどで中止フラグが消えることがある
dialog = QProgressDialog("書き出しています…", "中止", 0, total, parent)

# 直した後: 自動で戻さない
dialog = QProgressDialog("書き出しています…", "中止", 0, total, parent)
dialog.setAutoReset(False)
```

このアプリの書き出し（`project_io._run_export`）では、**「何枚書けたか」
（`len(written) < len(indexes)`）だけで中止を判定し、`wasCanceled()` を
最終判定には使わない**形にしたため、上のリセットが起きても実害は無い。
それでも `setAutoReset(False)` は残してある。ループの途中（`on_page` の
中）では `wasCanceled()` を読んでおり、**呼ぶ順序を変えたときに同じ穴を
踏まないための保険**になる。

### 教訓（次に同じ形で迷わないために）

- **「値を上限まで進める」という、進捗窓ならほぼ必ず起きる操作が引き金。**
  滅多に通らない経路ではなく、**正常に完走したときにこそ起きる**
- **`wasCanceled()` の値を、あとから読み直す設計にしない。** 読んだその場で
  使い切るか、`len(written) < len(indexes)` のように**自分で数えた事実**
  を正とするほうが、Qt 側の内部状態に振り回されない
- **`setAutoReset(False)` は、ループを自分で終わらせる設計なら基本セット
  で付けてよい。** 副作用を消しておけば、あとから読む人がこの挙動を
  知らなくても踏まずに済む

---

## 7. 8bit の画像に `setPixel` で 255 を書くと、39 になる

### 一言でいうと

**`QImage.setPixel` の第3引数は「色そのもの」ではなく「色表の番号」。**
`Format_Grayscale8` や `Format_Alpha8` のような1画素1バイトの絵に対しては、
渡した数がそのまま濃さにならない。**例外は出ない。**

### 何が起きたか

切り抜きのマスク（→ [要件定義.md](要件定義.md) の 10.3）を試すために、
グレースケールの絵へ `setPixel(x, y, 255)` で白を書いた。読み返すと 39 で、
掛け合わせの試験が「マスクが効いていない」ように見えた。**マスクの作り方が
間違っているのに、掛け合わせの実装を疑って半日ぶんの遠回りをしかけた。**

### 実験で確かめたこと

```
setPixel      で [255, 128, 64, 0] を書く → 読み返すと [39, 20, 10, 0]
setPixelColor で同じ値を書く              → 読み返すと [255, 128, 64, 0]
```

（根拠: `QImage(4, 1, Format_Grayscale8)` へ書いて `qGray(pixel())` で読み返す /
確認日: 2026-08-27）

### なぜ起きるか

`setPixel(x, y, index_or_rgb)` は、8bit 以下の絵では**色表（カラーテーブル）の
番号**として扱う。`Format_Grayscale8` に色表は無いので、番号は暗黙の変換に
掛かって別の濃さになる。32bit の絵では同じ引数が RGB 値として働くため、
**書き方は同じなのに、絵の形式で意味が変わる。**

### どう直したか

**`setPixelColor(x, y, QColor(v, v, v))` を使う。** 色として渡せば、形式に
関わらず意図した濃さになる。まとめて塗るなら `fill(QColor(v, v, v))` か
`QPainter` で塗る。

### 歯止め

`tests/test_image_masks.py` の `gray_mask` / `half_mask` に、`setPixel` を
使わない理由をコメントで残してある。マスクを作る試験用の関数はここに集めて
あるので、次に書く人はまずこれを見る。

### 教訓（次に同じ形で迷わないために）

- **同じ名前の関数が、絵の形式で別の意味を持つことがある。** 32bit で
  動いた書き方を 8bit へ持っていくときは、まず1画素書いて読み返す
- **試験の道具が壊れていると、実装のほうを疑う。** 期待と違う値が出たら、
  **確かめる側が正しいか**を先に見る（→ 3 の「全部が対象」と同じ形で、
  それらしい絵が出てしまうのがたちが悪い）
- 3 とあわせて、**1画素1バイトの絵は Qt の中で扱いが揺れる**と覚えておく

---

## 8. 継承した `QTranslator` は、`""` を返すと訳を消し、持っていないと効かない

### 一言でいうと

Qt が出すボタンの言葉（`Save` / `Cancel` など）を日本語にするため `QTranslator` を
継承して被せたところ、**2つ続けて踏んだ。** 当てはまらない語に `""` を返すと
「訳は空である」の意味になり、**元の文字が消える**。そして `installTranslator` へ
渡しただけで Python 側の参照を持たないと、**訳が黙って効かなくなる**。
**どちらも例外にならない。**

### 何が起きたか

終了時の確認を「保存 / 保存しない / 中止」にしたかった（2026-08-27）。Qt 同梱の
日本語訳（`qtbase_ja.qm`）は `Cancel` を「キャンセル」、`Discard` を「変更を破棄」と
当てるので、**その2語だけ差し替える訳を上に重ねた。**

- 表に無い語へ `""` を返したところ、**フォントを選ぶ窓の見出しが全部消えた**
  （「フォント」「サイズ」などが空欄になる）
- 別の試しで `app.installTranslator(Words())` と書いて参照を持たなかったところ、
  **訳が1つも当たらなかった。** エラーは出ず、英語のまま出るだけ

### 実験で確かめたこと

4通りを同じプロセスで順に測った。左が `Cancel` ボタンの文字、右がフォント選択窓の見出し2つ。

```
A 独自訳なし        : キャンセル ['フォント(&F)', 'フォントスタイル(&Y)']
B 空文字を返す      : 中止 ['', '']
C None を返す       : 中止 ['フォント(&F)', 'フォントスタイル(&Y)']
D 参照を持たない    : キャンセル ['フォント(&F)', 'フォントスタイル(&Y)']
```

**B は狙った差し替え（中止）が効いているぶん、成功に見える。** 壊れたのは触っていない
ほうで、しかも別の窓に出るので、その場では気づけない。**D は差し替えが効いていない
だけで、他はすべて正常に見える。**

（根拠: `QTranslator` を継承した2種類を順に着脱して `QMessageBox` と `QFontDialog` の
文字を読み出した / `./venv/Scripts/python.exe` で実行 / 確認日: 2026-08-28）

### なぜ起きるか

- **`translate` の戻りは、「訳が無い」を空の文字列で表す約束ではない。** C++ 側では
  null の `QString` が「訳が無い」で、空文字は「空という訳がある」。Python から `""` を
  返すと後者になり、**下に敷いた `qtbase_ja.qm` へ落ちずに、そこで確定する。**
  `None` を返せば null になり、次の訳へ渡る
- 参照のほうは、この文書の **1 と同じ形**。**`installTranslator` は所有権を取らない。**
  Python 側の一時オブジェクトはその場で回収され、下の C++ は消えた相手を呼ばなくなる

### どう直したか

`manga_layout/ui/i18n.py` に集約した。

- 当てはまらない語には **`None` を返す**（`BUTTON_WORDS.get(source)` は当たらなければ
  `None` を返すので、そのまま返せばよい）
- 読み込んだ訳を、モジュールの `_installed` リストで**持ち続ける**

### 歯止め

`tests/test_ui_i18n.py` が2つを見る。終了時の3つが「保存 / 保存しない / 中止」で
出ること。そして**差し替えていない語（はい・いいえ・閉じる）が日本語のままである**こと。
**後者が B の壊れ方を捕まえる**——空文字を返す形に戻すと、そちらが落ちる。

### 教訓（次に同じ形で迷わないために）

- **「無い」を空の値で代用しない。** 戻り値に「無い」を表す値があるなら、空文字や 0 で
  済ませない。**代用したほうが短く書けるので、書いた本人には自然に見える**
- **触っていないほうが壊れる。** 差し替えたい語だけを見て確かめると通ってしまう。
  **上書きの仕組みを入れたら、上書きしなかったものも1つ測る**
- 参照は 1 と同じ話。**`install〜` / `add〜` で Qt へ渡したものは、Python 側でも持つ**

---

## 9. `QProgressDialog` の `minimumDuration` は、イベントを処理する隙がないと数えられない

記録日: 2026-09-05

### 一言でいうと

**`setMinimumDuration(500)` の 500ms は、時計ではなくタイマー（Qt のイベント）で
数えている。** 重い処理を画面と同じ流れの中で回していると、その処理が終わるまで
イベントが1つも処理されず、**500ms どころか何秒たっても窓は出ない。**

### 症状

書き出し（→ 要件定義 6.7）の進捗窓は `setMinimumDuration(500)` で「一瞬で終わる
書き出しでは出さない」ようにしてある。ところが **PSD で1ページ目を書いている
あいだ、窓が1つも出ていなかった**（本人の指摘 2026-09-05）。

`processEvents()` を呼ぶのは**1ページ書き終えた後**（`on_page` の中）だけなので、
1ページ目のあいだは隙が無い。PSD は1ページ 1.0 秒（→ 要件定義 6.28）で、画像の
多いページならもっとかかる。**押した直後の、いちばん「効いたのか」と思う時間帯が
そのまま無反応になる。**

```python
d = QProgressDialog("test", "cancel", 0, 3, None)
d.setMinimumDuration(500)
print("just after construction  visible =", d.isVisible())
time.sleep(1.2)   # 1ページ目を書いているつもり。processEvents は呼ばない
print("1.2s later, no events    visible =", d.isVisible())
d.setValue(1); app.processEvents()
print("after setValue(1)+events visible =", d.isVisible())
# just after construction  visible = False
# 1.2s later, no events    visible = False   ← 1.2 秒たっても出ない
# after setValue(1)+events visible = True    ← 1ページ目が終わって初めて出る
```

（根拠: 上のスクリプトを `venv/Scripts/python.exe` で実行した出力 / 確認日: 2026-09-05）

### なぜ気づきにくいか

**2ページ目からは正しく動く。** 1ページ書くごとに `processEvents()` が呼ばれるので、
そこからは数字も進み、中止ボタンも効く。**動いている姿を見てしまうと、出ていない
時間帯があったことのほうが記憶に残らない。**

PNG では起きない。1ページ 0.2 秒なので隙が毎秒5回でき、500ms の見積もりが
そのとおりに働く。**同じコードなのに、渡す仕事の重さで挙動が変わる。**

### 直した形

**重いと分かっている書き出し（PSD）だけ、`forceShow()` で先に出す。**

```python
if self.export_format in LAYERED_FORMATS:
    dialog.setLabelText(f"書き出しの準備をしています…（全 {len(indexes)} ページ）")
    dialog.forceShow()
    QApplication.processEvents()
```

- **`setMinimumDuration` を 0 にする形は採らない。** それだと PNG の1枚書き出し
  （0.2 秒）でも窓が一瞬だけ出て消える。「一瞬で終わる書き出しでは窓を出さない」
  （→ 要件定義 6.7）を捨てることになる
- **残る穴を承知で採っている。** 1枚が極端に大きい PNG では同じことが起きる。
  そこは倍率で決まるので、**形式で分けるこの切り方では拾えない**

### 歯止め

`tests/test_export.py` が2つを見る。**PSD では1ページ目を書き始める時点で窓が
見えている**こと（`test_PSDは1ページ目を書く前に進捗窓を出す`）。そして
**PNG では見えていない**こと（`test_PNGでは進捗窓を先に出さない`）。
**後者が「全部 forceShow すればいい」への引き戻しを捕まえる。**

### 教訓（次に同じ形で迷わないために）

- **「時間で出る」と書いてある仕組みは、時間を数える隙があるかを疑う。** 待たせて
  いる当人が隙を作らない限り、時間は進まない
- **フィードバックの仕組みは、いちばん重い1回で確かめる。** 軽い場面で確かめると、
  重い場面でだけ消えることに気づけない
- **落とし穴 6 と同じ相手（`QProgressDialog`）で、壊れ方が逆。** あちらは
  「進めすぎると状態が消える」、こちらは「進める前に何も出ない」。
  **ループの最初と最後の両端に穴がある**と思っておくとよい
